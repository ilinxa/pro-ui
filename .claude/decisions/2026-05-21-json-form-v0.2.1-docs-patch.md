---
date: 2026-05-21
type: docs + fix
commits: [fc75cd2, 425a9b1]
components: [json-form]
findings: [F-01-dependsOn-jsdoc, F-02-allValues-snapshot-freshness-jsdoc, F-06-devtools-no-provider-fallback, F-08-bigint-replacer, smoke-clean]
status: shipped
---

# json-form v0.2.1 — bundled JSDoc + devtools patch

## Summary

Patch bump closing 4 of the 5 open follow-ups from the v0.2.0 GATE 3 full-checklist review in a single commit. No public-API additions, no behavioral changes for any existing v0.1.x / v0.2.0 consumer. One small internal-only export (`useJsonFormContextOptional`) added in `json-form-context.tsx` — used only by the devtools body; NOT exported from the package barrel.

| Finding | File | Change |
|---|---|---|
| F-01 — `dependsOn` JSDoc | `types.ts` | Rewrites the `FieldDefinition.dependsOn` JSDoc — drops the v0.1.7-only future-framing, documents all three semantic states (undefined / [] / non-empty), notes the FINAL renderer identity check, adds explicit stable-identity guidance ("module-scope const or `useMemo`-cache"). |
| F-02 — `allValues` snapshot freshness | `types.ts` | Adds JSDoc to `FieldRendererArgs.allValues` covering snapshot / narrow / fullbag modes. Snapshot mode notes the freshness contract (fresh on own value change via Controller, stale across other fields) + recommends `useJsonFormFieldValue` / `useJsonFormFieldsValue` for live values in snapshot-mode renderers. |
| F-06 — devtools soft-warning | `json-form-context.tsx`, `parts/json-form-devtools-body.tsx` | New internal `useJsonFormContextOptional()` accessor returns `null` instead of throwing. Devtools body short-circuits to a "No JsonForm context found" panel with a recover-action explanation instead of crashing the component tree when consumer forgets the `<JsonFormProvider>` wrap. Hooks-order rule preserved (the optional accessor is called first; tabs only mount when ctx is non-null). Bumped the devtools footer version label `v0.1.7` → `v0.2.1` while there. |
| F-08 — BigInt replacer | `parts/json-form-devtools-body.tsx` | `prettyReplacer` returns `${value.toString()}n` for `typeof === "bigint"` values; `JSON.stringify` itself throws on bigint so any form with a bigint in state would crash the devtools panel before v0.2.1. |

## What remains open from the v0.2.0 review

| Finding | Severity | Status |
|---|---|---|
| F-03 — default-registry whitelist drift lint | ⚠️ High | Open. Tracked at v0.2.1+ or v0.3.0 — needs its own script (`validate-meta-deps`-style) that scans each `BUILTIN_RENDERER_TYPES_SKIPPING_ALL_VALUES` member's source for `args.allValues` access. Out of scope for this docs-patch. |
| F-07 — `defineFieldRenderer` config-key narrowing | 🔹 Low | Indefinite defer per the original review — awaiting real consumer feedback. |

## Why patch bump (GATE 3 skipped)

Per [`.claude/rules/component-readiness-review.md`](../rules/component-readiness-review.md), patch bumps don't trigger GATE 3 when they're non-public-API-touching. v0.2.1 ships:

- 2 JSDoc edits (no runtime behavior change).
- 1 new internal-only context accessor (not exported from the package barrel; reachable only via the lazy-loaded devtools body).
- 1 prettyReplacer line addition (devtools body — defensive crash fix).
- 0 changes to any FieldRenderer-facing or FormSchema-facing public surface.

v0.2.0 spotcheck/checklist verdict carries forward.

## Verification

- `pnpm tsc --noEmit` clean.
- `pnpm validate:meta-deps` clean (45/45).
- `pnpm registry:build` clean; artifact `public/r/json-form.json` contains `useJsonFormContextOptional` × 2, `no-provider` × 1, `bigint` × 1.
- **Path-b smoke against fresh Vercel deploy — ✅ CLEAN.** Ran post-`fc75cd2` deploy: `pnpm dlx shadcn@4.6.0 add @ilinxa/json-form @ilinxa/json-form-fixtures` succeeded; consumer-side `pnpm tsc --noEmit` returned **0 errors in json-form OR article-body-01** (32 total errors, all pre-existing in flow-canvas-01 / pdf-viewer / code-block / ui-calendar — identical baseline to the v0.2.0 smoke). v0.2.1 source preserved verbatim through the shadcn 4.6.0 rewriter: `useJsonFormContextOptional` × 4 sites in the devtools body, definition + import in the context module, "No JsonForm context found" fallback panel + bigint replacer all carried through. No F-cross-13 surface, no F-S1 surface — patch ships clean.

## Memory updates

- [`memory/project_json_form_v01_to_v02_progression.md`](../../C:/Users/AsiaData/.claude/projects/e--2026-ilinxaDOC-ilinxa-ui-pro/memory/project_json_form_v01_to_v02_progression.md) — append v0.2.1 ship.
- `MEMORY.md` index entry already at v0.2.0 ship status; light edit to flip to "v0.1.5 → v0.2.1 SHIPPED" if appropriate.
