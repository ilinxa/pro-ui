---
date: 2026-05-27
session: post-card-01-v0.2.0-ship
phase: ship
type: minor-version-ship
commits: 13
components:
  - post-card-01
  - engagement-bar-01
status: shipped (local; push + Vercel redeploy + smoke-harness run pending)
related_decisions:
  - 2026-05-27-engagement-bar-01-v0.2.0-likers-strip-share-menu-extraction
findings:
  - F-01-followup-smoke-post-push
  - F-02-sub-export-asymmetry
  - F-03-multi-select-polls
  - F-04-closes-at-tick-cross-procomp
  - F-05-hardcoded-english-aria-label
---

# post-card-01 v0.2.0 shipped

**Tip SHA (pre-C12):** `6b2cc98` (C11). C12 commit lands this decision file + STATUS update + GATE 3 review + meta bump + guide.md extension + demo refresh + dummy-data expansion.

**Final tip:** (this commit) — see git log.

## What v0.2.0 lands

13-commit chain, each commit atomic:

| Commit | Step | Description |
|---|---|---|
| `7b453a3` | C0 | engagement-bar-01 v0.1.2 → v0.2.0 — `LikersStrip` + `ShareMenu` extracted as sub-exports + `EngagementLikerProfile` type. Blocking prerequisite per Q-P37=(a). |
| `70a7a76` | C1 | types.ts — 11 new types + 12 new optional `Post` fields + 26 new label keys + extended `PostCard01Props` + `@deprecated PostLikeUser` alias. |
| `ce99901` | C2 | `lib/permissions.ts` + dual-mode `defaultPostKebabActions` helper. Pure resolver. Legacy mode preserved. |
| `5acd2ff` | C3 | post-card-01.tsx local-mirror extensions (`pollVote`, `sensitiveRevealed`) + 5 new imperative-handle triggers. |
| `aa6c0fa` | C4 | 3 RSC badge parts (pinned, visibility, edited) + PostHeader extended with badges + replyTo sub-line + location chip + F-11 truncation discipline. |
| `d07788e` | C5 | `MentionText` + `TagChips` parts (`"use client"` per re-validation finding). Auto-rendered tag-chip row; opt-in mention highlight via `renderContent` slot. |
| `0503bec` | C6 | `SensitiveGate` overlay with backdrop-blur + reveal button; motion-reduce safe. |
| `fb05bee` | C7 | `LinkPreviewCard` OG-style card; polymorphic root; bare-URL guard. |
| `e97af84` | C8 | `RepostOfCard` nested compact mini-card; recursion-strip + foreground click overlay. |
| `77242a5` | C9 | `PollWidget` with optimistic vote + width-transition results bar + closed-poll state. |
| `8158f8d` | C10 | Responsive sweep — 15 surfaces ramped per §2.1-B step table; kebab touch-target h-8 → h-11 (WCAG 2.5.5); engagement-bar-01 v0.2.0 → v0.2.1 patch (likers-strip column + avatar). |
| `6b2cc98` | C11 | Kebab role-aware wiring — replaced kebabItems useMemo with extended `defaultPostKebabActions` call threading viewerMode + permissions + canPerformAction + 8 mutation handlers + onTranslate. |
| (this) | C12 | Demo refresh — 9 v0.1 tabs preserved + 3 new tabs (Repost / Poll / Sensitive); responsive TabsList. Dummy-data expanded with 9 new fixtures. Meta v0.1.1 → v0.2.0. Guide.md extended with v0.2.0 sections. GATE 3 spotcheck (Pass with follow-ups). STATUS update. |

## Key design locks (Q-D / Q-P)

All 10 Q-D# locks (GATE 1) and 19 Q-P# locks (GATE 2) honored without re-litigation during implementation. Highlights:

- **Q-D1=(b)** — Simple `viewerMode?: "owner" | "viewer"` toggle. No auto-derivation. No moderator tier (slot-driven via `kebabActions`).
- **Q-D2=(c)** — Permissions resolver scoped to post-card-01/lib/permissions.ts.
- **Q-D5=(a)** — Optimistic poll vote with local mirror.
- **Q-D6=(a)** — Per-post sensitive gate (not per-MediaItem).
- **Q-D7=(c)** — Demo refresh: 9 v0.1 tabs preserved in-place + 3 new tabs (Repost / Poll / Sensitive); 12 tabs total.
- **Q-P30** — Nested repost bar suppression via `engagementActions={() => []}` (not new prop).
- **Q-P37=(a)** — engagement-bar-01 v0.2.0 ships FIRST as blocking prerequisite. Done at C0.
- **Q-P42** — Library ships NO visibility picker UI; `onChangeVisibility` is a single trigger.
- **Q-P43** — `PostVisibility` is Facebook-style extensible string union (6 base values + `(string & {})`).

## GATE 3 verdict

**Pass with follow-ups** ([review file](../../docs/procomps/post-card-01-procomp/reviews/2026-05-27-v0.2.0-spotcheck.md)).

5 findings, all sub-Blocker:

| # | Finding | Severity | Bump target |
|---|---------|----------|-------------|
| F-01 | Smoke harness run deferred to post-push | 🔹 Low | (this session, post-push) |
| F-02 | Sub-export asymmetry (SensitiveGate, LinkPreviewCard) | 🔹 Low | v0.2.1 |
| F-03 | Multi-select polls UI not implemented | 🔸 Medium | v0.3 |
| F-04 | `closesAt` doesn't tick — cross-procomp | 🔹 Low | v0.3+ |
| F-05 | Hardcoded English aria-label in RepostOfCard | 🔹 Low | v0.2.1 |

Backwards-compat matrix verified — 7/7 v0.1 patterns compile + render identically.

## What's NOT in this ship (deferred to v0.2.x / v0.3 / separate procomp)

- **post-editor-01 procomp** — sequenced AFTER this GATE 3 closure per Q-D10. Its description ships separately in `docs/procomps/post-editor-01-procomp/` (not yet authored).
- **Multi-select polls** — single-vote UI rendered for both single + multi modes. Promoted to v0.3 with clean local-mirror migration.
- **Sub-export normalization** — `SensitiveGate` + `LinkPreviewCard` not sub-exported; v0.2.1 cleanup.
- **Localized aria-label for RepostOfCard** — v0.2.1.
- **Cross-procomp relative-time tick hook** — v0.3+.

## Verification

- `pnpm tsc --noEmit` clean repo-wide
- `pnpm validate:meta-deps` 49/49 clean
- `pnpm registry:build` clean (post-card-01.json artifact: 21 files; +1 lib/ + 8 new parts vs v0.1.1)
- Smoke harness (F-cross-11 path-b consumer-tsc) — deferred to post-push (F-01 follow-up).

## Net delta

- **post-card-01 only:** +10 files (+1 `lib/permissions.ts` + 9 new parts: pinned-badge, visibility-badge, edited-suffix, mention-text, tag-chips, sensitive-gate, link-preview-card, repost-of-card, poll-widget). +600 LOC est. +12 optional Post fields. +8 mutation handlers. +5 imperative-handle methods. +1 viewerMode toggle. +3 slot props. +4 disable opt-outs. 4 new sub-exports.
- **engagement-bar-01 v0.2.0 (C0):** +2 files moved in (likers-strip + share-menu) + 2 sub-exports + 1 type re-export + minor bump.
- **engagement-bar-01 v0.2.1 (C10):** likers-strip column + avatar step ramps; patch bump.

## Push readiness

- 17 commits unpushed total (12 pre-session + this 5 — actually 13 this session... see git log; pre-session = cms-panel-01 GATE 1 + STATUS slim + tier-charter).
- All 13 v0.2.0 commits are local-only by design — intermediate states have broken-deploy potential if pushed mid-chain (registry.json had v0.2.0 files but meta.ts version was still v0.1.1 between C2 and C12).
- C12 is the version-bump boundary; safe to push now.

## Post-push checklist

1. `git push origin master` — Vercel auto-rebuilds `pnpm vercel-build` (shadcn build + next build).
2. Wait ~2 min for Vercel deploy to complete.
3. `cd e:/tmp/ilinxa-smoke-consumer && pnpm install --frozen-lockfile && node scripts/smoke-all.mjs --slug post-card-01` against the new live endpoint.
4. Record result in `results/2026-05-27-post-card-v0.2.0-smoke.md`.
5. If install + consumer-tsc both pass → F-01 closed; full ship is complete.
6. If either fails → patch commit (v0.2.1) + re-smoke + amend GATE 3 review.
