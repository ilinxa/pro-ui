# ilinxa-ui-pro — Status

> Current snapshot of the project. Update when components ship, statuses change, or non-obvious decisions are made. This is the *now*, not a changelog.
>
> Last updated: **2026-05-06** (**`kanban-board-01` v0.2.0 — additive: `KanbanCardRenderer.dragHandle?: "shell" | "header"`, rich-card adapter pattern wired in the demo, native column scroll.** v0.1 sign-off had marked the component "shipped, not committed"; v0.2 lands the renderer-registry's marquee feature — heterogeneous columns mixing kanban-card / kanban-note / `<RichCard>` with all features intact. **`dragHandle: "header"`** renders a thin grip strip above the item; only that strip activates dnd-kit drag, so the body stays interactive for renderers with internal pointer interactions (`<RichCard>`'s click-to-edit + internal DnD). Default `"shell"` keeps v0.1 behavior — no consumer migration. **Rich-card adapter (demo-side):** `makeRichCardRenderer(updateData)` closes over a host setter; `<RichCard>`'s `onChange` walks the kanban data tree and replaces the matching item's `data`. Demo dataset mixes 3 rich-card items (exercising every predefined key — codearea/image/table/quote/list — plus nested children + `__rcmeta`) with 6 kanban-card + 2 kanban-note items across 4 columns × 2 swimlanes. **Column-body scroll fix:** swapped radix `<ScrollArea>` for native `overflow-y-auto` on a `flex-1 min-h-0` div; added `min-h-0` to the `Column` flex wrapper so flex-shrink semantics propagate. ScrollArea was failing to constrain inside the flex-1 chain when items expanded tall (rich-card with expanded children). `scroll-area` removed from `meta.ts` shadcn deps + `registry.json` `registryDependencies`. **Verification:** `pnpm tsc --noEmit` + `pnpm lint` clean (1 pre-existing rich-card warning, no new). **NOT yet:** committed (still on `968ba97 feat(article-body-01): v0.2`); browser-verified across all column scroll + rich-card drag scenarios. Earlier this session: **`flow-canvas-01` v0.1.0-alpha shipped — n8n-style node-and-edge canvas; `data` category; first xyflow-substrate component in the registry.** Built on `@xyflow/react@12.10.2` (new peer dep, MIT, fully open-source). **Data-first model** — every node is JSON discriminated by a `__type` field; unknown shapes fall back to a built-in `custom-json` renderer; recognized shapes route through a consumer-extendable renderer registry. **Three keystone registries** — `renderers` (`__type → React renderer`), `portTypes` (5 built-ins: `data`/`text`/`image`/`card`/`event` mapped to design tokens), `edgeTypes` (built-in smoothstep with stroke pulled from source-port type color; consumer dispatch deferred to v0.2). **Ports embedded in data, recursively** — `findPortInTree` walker resolves `nodeId:portId` lookups across any tree depth. **Source-vs-canvas-data boundary** — `exportRef.export({ withPorts })` toggles port-strip on save; deliberate `ports: []` is respected on reload. **Drop / paste pipeline** — DOM drag (3 MIMEs: `application/json`, `application/reactflow`, `text/plain`), clipboard `Cmd/Ctrl-V`, file drop; default port inflation at the drop boundary; `onBeforeDrop` interceptor. **Typed connection validation** — `connectionMode: Strict` + same-`type` + opposite-direction + `multi: false`-not-already-wired enforced at drag time; `onBeforeConnect` consumer chain for semantic validation. **Sub-object drag-extract** — `emitSubObjectDrag` helper + `data-draggable-subobject` convention; default copy, Alt-drag = move (parent loses sub-object via `onNodeUpdate`); `onSubObjectExtract` callback. **Right-click menus** — three contexts (canvas / node / edge) via shadcn `<ContextMenu>` (newly added at `src/components/ui/context-menu.tsx`), built-in items + `menuItems.{canvas,node,edge}` consumer extensions, `Extract <path>` keyboard fallback for sub-object extract (Q10), `readOnly` hides mutation items. **Performance** — every component memoized; `nodeTypes`/`edgeTypes` at module scope; single `'ilinxa-node'`/`'ilinxa-edge'` xyflow type with internal dispatch (the canonical xyflow-react-pro footgun fix); `onlyRenderVisibleElements` toggle exposed; 200-node `makeStressData` fixture. **Theming** — `--xy-controls-*` / `--xy-edge-stroke` / `--xy-handle-*` overrides in `globals.css` follow design tokens light + dark (slice of M7). **Demo** — single tabbed demo covering all five description-§5 archetypes: Workflow (LLM agent + ProjectCard adapter + sub-object extract), Read-only viewer, Custom JSON only, Stress (200 nodes). **39 files** — 1 root + 14 parts + 7 hooks + 4 registries + 7 lib + 6 fixed (types / dummy / demo / usage / meta / index); all 39 ship via registry except `demo.tsx` / `usage.tsx` / `meta.ts` (docs-only) per the locked convention. **Three-stage planning workflow** honored: description signed off after data-first reframe (description Q1–Q24); plan re-validation caught 8 substantive issues (selected-strip both nodes+edges, default port precedence, WeakSet over-engineering, sonner lock, etc.) — all fixed pre-implementation; guide.md authored alongside implementation, finalized at v0.1.0. **Plan-stage decisions added beyond description (16 items in plan appendix):** single xyflow node type `'ilinxa-node'`, `application/json` MIME (not `'application/reactflow'`), schema collision = last-wins-+-warn, no internal Zustand (xyflow's store is sufficient), `<PortsAt>` as the easy-case helper, sonner toast deferred (console.warn for parse errors), `aria-label` defaults to "Flow canvas", live-region a11y for connection events, `Backspace`+`Delete` key bindings, etc. **`globals.css` change:** added `.react-flow { --xy-* overrides }` block (~15 lines) for design-token theming + selected-edge stroke-width; CSS scoped to `.dark` not needed because we use the non-default xyflow variable names. **Two new shadcn primitives in repo:** `context-menu.tsx` (hand-written, mirrors `dropdown-menu.tsx` pattern). Available for any future component. **Attribution note (corrected 2026-05-08):** `proOptions: { hideAttribution: true }` is set. **xyflow (`@xyflow/react`) is MIT-licensed**, so this is fully permitted by the license — no Pro subscription is legally required. The xyflow team requests an honor-system "please consider Pro if you hide the badge" as community etiquette and a way to support the project; if flow-canvas-01 ships in a commercial deployment, consider supporting xyflow via their Pro subscription. Earlier framing of this as "technically requires xyflow Pro license" was wrong and has been corrected here. **Ready for first-consumer testing**; v0.2 candidates listed in description §2 out-of-scope (custom edge dispatch, undo/redo, marquee, minimap, DB-ref nodes, execution-state animation).)

## Components

| Slug | Category | Status | Version | Notes |
|------|----------|--------|---------|-------|
| `data-table` | data | alpha | 0.1.0 | Canonical template — generic, typed columns, no client state. Future tables compose on it. |
| `rich-card` | data | **beta** | 0.4.0 | JSON-driven recursive card-tree viewer + full structural editor + safety net. **Bumped to beta 2026-04-28** — feature-complete for current scope (v0.1–v0.4 all shipped), API stable, suitable for real use. Stable (1.0.0) gated on external consumer adoption + 30-day no-break window. Core surface: editing, drag-drop, bulk multi-select, permissions, custom predefined keys, virtualization, native search, validation hooks, undo/redo. v0.5 markdown adapter remains deferred indefinitely as a separate companion. Drag-drop reordering via `@dnd-kit` (2 scopes), bulk multi-select (shift-click + cmd-click + bulk toolbar), per-level/per-card/per-key permission matrix with predicate escape hatches, custom predefined-key registration, opt-in virtualization (`virtualize: 'auto'` enables at 500+ nodes), inline meta editing with custom renderers + audit trail, root-removal opt-in, promote-on-delete with configurable collision strategy, native data-model search (finds collapsed/virtualized/meta — auto-expands path). 12 v0.3 reducer actions, 5 v0.3 event types, 13 imperative handle methods, ~46 optional props. **One intentional breaking change in v0.3:** `onSelectionChange` signature shifts from `(id: string \| null) => void` to `(ids: readonly string[]) => void`. **Markdown intentionally dropped from rich-card itself.** Deps: `popover` + `separator` shadcn primitives, `lucide-react`, `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` + `@tanstack/react-virtual`. ~50 files. |
| `workspace` | layout | alpha | 0.1.0 | Splittable canvas: corner-drag split/merge, edge-drag resize, registry-driven content, responsive collapse + per-breakpoint depth cap. Largest component in the registry — 26 files, full procomp gate (description → plan → guide). |
| `properties-form` | forms | alpha | 0.1.0 | First Tier 1 graph-system pro-component shipped. Schema-driven controlled read/edit form: 6 built-in field types (string/number/boolean/date/select/textarea), three-state permissions (editable/read-only/hidden) with layered resolver (host predicate → declarative → default), sync per-field + form-level validation with first-error focus, counter-based dirty tracking, async onSubmit with 200ms-delayed spinner + aria-busy, custom-renderer slot via `field.renderer`, React 19 ref-as-prop imperative handle (isDirty/markClean/reset/focusField/submit). 25 files. Demo ships 5 sub-demos (read / edit+submit / mixed permissions / validation / custom tags renderer). Built on shadcn primitives Input/Select/Switch/Textarea/Tooltip/Button + Tabs/Badge for demo. ARIA-complete (label/required/invalid/describedby/error summary). Bundle ≤30KB envelope (component code only — primitives are shared infrastructure). **Verification:** `pnpm tsc --noEmit` + `pnpm lint` + `pnpm build` clean; SSR of `/components/properties-form` returns 200 with all 5 demos + dummy task data; `/components` index lists the new entry. **Not yet validated:** browser interactivity (hydration, tab switching, blur-driven error visibility, spinner timing, permission-tooltip hover/focus, custom-renderer keyboard) — no test runner wired in this repo, demo-driven manual verification still pending. |
| `detail-panel` | feedback | alpha | 0.1.0 | Second Tier 1 graph-system pro-component. Selection-aware compound container: `<DetailPanel.Header>` / `.Body` / `.Actions` via React Context, composite re-key on `${type}:${id}` change (slotted forms remount cleanly without state bleed), three mode configurations (controlled / uncontrolled / locked dev-warn), sticky header (top:0) + sticky footer-Actions (bottom:0), header-positioned actions opt-in, lifecycle precedence error > loading > content > empty, render-fn Actions context `{ mode, setMode, canEdit }`, layout-mirroring skeleton, error UI with optional retry, exported `<DetailPanelEmptyState>` for composition, focus management (selection→panel root, mode→edit→body, exit→trigger restore by id), generic "Selection changed" aria-live announcements, React 19 ref-as-prop handle (focusBody/resetMode). 18 files. Demo ships 7 sub-demos (empty / read / mode toggle / loading / error / selection switcher / custom empty). Built on shadcn primitives Button + Skeleton + Tabs/Badge. **Paired with properties-form, FULLY unblocks `force-graph` v0.3 implementation gate.** Bundle ≤8KB envelope. **Verification:** `pnpm tsc --noEmit` + `pnpm lint` + `pnpm build` clean; SSR of `/components/detail-panel` returns 200 (~80KB) with all 7 tab triggers + the empty-state default rendered; `/components` index lists the new entry. **Not yet validated:** browser interactivity (hydration, tab switching, mode toggle + focus restoration, sticky positioning under display:contents wrapper, selection-change announcements, dev-warn console output for locked mode) — no test runner wired in this repo, demo-driven manual verification still pending. |
| `filter-stack` | forms | alpha | 0.1.0 | Third Tier 1 graph-system pro-component. Schema-driven controlled filter panel: 4 built-in filter types (checkbox-list / toggle / text / custom), AND-across-categories composition with host-defined within-category semantics, mode toggle (Union/Intersection) on checkbox-list via shadcn `ToggleGroup`, per-option solo button with `Tooltip`, debounced text input (default 250ms) with flush-on-blur + ESC-clears, custom-render slot with error boundary + `fieldId` for label-association, per-category clear button + global clear-all in footer, dev-only schema validation (reserved `__mode` suffix + duplicate id + empty options + defaultMode-without-modeToggle), categories-reference-instability dev warning (>5 successive unstable renders), React 19 ref-as-prop imperative handle (clearAll/clear/isEmpty). 21 files. Demo ships 5 sub-demos (basic facets / mode toggle / solo buttons / custom range / all combined) — each demo + filtered-list pair shares an 8-node graph fixture. Built on shadcn primitives Button/Checkbox/Input/Switch/Toggle/ToggleGroup/Tooltip + Tabs/Badge for demo. **Unblocks `force-graph` v0.4 implementation gate.** Bundle ≤12KB envelope. **Verification:** `pnpm tsc --noEmit` + `pnpm lint` + `pnpm build` clean; SSR of `/components/filter-stack` returns 200 (~94KB) with all 5 tab triggers + the basic-facets default content (Pinned only, Search by name, Rina Okafor); `/components` index lists the new entry. **Not yet validated:** browser interactivity (hydration, tab switching, Checkbox / Switch / ToggleGroup keyboard nav, debounce timing + ESC-clears, solo-button hover-reveal + Tooltip, schema-instability dev-warn after 6 successive unstable renders, error-boundary fallback for crashing custom render). |
| `entity-picker` | forms | alpha | 0.1.0 | Fourth Tier 1 graph-system pro-component. Searchable typed picker: single OR multi mode via `mode` prop with mode-aware `value` typing through TypeScript function overloads, shadcn Command (cmdk) for search + Radix Popover for dropdown, kind badges via `kinds: Record<string, KindMeta>` map (OKLCH color literals), 3 custom-render slots (`renderItem` / `renderTrigger` / `renderEmpty`), multi-mode chip cluster with per-chip remove buttons + Backspace-on-empty-search-removes-last-chip, controlled-or-uncontrolled open state mirroring Radix convention, id-set selection equality (onChange fires only when ids change; survives upstream entity re-derivation), trigger as `<div role="button">` (NOT `<button>`) to allow nested chip remove buttons, `id?: string` for properties-form `<label htmlFor>` integration, React 19 ref-as-prop imperative handle (focus / open / close / clear). 18 files. Demo ships 8 sub-demos (single / multi / no kinds / custom match / custom item / custom empty / custom trigger / imperative handle) over an 18-node graph fixture + 8-user fixture. Built on shadcn primitives Badge/Button/Command/Popover + Tabs for demo (cmdk peer dep added at install time). Bundle ≤8KB envelope. **Verification:** `pnpm tsc --noEmit` + `pnpm lint` + `pnpm build` clean (function overloads + generics narrow correctly — Phase A end-gate smoke test passes); SSR of `/components/entity-picker` returns 200 (~81KB) with all 8 tab triggers + Single demo default content rendered (Pick a graph node + Search nodes…); `/components` index lists the new entry. **Not yet validated:** browser interactivity (hydration, popover open/close, cmdk filter + keyboard nav, chip remove + Backspace-on-empty, custom render slots, dev-warn for duplicate ids). Composed inside force-graph from v0.3 onward (linking-mode UI) + v0.4 (group-membership editor). |
| `page-hero-news-01` | marketing | alpha | 0.1.0 | Full-bleed gradient hero band — badge / title / titleHighlight / description / children slot. **Drops framer-motion** (~100KB peer dep) in favor of the existing pro-ui `reveal-up` CSS keyframe with 60ms staggered animation delays per the project's Motion mandate. Inherits `prefers-reduced-motion` respect for free via the keyframe's existing media query. 8 files (1 root + 1 part `HeroStats` + types + dummy + demo + usage + meta + index). 3 density levels (compact 40vh / default 70vh / full 100vh). `titleSlot?: ReactNode` for rich title rendering; default uses `title: string` + `titleHighlight: string` (block-rendered span in accent color). Optional Lucide-style `badgeIcon` accepted as `ComponentType<{ className?: string }>`. Bottom SVG wave fills with `--background` via Tailwind `fill-background` utility (resolves through @theme). `HeroStats` sub-component exported separately for the icon-circle + value + label triplet pattern (kasder NewsHero stats row productized). `aria-labelledby` wires section landmark to title id (computed via useId). Heading semantic level configurable via `headingAs` (h1/h2/h3). `disableReveal` prop for SSR-only contexts. Demo ships 5 sub-tabs (default / with HeroStats / CTA cluster / density variations / localized Turkish content). Migration origin: kasder commons/PageHero.tsx (engine) + sections/news/NewsHero.tsx (news shell). NewsHero.tsx becomes a demo asset only. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new). |
| `grid-layout-news-01` | layout | alpha | 0.1.0 | **Layout assembly for the news-domain family.** Slot-based magazine layout — optional hero / filter bar / sidebar + a generic main-column tower (1 large + 2-up + 3-up medium grid) + IntersectionObserver-driven infinite scroll. **Generic over `T`** (item type) via `<GridLayoutNews01<T>>`; renders cards via `renderItem(item, slot)` callback so the layout itself imports nothing from sibling registry components — sealed-folder convention preserved. 11 files (1 root + 2 parts: magazine-tower + infinite-loader + 2 hooks: use-infinite-scroll + use-magazine-filter + types + dummy + demo + usage + meta + index). Companion **`useMagazineFilter` hook** for in-memory filter + sort + page state with optional `simulatedLoadingMs` (the simple-consumer ergonomic; sophisticated consumers skip and drive props from React Query / their router directly). Featured-only-on-page-1 semantics via `featuredItem` prop (hook auto-clears on page > 1). Sticky sidebar via `<aside>` with `sticky top-24`. Bouncing-3-dots loader with `aria-live='polite'` and visually-hidden text label. End-of-list announcement also `aria-live='polite'`. `emptyState?: ReactNode` slot OR fallback to `labels.emptyStateText`. `renderFeatured` for distinct featured-card rendering vs. in-tower large. Demo ships 3 sub-tabs (slot composition with inlined demo cards mimicking content-card-news-01 visual + filter bar + sidebar; bare layout no slots; empty state) — demo uses inline cards rather than cross-importing sibling registry components per pro-ui demo convention. Migration origin: kasder NewsMagazineGrid.tsx (~320-line monolith) → distilled to ~120-line layout shell + 60-line filter hook; the original's filter / chip / search / date / sidebar logic moved into `filter-bar-01` + `category-cloud-01` + `newsletter-card-01` siblings. **Verification:** tsc + lint + build clean. **News-domain family COMPLETE** — 6 components shipped (this + page-hero-news-01 + filter-bar-01 + category-cloud-01 + newsletter-card-01 + content-card-news-01); detail-page-news-01 deferred per user. |
| `newsletter-card-01` | marketing | alpha | 0.1.0 | **First `marketing` category entry.** Brand-tinted CTA card with email signup. 2 variants (`inline-form` — email input + Subscribe button, the magazine-grid sidebar pattern; `cta-only` — full-width button only, the news-detail-page sidebar pattern). 3 tones via `tone` prop (primary / accent / muted). Controlled-or-uncontrolled email value (mirrors React input convention). Controlled-or-derived status — pass `status` to drive externally OR return a Promise from `onSubmit` for auto-tracking idle → pending → success/error. 9 files (1 root + 4 parts: inline-form, cta-only, tone resolver, status-message + types + dummy + demo + usage + meta + index). Migration origin: kasder `NewsMagazineGrid.tsx` sidebar + `(platform)/news/[id]/page.tsx` sidebar. Demo ships 5 sub-tabs (inline-form / cta-only / custom labels / error state / tones grid) with fake submit handlers in dummy-data. **a11y:** `<form>` for Enter-submit; input `aria-label`; status `aria-live=polite` (success) + `role=alert` (error); button `aria-busy` during pending; configurable heading level (h2/h3/h4). **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new). Composes inside `grid-layout-news-01` sidebar + `detail-page-news-01` sidebar (deferred). |
| `category-cloud-01` | forms | alpha | 0.1.0 | Always-visible flex-wrap of clickable category chips with optional inline counts. Single-select, controlled-or-uncontrolled, toggleable (re-click clears). Generic over category items via `string[]` shorthand or full `CategoryCloudItem[]` shape. 8 files (1 root + 1 part `category-chip` + types + dummy + demo + usage + meta + index). Migration origin: kasder `NewsMagazineGrid.tsx` sidebar Categories block. **vs `entity-picker`:** entity-picker = popover-driven select with cmdk search; category-cloud = always-visible flex-wrap with counts. Different ergonomics, different use cases — real new component. **a11y:** native `<button>` per chip with `aria-pressed`; `role=group` on container with auto-derived `aria-label` (from title); focus-visible ring per chip. Heading level configurable (h2/h3/h4). Demo ships 4 sub-tabs (basic / with counts / controlled / localized). **Verification:** tsc + lint + build clean. |
| `filter-bar-01` | forms | alpha | 0.1.0 | Composite filter bar — centered search + category pill row + date-range Popover + optional results count. Each sub-control independently controlled-or-uncontrolled (own value/defaultValue/onChange triplet). Combined `onChange` emits `{search, category, dateRange}` on any change. Sub-control hide flags (`hideSearch` / `hideCategories` / `hideDateRange`). Internal 250ms debounce on search in uncontrolled mode (controlled mode bypasses). 11 files (1 root + 3 parts: search-input + chip-row + date-range-picker + 1 lib format-default + types + dummy + demo + usage + meta + index). Migration origin: kasder `NewsMagazineGrid.tsx` lines 128-200 (header section). **Newly installed shadcn primitive:** `Calendar` (first user; transitively pulls `react-day-picker` + `date-fns`). Date formatting via native `Intl.DateTimeFormat` (no direct date-fns dep in component code). **a11y:** `<div role=search>` landmark; `<div role=group>` chip row; native `<button aria-pressed>` per chip; `aria-live=polite` results count; `aria-label` on clear-date X. Demo ships 4 sub-tabs (basic / controlled with JSON state display / partial usage / localized with TR labels + Intl-tr-TR formatter). **Lint deviation:** initial implementation had a `setState` in a `useEffect` for syncing controlled value → local state in `parts/search-input.tsx`; flagged by React Compiler-aware lint (`react-hooks/set-state-in-effect`). Fix: dropped the sync effect — local state is only used in uncontrolled mode where it's seeded from initial `value` and never needs re-sync. Single-source-of-truth: prop in controlled, local in uncontrolled. **Verification:** tsc + lint + build clean. |
| `content-card-news-01` | data | alpha | 0.1.0 | **First migration-pipeline ship.** Magazine-style content card with 5 visual variants (`featured` / `large` / `medium` / `small` / `list`) dispatched via single `variant` prop. Migration origin: kasder `kas-social-front-v0` `NewsCard.tsx` (preserved: 5-variant set, serif typography, category-color map, gradient overlay on featured, eye-chip on medium, kicker footer; rewrote: next/link → polymorphic `linkComponent` slot, hardcoded URL → `href` prop, Turkish strings → English defaults + `labels` object, `tr-TR` locale → `formatRelativeTime`/`formatDate` callbacks, `categoryColors` → `categoryStyles` prop, NewsType → strict ContentCardItem with soft-fail optional fields, single Link wrap → overlay-link pattern, no React.memo → memoized, 5 inline `if` blocks → 5 sealed `parts/<variant>.tsx`). 14 files (1 root + 5 parts + 1 hook + 1 lib + 4 sealed standard + meta + index + types). **Pulled forward to v0.1 per user (was deferred to v0.2): overlay-link pattern** — link `absolute inset-0 z-0` covers whole article; `actions` slot at `relative z-10` for nested interactives (bookmark/share); accessible name via `aria-labelledby={titleId}` (computed via `useId`); focus-visible ring covers full card via `:has(a:focus-visible)`. **New pro-ui-wide `--font-serif` CSS token** added to [globals.css](../src/app/globals.css#L13) defaulting to Playfair Display (loaded via `next/font/google` in [layout.tsx](../src/app/layout.tsx)) with `latin` + `latin-ext` subsets (covers Turkish chars). Consumer override at any DOM scope via `--font-serif: <other>`; per-card override via `titleClassName`. Featured-variant badge wears `bg-black/40 backdrop-blur-sm` chip wrapper for legibility on the dark gradient overlay (single-source-of-truth `categoryStyles` preserved; layout decides backdrop). Demo ships 7 sub-tabs (5 variants + Composed magazine layout + Actions slot demo with bookmark+share buttons proving overlay-link pattern). Built on shadcn primitives Badge + Button + Tabs + lucide-react icons (Calendar / Clock / User / ArrowRight / Eye / Bookmark / BookmarkCheck / Share2). Polymorphic root via `linkComponent: ElementType` (defaults to `'a'`; consumers pass `next/link` / RemixLink / etc). `motion-safe:` prefix on all transitions; reduced-motion users see static cards. RTL aware via `rtl:rotate-180` on chevron + arrow. Migration tsconfig exclude: `docs/migrations/**/original/**` (reference files don't compile here — they import kasder paths). ESLint rule override: `@next/next/no-img-element` disabled for `src/registry/components/**` (registry mandate is no `next/*`; native `<img>` is correct, consumers can wrap in their CDN component via `imageClassName`). Cleaned 2 redundant per-line `// eslint-disable-next-line @next/next/no-img-element` directives in rich-card parts (now covered by the rule override). **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning about TanStack Virtual / React Compiler — no new); `pnpm build` clean (compiled 20.6s + 7.3s tsc; all 17 routes prerendered including `/components/content-card-news-01`); SSR `HTTP 200` (~75KB) with all 7 tab triggers rendered (Featured / Large / Medium / Small / List / Composed / Actions slot). Registry artifacts at [public/r/content-card-news-01.json](../public/r/content-card-news-01.json) + sibling fixtures item; namespaced install: `pnpm dlx shadcn@latest add @ilinxa/content-card-news-01`. Procomp docs at [docs/procomps/content-card-news-01-procomp/](../docs/procomps/content-card-news-01-procomp/) — description + plan + guide all present. **Not yet validated in browser:** hydration, image hover scale + arrow translate (motion-safe gating), focus-visible ring on full card via `:has(a:focus-visible)` (CSS pseudo-class), `actions` slot click stopping propagation correctly, dark-mode rendering, `--font-serif` runtime override at section scope, RTL flip of chevron + arrow icons. Sibling migrations `page-hero-news-01` + `grid-layout-news-01` intakes drafted but not yet implemented; `detail-page-news-01` deferred per user. |
| `author-card-01` | marketing | alpha | 0.1.0 | **Card-framed person identity block.** Avatar (image OR icon fallback) + name + role + optional bio. Optionally clickable as a whole via polymorphic root + `href` + `linkComponent` slot (default `"a"`). 8 files (1 root + 2 parts: avatar + tone-resolver + types + dummy + demo + usage + meta + index). 3 tones (`primary` / `accent` / `muted`) — only affects the icon-fallback path; image variant renders as-is. Custom fallback icon via `fallbackIcon` prop (default `User` from lucide-react; e.g. `Users` for collectives). Configurable heading level (h2 / h3 / h4; default h3). i18n via `labels.heading` (default `"About the author"`). Image uses `loading="lazy"`. Demo ships 5 sub-tabs (default no-image / with image / clickable / muted tone with custom icon / Turkish content). Migration origin: kasder `kas-social-front-v0` `src/app/(platform)/news/[id]/page.tsx` lines 191–205 (Author Card sidebar block). **a11y:** clickable variant uses `aria-labelledby={nameId}` (computed via `useId`) so the accessible name is the author's name, not the heading; decorative icon `aria-hidden="true"`; image carries `alt` with `name` fallback; `focus-visible` ring renders only on link variant. **Slug convention:** no `-news-` infix — universal pattern (blogs, team pages, contributor cards, comment headers, doc page authors). **Decision: stayed with raw div + img + lucide icon, no shadcn Avatar.** Radix Avatar's fallback semantics differ from kasder visual (10% tint circle + full-saturation icon); replicating that on top of Radix requires fighting its primitives. Lower dep count; matches design DNA exactly. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 24 routes prerendered. Composes in same sidebar with `thumb-list-01` + `newsletter-card-01` — all share card chrome (`rounded-2xl p-6 border border-border/50`) + heading rhythm (`text-lg font-serif font-bold`). |
| `thumb-list-01` | data | alpha | 0.1.0 | **Linked thumbnail-list block.** Header (icon + title) + vertical stack of items, each with thumbnail + title + meta line + optional link wrap. 9 files (1 root + 2 parts: thumb-row + empty-state + types + dummy + demo + usage + meta + index). Fixed `ThumbListItem` shape (`{ id, title, imageSrc, imageAlt?, meta?, href? }`) — explicit and ergonomic over generic accessor pattern. Frame toggle (`framed: true` card-style default; `framed: false` borderless inline for dropdowns/modals). Polymorphic per-item link via `linkComponent` slot (default `"a"`). Custom meta render slot (`renderMeta(item)`) for dates / badges / scores; default renders `item.meta` as `<p>`. Custom or default header icon (default `BookOpen`; pass `null` to hide). Empty state: `emptyState` ReactNode wins; otherwise `labels.emptyText` fallback with `role="status" aria-live="polite"`. Demo ships 5 sub-tabs (default / no-frame / custom render-meta with `Intl.RelativeTimeFormat` / empty state / Turkish + custom icon). Migration origin: kasder `kas-social-front-v0` `src/app/(platform)/news/[id]/page.tsx` lines 208–234 (Related News sidebar block). **a11y:** `<ul>` / `<li>` semantics; per-item link with title-composed accessible name; `group-hover:text-primary` + `group-focus-visible:text-primary` for keyboard parity; decorative header icon `aria-hidden="true"`; lazy-loaded thumbnails. **Plan refinement during implementation:** swapped `<h4>` per-row title to `<p className="font-medium">` — heading inside list item creates landmark-order pitfalls when consumers pass `headingAs="h2"`. Headings are reserved for the section header; per-row text is just styled `<p>`. **Slug convention:** no `-news-` infix — universal pattern (related/popular/recent posts, search-suggestion dropdowns, "up next" media queues, file-picker recents, "more from author" rails, saved-items lists). **Decision: stayed with native `<img>` instead of `next/image`** — registry mandate forbids `next/*`. ESLint rule override (`@next/next/no-img-element` disabled for `src/registry/components/**`) handles the warning. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 24 routes prerendered. Together with `author-card-01` + `newsletter-card-01`, completes the clip-and-compose news-sidebar set. |
| `article-meta-01` | data | alpha | 0.1.0 | **Smallest pro-comp shipped (~2.5KB).** Horizontal flex-wrap strip of icon + value pairs for article headers / blog bylines / doc page metadata / video player meta lines. Data-driven `items` array (each: `{ id, icon?, value, href?, ariaLabel? }`). Polymorphic per-item link via `linkComponent` (default `"a"`). Optional bottom divider (`pb-8 border-b border-border`). 3 alignments (start / center / end). Tunable `gapClass` (default `gap-6`). Items can omit `icon` for text-only strips. 8 files (1 root + 1 part `meta-item` + types + dummy + demo + usage + meta + index). Demo ships 5 sub-tabs (default with divider / centered / clickable byline / text-only / tight video meta). Migration origin: kasder `kas-social-front-v0` `src/app/(platform)/news/[id]/page.tsx` lines 89–106. **a11y:** `<ul role="list">` (Safari workaround for `list-style: none`); decorative icons `aria-hidden="true"`; optional `ariaLabel` per item composes the link's accessible name when linked OR wraps unlinked content in a labeled `<span>`; `focus-visible:ring-ring` on linked items only. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 26 routes prerendered. |
| `article-body-01` | data | alpha | 0.2.0 | **First Plate-based pro-comp in pro-ui — bumped to v0.2 (2026-05-02).** v0.2 layered 4 features on top of v0.1: (1) **lowlight syntax highlighting** — selective registration of 15 languages (bash / css / diff / go / html / java / javascript / json / markdown / python / rust / shell / sql / typescript / xml / yaml) via new `lib/lowlight-instance.ts`; CodeBlockPlugin configured with `options: { lowlight }` in BOTH editor + viewer kits; token CSS classes (`.hljs-keyword`, `.hljs-string`, etc) added to `src/app/globals.css` mapped to `--chart-1` … `--chart-5` (analogous lime→blue chain) — chose own minimal token theme over importing a stock highlight.js theme for design-system fit + lower CSS weight. (2) **HTML serialization escape hatch** — `serializeArticleBodyToHtml(value, options?)` async helper in new `lib/serialize-html.ts` wrapping `serializeHtml(editor)` from `platejs/static`; defaults to `stripDataAttributes: true` for clean export output; composes the same `articleBodyViewerPlugins` as the static viewer for renderer-pipeline parity; exported from index.ts as the public API. (3) **Image resize + inline caption editor** — extracted to new `parts/image-element.tsx`; right-edge resize handle hover-revealed via `group/img` + `group-hover:opacity-100`; mouse-tracking drag commits width as `${pct}%` via `editor.tf.setNodes({ width }, { at: path })`; caption editing via native `<input>` controlled by `element.caption`; read-only mode hides handle + renders caption as `<figcaption>`. **Did NOT use** `@platejs/resizable`'s Resizable/ResizableProvider/ResizeHandle UI primitives — sparse docs, complex API requiring undocumented `setNodeWidth`/`setWidth` callbacks; hand-rolled mouse-tracking is ~50 LOC and easier to reason about. **Registered CaptionPlugin** for forward compat (caption-required validation hooks etc) but caption rendering uses inline-input pattern, not `<CaptionTextarea>`. Static side: `StaticImage` reads `element.width` (string-as-percent or number-as-pixels) and `element.caption` for fidelity. (4) **Floating toolbar** — new `parts/floating-toolbar.tsx` using `useEditorSelection()` + `@floating-ui/react`'s `useFloating({ middleware: [offset, flip, shift], whileElementsMounted: autoUpdate })` with virtual-element reference (`getBoundingClientRect: () => window.getSelection().getRangeAt(0).getBoundingClientRect()`). Visibility tracked via `useState`, updates on selection change + scroll/resize listeners. Refactored shared `MarkButton`/`BlockButton`/`useBlockToggle` from `parts/editor-toolbar.tsx` into new `parts/toolbar-buttons.tsx` so fixed + floating toolbars consume the same primitives; floating uses `size="sm"` variant. Two intentional eslint-disable suppressions documented inline: (a) `react-hooks/set-state-in-effect` on the early `setVisible(false)` — genuine external-system sync, the rule's escape-hatch case; (b) `react-hooks/refs` on `<div ref={refs.setFloating}>` — `setFloating` is a stable ref-callback, not a `.current` property access. **5 new files + 9 modified.** New deps (all MIT): `lowlight@^3.3.0`, `highlight.js@^11.11.1`, `@platejs/caption@^53.0.0`, `@platejs/resizable@^53.0.0` (registered for future use), `@floating-ui/react@^0.27.19`. Editor bundle estimate: ~150KB → ~165KB gzip after additions; viewer ~30KB → ~32KB. Demo grew from 5 → 8 sub-tabs: Editor / Viewer / Edit↔View / **Code (syntax)** (3 fixtures: javascript / typescript / bash) / **Image (resize+caption)** (Helsinki waterfront fixture at 75% width) / **HTML export** (live `serializeArticleBodyToHtml` output via `useEffect` async pattern) / Empty / JSON. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 27 routes prerendered; SSR HTTP 200 (~104KB) on default editor tab — inactive tabs lazy-mount per Radix Tabs default, so lowlight server-side render not directly verifiable without changing the default tab; build success + plugin set type-checks both ways = lowlight wiring correct, runtime visual verification deferred to in-browser smoke. **PRIOR v0.1:** First Plate-based pro-comp in pro-ui. Plate v53.0.3 substrate. Two exports from one sealed folder: `<ArticleBodyEditor>` (~150KB gzip, "use client") + `<ArticleBodyViewer>` (~30KB gzip, RSC-friendly via `platejs/static`). Both consume the same `ArticleBodyValue` (Plate `Value`) shape. **Storage format: JSON, not HTML** (Plate docs warn against HTML round-trips). 12 files (1 editor root + 1 viewer + 2 plugin kits: editor-kit + viewer-kit + 3 parts: editor-toolbar + element-renderers + leaf-renderers + 1 static-elements + types + dummy + demo + usage + meta + index). Plugin set: ParagraphPlugin + H1–H4 + Blockquote + HorizontalRule + Lists (bullet/ordered) + CodeBlock + CodeLine + CodeSyntax + Tables (4 plugins) + Image + MediaEmbed + Link + Bold/Italic/Underline/Strikethrough/Code/Highlight/Sub/Sup/Kbd + FontFamily/FontSize/FontColor/FontBackgroundColor + Indent. Toolbar v1: marks (8 buttons) + headings (H1/H2/H3) + blockquote + code-block + HR + bullet/ordered lists + link + image + table + font-family `<select>` + font-size `<select>` + text color `<input type="color">`. **Image upload contract:** prop `onImageUpload?: (file: File) => Promise<{src; alt?; width?; height?}>` — bring-your-own backend; URL prompt fallback when not provided. **Save shortcut:** Cmd/Ctrl+S → `onSave(currentValue)` from live `editor.children` (NOT React `value` prop — avoids batching staleness). Platform-aware key descriptor in footer. **Echo-guarded value sync** via `useRef` (NOT `useState` — react-hooks/set-state-in-effect lint). Greenfield component (no migration origin — kasder uses a markdown-text-renderer, different paradigm). **Phase 0 spike validated SSR:** `/components/article-body-01` prerenders `HTTP 200` (~94KB) with all 5 demo tabs (Editor / Viewer / Edit↔View / Empty / JSON output) — content "How sustainable cities…" appears server-rendered, proving PlateStatic works in RSC. **Plate quirks discovered + handled:** (1) `<PlateElement as="X">` discriminated-union typing fights TS — solved with central `withAs<As>(props, as)` helper that casts `attributes` via `never`; (2) `editor.tf.unsetMark` doesn't exist in v53 — used `removeMark(key)` for font-family / font-size clear; (3) static viewer needed parallel renderers — `static-elements/static-element-renderers.tsx` renders bare HTML with `props.attributes` spread (strips `ref` which TS can't narrow across element types); (4) `useMarkToolbarButton` + `useMarkToolbarButtonState` are first-party Plate hooks — used for mark toggle UI; (5) toolbar buttons use `onMouseDown` to preserve editor selection on click. **Decisions:** (a) toolbar built from existing pro-ui shadcn primitives (Button) + raw HTML selectors — chose this over Plate's UI shadcn-registry to keep dep count low and stay pro-ui-native; v0.2 candidate to upgrade selectors to shadcn DropdownMenu wrappers; (b) syntax highlighting NOT wired in v0.1 — `CodeSyntaxPlugin` registered but `lowlight` not installed; code blocks render unstyled; (c) toolbar exposes H1–H3 only (plugin set supports H1–H4); (d) tables insert-only via toolbar (3×3 default); row/col ops are keyboard-driven via Plate's table plugin; (e) selectors are native HTML (`<select>` + `<input type="color">`) — cheaper than Popover dropdown for v0.1. Demo: 5 sub-tabs (Editor with rich content + onSave handler / Viewer-only / Edit↔View live roundtrip / Empty starting state / JSON output side-by-side with code-block + table fixtures). **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 27 routes prerendered. **Architectural decision locked in memory** ([project_richtext_substrate.md](../C:\Users\AsiaData\.claude\projects\e--2026-ilinxaDOC-ilinxa-ui-pro\memory\project_richtext_substrate.md)): Plate is pro-ui's WYSIWYG substrate; future rich-content components compose on Plate. CodeMirror 6 stays as source-text editor for `markdown-editor`. JSON-as-storage convention. Editor + Viewer split convention (every Plate-based component ships TWO exports from one folder). **Not yet validated in browser:** hydration, toolbar button clicks, mark toggles, block toggles, image-upload flow (the demo uses a fake uploader returning object URLs), Cmd/Ctrl+S, table editing, font selectors. **v0.2 candidates:** `lowlight` for code blocks, floating toolbar (`@platejs/floating-toolbar`), slash-command menu (`@platejs/slash-command`), DnD reordering (`@platejs/dnd`), mention autocomplete (`@platejs/mention`), math (`@platejs/math`), comments + suggestions (`@platejs/comment` + `@platejs/suggestion`), image resizing (`@platejs/resizable`), HTML serialization escape hatch via `serializeHtml`, markdown autoformat (`@platejs/autoformat`). |
| `share-bar-01` | marketing | alpha | 0.1.0 | **Real, working share strip with 9 built-in platforms.** Horizontal cluster of social-share buttons + copy-link with success-state feedback. Built-in URL templates for Twitter / Facebook / LinkedIn / Reddit / WhatsApp / Telegram / Email / Threads / Bluesky in pure `parts/templates.ts` module. Copy-link button uses `navigator.clipboard.writeText` with `document.execCommand("copy")` fallback for older / insecure-context browsers; success/error feedback is visual (icon flips to `Check` tinted `text-primary` for 2s, or `X` tinted `text-destructive` for errors) + audible (`aria-live="polite"` for success, `role="alert"` for errors). Custom targets via `kind: "custom"` discriminator (arbitrary icon + onClick). Analytics hook via `onShare(targetKind)` plus granular `onCopySuccess` / `onCopyError`. Configurable URL / title / text / via / hashtags. Optional section heading (h2 / h3 / h4) with i18n labels. Optional top divider. External links use `target="_blank" rel="noopener noreferrer"`. SSR-safe — `window.location.href` read at click time, not render time. 11 files (1 root + 3 parts: share-button + templates + icons + 1 brand-icons + types + dummy + demo + usage + meta + index). **Lucide-react no longer ships brand icons** (Facebook, Twitter/X, LinkedIn removed in v0.x for trademark reasons): `parts/brand-icons.tsx` ships inline SVG components for TwitterX / Facebook / LinkedIn / Reddit / Threads / Bluesky (~6KB of path data) — chose this over pulling react-icons (~250KB) for 6 brand glyphs. WhatsApp / Telegram / Email / Copy use first-party Lucide icons (MessageCircle / Send / Mail / Copy). Demo ships 5 sub-tabs (default 4-target / full 9-platform with `onShare` analytics callback / custom target with `Send` icon + internal-dialog state / compact 2-target / Turkish localized). Cleanup: 2s reset timeout cleared on unmount via `useRef<ReturnType<typeof setTimeout>>` + cleanup effect — no `setState after unmount` warning. Migration origin: kasder `kas-social-front-v0` `src/app/(platform)/news/[id]/page.tsx` lines 163–184 — kasder source has 4 placeholder buttons with NO actual share logic; this version is a meaningful upgrade. **Decisions confirmed during analysis:** (1) `text-primary` (signal-lime) for copy-success tint, no new `--success` token; (2) native `title` attribute for tooltips, no shadcn Tooltip dep in v0.1; (3) `onShare(targetKind)` callback included in v0.1; (4) NO `navigator.share()` integration in v0.1 — different UX, defer to v0.2. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 26 routes prerendered. Composed in same article column with `article-meta-01` (top of article) — full kasder news detail page is one component away (only `article-body-01` Plate-based remains). |
| `registration-card-01` | data | alpha | 0.1.0 | **Fifth of 5 components extracted from the kasder event detail page — extraction COMPLETE.** Card-framed registration status display: capacity progress bar + spots-left counter (with destructive-color threshold via `urgentSpotsCount` default 10) + status-aware primary CTA + optional share / actions slot. **Architectural decision: independent of event-card-01's date state machine** (sealed-folder rule) — own simpler 4-state machine `open` / `lastSpots` / `full` / `closed` derived from `(capacity, registered, closed)`. Host owns event timing + passes `closed: true` when registration window has ended. Public helper kernel: `deriveRegistrationStatus(opts) => RegistrationStatus` exported as pure JS function — reusable for header counters / calendar coloring / filter logic / deterministic tests. **Real interactive primary CTA** via `onRegister: () => void` — NOT decorative (differs from event-card-01's grid variant). Status dispatch: `open`/`lastSpots` → enabled `default` "Register" / `full` → disabled `secondary` "Sold out" / `closed` → disabled `secondary` "Closed" / no-handler → disabled "Registration unavailable". Default share button when `onShare` provided; `actions` slot fully replaces (drop `share-bar-01` in for richer sharing). Configurable thresholds: `lastSpotsRatio` default 0.8 (state machine, percent) + `urgentSpotsCount` default 10 (counter color, absolute). No-quota mode: when `capacity` + `registered` both absent, bar + counter rows hide; status stays `open`. Heading optional, default `h3`. Frame toggle (`framed: true` default with `shadow-lg` for sidebar elevation). `statusOverride` for preview / what-if states. 11-key `labels` object for full i18n. 8 files (1 root + 1 lib `registration-status` + types + dummy + demo + usage + meta + index). Demo ships 5 sub-tabs (Default TR Kayıt Durumu verbatim 423/500 / All states stacked: open + lastSpots + urgent + full + closed / No-quota mode / Custom actions slot with cal+save+share cluster / Bare framed=false fundraising-relabeled with Goal+raised+Donate now). a11y: `aria-labelledby={headingId}` via `useId`, Radix Progress `role=progressbar` + `aria-valuenow`, configurable `aria-label`, Share2 icon `aria-hidden`, shadcn Button → `aria-disabled` automatic. **Plan deviations:** unescaped apostrophes in usage.tsx (`event-card-01's state` × 2) — fixed via `&apos;`; typo `"use client">` in demo.tsx — fixed immediately. Migration origin: kasder `kas-social-front-v0` `events/[id]/page.tsx` lines 237–274. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 33 routes prerendered. **Not yet validated in browser:** all 5 demo states rendering, urgent destructive-color threshold flip, disabled CTA states, default share vs custom actions dispatch, no-quota mode bar hiding, framed=false embedded layout, statusOverride precedence. |
| `people-grid-01` | data | alpha | 0.1.0 | **Fourth of 5 components extracted from the kasder event detail page.** Section heading + responsive N-column grid of person cards (round avatar + name + title) — compound (heading + grid + cards). Configurable: `columns: 2 | 3 | 4 | 5` (default 3, all rows start `grid-cols-1` on mobile and scale up at sm/md/lg), `avatarSize: "sm" | "md" | "lg"` (default lg matching kasder's `w-24 h-24`), `alignment: "center" | "start"` (default center). **Avatar fallback** when `item.image` missing: `bg-primary/10 text-primary font-semibold` circle with `getInitials(name)` — handles honorifics by skipping (Dr./Prof./Mr./Mrs./Ms./Sr./Jr.): "Dr. Ahmet Yılmaz" → "AY", "Madonna" → "M", "" → "?". **Public helper kernel:** `getInitials(name): string` pure JS function exported alongside the component — reusable for mention chips / comment headers / contact rows / presence badges (no React imports, tree-shakeable, server-component friendly). Item shape: `id` + `name` required; `title` / `image` / `imageAlt` / `href` optional (soft-failure). Linked cards via polymorphic `linkComponent: ElementType` + per-item `href` — overlay-link covers entire card; link's `aria-labelledby={nameId}` (id via `useId` per card) → screen-reader announces just person's name. Heading optional, configurable level (`headingAs` default `h2` since people grids are top-level page sections — differs from sidebar info-cards' `h3`). **No `framed` prop** — standalone sections, not nested cards. Custom `renderItem(item)` for full per-card takeover. 9 files (1 root + 1 part `person-card` + 1 lib `get-initials` + 6 standard). Demo ships 5 sub-tabs (Default TR Konuşmacılar verbatim / Initials fallback Board with 5 honorific-prefixed names / Columns 2-5 stacked / Linked cards with focus-visible ring / Custom renderItem with social-icon row using `AtSign + Briefcase + Globe` placeholders since lucide dropped brand icons). a11y: `<ul role="list">`, `<section aria-labelledby>`, per-card `<h4 id={nameId}>`, initials fallback `aria-hidden`, motion-safe hover. **Plan re-validation surfaced 3 refinements:** (1) `columns: 5` lookup → `grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5` (was starting at 2-col mobile — overflow risk with lg avatars); (2) `"use client"` in parts/person-card.tsx for consistency (uses `useId`); (3) documented `columns × avatarSize` interaction in usage.tsx. **Plan deviations:** lucide brand icons don't exist (Github/Linkedin/Twitter removed) → swapped to `AtSign + Briefcase + Globe`; removed initially-added `eslint-disable-next-line @next/next/no-img-element` directives — pro-ui's ESLint already overrides the rule for `src/registry/components/**`. Migration origin: kasder `kas-social-front-v0` `events/[id]/page.tsx` lines 215–231. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 32 routes prerendered. **Not yet validated in browser:** hydration, initials fallback at 3 avatar sizes, columns 2/3/4/5 layouts across breakpoints, focus-visible ring on linked variant, polymorphic NextLink, custom renderItem, hover-color transition. |
| `info-list-01` | data | alpha | 0.1.0 | **Third of 5 components extracted from the kasder event detail page.** Card-framed icon-prefixed details list — vertical rows of icon + primary + optional secondary + optional action. **Two-variant dispatch:** `comfortable` (stacked content with `w-5 h-5 text-primary` icon, primary bold + secondary muted + optional action, separators between rows by default — kasder Etkinlik Bilgileri pattern); `compact` (inline single-line with `w-4 h-4 text-muted-foreground` icon, no separators by default — kasder İletişim pattern). Compact intentionally ignores `secondary` + `action` (single-line by definition). Item shape: `id` + `icon` + `primary` required; `secondary` + `action` + `href` optional (soft-failure). Linked rows via `href` per item + polymorphic `linkComponent: ElementType` (default `"a"`); consumer passes full URLs (`tel:` / `mailto:` / `https://`) — no auto-prefixing. Frame toggle (`framed: true` default with card chrome / `false` for embedded). Separator toggle (`separated` auto-defaults per variant — comfortable=true, compact=false). Heading optional, configurable level (`headingAs` default `h3`). Empty state slot + `labels.emptyText` fallback. Custom `renderItem(item)` slot for full row takeover. 8 files (1 root + 1 part `info-row` + types + dummy + demo + usage + meta + index). Demo ships 5 sub-tabs (Default TR comfortable mirroring Etkinlik Bilgileri verbatim / Compact TR mirroring İletişim with linked tel + mailto / Both stacked / Bare with `framed=false` / Custom renderItem with avatar replacing icon). a11y: `<ul role="list">`, `<section aria-labelledby={headingId}>` (id via `useId`), decorative icons `aria-hidden`, link accessible-name = primary text. CSS top-border separators (`pt-3/4 border-t border-border/50` with `idx > 0` guard) — NOT shadcn `<Separator>` siblings (cleaner DOM). **Plan deviation:** `dummy-data.ts` → `dummy-data.tsx` rename — JSX in `action: ReactNode` slot rejected `.ts`. registry.json updated to reference the new path. Migration origin: kasder `kas-social-front-v0` `events/[id]/page.tsx` lines 276–336 — both Etkinlik Bilgileri + İletişim sidebar cards collapse into one component via `variant`. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 31 routes prerendered. **Not yet validated in browser:** hydration, comfortable separator rendering, compact `truncate` with long text, polymorphic NextLink with tel/mailto/https, hover-color transition, action-slot tab-order alongside linked primary. |
| `progress-timeline-01` | data | alpha | 0.1.0 | **Second of 5 components extracted from the kasder event detail page.** Horizontal progress bar with marker dot at current % + 3-caption row (start / dynamic state-aware center / end). 3-state state machine auto-derived (`before` / `active` / `after`) from `start` + `end` + `now`. **Public helper kernel** — `deriveTimelineState(start, end, now?) => TimelineState` exported as a pure JS function (no React imports, tree-shakeable, server-component friendly); returned shape: `{ status, percent, daysToStart, daysFromStart, daysToEnd, totalDays }`. Consumers use the kernel WITHOUT rendering for header counters / calendar coloring / filter logic / deterministic tests. Required props: `start` + `end` (Date \| string) — real prop-driven, NOT the kasder source's "assume 60 days before" hack. Optional dynamicity surfaces: `now` injection (no internal `setInterval` — consumer drives cadence), `value` escape hatch overrides time-derived percent for non-time progress (course %, fundraising %, etc.), `statusOverride` for preview/admin, 6-key `labels` object where `beforeText`/`activeText`/`afterText` accept `string \| ((state) => ReactNode)` (per-state functions get full `TimelineState` for formatting); `renderCenterLabel(state)` full center-caption takeover. Visual toggles: `framed: true` default (card chrome) / `false` (bare); `marker: "dot" | "none"`. Heading optional, configurable level (`headingAs` default `h3` since timelines are sub-sections), configurable icon (default `Timer` from lucide; pass `null` to omit). Soft-failure on invalid dates (clamps gracefully — no crash). 8 files (1 root + 1 lib `timeline-state` + types + dummy + demo + usage + meta + index). Demo ships 5 sub-tabs (Default TR mirroring kasder verbatim with "Zaman Çizelgesi" heading + Turkish labels showing "29 gün kaldı" center / 3 states stacked English / Bare for embedded contexts / Custom renderCenterLabel composing percent + days-left / `value` escape hatch demonstrating course-progress) — ALL pass `now={dummyNow}` (fixed `2026-06-01T12:00:00Z`) for deterministic SSR. a11y: Radix Progress emits `role=progressbar` + `aria-valuemin/max/now`; configurable `aria-label`; marker dot `aria-hidden`; section `aria-labelledby={headingId}` (id via `useId`) when heading provided. **Plan deviations:** **(1) Removed unused `labelMap` documentation const** during implementation — initially left a status→label-key Record but it was unused; pruned. **(2) `headingIcon === undefined ? Timer : headingIcon` ternary** handles 3-cases (undefined → default Timer / null → omit / ComponentType → use); cleaner than `??` which conflates null/undefined. Migration origin: kasder `kas-social-front-v0` `events/[id]/page.tsx` lines 159–187. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 30 routes prerendered. **Not yet validated in browser:** hydration, marker dot edges at 0%/100%, Radix Progress runtime render, custom render slot, dark-mode marker contrast. |
| `schedule-list-01` | data | alpha | 0.1.0 | **First of 5 components extracted from the kasder event detail page.** Vertical time-anchored agenda — `time \| title \| description` rows. Public types `ScheduleListItem` (id/time/title required; endTime/description/icon/href optional — soft-failure on every optional field), `ScheduleList01Labels` (2 keys: timeRangeSeparator + emptyText), `DEFAULT_SCHEDULE_LIST_LABELS` exported for consumer composition. Frame toggle (`framed: true` default with per-row card chrome `bg-card rounded-xl border border-border/50 p-4` + `space-y-4`; `framed: false` for bare rows + tighter `space-y-2`). Polymorphic per-row link via `linkComponent: ElementType` (default `"a"`) + `item.href`; overlay-link pattern with link's accessible name = `item.title`. Optional Lucide-style icon per row (`ComponentType<{ className? }>`) sits left of the time column. Optional time-range via `item.endTime` rendering `${time}${separator}${endTime}` with separator localizable. Custom render slots: `renderItem(item)` bypasses the default row entirely; `renderTime(item)` bypasses just the time renderer. Empty state slot + `labels.emptyText` fallback (`<p role="status">`). a11y: `<ol role="list">` (semantic — schedules ARE ordered), `<section aria-labelledby={headingId}>` when heading provided (id from `useId`), decorative icons `aria-hidden`, `motion-safe:` on all hover transitions, heading level configurable (`headingAs: "h2" | "h3" | "h4"` default h2). 8 files (1 root + 1 part schedule-row + types + dummy + demo + usage + meta + index). Demo ships 5 sub-tabs (Default TR matching kasder source verbatim / English / Bare rows / Icons + links / Custom renderItem with speaker-augmented row). Migration origin: kasder `kas-social-front-v0` `events/[id]/page.tsx` lines 197–213. **Plan deviation:** initial draft had `<li>` wrapping `ScheduleRow` (which itself renders `<li>`), producing invalid nested-list HTML; fixed by conditionally wrapping only consumer's `renderItem` output (default `ScheduleRow` is itself the `<li>`). No `Card` shadcn dep — raw `<li>` + Tailwind classes for chrome. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 29 routes prerendered. **Not yet validated in browser:** hydration, polymorphic NextLink linkComponent, focus-visible ring on framed vs bare, motion-safe hover transitions, custom renderItem rendering. |
| `event-card-01` | data | alpha | 0.1.0 | **Second migration-pipeline ship + first events-domain pro-comp.** Four-variant card (`grid` / `feed` / `list` / `compact`) with a 6-state status state machine derived from clock + capacity. Status logic: time-window first (`now > endDate` → `expired`; `now ∈ [start, end]` → `ongoing`), then capacity (`spotsLeft <= 0` → `full`, `≥80%` → `lastSpots`), then time-proximity (`daysUntil ≤ 7` → `upcoming`), else `open`. **Public helper kernel** exported alongside the component — `getEventStatus(event, now?)`, `EVENT_STATUS_CONFIG`, `formatEventDate(dateString, locale?)`, `getDaysUntilEvent(dateString, now?)`, `EventStatus` union, `EventStatusConfigEntry`, `DEFAULT_EVENT_CARD_LABELS`. Consumers use the kernel WITHOUT rendering the card (header counters, calendar day-cell coloring, status filter logic, deterministic tests — usage.tsx demonstrates with worked examples). **Decisions locked at intake:** (1) BOTH variants ship v0.1 — kasder source already extracted both layouts naturally; (2) ADD `--warning` token (amber `oklch(0.78 0.16 75)` light / `oklch(0.82 0.15 75)` dark + `--warning-foreground` near-black both modes), SKIP `--success` (pro-ui's `--primary` IS the success/positive-action color, redundancy avoided); status mapping: `open` → `bg-primary`, `upcoming`/`lastSpots` → `bg-warning`, `ongoing` → `bg-accent` + `motion-safe:animate-pulse`, `full` → `bg-destructive`, `expired` → `bg-muted` + opacity-grayscale-30 wrapper; (3) decorative CTA + overlay-link pattern (same as content-card-news-01) — `<div role="button" aria-hidden="true" pointer-events-none>` styled like a Button; clicks pass through to the wrapping link; consumers wanting a real Register button drop one in `actions`; (4) export the four helpers as the public kernel — kasder already extracted them naturally for `SocialEventCard`'s reuse, costs are paid; (5) featured = `border-t-4 border-primary` (grid) / `ring-2 ring-primary ring-inset` (feed) / `border-l-4 border-primary` (list) + `<Star>` title prefix — drop the bottom-right pill, frees image corners. **Mid-ship variant addition (per user request after grid + feed shipped):** `list` variant with event-specific density — square thumbnail (16/20px) + status badge + type badge + title (line-clamp-1) + 4-icon inline meta row (Calendar+date / Clock+time / MapPin+location / Users+spots-left, with destructive color when ≤5) + status-aware right slot (days-until count for upcoming/lastSpots/full/open / "Live now" pulsing pill for ongoing / chevron for expired / actions when supplied). **Event-specific delta over content-card-news-01's `list` variant:** news/list ships category badge + relative-time + excerpt + chevron (4 things); event/list ships **status state machine + type taxonomy + 4 event meta icons + status-aware indicator** (richer, dense by design — every cell carries event data). No description / progress bar in list (vertical rhythm; the meta row IS the value-add). **`compact` variant** (added second, after user clarified the screenshot pattern they actually wanted was simpler than `list`): text-only minimal row — title (font-semibold, line-clamp-2, optional Star prefix for featured) + type pill (top-right corner, OR moved to inline-meta when `actions` is present) + 3 stacked meta lines (Calendar+date / Clock+time / MapPin+location, all `w-3 h-3` icons + `text-xs` muted). NO thumbnail, NO status badge, NO capacity bar, NO days-until indicator, NO description. Featured signaled ONLY by Star prefix (no border treatment — keeps the simple aesthetic clean against `border-b` row dividers). Status's `cardClassName` still applies (expired events fade automatically). When `actions` is supplied, the type pill yields its top-right slot to actions and moves to a small inline pill as the first meta `<li>`. Use cases: homepage sidebar widget, dashboard "Upcoming events" ticker, email digest, mobile drawer — anywhere maximum density per pixel beats visual context. **First user of shadcn `progress` primitive** (`pnpm dlx shadcn add progress` — first install in pro-ui); used in grid variant's capacity bar with Radix's `progressbar` role + `aria-label={\`${prefix}: ${registered} ${separator} ${capacity}\`}`. 14 files (1 root + 4 parts: grid + feed + list + compact + 3 lib: event-status + format-default + image-fallback + types + dummy + demo + usage + meta + index). Demo ships 8 sub-tabs (Grid / Feed / List / Compact / Featured / Localized TR / Custom types / Actions slot) — ALL pass `now={dummyNow}` (fixed `2026-06-01T12:00:00Z`) so statuses are deterministic across SSR + CI screenshots. dummy-data exports 8 events covering all 6 statuses + `dummyTrEvents` Turkish-typed parallel array (`Konferans` / `Seminer` / `Çalıştay` / `Panel` / `Eğitim`) + `dummyTrTypeStyles` + `dummyTrLabels` + `dummyCustomTypeStyles` (5 OKLCH-tinted variants demonstrating arbitrary-value Tailwind syntax) + `formatDateTr`. **17-key `EventCard01Labels` interface** — every consumer-visible string is overridable: status × 6, image overlays × 2 (daysUntilSuffix / ongoingIndicator), capacity × 4 (spotsLeftSuffix / spotsLeftFull / capacityAriaPrefix / capacityAriaSeparator), CTA × 4 (ctaRegister / ctaJoin / ctaViewDetails / ctaSoldOut), a11y × 1 (featuredAriaLabel). `capacityAriaPrefix` + `capacityAriaSeparator` deliberately split during plan re-validation so consumers don't get half-translated `"Kayıtlı: 142 of 200"` strings. `DEFAULT_EVENT_CARD_LABELS` exported so consumers can spread defaults if they want explicit merging. **Soft-failure contract:** only `id` / `title` / `type` / `date` required; capacity-less events skip `full` / `lastSpots` states + omit progress bar + spots-left counter entirely; missing `time` / `location` / `description` collapse meta lines / description block; missing `image` renders a tinted `bg-muted` + `<Calendar>` icon placeholder. **Dynamicity surfaces:** `now?: Date` injection (testability + live-clock hosts; no internal `setInterval`), `statusOverride?: EventStatus` (preview / what-if / admin states), `formatDate?: (dateString) => string` callback, `typeStyles?: Record<string, { className: string }>` map, `linkComponent: ElementType` polymorphism (default `"a"`), `getHref?: (event) => string` callback alternative to `href`, `actions?: ReactNode` slot at `z-10` over the link overlay (`z-0`), `imageClassName` / `titleClassName` / `className` overrides, `ariaLabel?` to override the default `aria-labelledby={titleId}`. Migration origin: kasder `kas-social-front-v0` `EventCard.tsx` (grid variant — image-on-top with Status TL / Type TR / Days-until or Ongoing-pulse BL / capacity bar) + `SocialEventCard.tsx` (feed variant — full-bleed image background + content overlaid white-on-dark, two layered gradients for depth). Built on shadcn primitives Button (for `buttonVariants` on the decorative CTA) + Progress (capacity bar) + Tabs (demo) + lucide-react (Calendar / Clock / MapPin / Users / AlertCircle / CheckCircle / XCircle / Timer / Star / ArrowRight / Bookmark / BookmarkCheck / CalendarPlus / Share2). **Plan deviations:** **(1) `Required<EventCard01Labels>` resolution** via merging `DEFAULT_EVENT_CARD_LABELS` + consumer-passed labels at the root — exported alongside types so consumers can spread defaults if they want; **(2) Tailwind v4 canonical classes** — IDE flagged `bg-gradient-to-t` + `grayscale-[30%]` as legacy syntax during plan re-validation; corrected to `bg-linear-to-t` + `grayscale-30` before implementation; **(3) Plan re-validation surfaced 5 substantive refinements** — added `capacityAriaSeparator` for full i18n (`"Kayıtlı: 142 of 200"` half-translated avoidance), `dummyNow` constant injected across all demos for deterministic SSR, parallel `dummyTrEvents` array with Turkish `type` strings (`Konferans` / `Seminer` etc.) so the Localized demo's `typeStyles` keys match the events, dropped unused `@/components/ui/badge` from internal deps (status + type badges rendered as raw `<span>` for finer control over icon-gap / backdrop-blur / per-status `motion-safe:animate-pulse`), fixed "15 files" → "12 files" typo. **Pro-ui-wide additions:** `--warning` + `--warning-foreground` tokens in [globals.css](../src/app/globals.css#L31-L32) (light + dark + `@theme inline` mappings); `--color-warning` + `--color-warning-foreground` Tailwind utility mappings; shadcn `progress` primitive at [src/components/ui/progress.tsx](../src/components/ui/progress.tsx). Consumer install via `pnpm dlx shadcn@latest add @ilinxa/event-card-01` (base) + `@ilinxa/event-card-01-fixtures` (8-event dummy bundle). **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm build` clean (compiled 11.8s + tsc 12.0s; 28 routes prerendered including `/components/event-card-01`); `pnpm registry:build` produced `event-card-01.json` + `event-card-01-fixtures.json` artifacts. Procomp docs at [docs/procomps/event-card-01-procomp/](../docs/procomps/event-card-01-procomp/) — description + plan + guide all present. **Not yet validated in browser:** hydration, image hover scale + arrow translate (motion-safe gating), focus-visible ring on full card via overlay-link `:has(a:focus-visible)`, actions-slot click stopping propagation, dark-mode rendering, RTL chevron flip in feed variant, capacity progress-bar Radix render at runtime, Turkish characters at all sizes, custom OKLCH typeStyles colors at runtime, ongoing-pulse animation visibility. |
| `media-carousel-01` | media | alpha | 0.1.0 | **Third of 8 in the social-posts-system arc; FIRST cross-folder import in pro-ui's registry.** Embla-driven image+video carousel with two variants (`gallery` peek-scale matching kasder Instagram-post style / `linear` full-width snap). Composes `video-player-01` directly via `<VideoPlayer01 isActive={index === currentIndex}>` for built-in video item handler — the `isActive` contract from video-player-01 delivers the kasder UX (videos in inactive slides pause cleanly) out of the box, no consumer wiring required. shadcn `registryDependencies: ["video-player-01"]` handles install dependency. **Refines the project-wide sealed-folder rule:** cross-folder imports allowed within a designed system (declared via `registryDependencies`); not allowed across unrelated domains. Sets precedent for comment-thread-01 + post-card-01 to import sibling primitives directly. **Single-item shortcut** as separate `<SingleItem>` component — bypasses Embla entirely AND honors React rules-of-hooks (own component = own hook count, dispatch via component swap NOT conditional hook calls in root). **`useEmblaWithState` hook** uses `useSyncExternalStore` (canonical external-subscription pattern; matches video-player-01's `usePrefersReducedMotion` precedent) — `currentIndex` syncs with Embla's `select` event without setState-in-effect violations. **`onSlideChange` mount-sync silent** — separate effect with `isFirstFireRef` skips first-fire so user callback only fires on real slide changes (not phantom mount-time `onSlideChange(0)`). **Embla options memoized** via `useMemo([variant, loop, rtl])` — `useEmblaCarousel` re-initializes on options identity change; inline objects would cause constant re-init. **Imperative ref handle** (`scrollTo` / `scrollPrev` / `scrollNext` / `getCurrentIndex`) uses `currentIndexRef` for stable handle identity across slide changes (host's ref.current doesn't churn on every snap). **WAI-ARIA APG carousel pattern** — `role="region" aria-roledescription="carousel"` + per-slide `role="group" aria-roledescription="slide" aria-label="Slide N of M"` (template `{index}` / `{total}` placeholders configurable via `labels.slideAriaLabel`). Indicator dots have `aria-current="true"` when active. **Variants:** gallery uses 85%-width slides with `peekRatio` default 0.075 + scale-95-opacity-60 on inactive + transition-all duration-300; linear uses full-width snap with configurable `aspect: "square" | "video" | "portrait" | "auto"` (default square). Single-item shortcut always uses `aspect-square`. **Discriminated `MediaItem` union** — `{ id, type: "image", url, alt? }` | `{ id, type: "video", url, alt?, poster? }`. Strict shape; consumers wanting custom shapes use `renderItem` slot with TS intersection. **Unified `onDoubleTap(item, index)`** — image items get `useDoubleTap` at slide-wrapper level; video items forward to `<VideoPlayer01 onDoubleTap>`; both fire the same callback. `onSlideChange` debounced to Embla's `select` event (post-snap, not during drag). RTL via Embla `direction: "rtl"` + chevron `rtl:rotate-180`. 13 files (1 root + 5 parts: single-item + carousel-track + slide-renderer + nav-buttons + indicator-dots + 1 hook + 6 standard). 5-tab demo (Gallery mixed kasder UX with video at index 3 auto-pausing / Linear photos product gallery / Single-item shortcut for both image AND video / Custom renderItem with slide-counter overlay / Imperative ref demo with Prev/Next/Jump-to-3 buttons). dummy-data exports 4 sets (mixed image+video kasder set, photos-only product set, single image, single video) — real Pexels URLs. **Plan re-validation surfaced 4 substantive refinements:** (1) **CRITICAL** — single-item shortcut extracted to `parts/single-item.tsx` (was conditional `useDoubleTap` in root → React rules-of-hooks violation when items.length changes); (2) `onSlideChange` mount-sync no longer fires user callback (separate effect with `isFirstFireRef`); (3) Embla options memoized to avoid re-init on every render; (4) imperative handle uses `currentIndexRef` for stable identity. **Implementation deviations from plan:** **(1) TS error: `Cannot find module 'embla-carousel'`** — types are in core package but only `embla-carousel-react` was installed; fix: `pnpm add embla-carousel` as direct dep. **(2) `react-hooks/set-state-in-effect` lint error in `useEmblaWithState`** — refactored entirely to `useSyncExternalStore`. **(3) Two unused `@next/next/no-img-element` eslint-disable directives** removed (rule already overridden for `src/registry/components/**`). Migration origin: kasder `kas-social-front-v0` `PostMediaCarousel.tsx` (142 LOC). **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 37 routes prerendered (was 36 → +1 for `/components/media-carousel-01`); registry artifacts produced. Procomp docs at [docs/procomps/media-carousel-01-procomp/](../docs/procomps/media-carousel-01-procomp/) — description + plan + guide all present. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/media-carousel-01` (auto-installs `@ilinxa/video-player-01` via `registryDependencies`) + `@ilinxa/media-carousel-01-fixtures`. New peer deps: `embla-carousel-react@8.6.0` + `embla-carousel@8.6.0`. **Not yet validated in browser:** hydration, gallery peek-scale active vs inactive transitions, indicator dot elongation + click-to-jump, nav chevrons, video at index 3 auto-pausing when off-screen (kasder UX demo), single-item bypass (no nav, no indicators), custom renderItem slide-counter overlay rendering, imperative ref handle Prev/Next/Jump, RTL drag direction + chevron flip, touch drag on mobile, Embla arrow-key keyboard nav. **Composes inside upcoming `post-card-01`** for the full kasder post-card. |
| `video-player-01` | media | alpha | 0.1.0 | **Second of 8 in the social-posts-system arc; FIRST occupant of the `media` category.** `<video>` wrapper with muted-autoplay-friendly defaults (muted=true / loop=true / playsInline=true / preload=metadata), slot-based controls via `renderControls(state) => ReactNode` (full takeover; ref intentionally NOT exposed to prevent state-machine bypass), `isActive` auto-pause for carousel coordination (when `false`, hook calls `videoElRef.current?.pause()`; consumer drives play on reactivation, no auto-resume), double-tap callback (300ms window) via standalone-exported `useDoubleTap` hook, caption tracks via `tracks: VideoTrack[]` rendering `<track>` children, rAF-throttled `onTimeUpdate` (caps at display refresh rate vs native ~30×/sec). **State machine via `useReducer`** — 8 actions (play / pause / set-muted / loadstart / loaded / set-current-time / ended / reset) with atomic transitions. Browser drives state via DOM events; we mirror it. `loadstart` action clears stale state on `src` change (no transient `(0, oldDuration)` window in onTimeUpdate callbacks). **Controls auto-hide** during playback after `controlsAutoHideMs` default 2000ms. Pass `0` to disable. Under `prefers-reduced-motion: reduce` (detected via `useSyncExternalStore`), auto-hide skipped entirely — controls always visible. Default control overlay matches kasder pattern: big center play button (always visible when paused) + bottom-right mute toggle + bottom-left small pause indicator (only when playing + showing controls). Mute uses `aria-pressed={isMuted}` + action-verb labels ("Mute"/"Unmute"). **Object-fit:** `cover` (default Instagram-style) or `contain` (letterboxed for editorial). **Keyboard:** `tabIndex={0}` on `<video>`; Space toggles play/pause; M toggles mute (focus-required, per Q-P4 — avoids multi-video focus contention). **5-key i18n labels** (play / pause / mute / unmute / videoLabel). All 8 `videoEventHandlers` wrapped in `useCallback([])` + memoized via `useMemo` — prevents re-attaching listeners every render (critical for hot-path `timeupdate`). Lifecycle callbacks: onPlay / onPause / onEnded / onTimeUpdate(t,d) / onLoadedMetadata(d) / onError. 11 files (1 root + 2 parts: video-element + default-controls + 2 hooks: use-video-state + use-double-tap + 6 standard). 5-tab demo (Default / Custom controls with progress bar via slot / Captions / Decorative no-controls + autoplay-loop / isActive carousel proving 3-video coordination). dummy-data exports 5 royalty-free Pexels MP4 URLs (landscape / vertical / loop / 2 carousel demos) + sample English caption track. **Plan re-validation surfaced 4 substantive refinements:** (1) `isActive` pause effect moved INSIDE `useVideoState` — hook owns video DOM, single dep `[isActive]`; (2) all event handlers `useCallback([])` + handlers object `useMemo`'d; (3) `parts/video-element.tsx` got `onToggleMute` prop to complete M-keyboard handler (was TODO); (4) `loadstart` event added to clear stale state on src change. **Implementation deviations from plan:** **(1) Two `react-hooks/set-state-in-effect` lint errors caught during build verification** — `usePrefersReducedMotion` had `setReduced(mql.matches)` synchronously in useEffect; auto-hide timer effect had `setShowControls(true)` in body. Fixes: usePrefersReducedMotion → `useSyncExternalStore` (canonical external-subscription pattern with SSR snapshot); auto-hide timer → derived `alwaysVisible` from props + `hidden` state + `wakeKey` counter dep that increments on wake gestures (re-arms timer via effect re-run). Same lint family that bit detail-panel + filter-stack earlier. Migration origin: kasder `kas-social-front-v0` `PostVideoPlayer.tsx` (128 LOC). **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 36 routes prerendered (was 35 → +1 for `/components/video-player-01`); registry artifacts produced. Procomp docs at [docs/procomps/video-player-01-procomp/](../docs/procomps/video-player-01-procomp/) — description + plan + guide all present. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/video-player-01` + `@ilinxa/video-player-01-fixtures`. Pulls only `button` shadcn primitive + `lucide-react`. **Composes inside upcoming `media-carousel-01`** (next ship — sets `isActive` per slide) + `story-viewer-01` (later ship — `controls={false}` + onTimeUpdate for segmented progress fill). **Not yet validated in browser:** hydration, video element keyboard focus + Space/M shortcuts, double-tap firing onDoubleTap, mute toggle aria-pressed announce, captions track loading + browser CC button, carousel demo `isActive` pause/resume across 3 videos, rAF throttling actually capping at ~60×/sec, reduced-motion controls-always-visible behavior, autoplay rejection silent fallback. |
| `expandable-text-01` | data | alpha | 0.1.0 | **First of 8 components in the social-posts-system migration arc.** Truncate-and-expand plain-text block. **Measure-based detection** — `useEffect` reads `getComputedStyle(el).lineHeight` + compares `el.scrollHeight > lineHeight × maxLines + 1` (sub-pixel slop), sets `isTruncated` state. Toggle button only renders when actually truncated (pure CSS line-clamp clips silently with no signal — this component restores the signal). `maxLines` default 3. Controlled-or-uncontrolled `expanded` via `expanded?` / `defaultExpanded?` / `onExpandedChange?` (mirrors React form-input convention). i18n via 2-key `labels` object (`showMore` / `showLess`, English defaults). `renderToggle` slot for full toggle takeover (e.g. chevron icon — preserved a11y is consumer's responsibility, slot receives `isExpanded` + `setExpanded`). **Public `useLineClampDetect` hook export** for advanced consumers who want detection without our component — `content` parameter typed `unknown` so external use isn't restricted to `string`. Hook tracks element via `useState` (not `useRef` + ref-callback) so consumer attachment to a `<p>` inside a list with keys re-triggers observer setup on element identity change; `setEl` IS the ref callback (React guarantees stable identity). Single `useEffect` for measure + ResizeObserver + cleanup, deps `[el, content, maxLines]`. **`display: -webkit-box` always applied** to the content `<p>` (toggle only `WebkitLineClamp` between `maxLines` and `"unset"`) — `scrollHeight` reflects natural content height even when `-webkit-box` clamp is applied (verified via kasder pattern; no flash-of-untruncated-content). Empty content guard — when `content` is empty/null, returns `null` (no shell rendered). 8 files (1 root + 1 hook + types + dummy + demo + usage + meta + index). Demo ships 4 sub-tabs (Default with long+short content side-by-side / Custom maxLines 2 + 6 / Localized TR with `Daha fazla göster`/`Daha az göster` labels / Custom toggle with chevron icons via `renderToggle` slot in controlled mode). Dummy data exports 4 sample strings (SHORT_EN / LONG_EN / SHORT_TR / LONG_TR). a11y: real `<button type="button">` with `aria-expanded={expanded}` + `aria-controls={contentId}` (id from `useId`); `<p>` carries `id={contentId}`; `focus-visible:ring-2 focus-visible:ring-ring` on toggle. Migration origin: kasder `kas-social-front-v0` `PostContent.tsx` (55 LOC). **Plan re-validation surfaced 3 substantive refinements:** (1) hook tracks element via `useState` (not `useRef` capture) — handles consumer remount cases; (2) hook's `content` parameter typed `unknown` — never reads value, only uses identity for re-measurement trigger; (3) dropped scratch-route step from Phase B — smoke-test directly inside `demo.tsx`. **Tailwind v4 canonical class fix during plan re-validation:** `break-words` → `wrap-break-word` (IDE-flagged). **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 35 routes prerendered (was 34 → +1 for `/components/expandable-text-01`); registry artifacts produced. Procomp docs at [docs/procomps/expandable-text-01-procomp/](../docs/procomps/expandable-text-01-procomp/) — description + plan + guide all present. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/expandable-text-01` + `@ilinxa/expandable-text-01-fixtures`. Zero peer deps beyond React. **Not yet validated in browser:** hydration, ResizeObserver re-measurement on window resize, controlled mode persistence across remount, custom renderToggle chevron rotation, Turkish character measurement with latin-ext font subset. **Composes inside upcoming `comment-thread-01`** (next ship) for comment bodies. |
| `engagement-bar-01` | data | alpha | 0.1.0 | **Fourth of 8 in the social-posts-system arc; FIRST `registry:file` precedent (sibling CSS) in pro-ui.** Discriminated-union action row (like / comment / share / bookmark / view-count / custom) with realtime `Subscribe<EngagementDelta>` contract, CSS-keyframe heart-burst as RSC-compatible sibling sub-export, per-action controlled vs uncontrolled mode (hybrid), slot-based likers preview, three variants (default / compact / stacked). **No framer-motion** — adoption gated on `story-viewer-01`'s swipe-to-dismiss. **Heart-burst sub-export** (`<EngagementHeartBurst>`): RSC-compatible (no `"use client"`); `key={trigger}` remount pattern restarts the keyframe each time the host increments the counter; sibling `engagement-heart-burst.css` shipped via shadcn `registry:file` (FIRST non-`registry:component` entry in pro-ui's registry — sets precedent for future components needing self-contained stylesheets). Component owns burst placement via `className`; bar handle does NOT include `triggerHeartBurst()` (would be misleading — host-owned counter is the canonical pattern). **Hybrid controlled/uncontrolled per-action**: passes `liked` / `bookmarked` → controlled (host owns); omit → bar flips state internally. Same prop, dual mode, matches React `<input value>` convention. Mode is determined per-action, per-render. **Subscription effect uses `controlledRef` + `onSubscribeDeltaRef` mirrors** so re-runs only on `subscribe` identity change — no in-flight delta drops between cleanup + re-call. **`onSubscribeDelta?` callback** fires for every delta regardless of mode; controlled-mode hosts use it to translate deltas into prop updates. **Stacked variant root layout is vertical** (`flex flex-col items-center gap-3` for TikTok/Reels overlay style — actions stack along the right edge of host's content); `align` is silently ignored in stacked mode. Default + compact variants use horizontal `flex justify-between` with left/right action groups split by `align`. **Default align rule**: `bookmark` + `view-count` right; everything else left; per-action `align?` overrides. Stacked variant ignores it. **`engagementReducer` + `useEngagementState` + `deriveStateFromActions` + `formatEngagementCount` + `EngagementHeartBurst` all public exports** — external state coordination is a "plausible consumer override" per pro-ui's dynamicity rule. **Likers preview is a `ReactNode` slot** rendered below the action row when provided — hosts wire avatar pile / popover / "+12 others" pill / etc. The bar doesn't ship a `LikersStrip` of its own (3 different consumer patterns; baking in one constrains the others). 16 component files + 2 docs-only (1 root + 8 parts: action-button dispatcher + 6 per-kind action parts: like / comment / share / bookmark / view-count / custom + heart-burst.tsx + heart-burst.css + 1 hook use-engagement-state + 1 util format-count + 6 standard). **Click handlers live INSIDE each action part** (closure over their own props: state / dispatch / controlled / action callbacks); upstream `useCallback` in `.map()` would be hooks-rules violation; `React.memo` on each action part keeps re-renders cheap. 6-tab demo (Default post with likers strip / Compact news-card retrofit preview / Stacked video overlay on dark bg / Realtime subscribe with synthetic 4-second tick + console-logging deltas / Heart-burst with double-click image triggering bar's triggerLike() + setBurstKey(k+1) / Custom action via Wand2 icon). dummy-data exports 3 action sets (post / news-card / video-stacked) + 5 sample like users + `createDummySubscribe()` factory for sandbox realtime demos. **Plan re-validation surfaced 5 substantive refinements:** (1) **stacked variant root** changed from horizontal `flex justify-between` to vertical `flex flex-col items-center gap-3` (correct for Reels-style overlay); (2) dropped "memoize click handler upstream with useCallback in `.map()`" pattern from plan (would be hooks-rules violation); each action part owns its own click handler internally with `React.memo` for cheap re-renders; (3) subscription effect uses `controlledRef` + `onSubscribeDeltaRef` mirrors instead of including them in deps (so re-runs only on `subscribe` identity change); (4) `view-count-action` renders as `<div role="group" aria-label>` (cleaner than `<span aria-label>`); (5) `format = labels.formatCount ?? formatEngagementCount` resolved at root, passed down to every action part. **Description re-validation surfaced 4 refinements:** (1) CSS portability locked via shadcn `registry:file` (sibling `engagement-heart-burst.css`); (2) dropped `triggerHeartBurst()` from bar handle (host-owned counter pattern); (3) added `onSubscribeDelta?` callback for controlled-mode subscribe contract; (4) `<EngagementHeartBurst>` is RSC-compatible (no `"use client"`). **Implementation deviations from plan:** (1) Two `react-hooks/refs` lint errors caught — `stateRef.current = state` and `actionsRef.current = actions` written during render; fix: moved both ref-mirror writes into a passive `useEffect` (matches media-carousel-01's pattern). (2) `react-hooks/use-memo` lint error in demo's `RealtimeDemo` — `useCallback(createDummySubscribe(142), [])` passed a function-call result instead of an inline function; fix: switched to `useMemo(() => createDummySubscribe(142), [])`. (3) `react-hooks/exhaustive-deps` warning on subscription effect with `[opts.subscribe]` — extracted `const subscribe = opts.subscribe` to local + dep on `[subscribe]` (cleaner). (4) Initially used dynamic `import("./hooks/...")` inside `reset()` handle method — code smell (Promise-based dispatch); fix: imported `deriveStateFromActions` at top of file. **17-key meta features list** documents every public surface. Migration origin: kasder `kas-social-front-v0` `PostEngagementPanel.tsx` (468 LOC); we extract only the action-row concern, decomposing comments → `comment-thread-01` (next ship) and likers carousel → `likersPreview` slot. **Verification:** tsc + lint + build clean (1 pre-existing rich-card warning, no new); 38 routes prerendered (was 37 → +1 for `/components/engagement-bar-01`); registry artifacts produced; spot-check confirms `engagement-heart-burst.css` shows up in JSON `files[]` with `type: "registry:file"`. Procomp docs at [docs/procomps/engagement-bar-01-procomp/](../docs/procomps/engagement-bar-01-procomp/) — description + plan + guide all present. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/engagement-bar-01` + `@ilinxa/engagement-bar-01-fixtures`. New shadcn primitive: `avatar` (installed during this ship for the demo's likers strip). Registry now 58 items (29 base + 29 fixtures). **Smoke-test from tmp consumer remains a recommended manual step** to verify shadcn CLI v4 actually copies the `registry:file` content to the target path (artifact well-formed; install behavior is what's untested). **Not yet validated in browser:** hydration, all 6 demo tabs rendering, double-click image triggering heart-burst overlay + bar like-flip, like/bookmark scale-bump CSS transitions, subscribe synthetic tick visibly updating count every 4s, `aria-pressed` / `aria-live` screen-reader behavior, stacked variant rendering as vertical column on dark background, compact variant fitting inside news-card, custom Wand2 icon rendering. **Composes inside upcoming `comment-thread-01`** (per-comment in `compact` variant for like / reply) + `post-card-01` (full kit). Retrofits cleanly into already-shipped `content-card-news-01` + `event-card-01` `actions` slots. |
| `story-viewer-01` | media | alpha | 0.1.0 | **Eighth (and final) ship in the social-posts-system arc; story doublet part 2; closes the arc.** Full-screen sequential story viewer composing kasder's `StoryViewer.tsx` (302 LOC). Mobile fullscreen `h-dvh w-screen` / desktop centered portrait `md:h-175 md:w-100 md:rounded-2xl` (kasder-exact 400×700) via Radix Dialog (focus trap + portal + Escape + backdrop click free). **CSS-only animations** — `framer-motion` deliberately NOT adopted yet; locked motion-substrate gate is swipe-to-dismiss, deferred to v0.2. **Segmented progress bars** with CSS `transition-[width] duration-100 ease-linear` fill + per-segment `role="progressbar"` ARIA. **Pause-preserving accumulator timer** in `useStoryProgress` hook tracking `accumulatedMs` across pause/resume cycles — kasder's naive `Date.now() - startTime` recompute silently advanced ~50ms per cycle. **Item duration resolution priority:** explicit `currentItem.duration` → video metadata via `<VideoPlayer01 onLoadedMetadata>` → `defaultItemDuration` (default 5s). **Tap zones** (left/middle/right thirds for prev item / pause / next item) at z-10, header at z-20, nav arrows at z-30 — clickable layers preserved without conflict. Tap-zone buttons `tabIndex={-1}` + `aria-hidden="true"` (touch only; keyboard users use arrow keys). **Desktop nav arrows** for story-level navigation (hidden on mobile via `md:flex`). **Keyboard nav** via `useStoryKeyboardNav`: ArrowLeft/Right (item nav) + Space (pause; preventDefault) + Escape (close — funneled through Radix `onOpenChange`). **video-player-01 cross-folder import** for video items (FOURTH cross-folder precedent in pro-ui after media-carousel-01, comment-thread-01, post-card-01). **Single-item-at-a-time render** — viewer renders only `currentStory.items[currentItemIndex]`; off-screen items don't mount; `<VideoPlayer01 isActive={!isPaused}>` is the only video coordination needed. **Always-uncontrolled** with `reset(next)` + `dispatch(action)` + `goToStory` / `goToItem` / `setPaused` imperative handle (mirrors story-rail-01 / post-card-01 / comment-thread-01). **ID-anchored cursor** — internal cursor tracked by `(storyId, itemId)` strings, NOT indices; derived `(storyIndex, itemIndex)` via `useMemo`. Realtime insertions/removals don't desync. **Cursor reset semantics** — re-seeds whenever **`(initialStoryIndex, isOpen)` pair** changes (matches kasder; re-opening with same index goes to item 0). Lazy `useState` initializer + `initialStoriesRef` mirror keeps deps narrow. **Subscribe contract** (locked arc convention): `Subscribe<StoryViewerDelta>` — `story-added` / `story-removed` / `item-added` / `item-removed` / `story-viewed`. Mount-scoped lifecycle (NOT `isOpen`-scoped). `onSubscribeDelta` callback fires for every delta. **Forward-only `onStoryViewed` semantics** — fires on last item completion / forward navigation / auto-close at end; NOT on backward navigation (matches Instagram). `onItemViewed` separate for per-item analytics granularity. **Auto-close at end** — `onAutoCloseAtEnd?` synchronous callback fires immediately before `onClose()` so hosts can intercept (show "all caught up" overlay) by keeping `isOpen=true` themselves. **`renderItem?` slot** for full takeover (Lottie / polls / sponsored / etc.); hosts using it MUST set `item.duration` explicitly for non-video items. **`useStoryProgress` + `useStoryKeyboardNav` + `storyViewerReducer` + `useStoryViewerState` publicly exported.** **i18n via 10-key `labels`** (defaults English + native `Intl.DateTimeFormat`; no `date-fns` peer dep). **a11y:** `<DialogTitle>` (sr-only with `viewerLabel`), per-button `aria-label`s + mute toggle `aria-pressed`, per-segment `role="progressbar"` + `aria-valuenow`, tap zones `aria-hidden + tabIndex={-1}`, keyboard nav covers all interactions. **No new shadcn primitives** (`dialog` / `avatar` / `button` already installed). **No framer-motion** (motion-substrate gate moved to v0.2 swipe-to-dismiss). 16 files in sealed folder (1 root + 6 parts + 3 hooks + 1 lib + 6 standard); 13 ship via the registry. 6-tab demo (Image only / Video only / Mixed / Multi-story nav with Open-from-N buttons / Realtime synthetic 8s item-added + 15s story-added / Custom renderItem promo placement). dummy-data exports 3 mock stories matching kasder's URL fixtures verbatim (Unsplash images + w3schools `mov_bbb.mp4` + `movie.mp4` videos). **3 Stage-1 description re-validation refinements + 8 Stage-2 plan re-validation refinements applied** (R-D-1..8 + R-Plan-1..7). All 4 description Q-Ps committed silently as carry-overs from prior arc components per the locked-pattern memory; only 4 genuinely-novel calls surfaced (modal animation strategy, markViewed granularity, auto-close behavior, subscribe-delta scope). **Implementation deviations:** (1) `react-hooks/refs` lint — `initialStoryIdRef.current` read-during-render in `useState` initializer; fix: dropped refs entirely, used lazy `useState(() => { ... })`. (2) `react-hooks/set-state-in-effect` — `setProgress(0)` and `setPaused(false)` in itemKey-change effects; fix: React 19 setState-during-render pattern; ref resets stayed in separate effect (allowed). (3) Two unused `eslint-disable @next/next/no-img-element` directives removed (same pattern as people-grid-01 / media-carousel-01 / post-card-01). (4) `md:h-[700px] md:w-[400px]` IDE-flagged → fixed to `md:h-175 md:w-100` during plan re-validation BEFORE implementation. (5) `h-[100dvh]` IDE-flagged → fixed to `h-dvh`. **Public exports:** `StoryViewer01` (memoized) + `useStoryViewerState` + `useStoryProgress` + `useStoryKeyboardNav` + `storyViewerReducer` + 10 type exports + `DEFAULT_STORY_VIEWER_LABELS` + `meta`. **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm registry:build` produced `story-viewer-01.json` (13 files, all `registry:component`, target convention locked, registryDependencies = `video-player-01` cross-folder + 3 shadcn primitive URLs) + `story-viewer-01-fixtures.json`. **Components page renders at HTTP 200** on local dev. Procomp docs at [docs/procomps/story-viewer-01-procomp/](../docs/procomps/story-viewer-01-procomp/) — description + plan + guide all present. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/story-viewer-01` + `@ilinxa/story-viewer-01-fixtures`. Registry now 66 items (33 base + 33 fixtures). **Not yet validated in browser:** hydration, all 6 demo tabs rendering, mobile vs desktop modal layout (h-dvh fullscreen vs portrait modal), tap-zone touch behavior, segmented progress bars filling at correct rate per item, video metadata duration override applied for w3schools clips, pause-preserving accumulator across pause/resume, multi-story nav with keyboard + button clicks, realtime tab's item-added growing progress bars without losing position, custom renderItem rendering, auto-close at end firing `onAutoCloseAtEnd` synchronously before `onClose`. **Migration origin:** kasder `kas-social-front-v0` `StoryViewer.tsx` (302 LOC) + `types.ts` (16 LOC). **Social-posts-system arc COMPLETE: 8/8 done.** Next natural step: `/sandbox/social-feed-page-01` Tier-3 composition (host code, NOT a registry component). framer-motion adoption gate remains pending in v0.2 swipe-to-dismiss. |
| `story-rail-01` | data | alpha | 0.1.0 | **Seventh of 8 in the social-posts-system arc; story doublet part 1.** Horizontal stories rail mirroring kasder's `StoriesSection.tsx` + `StoryThumbnail.tsx`. Embla used inline (drag-free skim-scroll; `align: "start"`, `containScroll: "trimSnaps"`, `dragFree: true`); no wrapper hook, no indicator dots (skim ≠ snap). Kasder-exact thumbnail: `w-20 h-28` portrait outer, `p-0.5 rounded-2xl` ring (gradient `from-accent via-warning to-destructive` when unread, `bg-muted` when read), inner `rounded-[14px] border-2 border-card` image frame, `motion-safe:group-hover:scale-105` on the preview, avatar+username row below (`h-5 w-5` avatar, `max-w-14 truncate` username). **`<AddStoryThumbnail>` standalone sub-export** — kasder's dashed-border placeholder + 50%-opacity user avatar + `+` badge bottom-right. **`leading?: ReactNode` slot** — `<AddStoryThumbnail>` for kasder UX, or any custom React node (Live indicator / Pinned callout / etc.). **Mode-aware edge-fade gradients** (R-D-1): `from-card` + `left-4 right-4` when `framed: true` (default; matches kasder Card wrapper); `from-background` + `left-0 right-0` when `framed: false`. Both `w-12`, `pointer-events-none`, `aria-hidden`. Edge gradients render ONLY when items present (R-Plan-2; not over empty state). **Always-uncontrolled** with `reset(next)` + `dispatch(action)` imperative escape hatches mirroring comment-thread-01 / post-card-01; component captures `items` as initial state on mount + manages internal store via `useStoryRailState`. **Realtime via `Subscribe<StoryRailDelta>`** — same shape as engagement-bar-01 / comment-thread-01 / post-card-01. Delta union: `added` (with `position?: "start" | "end"` defaulting to `"start"`) / `removed` / `viewed` (sets `hasUnread=false`) / `updated` (partial patch). `onSubscribeDelta` callback fires for every delta (analytics / cross-component). `controlledRef` + `onSubscribeDeltaRef` mirror pattern; subscription effect re-runs only on `subscribe` identity change. **Click does NOT auto-mark-viewed** (R-Plan-3) — matches Instagram; host calls `ref.current.markViewed(itemId)` when their viewer closes. Decoupled from the viewer entirely — `onItemClick(item, index)` hands off to whatever the host renders. **`StoryRailItem` shape: minimal preview** (Q1) — `id` / `username` / `avatar?` / `previewImage` / `hasUnread?` / `userId?`. Story-viewer-01 (eighth ship) will define its own full Story shape with `items[]` array of inner content; TS structural typing means hosts can pass a Story to story-rail-01 if its fields are a superset (no adapter layer). **Embla used directly, NOT cross-importing media-carousel-01** — sealed-folder rule; story-rail's start-aligned + drag-free + small-rectangle config differs structurally from gallery's centered+snap+large-square. **No shadcn `card` dep** (R-Plan-1) — render `<section>` always with conditional card-style classes (`bg-card rounded-xl border border-border/50 p-4`) when `framed: true`. Removes one shadcn dep + cleaner JSX. **Polymorphic `linkComponent` + `getHref`** for navigation-mode rails (rare; stories usually open modal viewers). When provided, thumbnail renders as `<linkComponent href={...}>` instead of `<button>`. `onItemClick` still fires on click (analytics + navigation co-exist). **`renderThumbnail?(item, isUnread, helpers)` slot** — full takeover for the thumbnail visual. Helpers carry `{ index, onClick, baseId }` (baseId via `useId()` for ARIA wiring). **`storyRailReducer` + `useStoryRailState` publicly exported** (per dynamicity primacy — external state coordination). **Imperative handle:** `scrollTo(index)` / `getCurrentItems()` / `reset(next)` / `dispatch(action)` / `markViewed(itemId)`. **i18n via 5-key `labels`** (railLabel / addStoryLabel / addStoryAriaLabel / `thumbnailAriaLabel?: (item) => string` for unread/viewed string / emptyState). **a11y:** `<section role="region" aria-label={labels.railLabel}>`, `<button aria-label={labels.thumbnailAriaLabel(item)}>` per thumbnail (default returns `"{username}, unread story"` / `"{username}, viewed"`), edge gradients aria-hidden, drag-scroll inherits Embla's keyboard arrow-key support, `motion-safe:` on hover scale. **No new shadcn primitives** — `avatar` already installed. **Embla peer dep already installed** (shared with media-carousel-01). **No framer-motion** — CSS transitions only. 11 files in sealed folder (1 root + 2 parts: story-thumbnail + add-story-thumbnail + 1 hook use-story-rail-state + types + dummy-data + demo + usage + meta + index); 6 ship via the registry. **7-tab demo** (Default 7 stories / + Add with `<AddStoryThumbnail>` leading and simulated viewer mark-viewed pattern / Mixed unread+read / Realtime synthetic 5s tick alternating added/viewed/updated / Custom render with square chip + saturate-filter variant / Bare with `framed: false` / Empty). dummy-data exports 7 mock users matching kasder's fixture verbatim + viewer-avatar fixture + `createDummyStoryRailSubscribe()` factory firing synthetic deltas. **Architectural decisions: 23 Q-Ps cite their sources** (most pattern carry-overs; only Q1 genuinely-novel: minimal-preview shape over cross-import full Story). **3 plan re-validation refinements applied:** R-Plan-1 dropped shadcn `card` dep (render `<section>` with conditional classes; cleaner JSX); R-Plan-2 edge gradients only render when items present; R-Plan-3 no auto-mark-viewed on click. **Implementation deviations:** none — clean ship through tsc + lint. **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm registry:build` produced `story-rail-01.json` (6 files, all `registry:component`, target convention locked) + `story-rail-01-fixtures.json`. **Components page renders at HTTP 200** on local dev (`/components/story-rail-01`). Procomp docs at [docs/procomps/story-rail-01-procomp/](../docs/procomps/story-rail-01-procomp/) — description + plan + guide all present. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/story-rail-01` (auto-pulls only `avatar` shadcn primitive) + `@ilinxa/story-rail-01-fixtures`. Registry now 64 items (32 base + 32 fixtures). **Not yet validated in browser:** hydration, all 7 demo tabs rendering, gradient ring on unread thumbnails vs muted ring on read, edge-fade gradients (framed vs bare modes), drag-scroll touch behavior on mobile, Embla keyboard arrow-key navigation, `markViewed` flipping the ring after the simulated viewer-close, `renderThumbnail` slot custom render, polymorphic `linkComponent` with NextLink. **Social-posts-system arc:** 7/8 done (expandable-text-01 + video-player-01 + media-carousel-01 + engagement-bar-01 + comment-thread-01 + post-card-01 + story-rail-01); 1 pending — `story-viewer-01` (eighth ship, story doublet part 2 + FM adoption gate). After 8/8 ship: `/sandbox/social-feed-page-01` Tier-3 composition wires the whole arc. |
| `post-card-01` | data | alpha | 0.1.0 | **Sixth of 8 in the social-posts-system arc; THIRD cross-folder import precedent + FIRST Tier-2 composite in pro-ui.** Variant dispatcher with 4 layouts (`feed` / `compact` / `list` / `detail`) composing all five Tier-1 social siblings — `expandable-text-01` (body), `media-carousel-01` (carousel), `engagement-bar-01` (actions), `comment-thread-01` (detail thread), `video-player-01` (transitive via media-carousel-01). Migration origin: kasder `kas-social-front-v0` `AdvancedPostCard.tsx` (167 LOC). **Variant dispatch** via single `<PostCard01 variant="...">` (matches event-card-01 precedent); per-variant parts in `parts/<variant>-variant.tsx`. **Auto-wired heart-burst** heuristic — wires when `post.media?.length > 0` AND `onLike` provided AND `variant ∈ {feed, detail}`. `disableHeartBurst: true` opts out. Hosts wanting full burst control use `renderMedia` slot. **Always-uncontrolled** with `reset(next: Post)` + `dispatch(action)` imperative escape hatches mirroring comment-thread-01's pattern; component captures `post` as initial state on mount + manages `statefulPost` going forward. **Local engagement mirror** (R-Plan-1) — initialized from `post` on mount via `useState`. Bar runs always-controlled with mirror values; optimistic `onLike`/`onBookmark` dispatch mirror first then fire host handler; realtime `engagementSubscribe` deltas patch mirror + fire `onSubscribeEngagementDelta` callback (R-Plan-4). Resolves the always-uncontrolled ↔ engagement-bar-01-controlled-mode conflict that would otherwise cause "click flips no visible change" (since `post.isLiked` never updates without prop change). **Wrapped `PostHandlers`** (R-Plan-2) — consumer-provided `engagementActions(post, wrappedHandlers, variant)` slot receives pre-wrapped handlers; their actions get correct optimistic behavior without knowing about the mirror. `defaultPostEngagementActions(post, handlers, variant)` exported for "extend, not replace" use cases. **Default kebab** (`Bookmark` / `Share` / `Copy link` / `Report`) via `defaultPostKebabActions(post, handlers, labels)` — items appear only when their handlers are wired (or, for Copy link, when `getHref` is provided). `kebabActions?(post): CommentMenuItem[]` slot for full takeover; returning `[]` hides kebab entirely. **`getHref` double-duty** (R-Plan-5): single prop drives (a) overlay-link in feed/compact/list variants AND (b) "Copy link" kebab item that writes resolved URL to `navigator.clipboard.writeText`. Detail variant ignores overlay-link (destination doesn't link to itself); kebab Copy-link still works. `linkComponent?: ElementType` for polymorphic root. **Two separate subscribe props** — `engagementSubscribe?: Subscribe<EngagementDelta>` (owned by post-card; deltas → mirror) + `commentSubscribe?: Subscribe<CommentDelta>` (forwarded directly to embedded `<CommentThread01>`). Distinct delta types stay distinct — no unified union forces consumers to learn a third type. **Detail variant** embeds `<CommentThread01 ref={threadRef} comments={commentThread} pageSize={commentPageSize}>` automatically; `renderCommentSection?(post, handlers)` slot for full takeover (e.g., custom threaded reactions / video comments / AI summary). `commentThread?: Comment[]` + `commentPageSize?: number` are card-level props, NOT on `Post` (R-D-1: posts and comment trees are typically separate API endpoints). **Imperative handle:** `openKebab` / `triggerLike` (card-level: bar.triggerLike() + setBurstKey(k+1) when wired — distinct from `getEngagementHandle().triggerLike()` which only flips the bar) / `getCurrentPost` / `reset(next: Post)` / `getEngagementHandle` / `getThreadHandle` (detail variant only; null otherwise). **Slots at every connective seam:** `renderHeader(post, helpers)` / `renderContent(post)` / `renderMedia(media, { onDoubleTap })` / `engagementActions(post, handlers, variant)` / `renderEngagementBar(post, defaults)` / `kebabActions(post)` / `commentActions(comment, helpers)` (forwarded to thread) / `renderCommentSection(post, handlers)` (detail only). **Verified badge** — sealed `<VerifiedBadge>` part, RSC-compatible (no `"use client"`), inline-SVG kasder pattern, configurable aria-label. 14 files in sealed folder (1 root + 6 parts: feed-variant + compact-variant + list-variant + detail-variant + post-header + verified-badge + variant-shared.ts internal type bundle + 1 lib `defaults.tsx` (JSX in icon defaults) + types + dummy-data + demo + usage + meta + index); 11 ship via the registry. Demo ships 7 sub-tabs (Feed with multi-image carousel + heart-burst + console-logged handlers / Compact sidebar widget / List admin row with custom kebab actions / Detail variant with embedded thread + featured post / Text-only mode in feed / Realtime detail variant with both engagement + comment subscribes / Custom engagement actions extending defaults with Wand2 Remix). dummy-data exports 7 sample posts (text-only / single-image / multi-image / video / mixed-media / featured / long-text), detail-mode comment thread, viewer fixture, and synthetic `createDummyEngagementSubscribe` + `createDummyCommentSubscribe` factories. **No new shadcn primitives needed** — `avatar` / `button` / `card` / `dropdown-menu` all already in `src/components/ui/`. **No framer-motion.** Inherits engagement-heart-burst CSS keyframe from engagement-bar-01 sibling. **All Stage-1 + Stage-2 locks committed silently per the locked-pattern memory** ([feedback_no_meta_questions_when_pattern_is_locked.md](../C:/Users/AsiaData/.claude/projects/e--2026-ilinxaDOC-ilinxa-ui-pro/memory/feedback_no_meta_questions_when_pattern_is_locked.md)) — 24 Q-Ps cite their sources (Stage-1 sign-offs: Q1 single dispatcher / Q2 slot-with-auto-default / Q3 heuristic burst / R-D-1 commentThread off Post / R-D-2 PostHandlers shape / R-D-3 triggerLike semantics / R-D-5 getHref double-duty + Stage-2 R-Plan-1..4: local mirror / wrapped handlers / reset+dispatch handle / card owns engagementSubscribe). **Implementation deviations:** (1) `lib/defaults.ts` → `lib/defaults.tsx` rename — JSX in icon defaults required `.tsx` (same pattern as info-list-01's dummy-data rename). (2) Two unused `eslint-disable @next/next/no-img-element` directives in compact-variant and list-variant — pro-ui ESLint already overrides for `src/registry/components/**`; removed (same pattern as people-grid-01 + media-carousel-01). (3) `cn` + `PostCard01Handle` unused imports in root — removed during lint pass. (4) Stage-2 re-validation surfaced 4 architectural consequences of the always-uncontrolled lock (R-Plan-1..4) — they're not optional, they're required for the lock to work coherently with engagement-bar-01's controlled-mode shape. **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm registry:build` produced `post-card-01.json` (11 files, all `registry:component`, target convention locked, registryDependencies = 5 cross-folder siblings + 4 shadcn primitive URLs) + `post-card-01-fixtures.json`. **Components page renders at HTTP 200** (`/components/post-card-01` on local dev). Procomp docs at [docs/procomps/post-card-01-procomp/](../docs/procomps/post-card-01-procomp/) — description + plan + guide all present. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/post-card-01` (auto-pulls all 5 Tier-1 social siblings + 4 shadcn primitives) + `@ilinxa/post-card-01-fixtures`. Registry now 62 items (31 base + 31 fixtures). **Not yet validated in browser:** hydration, all 7 demo tabs rendering, double-tap heart-burst on multi-image carousel in feed variant, Detail variant's embedded thread receiving comments, realtime tab's both subscribes ticking simultaneously, custom kebab + custom engagement actions tabs rendering correctly, polymorphic linkComponent with NextLink, overlay-link covering article surface in feed/compact/list, mobile touch behavior on `pointer-coarse:opacity-100` kebab inheritance from comment-thread-01. **Social-posts-system arc:** 6/8 done (expandable-text-01 + video-player-01 + media-carousel-01 + engagement-bar-01 + comment-thread-01 + post-card-01); 2 pending — story-rail-01 (next) + story-viewer-01 (FM adoption gate). After 8/8 ship, the Tier-3 composition `/sandbox/social-feed-page-01` is the natural next step. |
| `comment-thread-01` | data | alpha | 0.1.0 | **Fifth of 8 in the social-posts-system arc; SECOND cross-folder import precedent (after media-carousel-01 → video-player-01).** Recursive comment tree composing `expandable-text-01` (long bodies) + `engagement-bar-01` `variant="compact"` (per-row like). Inline-expand past `maxDepth` (default 2), 24px indent per depth, optimistic add/like/delete via internal reducer, `Subscribe<CommentDelta>` realtime contract identical to engagement-bar-01's `Subscribe<TDelta>` shape (single mental model across the family). Composer: autosize textarea (roll-our-own ~25-LOC `useAutosizeTextarea` — pure DOM mutation in `useLayoutEffect`, no `react-textarea-autosize` peer dep), Enter-submits / Shift+Enter newline / Escape cancels. Inline ephemeral reply composer per row (single in DOM at a time, `replyParentId` lives on thread root). Default kebab: own-only Delete + Report (when `onReportComment` wired); `commentActions?(comment, helpers)` slot returns `CommentMenuItem[]` for full takeover. Standalone `<CommentComposer>` exported (article-page hero CTAs without the thread). `useAutosizeTextarea` + `commentReducer` + `useCommentState` + `defaultRelativeTime` + `toDate` all publicly exported per dynamicity primacy. **Always-uncontrolled** with `reset(next)` + `dispatch(action)` imperative escape hatches for external state sync (Q-P15 walked back description's "hybrid controlled/uncontrolled" carry-over from engagement-bar-01 — the right call for engagement actions, the wrong call for a recursive stateful tree where prop-identity churn would silently re-sync). **Per-row `<EngagementBar01>` always-controlled by thread reducer** (Q-P12) — `liked: comment.isLiked ?? false` + `count: comment.likes` per render activates engagement-bar-01's per-field controlled detection; bar fires `onToggle(next)` without flipping internal state; thread reducer is single source of truth under realtime + optimistic flow. Optimistic add: head insertion for top-level (newest first; pairs with "Load older" at bottom), tail insertion for replies (chronological under each parent). Optimistic temp id via `crypto.randomUUID()` (no `nanoid` peer dep); when host returns a real `Comment` from `onAddComment`, reducer dispatches `swap-temp` to replace temp by id. Reducer is idempotent on `like-toggle` (no count flip when `isLiked === nextLiked`). Tree-walk on every delta is `O(n)` per delta (acceptable <1k nodes; flat-map index is v0.2 candidate). `defaultRelativeTime` formatter (Just now / 5m / 2h / 3d / 2w / "March 5") — tighter granularity than content-card-news-01's day-level formatter, no `date-fns` peer dep. `now` resolved fresh per render via `format(toDate(...))` default arg (no per-node `nowRef` — that captured mount time and went stale across re-renders). `useId()` per `<CommentNode>` for `aria-labelledby={authorNameId}` + `aria-controls={repliesContainerId}` on view-replies link. Slots: `renderNode(comment, depth, helpers)` (full row takeover; thread still owns recursive structure + inline composer + kebab); `renderViewReplies(parentId, count)` (override inline-expand for navigate-to-detail mode); `renderComposer(state, helpers)` (mention picker / emoji / attachments); `composerEmptyState` (sign-in CTA when `currentUser` absent — composer is hidden, not visible-but-disabled, per Q7 lock); `emptyState`. **`comments` prop is initial state ON MOUNT only** — subsequent prop reference changes are IGNORED (Q-P17). Documented as a footgun in usage.tsx + meta.notes; hosts wanting external state push use `ref.current.reset(next)`. Inline reply composer focus restoration: on submit/cancel, `requestAnimationFrame(() => trigger.focus())` returns focus to the parent comment's "Reply" button (a11y best practice; trigger captured via `e.currentTarget` at click site, not via ref-during-render). Touch-friendly kebab via Tailwind v4's `pointer-coarse:opacity-100` modifier (R-Plan-8) + `group-focus-within:opacity-100` for keyboard users. 15 files in sealed folder (1 root + 5 parts: comment-node + comment-composer + view-replies-link + comment-empty-state + comment-kebab + 2 hooks: use-comment-state + use-autosize-textarea + 1 lib: format-time + types + dummy-data + demo + usage + meta + index); 11 ship via the registry. Demo ships 7 sub-tabs (Flat / Nested d2 / Nested d3 / Paginated / Realtime / No-user / Compact). dummy-data exports 4 trees (flat / nested-2 / nested-3 / large-paginated) + viewer fixture + `generateOlderPage(page)` synthetic generator + `createDummySubscribe()` factory firing `added` / `liked` / `edited` deltas every 6s. **Plan-stage refinements applied (R-Plan-1..12):** R-Plan-1 walked back "hybrid controlled/uncontrolled" → always-uncontrolled with `reset()` + `dispatch()`; R-Plan-2 per-row engagement-bar-01 always-controlled; R-Plan-3 indentPx default 24px (not 16; matches Twitter/Slack), bodyMaxLines 4 default / 2 compact; R-Plan-4 Reply button hidden when no currentUser; R-Plan-5 inline composer focus restoration; R-Plan-6 loud `comments` prop staleness callout in usage; R-Plan-7 standalone composer onSubmit is `Promise<void>`-only; R-Plan-8 `pointer-coarse:opacity-100` for kebab on touch; R-Plan-9 dropped unused `linkComponent` prop (non-breaking to add later); R-Plan-10 fresh `now` per render (not per-node `nowRef`); R-Plan-11 idempotent `like-toggle`; R-Plan-12 `useId()` per node for a11y. **Implementation deviations from plan:** (1) `react-hooks/refs` lint error caught — initial draft had `if (autoFocus && !didAutoFocus.current && textareaRef.current)` block during render writing `didAutoFocus.current = true`; fix: moved auto-focus into `useEffect(() => { if (autoFocus) ref.current?.focus() }, [])`. (2) Same lint family caught in `comment-node.tsx` — `helpers.onReply` closure captured `replyTriggerRef.current` inside `useMemo`, lint flagged the deps as "ref accessed during render"; fix: removed `replyTriggerRef` from node; default Reply button reads `e.currentTarget` at click site; helpers' `onReply` passes `null` trigger (slot consumers handle their own focus restoration). (3) Removed unused `variant` destructure from `<CommentNode>` (passed through via `{...props}` spread to children; not used locally). **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm registry:build` produced `comment-thread-01.json` (11 files, all `registry:component`, target convention locked) + `comment-thread-01-fixtures.json`. Procomp docs at [docs/procomps/comment-thread-01-procomp/](../docs/procomps/comment-thread-01-procomp/) — description + plan + guide all present. **Components page renders at HTTP 200** (`/components/comment-thread-01` on local dev). Consumer install via `pnpm dlx shadcn@latest add @ilinxa/comment-thread-01` (auto-pulls `expandable-text-01` + `engagement-bar-01` siblings + 4 shadcn primitives: avatar, button, dropdown-menu, textarea). No new shadcn primitives installed during this ship (all four already in `src/components/ui/`). **Memory updated:** [feedback_no_meta_questions_when_pattern_is_locked.md](../C:/Users/AsiaData/.claude/projects/e--2026-ilinxaDOC-ilinxa-ui-pro/memory/feedback_no_meta_questions_when_pattern_is_locked.md) — don't surface "Q-P" or "open question" entries when the answer is "what does the most recent similar component do?"; commit them as locks with the precedent cited and save questions for genuinely-novel architectural calls. **Migration origin:** kasder `kas-social-front-v0` `PostEngagementPanel.tsx` (468 LOC), `CommentItem` sub-component lines 413–467 as the visual reference. **Not yet validated in browser:** hydration, autosize on first composer mount before font load, all 7 demo tabs rendering, double-tap heart-burst NOT applicable here (per-row bar is compact + like-only), inline reply composer focus restoration after submit/cancel, view-N-replies inline-expand on the depth-3 fixture, "Load older comments" appending pages chronologically, realtime deltas patching the tree without UI tear, kebab visibility on touch (pointer-coarse). **Social-posts-system arc:** 5/8 done (expandable-text-01 + video-player-01 + media-carousel-01 + engagement-bar-01 + comment-thread-01); 3 pending — post-card-01 (Tier-2 composite, depends on all 1–5), story-rail-01, story-viewer-01 (FM adoption gate). Registry now 60 items (30 base + 30 fixtures). |
| `markdown-editor` | forms | alpha | 0.1.0 | Fifth (final) Tier 1 graph-system pro-component. **Heaviest pro-component by bundle weight** (~180KB ceiling per description success #9; ~150KB is CM6 substrate per decision #26). CodeMirror 6 substrate (10 npm packages: @codemirror/{state, view, commands, language, lang-markdown, autocomplete, search} + @lezer/{markdown, highlight} + marked) with **per-instance `new Marked()`** for preview parsing (Q-P1 — avoids global mutation). Three view modes (edit / split / preview) with controlled-or-uncontrolled dispatch + container-query stacking <480px (description §8.5 #2). Default 8-item toolbar (bold/italic/code/link/lists/blockquote/heading-cycle) extensible by spreading `defaultMarkdownToolbar`; toolbar items receive `ToolbarCtx` with live EditorView + 3 dispatch helpers; toolbar separators via `id` prefix marker. **`[[wikilink]]` autocomplete + decoration** — wikilinkCandidates flow through CM6 `StateField` + `StateEffect` so host-side updates re-decorate without remount (Q-P5 + validate-pass refinement #2 — was: bake-in at extension creation; broke on graph mutations). Single canonical `WIKILINK_PATTERN` source string + `makeWikilinkRegex()` factory shared by CM6 `MatchDecorator`, autocomplete trigger, and `marked` preview extension (Q-P2 — fresh `g`-flag regex per use, no `lastIndex` pollution). Autocomplete capped at 50 results with sentinel "+ N more (refine search)" row (Q-P3 — `apply: closeCompletion`, `type: "text"`, `boost: -100`). Preview wikilinks are click + keyboard activatable (`role="link"` + `tabindex="0"` + Enter/Space via PreviewPane delegation; only when `onWikilinkClick` supplied — validate-pass refinement #4). Echo-guarded value-prop sync via `SyncAnnotation` + `lastSyncedValueRef` (Q-P9 — prevents infinite loop if host calls `setValue` from inside `onChange`). Cmd/Ctrl+S → `onSave(currentDoc)` payload from live CM6 doc, NOT React `value` prop (description §8.5 #5 — avoids React-batching staleness); `preventDefault` only when handler is supplied (description §8.5 #4). User CM6 extensions appended LAST = OUR defaults win conflicts (Q-P8 — earlier-in-array = HIGHER CM6 default precedence; reasoning fixed on validate pass). React 19 `useDeferredValue` schedules preview parse at low priority (Q-P7 + validate-pass refinement #9; replaces "no debounce" deferred-to-v0.2 deferral). Imperative handle: focus / undo / redo / insertText / getSelection / getValue / getView (escape hatch — substrate-leak risk acknowledged per §8.5 #6). React 19 ref-as-prop preserves `<MarkdownEditor<TCandidate>>` generic. Theme via CSS-variable mapping in `EditorView.theme()` — dark/light flip with no remount (Q5). 28 files (matches plan §8.1 exactly). Demo ships 8 sub-demos (basic / view modes / wikilinks / custom toolbar / read-only / onSave / no toolbar / imperative handle) over a 12-node graph fixture. Built on shadcn primitives Badge/Button/Tabs/Tooltip + lucide-react icons. **Plan deviations:** **(1) `useCodeMirror` exposes `view` as React state** (was: `viewRef` per plan §8.2). React Compiler-aware lint flags `viewRef.current` reads during render; `markdown-editor.tsx` needs the live view to compute `ToolbarCtx` + per-button `isActive()` at render time. View state is set in the mount effect; sync effects depend on `[view, ...]` so they fire after mount. Consistent with prior ships' similar refactors (e.g., entity-picker deviation #1 — useState for trigger node). **(2) `useMarkdownEditorHandle` accepts `view` (state) instead of `viewRef`.** Handle is recomputed when view changes; methods always close over latest view. **(3) Mount-time prop snapshot via per-prop refs updated in a passive effect.** Allows mount effect to read latest props with `[container]` deps only, avoiding remount cascade on prop changes. All subsequent prop changes flow through the 4 sync effects per §5.2 (value / readOnly / wikilinkCandidates / extensions). **(4) Toolbar separator items use id-prefix marker (`"sep"`)** instead of a discrete `type` discriminator. Keeps `ToolbarItem` flat; sep items have a no-op `run()` that's never called. **(5) Bundle size verification via `size-limit`** deferred to a future audit pass (plan §13.5 #4); no prior ship has wired size-limit, consistent posture. **Verification:** `pnpm tsc --noEmit` + `pnpm lint` (1 pre-existing rich-card warning; no new) + `pnpm build` (compiled in 26.5s, all 13 pages prerendered including `/components/markdown-editor`). SSR served `HTTP 200` (~96KB) with all 8 tab triggers + Basic demo default content rendered; `/components` index lists the new entry. Phase A end-gate fixture verified (7/7): `[[label]]`, `[[label\|alias]]`, `![[image.png]]` (skipped), `[[label with spaces]]`, `[[]]` (skipped), `[[a]] [[b]]` (both), `[[outer[[inner]]]]` (inner only). **Not yet validated in browser:** hydration, CM6 EditorView mount + lifecycle, echo-guarded value sync (Q-P9 smoke per §13.5 #1), wikilink decoration runtime candidates update (§13.5 #20), Cmd+S onSave handler, autocomplete popup positioning + kind badges + 50-result cap sentinel, keyboard activation of preview wikilinks (Enter/Space — §13.5 #17), container-query split-view stacking at <480px (§8.5 #2), theme dark/light flip without remount (§13.5 #3), imperative handle methods. Composed inside force-graph from v0.5 onward (doc nodes + wikilink reconciliation per decision #36 — reconciliation lives in force-graph, NOT here; the editor's contract is `onSave(value)` only). |
| `kanban-board-01` | data | alpha | 0.2.0 | **Column-based board with renderer-registry, swimlanes, color-tinted columns, optional CRUD, optional swimlanes, soft `maxItems` cap, native column scroll.** Items are pure-JSON `{ id, rendererId, data, swimlaneId?, locked? }` records; the board delegates rendering to a renderer looked up by `rendererId`. Three renderers in the demo: built-in `kanbanCardRenderer` (lightweight title + tags + assignees + meta) + built-in `kanbanNoteRenderer` (title + body) + a demo-side **rich-card adapter** that wraps `<RichCard>` from `@ilinxa/rich-card` with full feature passthrough (editable, predefined keys, internal DnD, `onChange` flowing back through hoisted board state). Built-in renderers ship inside the kanban folder; the rich-card adapter lives in `demo.tsx` (docs-only). **v0.2 additive:** `KanbanCardRenderer.dragHandle?: "shell" \| "header"` — `"shell"` (default) keeps v0.1 whole-card grab; `"header"` renders a `h-7` grip strip on top with the dnd-kit listeners scoped to it, body stays fully interactive (required for renderers with internal pointer interactions like rich-card). DnD via `@dnd-kit/core` + `@dnd-kit/sortable`. Per-column movement controls (`allowReorder` / `allowIncoming` / `allowOutgoing` / `acceptsRendererIds`), per-item lock, board-level `readOnly`. Inline editor per renderer via `editForm`; renderers without an `editForm` fire the consumer callback only. 6-swatch color palette (overridable). Collapsible columns (~40px vertical strip; auto-expand on drop). Swimlanes — each `(col × lane)` cell is its own droppable. Keyboard DnD via dnd-kit's keyboard sensor (Space lift / arrows / Space drop / Escape cancel). **Column-body scroll** uses native `overflow-y-auto` on a `flex-1 min-h-0` div (replaced radix `<ScrollArea>` after it failed to constrain inside the flex-1 chain when items expanded tall). 26 files in sealed folder (parts/ + hooks/ + lib/); deps: 8 shadcn primitives (`avatar`, `badge`, `button`, `dropdown-menu`, `input`, `popover`, `textarea`, `tooltip`) + 4 npm packages (`@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities`, `lucide-react`). Demo seeds heterogeneous dataset: 3 rich-card items (codearea + image + table + quote + list + nested children + `__rcmeta`) + 6 kanban-card + 2 kanban-note across 4 columns × 2 swimlanes. **Verification:** `pnpm tsc --noEmit` + `pnpm lint` clean. **Not yet committed** (still on `968ba97`); browser-validated against drag (header-mode + shell-mode), column scroll, and rich-card edit roundtrip pending. |
| `project-card-01` | data | alpha | 0.1.0 | **Project / case-study preview card — third in the kasder card lineup (after `content-card-news-01` editorial + `event-card-01` events).** Migration origin: kasder `kas-social-front-v0` `ProjectCard.tsx` (public landing) + `BusinessProjectsSection.tsx` (embedded business-profile widget). 2 visual variants — `grid` (vertical image-on-top, neutral-darken gradient `from-black/60 via-black/15` for badge legibility — kasder's `from-primary/80` lime tint was revised after first-render review since it overpowered photographic content, hover-reveal "View details" CTA with `bg-primary` lime pill, lift-on-hover) + `feature` (full-bleed image background, heavier neutral-darken gradient `from-black/90 via-black/40`, white-on-dark text, NO hover-CTA, NO meta row — designed for the future `bento-grid-01`). 3-state **editorial** status (`completed` / `ongoing` / `planned`) — set on data, NOT derived from a clock; differs from `event-card-01`'s 6-state time-window kernel by design. Maps to existing tokens only — `bg-primary` (lime, completed) / `bg-chart-3` (teal, ongoing — revised from `bg-accent` after first-render review since pro-ui's `--accent` is a near-white surface token, not a brand color) / `bg-muted` + border (planned). **Cleanest dependency footprint of the three card migrations:** zero new design-system tokens, zero new shadcn primitives, zero new peer deps (`framer-motion` from source dropped — card-level reveal is layout-orchestrated). Public `PROJECT_STATUS_CONFIG` kernel — pure data, server-component-importable, tree-shakeable. Polymorphic `linkComponent` slot, overlay-link pattern with `actions` slot (yields category to bottom-right on grid; stacks status under category at top-left on feature), 3-layer href precedence chain (`getHref(project) > href > project.href > '#'`), `categoryStyles` map (default empty → universal `Building2` icon + neutral chip), `featured` flag (top accent border on grid / inset ring on feature + Star title prefix), image fallback (`bg-muted` block + Building2 when `image` empty). 11 files in sealed folder; 7 ship via the registry (sealed source + lib helpers, NOT `demo.tsx` / `usage.tsx` / `meta.ts`). **Public-page assembly composes already-shipped pro-comps with zero new code:** `page-hero-news-01` (subsumes kasder `ProjectsHero`) + `filter-bar-01` (subsumes category-pill row) + `grid-layout-news-01` (subsumes 1/2/3-col grid + infinite-scroll via the shipped `useMagazineFilter` hook). **Future siblings deferred to separate intakes:** `bento-grid-01` (layout, generic — owns the kasder `getLgPattern`/`getMdPattern` 5-rhythm kernel), `project-detail-dialog-01` (data, sequential prev/next viewer composing `info-list-01` + `expandable-text-01` + `media-carousel-01` + `engagement-bar-01` + `story-viewer-01`-style state machine). |

## Roadmap

Next candidates, ordered by team utility:

1. `data/stat-card` — value + label + delta + sparkline. Universal in dashboards.
2. `feedback/empty-state` — icon + title + body + primary action.
3. `forms/multi-select` — combobox with tag chips (shadcn has Command, no real multi-select).
4. `layout/page-header` — title + breadcrumbs + actions slot.
5. `feedback/notification-feed` — grouped, time-bucketed, read/unread.
6. `navigation/command-palette` — cmd+k, grouped results.
7. `media/dropzone` — drag-drop + progress + previews.

**`rich-card` v0.2 follow-ups** (deferred from v0.1.0, by phase per [rich-card-procomp-plan.md](../docs/procomps/rich-card-procomp/rich-card-procomp-plan.md)): v0.2 inline editor (add/edit/remove fields/cards/predefined-key elements, granular change events, dirty tracking, explicit selection); v0.3 drag-drop (2 scopes), bulk ops, per-level/per-card/per-key permissions, custom predefined-key registration, virtualization if needed; v0.4 sync validation hooks + per-commit undo/redo; v0.5 (deferred indefinitely, separate companion) markdown ↔ JSON adapter. The v0.1 API surface is designed so each phase adds props rather than changing them.

**`workspace` v0.2 follow-ups** (deferred from v0.1.0): detach-to-OS-window for multi-monitor users; touch / pen gesture support; aligned-edge linked resize (multiple edges move together); DnD between areas; cross-breakpoint transition animation. The v0.1 API surface is designed so v0.2 doesn't break it.

## Open decisions / TODOs

- ~~**Phase 0 risk spike PENDING — gates `force-graph` v0.1 implementation.**~~ **CLOSED 2026-04-30 per system decision #38** — Phase 0 risk spike CANCELLED; the dashed-edge feature was dropped and replaced with stock Sigma `EdgeRectangleProgram` + `@sigma/edge-arrow` (per-edge `color` + `size` attribute differentiation between "soft" and "default" edges). **Force-graph v0.1 implementation gate is now unblocked at the planning level** — no GPU benchmark prerequisite. The original 2-day spike budget reverts to v0.1 implementation. Spike brief at [force-graph-phase-0-spike-brief.md](../docs/procomps/force-graph-procomp/force-graph-phase-0-spike-brief.md) preserved with SUPERSEDED banner; methodology sections may be reused if real-world performance regression surfaces in v0.6 perf-hardening.
- ~~**Chart palette still neutral:** `--chart-1..5` are greyscale.~~ **CLOSED 2026-05-01** — analogous lime → emerald → teal → cyan → blue palette defined in [globals.css:69-74](../src/app/globals.css#L69) (light) + [globals.css:104-109](../src/app/globals.css#L104) (dark). Light mode L drifts 0.74→0.58 as hue rotates 132°→260°; dark mode lifts to 0.80→0.66 for the graphite background. All chroma ≤ 0.18 (under the 0.20 mandate).
- ~~**Site nav has only "Components":**~~ **CLOSED 2026-05-01** — `Docs` nav link + GitHub icon-button (linking https://github.com/ilinxa/pro-ui) added to [site-header.tsx](../src/components/site/site-header.tsx). Mobile menu (sheet/drawer) still unnecessary at 2 nav items — revisit when 4+.
- **Reserved-but-unrendered meta fields:** `RegistryEntry.examples`, `ComponentMeta.thumbnail`, `ComponentMeta.subcategory`, `ComponentMeta.related` exist on the types but render nowhere yet. Decide whether to wire any (each adds detail-page surface) or remove them.
- ~~**`alpha` and `beta` badges look identical:**~~ **CLOSED 2026-05-01** — variant mapping in both [components/page.tsx:76-87](../src/app/components/page.tsx#L76) + [[slug]/page.tsx:54-65](../src/app/components/[slug]/page.tsx#L54) updated: `alpha` → `outline` (border-only, lightest weight), `beta` → `secondary` (filled muted), `stable` → `default` (lime), `deprecated` → `destructive`. Visual progression now reads alpha → beta → stable → deprecated. SSR-verified: rich-card (beta) emits `data-variant="secondary"`, force-graph (alpha) emits `data-variant="outline"`.
- ~~**Footer version is hardcoded** (`v0.1.0`) in `site-footer.tsx`.~~ **CLOSED 2026-05-01** — [site-footer.tsx](../src/components/site/site-footer.tsx) imports `version` from root [package.json](../package.json) (tsconfig already had `resolveJsonModule: true`); single source of truth. Bump `package.json#version` and the footer follows automatically.
- **Lime contrast pattern is dark-on-lime.** Both modes use near-black `--primary-foreground`. Don't switch to white — contrast fails. If a use case really wants white text on a green action, use a darker green (e.g. `oklch(0.45 0.18 142)` forest) and pair white text — but that becomes a separate token, not `--primary`.
- **MDX for usage docs:** currently `usage.tsx`. Consider migrating to `usage.mdx` once we cross ~5 components with prose-heavy guidance.
- ~~**Public registry build:** no `pnpm dlx shadcn build` config or `public/r/*.json` output yet. Defer until publish phase.~~ **CLOSED 2026-05-01** — `registry.json` authored at repo root with 16 items; `pnpm registry:build` (standalone) + `pnpm vercel-build` (chained with `next build`) both wired; `public/r/*.json` artifacts committed and auto-regenerated by Vercel on every deploy.
- **NPM publish artifacts:** no `tsup`/`rollup`, no `package.json` exports map. Distribution shipped via shadcn-registry instead (lower friction for the team-internal use case). NPM may still be worth doing for the heavyweights (rich-card 51 files, markdown-editor 24 files + 10 codemirror peer deps) — defer until an external consumer or a real updates-friction pain point shows up.
- **Test debt — `workspace`:** workspace ships v0.1.0 without unit tests because no test runner is wired in this repo. The pure modules in `src/registry/components/layout/workspace/lib/` (`tree.ts`, `reducer.ts`, `geometry.ts`) are written to be testable in isolation when Vitest lands. Verification for v0.1.0 is demo-driven (the dummy registry's `counter` component proves Q3 state preservation across split visually). Land Vitest as a separate STATUS decision before workspace ships v0.2.
- **Test debt — `rich-card`:** same posture. The pure modules in `src/registry/components/data/rich-card/lib/` (`parse.ts`, `serialize.ts`, `classify-key.ts`, `infer-type.ts`, `reducer.ts`) are pure functions and trivially testable when Vitest lands. The single highest-value test is a **round-trip property test** — random `RichCardJsonNode` → `parse` → `serialize` → `parse` → assert tree-equal — which would lock down the Q3 fixed-point contract. v0.1.0 verification is demo-driven via the three demos in [`dummy-data.ts`](../src/registry/components/data/rich-card/dummy-data.ts) and the manual "Read canonical JSON" button in [demo.tsx](../src/registry/components/data/rich-card/demo.tsx).

## Recent decisions (keep latest ~10)

- 2026-05-08 — **Procomp review sweep — session 3: `flow-canvas-01` v0.1.0 (solo) + xyflow-attribution correction + F-cross-08.** Tier 1 review #3: `flow-canvas-01` v0.1.0 → **Pass with follow-ups** (7 findings: 3 High [F-01 not in `registry.json` / 404 at install / carries F-cross-03; F-02 `meta.ts.dependencies` under-declared by 4 shadcn primitives + lucide-react; F-03 `process.env.NODE_ENV` use violates description §6 #10], 3 Medium [F-04 stale `related: ["force-graph"]`; F-05 plan-vs-actual file count drift 35 → 30 + `controls.tsx` not authored; F-06 STATUS file-count claim 39 vs actual 30], 1 Low [F-07 xyflow Pro etiquette note]). Recommended: v0.1.1 patch + ship to `registry.json` (closes F-01 + F-cross-03). Full report at [docs/procomps/flow-canvas-01-procomp/reviews/2026-05-08-v0.1.0-review.md](../docs/procomps/flow-canvas-01-procomp/reviews/2026-05-08-v0.1.0-review.md). **Solo review** (force-graph paired-partner removed earlier this session). **xyflow-react-pro footgun fix correctly applied** (module-scope `NODE_TYPES`/`EDGE_TYPES`). **STATUS.md attribution-note corrected**: `proOptions: { hideAttribution: true }` is permitted by MIT — no xyflow Pro license legally required; the prior "technically requires" framing was wrong. **F-cross-08 added** to tracker: `process.env.NODE_ENV` use likely systemic across the library (4 occurrences in flow-canvas-01); recommend relaxing description §6 #10 to allow standard NODE_ENV dev-warn gates. Tier 1 progress: 3/9 reviewed; total 3/36. Next session (session 4): paired Tier 1 — `article-body-01` v0.2 + `data-table` v0.1.

- 2026-05-08 — **`force-graph` removed pending recreation under new design + plan.** v0.2-frozen source + all 4 versioned procomp planning docs (description, v0.1/v0.2/v0.3 plans, phase-0 spike brief, 2 obsidian.jsx reference examples) **archived** to [docs/migrations/force-graph/](../docs/migrations/force-graph/). Intake notes at [docs/migrations/force-graph/source-notes.md](../docs/migrations/force-graph/source-notes.md) — frames the v0.2-frozen → v3-TBD transition as a self-migration. **Surfaces touched:** `src/registry/components/data/force-graph/` deleted; `src/registry/manifest.ts` lines 33-35 + REGISTRY entry removed; [docs/component-versions.md](../docs/component-versions.md) total 37 → 36 (data 21 → 20); sweep-tracker Tier 1 row removed (now 9 Tier 1, total 36); F-cross-03 narrowed to `flow-canvas-01` only; F-cross-05 unexpected-fail count 5 → 4; smoke-driver `SLUGS` array entry removed. **Zero consumer impact** — component was never in `registry.json` / never installable via shadcn add. Internal docs-site `/components/force-graph` route goes away. The `.claude/skills/sigma-react-pro/` skill is **retained** as v3 reference — re-evaluate if recreation chooses a different graph library (cosmos / cytoscape / d3-force-3d / xyflow-as-graph). Session 3 of the review sweep becomes solo `flow-canvas-01` instead of paired (~60-90 min vs 120 budgeted).

- 2026-05-08 — **Procomp review sweep — session 2: rich-card v0.4 + F-cross-05 root cause + 2 new cross-cutting findings.** Tier 1 review #2: `rich-card` v0.4.0 → **Pass with follow-ups** (7 findings: 2 High [F-01 virtualization-prop-not-wired-to-render + F-02 dep version drift `@dnd-kit/sortable: ^11.x` vs producer's `^10.0.0`], 4 Medium [F-03 unused `separator`, F-04 usage import-path drift, F-05 demo says "v0.3 demo", F-06 use-virtualizer.ts dead code], 1 Low [F-07 demo amber colors]). v0.4.1 patch recommended. Beta status appropriate; stable promotion gates documented. Full report at [docs/procomps/rich-card-procomp/reviews/2026-05-08-v0.4.0-review.md](../docs/procomps/rich-card-procomp/reviews/2026-05-08-v0.4.0-review.md). **F-cross-05 root cause identified:** 4 of 5 unexpected-fails (`comment-thread-01`, `post-card-01`, `media-carousel-01`, `story-viewer-01`) declare internal sibling deps as bare slug names (e.g. `"expandable-text-01"`) in `registry.json` `registryDependencies`; shadcn CLI looks them up as built-in primitives and 404s. Fix: namespace as `@ilinxa/<slug>`. The 5th (`force-graph`) is the same root cause as `flow-canvas-01` — absent from `registry.json` (F-cross-03 expanded). **Two new cross-cutting findings:** F-cross-06 (sweep-wide usage import-path drift — affirmed across 2/2 components reviewed) + F-cross-07 (cross-component dep version drift). Tracker: [docs/reviews/sweep-tracker.md](../docs/reviews/sweep-tracker.md). Next session: kanban v0.2.1 fix-up + first paired Tier 1 (`flow-canvas-01` + `force-graph` — both F-cross-03 carriers).

- 2026-05-08 — **Procomp review sweep — session 1 pilot.** First execution of the new review system at [docs/reviews/](../docs/reviews/). Pilot: `kanban-board-01` v0.2.0 → **Pass with follow-ups** (6 findings: 2 High [F-01 hardcoded amber tokens in kanban-note, F-02 STATUS staleness], 4 Medium [F-03 dead-code use-keyboard-actions, F-04 usage import-path drift, F-05 demo indigo swatch, F-06 onDragOver visual feedback]). v0.2.1 patch recommended for F-01/F-03/F-04/F-05. Full report at [docs/procomps/kanban-board-01-procomp/reviews/2026-05-08-v0.2.0-review.md](../docs/procomps/kanban-board-01-procomp/reviews/2026-05-08-v0.2.0-review.md). Also shipped infra: sweep-tracker at [docs/reviews/sweep-tracker.md](../docs/reviews/sweep-tracker.md), Tier 2 spot-check template, smoke harness at `e:/tmp/ilinxa-smoke-consumer/`. First 37-slug smoke run against production Vercel registry: **31 pass / 1 expected-fail (`flow-canvas-01`) / 5 unexpected-fail** (`comment-thread-01`, `force-graph`, `post-card-01`, `media-carousel-01`, `story-viewer-01` — shadcn-CLI version-mismatch pattern; tracked as cross-cutting F-cross-05). Note: STATUS.md size (~88K tokens) flagged as F-cross-02; archive split recommended. Plan: [C:\Users\AsiaData\.claude\plans\now-as-we-have-snazzy-raccoon.md](../../C:\Users\AsiaData\.claude\plans\now-as-we-have-snazzy-raccoon.md). Next session: kanban v0.2.1 fix-up + first Tier 1 paired session (`flow-canvas-01` + `force-graph`). Pre-existing kanban v0.2 STATUS-row claim "not yet committed" is stale (committed in `7263286`) — F-02 follow-up.

- 2026-05-06 — **`kanban-board-01` v0.2 — `dragHandle` field + rich-card adapter pattern + native column scroll.** [src/registry/components/data/kanban-board-01/](../src/registry/components/data/kanban-board-01/) — three additive changes since v0.1 sign-off, all non-breaking. **(1) `KanbanCardRenderer.dragHandle?: "shell" | "header"`** — opt-in per renderer; "shell" (default) keeps v0.1 whole-card grab, "header" renders a `h-7` grip strip on top with dnd-kit listeners scoped to it so the body stays interactive. Required for renderers with internal pointer interactions. **(2) Rich-card adapter (demo-side)** — wires `<RichCard>` (sibling registry component) as a third renderer with full feature passthrough (`editable`, `defaultCollapsed`, `onChange`, predefined keys, internal DnD). State for rich-card items lives in the demo's hoisted `boardData`; renderer closes over a setter that walks columns to update the matching item's `data` on RichCard's `onChange`. No core-kanban changes were needed beyond `dragHandle` — the renderer-registry already supported any consumer renderer; the demo just makes the pattern visible. **(3) Column-body scroll fix** — swapped radix `<ScrollArea>` for native `overflow-y-auto` on a `flex-1 min-h-0` div + added `min-h-0` to the `Column` flex wrapper so flex-shrink propagates. Radix's ScrollArea was failing to constrain inside the flex-1 chain when items expanded tall (rich-card with expanded children + tall predefined-key blocks). Native scroll is more reliable. `scroll-area` removed from `meta.ts` shadcn deps + `registry.json` registryDependencies. **Demo dataset:** 4 columns × 2 swimlanes hosting 3 rich-card items (every predefined key + nested children + `__rcmeta`) + 6 kanban-card + 2 kanban-note items. Heterogeneous columns finally demonstrated. **Locks worth holding:** rich-card-as-renderer adapter pattern is the canonical way to plug ANY sibling rich component into kanban — copy `makeRichCardRenderer` from kanban-board-01's demo for future hosts (workspace, flow-canvas-01 sub-view, etc.) that want similar heterogeneous content. Dragh handle "header" mode is the right answer whenever a renderer owns its own pointer events. Updated meta.ts (v0.2.0; 2026-05-06; new feature lines), registry.json description + deps, all 3 procomp docs (description + plan + guide each gained a v0.2-update section), usage.tsx (replaced project-card example with rich-card adapter + dragHandle modes table). **NOT yet:** committed; browser-verified; STATUS table row was missing for v0.1 (added now together with v0.2 update).

- 2026-05-03 — **`project-card-01` v0.1 shipped — third in the kasder card lineup (after `content-card-news-01` editorial + `event-card-01` events); cleanest dependency footprint of the three card migrations.** [src/registry/components/data/project-card-01/](../src/registry/components/data/project-card-01/) — 11 files in sealed folder; 7 ship via the registry. Migration origin: kasder `kas-social-front-v0` `ProjectCard.tsx` (public landing) + `BusinessProjectsSection.tsx` (embedded business-profile widget). 2 visual variants — `grid` (vertical image-on-top, neutral-darken gradient `from-black/60 via-black/15` for badge legibility — kasder's `from-primary/80` lime tint was revised after first-render review since it overpowered photographic content, hover-reveal "View details" CTA with `bg-primary` lime pill, lift-on-hover) + `feature` (full-bleed image background, heavier neutral-darken gradient `from-black/90 via-black/40`, white-on-dark text, NO hover-CTA, NO meta row). 3-state **editorial** status (`completed` / `ongoing` / `planned`) — set on data, NOT derived from a clock; differs from `event-card-01`'s 6-state time-window kernel by design. Maps to existing tokens only (`bg-primary` / `bg-accent` / `bg-muted`). **Zero new design-system tokens, zero new shadcn primitives, zero new peer deps** — `framer-motion` from source dropped (card-level reveal is layout-orchestrated). Public `PROJECT_STATUS_CONFIG` kernel — pure data, server-component-importable, tree-shakeable. Polymorphic `linkComponent`, overlay-link with `actions` slot (yields category to bottom-right on grid; stacks status under category at top-left on feature — single-yield resolution after fixing a plan inconsistency on validate pass), 3-layer href precedence chain (`getHref(project) > href > project.href > '#'`), `categoryStyles` map (default empty → universal `Building2` icon + neutral chip), `featured` flag (`border-t-4 border-t-primary` on grid / `ring-2 ring-primary ring-inset` on feature + Star title prefix), image fallback (`bg-muted` block + Building2 icon when `image` empty). 5-tab demo (Grid / Feature bento / Featured comparison / Localized TR / Actions slot + custom href). **Plan validate-pass surfaced 5 fixes:** (1) `meta.ts` shape — used real `ComponentMeta` from `src/registry/types.ts` (description + context + tags + createdAt + updatedAt + author + dependencies + related), `meta` export name not `projectCard01Meta`. (2) Featured class precision — `border-t-4 border-t-primary` (top-only color), not `border-t-4 border-primary`. (3) `aria-labelledby={ariaLabel ? undefined : titleId}` so consumer-supplied `ariaLabel` wins cleanly. (4) `feature` variant actions-yield logic was contradictory (category yields to bottom-right AND status stacks below it at top-left — impossible); resolved by making category stay top-left and status stack below it when actions take top-right. (5) Hover-CTA outer absolute container needed `pointer-events-none` so clicks pass through to the link overlay underneath. **Public-page assembly with zero new code:** consumer composes already-shipped `page-hero-news-01` (subsumes kasder `ProjectsHero`) + `filter-bar-01` (subsumes category-pill row) + `grid-layout-news-01` + `useMagazineFilter` (subsumes 1/2/3-col grid + infinite scroll). **Future siblings deferred to separate intakes:** `bento-grid-01` (layout, generic — owns kasder `getLgPattern`/`getMdPattern` 5-rhythm kernel), `project-detail-dialog-01` (data, sequential prev/next viewer composing `info-list-01` + `expandable-text-01` + `media-carousel-01` + `engagement-bar-01` + `story-viewer-01`-style state machine). Verification: `pnpm tsc --noEmit` clean, `pnpm lint` clean (only pre-existing rich-card warning), `pnpm registry:build` produced `public/r/project-card-01.json` + `public/r/project-card-01-fixtures.json`. **2026-05-03 post-render visual revisions (5 fixes after first-render review):** (1) Grid gradient — kasder's `from-primary/80 via-primary/20` (signal-lime) overpowered photographic content; replaced with neutral `from-black/60 via-black/15` (mirrors event-card-01). (2) Meta icons (MapPin / Calendar) used `text-accent` and were invisible — `--accent` is a near-white surface token in pro-ui's neutral preset, NOT a brand color; switched to `text-muted-foreground/70`. (3) Title hover used `motion-safe:group-hover:text-accent` (same surface-token problem); switched to `motion-safe:group-hover:text-primary` (matches event-card-01). (4) `dummyCategoryStyles` used light-tint pattern (`bg-X/15 text-X border border-X/20`) — readable on sidebars but unreadable over photographic backgrounds; switched to solid `bg-X/90 text-{token}-foreground` / `text-white`. (5) `PROJECT_STATUS_CONFIG.ongoing` was `bg-accent text-accent-foreground` (same root cause); switched to `bg-chart-3 text-white` (teal — sits between completed-lime and planned-grey). Hover-CTA pill also switched from `bg-accent` → `bg-primary` for the same reason. **Lesson logged for future migrations:** "preserve source DNA verbatim" is fragile when the source uses a different design system — pro-ui's `--accent` is a SURFACE token (where shadcn-base puts hover-bg / muted-bg), NOT a brand color. The kasder source treated `accent` as a brand teal — that's a context-translation, not a direct copy. **Full doc resync 2026-05-03:** `source-notes.md` locked decisions #1 + #8 revised; `analysis.md` design-DNA tables rewritten; `procomp-description.md` status mapping + API sketch updated; `procomp-plan.md` part anatomy + `PROJECT_STATUS_CONFIG` source + Risk #6 (now "REALIZED & RESOLVED") rewritten; `procomp-guide.md` (consumer-facing, Stage 3) authored fresh with corrected status visual cheat-sheet + categoryStyles tip + variant decision matrix; `usage.tsx` 3-statuses descriptions corrected; `dummy-data.ts` both English + Turkish category style maps switched to solid backgrounds.

- 2026-05-03 — **`story-viewer-01` v0.1 shipped — eighth (and final) ship in the social-posts-system arc; story doublet part 2; closes the arc.** [src/registry/components/media/story-viewer-01/](../src/registry/components/media/story-viewer-01/) — 16 files in sealed folder; 13 ship via the registry. Full-screen sequential story viewer composing kasder's `StoryViewer.tsx` (302 LOC) + `types.ts` (16 LOC) almost exactly. Mobile fullscreen `h-dvh w-screen` / desktop centered portrait `md:h-175 md:w-100 md:rounded-2xl` (kasder-exact 400×700) via Radix Dialog (focus trap + portal + Escape + backdrop click free). **CSS-only animations** — `framer-motion` deliberately NOT adopted yet; locked motion-substrate gate moved to v0.2 swipe-to-dismiss per [project_motion_substrate.md](../C:/Users/AsiaData/.claude/projects/e--2026-ilinxaDOC-ilinxa-ui-pro/memory/project_motion_substrate.md). **Segmented progress bars** with CSS fill + per-segment `role="progressbar"`. **Pause-preserving accumulator timer** in `useStoryProgress` — tracks `accumulatedMs` across pause/resume cycles so users pausing for arbitrary durations don't lose position; kasder's naive `Date.now() - startTime` recompute silently advanced ~50ms per cycle. **Item duration resolution priority** (R-Plan-4): explicit `currentItem.duration` → video metadata via `<VideoPlayer01 onLoadedMetadata>` → `defaultItemDuration` (default 5s); fixes the kasder bug where 30s videos auto-advanced at 5s before metadata loaded. **Tap zones** (left/middle/right thirds) at z-10, header at z-20, nav arrows at z-30. Tap-zone buttons `tabIndex={-1}` + `aria-hidden="true"` (touch only; keyboard uses arrow keys). **Desktop nav arrows** for story-level navigation (hidden on mobile). **Keyboard nav** via `useStoryKeyboardNav`: ArrowLeft/Right (item nav) + Space (pause; preventDefault for page scroll) + Escape (close — funneled through Radix `onOpenChange`, NOT redundant window listener). **video-player-01 cross-folder import** — FOURTH cross-folder precedent in pro-ui after media-carousel-01, comment-thread-01, post-card-01. **Single-item-at-a-time render** (R-Plan-6) — viewer renders only `currentStory.items[currentItemIndex]`; off-screen items don't mount; `<VideoPlayer01 isActive={!isPaused}>` is the only video coordination needed. **Always-uncontrolled** with `reset(next)` + `dispatch(action)` + `goToStory` / `goToItem` / `setPaused` imperative handle. **ID-anchored cursor** (R-Plan-1) — internal cursor by `(storyId, itemId)` strings, NOT indices; derived `(storyIndex, itemIndex)` via `useMemo`. Realtime insertions/removals don't desync. `subscribe`-delta `position` defaults to `"end"` for `story-added` (cursor-stable; `"start"` works correctly because cursor is ID-based — R-D-8). **Cursor reset semantics** (R-Plan-7) — re-seeds whenever **`(initialStoryIndex, isOpen)` pair** changes (matches kasder; re-opening with same index goes to item 0). Lazy `useState` initializer + `initialStoriesRef` mirror keeps deps narrow without `eslint-disable` (R-Plan-2). **Subscribe contract** (locked arc convention): `Subscribe<StoryViewerDelta>` mirrors story-rail-01's shape: `story-added` / `story-removed` / `item-added` / `item-removed` / `story-viewed`. Mount-scoped lifecycle (NOT `isOpen`-scoped — hosts wanting visibility scoping pass `isOpen ? subscribe : undefined`). `onSubscribeDelta` callback fires for every delta. `controlledRef`-style mirrors keep effect deps `[subscribe]`-only. **Forward-only `onStoryViewed` semantics** (R-D-3) — fires on last item completion / forward navigation / auto-close at end; does NOT fire on backward navigation (matches Instagram). `goToStoryIndex` from imperative handle fires onStoryViewed only for forward jumps. **`onItemViewed` separate** for analytics granularity. **Auto-close at end** (R-D-Q3) — `onAutoCloseAtEnd?` synchronous callback fires immediately before `onClose()` so hosts can intercept (show "all caught up" overlay) by keeping `isOpen=true` themselves. **`renderItem?` slot** for full takeover; hosts using it MUST set `item.duration` explicitly for non-video items. **Public exports:** `StoryViewer01` (memoized) + `useStoryViewerState` + `useStoryProgress` + `useStoryKeyboardNav` + `storyViewerReducer` + 10 type exports + `DEFAULT_STORY_VIEWER_LABELS` + `meta`. **i18n via 10-key `labels`** (defaults English + native `Intl.DateTimeFormat`; no `date-fns` peer dep). **a11y:** `<DialogTitle>` (sr-only with `viewerLabel`), per-button `aria-label`s + mute toggle `aria-pressed`, per-segment `role="progressbar"` + `aria-valuenow`. **No new shadcn primitives** (`dialog` / `avatar` / `button` already installed). **No framer-motion** (motion-substrate gate moved to v0.2 swipe-to-dismiss). **6-tab demo** (Image only / Video only / Mixed / Multi-story nav with Open-from-N buttons / Realtime synthetic 8s item-added + 15s story-added / Custom renderItem promo placement). dummy-data exports 3 mock stories matching kasder's URL fixtures verbatim (Unsplash + w3schools `mov_bbb.mp4` / `movie.mp4`). **Architectural decisions: 4 genuinely-novel Q-Ps surfaced** (modal animation strategy / markViewed granularity / auto-close behavior / subscribe-delta scope) + ALL other carry-overs committed silently per [feedback_no_meta_questions_when_pattern_is_locked.md](../C:/Users/AsiaData/.claude/projects/e--2026-ilinxaDOC-ilinxa-ui-pro/memory/feedback_no_meta_questions_when_pattern_is_locked.md). **8 Stage-1 description re-validation refinements (R-D-1..8) + 7 Stage-2 plan re-validation refinements (R-Plan-1..7) applied.** **Implementation deviations from plan:** (1) `react-hooks/refs` lint — `initialStoryIdRef.current` read-during-render in `useState` initializer; fix: dropped refs entirely, used lazy `useState(() => { ... })`. (2) `react-hooks/set-state-in-effect` — `setProgress(0)` and `setPaused(false)` in itemKey-change effects; fix: React 19 setState-during-render pattern (`if (renderedItemKey !== itemKey) { setRenderedItemKey(itemKey); setPaused(false); }`); ref resets stayed in separate effect (allowed). (3) Continuation — `accumulatedRef.current = 0` written during render; fix: split into two effects — setState in render via React 19 pattern, ref resets in `useEffect(() => { ... }, [itemKey])`. (4) Two unused `eslint-disable @next/next/no-img-element` directives removed (same pattern as people-grid-01 / media-carousel-01 / post-card-01). (5) `md:h-[700px] md:w-[400px]` IDE-flagged → fixed to `md:h-175 md:w-100` during plan re-validation BEFORE implementation. (6) `h-[100dvh]` IDE-flagged → fixed to `h-dvh`. **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm registry:build` produced `story-viewer-01.json` (13 files, all `registry:component`, target convention locked, registryDependencies = `video-player-01` cross-folder + 3 shadcn primitive URLs) + `story-viewer-01-fixtures.json`. **Components page renders at HTTP 200** on local dev (`/components/story-viewer-01`). Procomp docs at [docs/procomps/story-viewer-01-procomp/](../docs/procomps/story-viewer-01-procomp/) — description + plan + guide all present. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/story-viewer-01` (auto-pulls `video-player-01` + 3 shadcn primitives) + `@ilinxa/story-viewer-01-fixtures`. Registry now 66 items (33 base + 33 fixtures). **Migration origin:** kasder `kas-social-front-v0` `StoryViewer.tsx` (302 LOC) + `types.ts` (16 LOC). **Social-posts-system arc COMPLETE: 8/8 done** (expandable-text-01 + video-player-01 + media-carousel-01 + engagement-bar-01 + comment-thread-01 + post-card-01 + story-rail-01 + story-viewer-01). After this ship: `/sandbox/social-feed-page-01` Tier-3 composition wires the whole arc into one assembled feed page (host code, NOT a registry component — Tier-3 lives in `src/app/sandbox/`). framer-motion adoption gate remains pending in v0.2 of story-viewer-01 (swipe-to-dismiss). **Not yet validated in browser:** hydration, all 6 demo tabs rendering, mobile vs desktop modal layout (h-dvh fullscreen vs md:h-175 md:w-100 portrait), tap-zone touch behavior, segmented progress bars filling at correct rate per item, video metadata duration override applied for the longer w3schools clips, pause-preserving accumulator across pause/resume, multi-story nav with arrow keys + button clicks, realtime tab's item-added growing the progress bars without losing position, custom renderItem promo placement rendering, auto-close at end firing `onAutoCloseAtEnd` synchronously before `onClose`.

- 2026-05-03 — **`story-rail-01` v0.1 shipped — seventh of 8 in the social-posts-system arc; story doublet part 1.** [src/registry/components/data/story-rail-01/](../src/registry/components/data/story-rail-01/) — 11 files in sealed folder; 6 ship via the registry. Horizontal stories rail mirroring kasder's `StoriesSection.tsx` + `StoryThumbnail.tsx` exactly. **Embla used inline** (drag-free skim-scroll, no wrapper hook, no indicator dots — story rails are skim-scroll, not snap carousels). **Kasder-exact thumbnail aesthetic** (`w-20 h-28` portrait outer, `p-0.5 rounded-2xl` ring with `bg-linear-to-br from-accent via-warning to-destructive` when unread / `bg-muted` when read, inner `rounded-[14px] border-2 border-card` image frame, `motion-safe:group-hover:scale-105`, avatar+username row below). **`<AddStoryThumbnail>` standalone sub-export** (kasder's `+` placeholder) + **`leading?: ReactNode` slot** for any custom prefix. **Mode-aware edge-fade gradients** — `from-card` + `left-4 right-4` when framed (matches kasder); `from-background` + `left-0 right-0` when bare. Edge gradients render only when items present (R-Plan-2). **Always-uncontrolled** with `reset(next)` + `dispatch(action)` + `markViewed(itemId)` imperative escape hatches. **Realtime via `Subscribe<StoryRailDelta>`** — same shape as engagement-bar-01 / comment-thread-01 / post-card-01 (added/removed/viewed/updated). **Click does NOT auto-mark-viewed** (R-Plan-3) — matches Instagram; host calls `ref.current.markViewed(itemId)` from their viewer's `onClose`. **Decoupled from the viewer** — `onItemClick(item, index)` hands off. **`StoryRailItem` shape: minimal preview** (Q1 sign-off) — `id` / `username` / `avatar?` / `previewImage` / `hasUnread?` / `userId?`. Story-viewer-01 will define its own full Story shape; TS structural typing covers the gap. **Embla used directly, NOT cross-importing media-carousel-01** — sealed-folder rule (story-rail's start-aligned + drag-free + small-rectangle config differs structurally from gallery's centered+snap+large-square). **No shadcn `card` dep** (R-Plan-1) — render `<section>` with conditional card-style classes; cleaner JSX, fewer deps. **Polymorphic `linkComponent` + `getHref`** for navigation-mode (rare). **`renderThumbnail?(item, isUnread, helpers)` slot** — full takeover, helpers carry `{ index, onClick, baseId }` (baseId via `useId()`). **`storyRailReducer` + `useStoryRailState` publicly exported** (per dynamicity primacy). **Imperative handle:** scrollTo / getCurrentItems / reset / dispatch / markViewed. **Architectural decisions: 23 Q-Ps cite their sources.** Most carry-over from prior 6 ships per [feedback_no_meta_questions_when_pattern_is_locked.md](../C:/Users/AsiaData/.claude/projects/e--2026-ilinxaDOC-ilinxa-ui-pro/memory/feedback_no_meta_questions_when_pattern_is_locked.md); only Q1 (item-shape) genuinely-novel. **3 plan-stage refinements** from re-validation: R-Plan-1 (drop card dep, render `<section>` with conditional card-style classes), R-Plan-2 (gradients only when items present), R-Plan-3 (no auto-mark-viewed). **Implementation deviations:** none — clean ship through tsc + lint on first try. **Public exports:** `StoryRail01` + `AddStoryThumbnail` + `storyRailReducer` + `useStoryRailState` + 9 type exports + `DEFAULT_STORY_RAIL_LABELS` + `meta`. **7-tab demo** (Default / + Add / Mixed / Realtime / Custom render / Bare / Empty). dummy-data exports 7 mock users matching kasder's fixture verbatim + viewer-avatar fixture + `createDummyStoryRailSubscribe()` factory. **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm registry:build` produced `story-rail-01.json` (6 files, all `registry:component`, target convention locked) + `story-rail-01-fixtures.json`. **Components page renders at HTTP 200** on local dev. Procomp docs at [docs/procomps/story-rail-01-procomp/](../docs/procomps/story-rail-01-procomp/) — description + plan + guide all present. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/story-rail-01` (auto-pulls only `avatar` shadcn primitive; `embla-carousel-react` peer dep already shared with media-carousel-01) + `@ilinxa/story-rail-01-fixtures`. Registry now 64 items (32 base + 32 fixtures). **Migration origin:** kasder `kas-social-front-v0` `StoriesSection.tsx` + `StoryThumbnail.tsx`. **Social-posts-system arc:** 7/8 done; 1 pending — `story-viewer-01` (eighth ship, story doublet part 2 + FM adoption gate). After 8/8: `/sandbox/social-feed-page-01` Tier-3 composition.

- 2026-05-02 — **`post-card-01` v0.1 shipped — sixth of 8 in the social-posts-system arc; THIRD cross-folder import precedent + FIRST Tier-2 composite in pro-ui.** [src/registry/components/data/post-card-01/](../src/registry/components/data/post-card-01/) — 14 files in sealed folder; 11 ship via the registry. Variant dispatcher with 4 layouts (`feed` / `compact` / `list` / `detail`) composing all five Tier-1 social siblings (expandable-text-01 + media-carousel-01 + engagement-bar-01 + comment-thread-01 + video-player-01 transitively) via `registryDependencies`. **Auto-wired heart-burst** heuristic — wires when `post.media?.length > 0` AND `onLike` AND `variant ∈ {feed, detail}`; `disableHeartBurst: true` opts out. **Always-uncontrolled** with `reset(next: Post)` + `dispatch(action)` imperative escape hatches mirroring comment-thread-01's pattern. **Local engagement mirror** (R-Plan-1) initialized from `post` on mount via `useState`; bar runs always-controlled with mirror values; optimistic toggles dispatch mirror first then fire host handler; realtime `engagementSubscribe` deltas patch mirror + fire `onSubscribeEngagementDelta`. **Wrapped `PostHandlers`** (R-Plan-2) — consumer slots receive pre-wrapped handlers; their actions get correct optimistic behavior without knowing about the mirror. `defaultPostEngagementActions(post, handlers, variant)` exported for "extend, not replace." **Default kebab** (Bookmark / Share / Copy link / Report) with `defaultPostKebabActions` builder; `kebabActions` slot for full takeover. **`getHref` double-duty** (R-Plan-5): single prop drives overlay-link in non-detail variants AND Copy-link kebab item that writes to clipboard. Detail variant ignores overlay-link (destination doesn't link to itself). **Two separate subscribe props** — engagementSubscribe owned by post-card; commentSubscribe forwards directly to embedded thread. **Detail variant** auto-renders `<CommentThread01>` (Stage-1 Q2: slot with auto-default — `renderCommentSection?` for full takeover). **`commentThread`/`commentPageSize` are card-level props, NOT on `Post`** (Stage-1 R-D-1) — posts and comment trees are separate API endpoints. **Imperative handle:** `openKebab` / `triggerLike` (card-level: bar + burst, distinct from `getEngagementHandle().triggerLike()` which only flips the bar — Stage-1 R-D-3) / `getCurrentPost` / `reset` / `getEngagementHandle` / `getThreadHandle`. **Slots at every connective seam:** `renderHeader` / `renderContent` / `renderMedia` (with onDoubleTap helper) / `engagementActions` / `renderEngagementBar` / `kebabActions` / `commentActions` / `renderCommentSection`. **Verified badge** sealed `<VerifiedBadge>` part — RSC-compatible, inline-SVG kasder pattern. **Migration origin:** kasder `kas-social-front-v0` `AdvancedPostCard.tsx` (167 LOC). **Architectural decisions: 24 Q-Ps cite their sources** (Stage-1 sign-offs Q1/Q2/Q3 + R-D-1/2/3/5 + Stage-2 architectural consequences R-Plan-1/2/3/4). All carry-over locks committed silently per [feedback_no_meta_questions_when_pattern_is_locked.md](../C:/Users/AsiaData/.claude/projects/e--2026-ilinxaDOC-ilinxa-ui-pro/memory/feedback_no_meta_questions_when_pattern_is_locked.md) — Stage-1 surfaced only 3 genuinely-novel calls (variant dispatcher / comment thread embed / heart-burst heuristic) + 4 re-validation refinements (commentThread placement / PostHandlers shape / triggerLike semantics / getHref double-duty); Stage-2 surfaced 4 architectural consequences of the always-uncontrolled lock (mirror / wrapped handlers / reset+dispatch handle / card owns engagementSubscribe). **Implementation deviations:** (1) `lib/defaults.ts` → `lib/defaults.tsx` rename — JSX in icon defaults required `.tsx` (same pattern as info-list-01's dummy-data rename). (2) Two unused `eslint-disable @next/next/no-img-element` directives — pro-ui ESLint already overrides for `src/registry/components/**`; removed (same pattern as people-grid-01 + media-carousel-01). (3) Unused `cn` + `PostCard01Handle` imports in root — removed during lint pass. **Public exports:** `PostCard01` + `VerifiedBadge` + `defaultPostEngagementActions` + `defaultPostKebabActions` + 7 type exports + `DEFAULT_POST_CARD_LABELS` + 9 re-exports for consumer convenience (MediaItem / Comment / CommentDelta / CommentMenuItem / Subscribe / etc.) + `meta`. **7-tab demo** (Feed multi-image with heart-burst / Compact sidebar / List admin with custom kebab / Detail with embedded thread / Text-only / Realtime detail with both subscribes / Custom actions extending defaults with Wand2 Remix). dummy-data exports 7 sample posts + detail-thread + viewer + 2 synthetic subscribe factories. **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm registry:build` produced `post-card-01.json` (11 files, all `registry:component`, target convention locked, registryDependencies declares 5 cross-folder siblings + 4 shadcn primitive URLs) + `post-card-01-fixtures.json`. **Components page renders at HTTP 200** on local dev. Procomp docs at [docs/procomps/post-card-01-procomp/](../docs/procomps/post-card-01-procomp/) — description + plan + guide all present. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/post-card-01` (auto-pulls all 5 Tier-1 social siblings + 4 shadcn primitives) + `@ilinxa/post-card-01-fixtures`. Registry now 62 items (31 base + 31 fixtures). No new shadcn primitives needed (avatar / button / card / dropdown-menu present). **Not yet validated in browser:** hydration, all 7 demo tabs rendering, double-tap heart-burst on carousel in feed/detail, embedded thread accepting comments, realtime tab's both subscribes ticking simultaneously, polymorphic linkComponent with NextLink, overlay-link covering article surface, mobile touch behavior. **Social-posts-system arc:** 6/8 done (expandable-text-01 + video-player-01 + media-carousel-01 + engagement-bar-01 + comment-thread-01 + post-card-01); 2 pending — story-rail-01 (next) + story-viewer-01 (FM adoption gate). After 8/8 ship, the Tier-3 composition `/sandbox/social-feed-page-01` is the natural next step.

- 2026-05-02 — **`comment-thread-01` v0.1 shipped — fifth of 8 in the social-posts-system arc; SECOND cross-folder import precedent in pro-ui (after media-carousel-01 → video-player-01).** [src/registry/components/data/comment-thread-01/](../src/registry/components/data/comment-thread-01/) — 15 files in sealed folder; 11 ship via the registry. Recursive comment tree composing `expandable-text-01` (long bodies) + `engagement-bar-01` `variant="compact"` (per-row like). Inline-expand past `maxDepth` (default 2), 24px indent per depth, optimistic add/like/delete, autosize composer (~25-LOC roll-our-own hook — no `react-textarea-autosize` peer dep), `Subscribe<CommentDelta>` realtime contract identical to engagement-bar-01's `Subscribe<TDelta>` shape. **Always-uncontrolled** with `reset(next)` + `dispatch(action)` imperative escape hatches for external state sync (Q-P15: walked back description's "hybrid controlled/uncontrolled" carry-over from engagement-bar-01 — right call there, wrong call for a recursive stateful tree where prop-identity churn would silently re-sync). Per-row `<EngagementBar01>` always-controlled by thread reducer (Q-P12) — single source of truth under realtime + optimistic flow. Optimistic add: head insertion top-level (newest first; pairs with "Load older" at bottom), tail insertion replies (chronological under each parent). Optimistic temp id via `crypto.randomUUID()`. **Architectural decisions locked in plan via 21 Q-Ps** + **12 plan-stage refinements (R-Plan-1..12) applied during re-validation:** R-Plan-1 always-uncontrolled lock; R-Plan-2 per-row bar always-controlled; R-Plan-3 `indentPx=24` (not 16; matches Twitter/Slack), `bodyMaxLines=4` default / `2` compact; R-Plan-4 Reply button hidden when no currentUser; R-Plan-5 inline composer focus restoration via `requestAnimationFrame(() => trigger.focus())`; R-Plan-6 loud `comments` prop staleness callout in usage.tsx + meta.notes; R-Plan-7 standalone composer onSubmit is `Promise<void>`-only; R-Plan-8 touch-friendly kebab via Tailwind v4's `pointer-coarse:opacity-100`; R-Plan-9 dropped unused `linkComponent` prop (non-breaking to add later); R-Plan-10 fresh `now` per render (not per-node `nowRef` — captured mount time, went stale across re-renders); R-Plan-11 idempotent `like-toggle` reducer (no count flip when `isLiked === nextLiked`); R-Plan-12 `useId()` per `<CommentNode>` for `aria-labelledby={authorNameId}` + `aria-controls={repliesContainerId}`. **Implementation deviations from plan:** (1) `react-hooks/refs` lint error caught — initial composer draft had `didAutoFocus.current = true` written during render to gate auto-focus; fix: moved into `useEffect(() => { if (autoFocus) ref.current?.focus() }, [])`. (2) Same lint family caught in comment-node — `helpers.onReply` captured `replyTriggerRef.current` inside `useMemo` deps; fix: removed `replyTriggerRef` from node entirely; default Reply button reads `e.currentTarget` at click site; helpers' `onReply` passes `null` trigger (slot consumers handle their own focus restoration). (3) Removed unused `variant` destructure from `<CommentNode>` (passed through via `{...props}` spread to children; not used locally). **Public exports:** `CommentThread01` + `CommentComposer` (standalone, ref-as-prop) + `commentReducer` + `useCommentState` + `useAutosizeTextarea` + `defaultRelativeTime` + `toDate` + 14 type exports + `DEFAULT_COMMENT_THREAD_LABELS` + `meta`. **7-tab demo** (Flat / Nested d2 / Nested d3 with view-N-replies inline-expand / Paginated with Load-older / Realtime with synthetic 6s tick firing added/liked/edited deltas / No-user with sign-in CTA in `composerEmptyState` slot / Compact variant). dummy-data exports 4 trees + viewer + `generateOlderPage(page)` + `createDummySubscribe()`. **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm registry:build` produced `comment-thread-01.json` (11 files, all `registry:component`, target convention locked, `registryDependencies: ["expandable-text-01", "engagement-bar-01", "avatar", "button", "dropdown-menu", "textarea"]`) + `comment-thread-01-fixtures.json`. **Components page renders at HTTP 200** on local dev (`/components/comment-thread-01`). Procomp docs at [docs/procomps/comment-thread-01-procomp/](../docs/procomps/comment-thread-01-procomp/) — description + plan + guide all present. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/comment-thread-01` (auto-pulls 2 cross-folder siblings + 4 shadcn primitives) + `@ilinxa/comment-thread-01-fixtures`. No new shadcn primitives installed during this ship (avatar / button / dropdown-menu / textarea all already in `src/components/ui/`). Registry now 60 items (30 base + 30 fixtures). **Memory updated:** [feedback_no_meta_questions_when_pattern_is_locked.md](../C:/Users/AsiaData/.claude/projects/e--2026-ilinxaDOC-ilinxa-ui-pro/memory/feedback_no_meta_questions_when_pattern_is_locked.md) — when drafting procomp description / plan, don't surface "Q-P" or "open question" entries whose answer is "what does the most recent similar component do?"; lock + cite + move on. The procomp arc is a series of homogeneous components, the precedents (engagement-bar-01 / expandable-text-01 / media-carousel-01 / video-player-01 / content-card-news-01) set the template — re-asking the same questions every component is friction, not rigor. The re-validation pass is for *genuinely-novel* architectural calls (walking back wrong inherited assumptions, surfacing edge cases the precedent didn't cover). **Migration origin:** kasder `kas-social-front-v0` `PostEngagementPanel.tsx` (468 LOC), `CommentItem` lines 413–467 as the visual reference. **Not yet validated in browser:** hydration, autosize on first composer mount before font load, all 7 demo tabs rendering, inline reply composer focus restoration after submit/cancel, view-N-replies inline-expand on the depth-3 fixture, "Load older comments" appending pages, realtime deltas patching the tree without UI tear, kebab visibility on touch (pointer-coarse). **Social-posts-system arc:** 5/8 done (expandable-text-01 + video-player-01 + media-carousel-01 + engagement-bar-01 + comment-thread-01); 3 pending — post-card-01 (Tier-2 composite, depends on all 1–5), story-rail-01, story-viewer-01 (FM adoption gate).

- 2026-05-02 — **`engagement-bar-01` v0.1 shipped — fourth of 8 in the social-posts-system arc; FIRST `registry:file` precedent (sibling CSS) in pro-ui.** [src/registry/components/data/engagement-bar-01/](../src/registry/components/data/engagement-bar-01/) — 16 component files. Discriminated-union action row (like / comment / share / bookmark / view-count / custom) with realtime `Subscribe<EngagementDelta>` contract, CSS-keyframe heart-burst as RSC-compatible sibling sub-export, per-action controlled vs uncontrolled mode (hybrid), slot-based likers preview, three variants (default / compact / stacked). **No framer-motion** — adoption gate moved to `story-viewer-01`'s swipe-to-dismiss; before that, every animation in the social-posts arc is CSS-keyframe-based. **Highest-leverage primitive in the arc** — retrofits cleanly into already-shipped `content-card-news-01` and `event-card-01` `actions` slots for instant social upgrade with zero JS animation cost. **Migration origin:** kasder `kas-social-front-v0` `PostEngagementPanel.tsx` (468 LOC); we extract only the action-row concern, decomposing comments → `comment-thread-01` (next ship) and likers carousel → `likersPreview` slot. **Architectural decisions locked in plan via 12 Q-Ps:** (Q-P1) `actions[]` order preserved as written (no auto-sort); (Q-P2) hybrid controlled/uncontrolled per-field, per-render; (Q-P3) `engagementReducer` public from day-1; (Q-P4) heart-burst sub-export at `./parts/engagement-heart-burst.tsx` re-exported from `index.ts`, sibling CSS via `registry:file`; (Q-P5) `view-count` display-only (no `onClick` in union); (Q-P6) counts ≤ 0 render as `"0"` (predictable layout); (Q-P7) `like` action with no `liked` prop defaults to `false`; (Q-P8) local state flips first, then `onToggle(next)` fires (standard optimistic UI); (Q-P9) `subscribe` re-subscription on identity change without debouncing; (Q-P10) heart-burst placement host-owned (bar handle does NOT include `triggerHeartBurst()`); (Q-P11) `view-count` icon locked to `Eye` (consumers wanting different use `kind: "custom"`); (Q-P12) `comment.onClick` no payload (host has post.id in scope). **5 plan-stage refinements applied during re-validation:** (1) **stacked variant root** changed from horizontal `flex justify-between` to vertical `flex flex-col items-center gap-3` (Reels-style overlay layout); `align` is silently ignored in stacked mode; (2) dropped "memoize click handler upstream with useCallback in `.map()`" pattern (would be hooks-rules violation); each action part owns its own click handler internally with `React.memo` for cheap re-renders; (3) subscription effect uses `controlledRef` + `onSubscribeDeltaRef` mirrors instead of including them in deps — re-runs only on `subscribe` identity change (no in-flight delta drops between cleanup + re-call); (4) `view-count-action` renders as `<div role="group" aria-label>` (cleaner than `<span aria-label>`); (5) `format = labels.formatCount ?? formatEngagementCount` resolved at root, passed down to every action part. **4 description-stage refinements applied during re-validation:** (1) CSS portability locked via shadcn `registry:file` (sibling `engagement-heart-burst.css` shipped alongside the burst .tsx; first non-`registry:component` entry in pro-ui's registry); (2) dropped `triggerHeartBurst()` from bar handle (host-owned counter pattern is canonical — bar doesn't render the burst node); (3) added `onSubscribeDelta?(delta)` callback for controlled-mode subscribe contract (deltas never mutate controlled fields; host translates via callback); (4) `<EngagementHeartBurst>` is RSC-compatible (no `"use client"`) — uses `key={trigger}` remount pattern; retrofitted news-card / event-card surfaces don't pay a client-component boundary just to render a burst. **4 implementation-time deviations:** (1) Two `react-hooks/refs` lint errors caught — `stateRef.current = state` and `actionsRef.current = actions` written during render; fix: moved both ref-mirror writes into a passive `useEffect` (matches media-carousel-01's pattern). (2) `react-hooks/use-memo` lint error in demo's `RealtimeDemo` — `useCallback(createDummySubscribe(142), [])` passed function-call result instead of inline function; fix: switched to `useMemo(() => createDummySubscribe(142), [])`. (3) `react-hooks/exhaustive-deps` warning on subscription effect with `[opts.subscribe]` — extracted `const subscribe = opts.subscribe` to local + dep on `[subscribe]`. (4) Initially used dynamic `import("./hooks/...")` inside `reset()` handle method — code smell (Promise-based dispatch); fix: imported `deriveStateFromActions` at top of file. **Surfaces:** `actions: EngagementAction[]` (required, strict discriminated union, order preserved) / `variant?: "default" | "compact" | "stacked"` (default `default`) / `subscribe?: Subscribe<EngagementDelta>` / `onSubscribeDelta?(delta)` / `likersPreview?: ReactNode` slot / `labels?` (8 keys including `formatCount?` escape hatch) / `className` + `actionClassName` overrides + ref-as-prop for `EngagementBar01Handle`. **Public exports:** `EngagementBar01` + `EngagementHeartBurst` + `engagementReducer` + `useEngagementState` + `deriveStateFromActions` + `formatEngagementCount` + 11 type exports + `DEFAULT_ENGAGEMENT_BAR_LABELS` + `meta`. **6-tab demo** (Default post / Compact news-card / Stacked video overlay / Realtime subscribe with synthetic 4-second tick / Heart-burst with double-click image / Custom action via Wand2 icon). dummy-data exports 3 action sets + 5 sample like users + `createDummySubscribe()` factory. **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm build` clean (compiled 12.4s; 38 routes prerendered including `/components/engagement-bar-01`, was 37); `pnpm registry:build` produced `engagement-bar-01.json` + `engagement-bar-01-fixtures.json` artifacts; spot-check confirms `engagement-heart-burst.css` shows up in JSON `files[]` with `type: "registry:file"`. Procomp docs at [docs/procomps/engagement-bar-01-procomp/](../docs/procomps/engagement-bar-01-procomp/) — description + plan + guide all present. Registry now 58 items (29 base + 29 fixtures). New shadcn primitive: `avatar` (installed during this ship for the demo's likers strip). Consumer install via `pnpm dlx shadcn@latest add @ilinxa/engagement-bar-01` + `@ilinxa/engagement-bar-01-fixtures`. **Memory updated:** [project_motion_substrate.md](../C:/Users/AsiaData/.claude/projects/e--2026-ilinxaDOC-ilinxa-ui-pro/memory/project_motion_substrate.md) — framer-motion not a project peer dep yet; CSS keyframes for everything until `story-viewer-01`'s swipe gesture genuinely requires FM. **Smoke-test from tmp consumer remains a recommended manual step** to verify shadcn CLI v4 actually copies the `registry:file` content to the target path (artifact is well-formed; install behavior is what's untested). **Not yet validated in browser:** hydration, all 6 demo tabs rendering, double-click image triggering heart-burst overlay + bar like-flip, like/bookmark scale-bump CSS transitions, subscribe synthetic tick visibly updating count every 4s, `aria-pressed` / `aria-live` screen-reader behavior, stacked variant rendering as vertical column on dark background, compact variant fitting inside news-card, custom Wand2 icon rendering. **Social-posts-system arc:** 4/8 done (expandable-text-01 + video-player-01 + media-carousel-01 + engagement-bar-01); 4 pending — comment-thread-01 (next; per-comment uses engagement-bar-01 in `compact` variant for like / reply), post-card-01, story-rail-01, story-viewer-01 (FM adoption gate).

- 2026-05-02 — **`media-carousel-01` v0.1 shipped — third of 8 in the social-posts-system arc; FIRST cross-folder import in pro-ui's registry.** [src/registry/components/media/media-carousel-01/](../src/registry/components/media/media-carousel-01/) — 13 files. Embla-driven image+video carousel with two variants (`gallery` peek-scale matching kasder Instagram-post / `linear` full-width snap). **Cross-folder import: `<VideoPlayer01 isActive={...}>` for built-in video item handler.** shadcn `registryDependencies: ["video-player-01"]` handles install. **Refines the project-wide sealed-folder rule:** cross-folder imports allowed within a designed system (declared via `registryDependencies`); not allowed across unrelated domains. Sets precedent for the rest of the social-posts arc — comment-thread-01, post-card-01 will all cross-import sibling primitives. **Migration origin:** kasder `kas-social-front-v0` `PostMediaCarousel.tsx` (142 LOC). **Architectural decisions locked in plan via 10 Q-Ps:** (Q-P1) cross-folder import allowed; (Q-P2) `peekRatio` default 0.075; (Q-P3) linear `aspect` default `square`; (Q-P4) `showIndicators` hideable; (Q-P5) `showNavButtons` hideable; (Q-P6) image items get `isActive` in `RenderItemContext` (built-in handler ignores); (Q-P7) double-tap unified across image+video; (Q-P8) imperative handle = scrollTo / scrollPrev / scrollNext / getCurrentIndex; (Q-P9) `onSlideChange` fires only on Embla `select` event (post-snap); (Q-P10) `MediaItem` strict discriminated union. **4 plan-stage refinements applied during re-validation:** (1) **CRITICAL** — single-item shortcut extracted to `parts/single-item.tsx` (was conditional `useDoubleTap` in root → React rules-of-hooks violation when items.length changes between renders); (2) `useEmblaWithState` mount-sync no longer fires user `onSelect` callback (separate effect with `isFirstFireRef` skips first fire); (3) Embla options memoized via `useMemo([variant, loop, rtl])` (`useEmblaCarousel` re-initializes on options identity change; inline objects would cause constant re-init); (4) imperative handle uses `currentIndexRef` written in passive useEffect for stable identity across slide changes (host's ref.current doesn't churn on every snap). **3 implementation-time deviations caught at build verification:** (1) TS error `Cannot find module 'embla-carousel'` — types are in core `embla-carousel` package but only `embla-carousel-react` was installed (transitive). Fix: `pnpm add embla-carousel` as direct dep. (2) `react-hooks/set-state-in-effect` lint error on `setCurrentIndex(emblaApi.selectedScrollSnap())` mount-sync — refactored entirely to `useSyncExternalStore` (subscribe + getSnapshot + getServerSnapshot pattern; canonical external-subscription approach matching video-player-01's `usePrefersReducedMotion`). (3) Two unused `eslint-disable-next-line @next/next/no-img-element` directives in slide-renderer.tsx + demo.tsx — pro-ui's ESLint already overrides the rule for `src/registry/components/**` (per content-card-news-01 + people-grid-01 STATUS); removed. **Surfaces:** `items: MediaItem[]` (discriminated union, strict) / `variant: "gallery" | "linear"` (required) / per-variant config (`peekRatio` 0.075 default for gallery, `aspect` square default for linear) / `loop?` (default `items.length > 1`) / `showIndicators?` (default true, hidden when single) / `showNavButtons?` (default true, hidden when single) / `rtl?` / `renderItem?(item, ctx)` slot / `onSlideChange?(index)` (post-snap, mount-silent) / `onDoubleTap?(item, index)` (unified image+video) / `labels?` (5 keys with `{index}` / `{total}` placeholders) / `className` + `slideClassName` overrides + ref-as-prop for `MediaCarousel01Handle`. **Public exports:** `MediaCarousel01` + `MediaCarousel01Props` + `MediaCarousel01Handle` + `MediaCarousel01Labels` + `MediaCarousel01Variant` + `MediaItem` + `RenderItemContext` + `LinearAspect` + `DEFAULT_MEDIA_CAROUSEL_LABELS` + `meta`. `useEmblaWithState` is INTERNAL-only. **Default control behavior:** gallery slides at 85% width with `mx-1 first:ml-[7.5%] last:mr-[7.5%]` + `scale-95 opacity-60` on inactive + `scale-100 opacity-100` on active + `transition-all duration-300`; linear full-width snap with `aspect-square` default; indicator dots elongate `w-1.5` → `w-4` on active; nav chevrons `h-8 w-8 rounded-full opacity-80 hover:opacity-100` with `rtl:rotate-180`; single-item bypass = bare `aspect-square overflow-hidden bg-muted` wrapper, no Embla. 5-tab demo (Gallery mixed kasder UX with video at index 3 auto-pausing / Linear photos product gallery / Single-item shortcut for both image AND video / Custom renderItem with slide-counter overlay / Imperative ref demo with Prev/Next/Jump-to-3 buttons). dummy-data exports 4 sets — real Pexels image + video URLs. **Verification:** `pnpm tsc --noEmit` clean (after embla-carousel install); `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm build` clean (compiled 11.something + tsc clean; 37 routes prerendered including `/components/media-carousel-01`); `pnpm registry:build` produced `media-carousel-01.json` + `media-carousel-01-fixtures.json` artifacts. Procomp docs at [docs/procomps/media-carousel-01-procomp/](../docs/procomps/media-carousel-01-procomp/) — description + plan + guide all present. Registry now 56 items (28 base + 28 fixtures). New peer deps: `embla-carousel-react@8.6.0` + `embla-carousel@8.6.0`. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/media-carousel-01` (auto-installs `@ilinxa/video-player-01` via `registryDependencies`) + `@ilinxa/media-carousel-01-fixtures`. **Not yet validated in browser:** hydration, gallery peek-scale active vs inactive transitions, indicator dot elongation + click-to-jump, nav chevrons, video at index 3 auto-pausing when off-screen (the kasder UX flagship demo), single-item bypass (no nav, no indicators), custom renderItem slide-counter overlay rendering, imperative ref handle Prev/Next/Jump, RTL drag direction + chevron flip, touch drag on mobile, Embla arrow-key keyboard nav, useSyncExternalStore SSR snapshot rendering. **Social-posts-system arc:** 3/8 done (expandable-text-01 + video-player-01 + media-carousel-01); 5 pending — engagement-bar-01 (next; highest leverage; retrofits into already-shipped news-card / event-card actions slots), comment-thread-01, post-card-01, story-rail-01, story-viewer-01.

- 2026-05-02 — **`video-player-01` v0.1 shipped — second of 8 in the social-posts-system arc; FIRST occupant of the `media` category.** [src/registry/components/media/video-player-01/](../src/registry/components/media/video-player-01/) — 11 files. `<video>` wrapper with muted-autoplay-friendly defaults, slot-based controls via `renderControls(state) => ReactNode`, `isActive` auto-pause for carousel coordination, double-tap callback (300ms window), caption tracks via `tracks: VideoTrack[]`, rAF-throttled `onTimeUpdate`, full lifecycle callbacks, `useDoubleTap` hook exported standalone for non-video consumers. **Migration origin:** kasder `kas-social-front-v0` `PostVideoPlayer.tsx` (128 LOC). **Architectural decisions locked in plan via 10 Q-Ps:** (Q-P1) category=`media` (NEW first occupant — pivots 4 of remaining 7 social-posts components to `media`); (Q-P2) `controlsAutoHideMs` default 2000ms; (Q-P3) rAF-throttled `onTimeUpdate` non-negotiable; (Q-P4) keyboard scope focus-only via `tabIndex={0}` (avoids multi-video focus contention); (Q-P5) `onDoubleTap` window 300ms hard-coded at component level, configurable on hook; (Q-P6) `tracks` prop = array of objects (data-driven, ergonomic); (Q-P7) `renderControls` receives state ONLY, never video ref (prevents state-machine bypass); (Q-P8) fullscreen DROPPED from default controls (consumers add via `renderControls`); (Q-P9) `controls={false}` keeps Space/M keyboard shortcuts working; (Q-P10) mute uses `aria-pressed={isMuted}` + action-verb labels. **State machine via `useReducer`** — 8 actions including `loadstart` for src-change cleanup. Browser drives state via DOM events; we mirror it. **4 plan-stage refinements applied during re-validation:** (1) `isActive` pause effect moved INSIDE `useVideoState` (hook owns video DOM, single dep `[isActive]`, cleaner ownership); (2) all 8 `videoEventHandlers` wrapped in `useCallback([])` + object `useMemo`'d (prevents re-attaching listeners every render — critical since `timeupdate` fires up to 60×/sec); (3) `parts/video-element.tsx` gained `onToggleMute` prop (M-keyboard handler had TODO comment); (4) `loadstart` event added to clear stale `duration`/`currentTime`/`isPlaying`/`isLoaded` (preserves `isMuted`) — prevents transient `(0, oldDuration)` window in onTimeUpdate callbacks during src changes. **2 implementation-time `react-hooks/set-state-in-effect` lint errors caught at build verification:** (1) `usePrefersReducedMotion` had `setReduced(mql.matches)` synchronously in useEffect → refactored to `useSyncExternalStore` (canonical external-subscription pattern; SSR-safe via `getServerSnapshot`); (2) auto-hide timer had `setShowControls(true)` in effect body → derived `alwaysVisible` from props + `hidden` state + `wakeKey` counter dep that increments on wake gestures (re-arms timer via effect re-run; both setStates in event-handler scope, not effect body). Same lint family that bit detail-panel deviation #1 + filter-stack deviation #4 earlier. **Surfaces:** `src` / `poster?` / `tracks?: VideoTrack[]` / `isActive?` (default true) / `loop?` / `muted?` / `playsInline?` / `preload?` / `autoPlay?` / `objectFit?: "cover" | "contain"` / `controls?` (default true) / `renderControls?(state): ReactNode` / `controlsAutoHideMs?` (default 2000) / `onDoubleTap?` / `onPlay?` / `onPause?` / `onEnded?` / `onTimeUpdate?(t,d)` / `onLoadedMetadata?(d)` / `onError?(MediaError|null)` / `labels?` (5 keys) / `className` + `videoClassName` + `controlsClassName` overrides. **Public exports:** `VideoPlayer01` (memoized) + `VideoPlayer01Props` + `VideoPlayer01Labels` + `VideoState` + `VideoTrack` + `DEFAULT_VIDEO_PLAYER_LABELS` + `useDoubleTap` hook + `UseDoubleTapOptions` + `meta`. `useVideoState` is INTERNAL-only (state delivered via `renderControls` slot). **Default control overlay** matches kasder pattern: big center play button (always visible when paused) + bottom-right mute toggle + bottom-left small pause indicator (only when playing + showing controls). 5-tab demo (Default / Custom controls with progress bar via slot / Captions / Decorative no-controls + autoplay-loop / isActive carousel proving 3-video coordination — exactly the contract media-carousel-01 will use). dummy-data ships 5 royalty-free Pexels MP4 URLs + sample English WebVTT track. **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm build` clean (compiled 11.2s + tsc 12.2s; 36 routes prerendered including `/components/video-player-01`); `pnpm registry:build` produced `video-player-01.json` + `video-player-01-fixtures.json`. Procomp docs at [docs/procomps/video-player-01-procomp/](../docs/procomps/video-player-01-procomp/) — description + plan + guide all present. Registry now 54 items (27 base + 27 fixtures). Pulls only `button` shadcn primitive + `lucide-react`. Consumer install via `pnpm dlx shadcn@latest add @ilinxa/video-player-01` + `@ilinxa/video-player-01-fixtures`. **Not yet validated in browser:** hydration, video element keyboard focus + Space/M shortcuts, double-tap firing onDoubleTap, mute toggle aria-pressed announce, captions track loading + browser CC button, carousel demo `isActive` pause/resume across 3 videos, rAF throttling actually capping at ~60×/sec, reduced-motion controls-always-visible behavior, autoplay rejection silent fallback under unmuted+autoPlay attempt. **Social-posts-system arc:** 2/8 done (expandable-text-01 + video-player-01); 6 pending — media-carousel-01 (next; composes video-player-01 via `isActive` per slide), engagement-bar-01, comment-thread-01, post-card-01, story-rail-01, story-viewer-01.

- 2026-05-02 — **`expandable-text-01` v0.1 shipped — first of 8 components in the social-posts-system migration arc.** [src/registry/components/data/expandable-text-01/](../src/registry/components/data/expandable-text-01/) — 8 files. Truncate-and-expand plain-text block; measure-based detection (`useEffect` reads `getComputedStyle(el).lineHeight` × `maxLines` and compares against `el.scrollHeight + 1` slop), so the toggle button only renders when truncation actually occurs. Pure CSS line-clamp clips silently — this restores the signal. **Migration origin:** kasder `kas-social-front-v0` `PostContent.tsx` (55 LOC). **Architectural decisions locked in plan via 10 Q-Ps:** (Q-P1) `useLineClampDetect` hook is publicly re-exported for advanced consumers; (Q-P2) `renderToggle` slot for full takeover (label-only customization stays in `labels`); (Q-P3) single `onExpandedChange(next)` callback (matches React form-input convention); (Q-P4) inline `style` for `WebkitLineClamp` since Tailwind generates static classes only; (Q-P5) `ResizeObserver` for re-measurement on container resize; (Q-P6) `maxLines` default 3; (Q-P7) `content` locked to `string` for component (rich content uses `article-body-01`); (Q-P8) `data` category; (Q-P9) `display: -webkit-box` always applied — toggle only `WebkitLineClamp` between `maxLines` and `"unset"` (verified via kasder that `scrollHeight` reflects natural content height even when `-webkit-box` clamp is applied; no flash-of-untruncated-content); (Q-P10) empty/null content → renders nothing. **3 plan-stage refinements applied during re-validation:** (1) hook tracks element via `useState` (not `useRef` + ref-callback capture) — `setEl` IS the ref callback and React guarantees its stable identity; handles consumer attachment to elements that re-mount (lists with keyed items); (2) hook's `content` parameter typed `unknown` (component's stays `string | null | undefined`) — hook never reads value, only uses identity as re-measurement trigger; opens hook to non-string content for external use; (3) dropped scratch-route step from Phase B — smoke-test directly inside `demo.tsx` while authoring (no throwaway `src/app/scratch/` files). **Tailwind v4 canonical class fix surfaced during plan re-validation:** `break-words` → `wrap-break-word` (IDE-flagged; same pattern as event-card-01's `bg-gradient-to-t` → `bg-linear-to-t` correction; this is a recurring kasder→pro-ui translation since kasder uses Tailwind v3 syntax). **Surfaces:** `content?: string | null | undefined` / `maxLines?: number` (default 3) / controlled-or-uncontrolled `expanded?` + `defaultExpanded?` + `onExpandedChange?` triplet / `labels?: { showMore?, showLess? }` (English defaults) / `renderToggle?(state): ReactNode` slot / `className` + `contentClassName` + `toggleClassName` overrides. **Public exports:** `ExpandableText01` (memoized) + `ExpandableText01Props` + `ExpandableText01Labels` + `ExpandableText01ToggleRenderProps` + `DEFAULT_EXPANDABLE_TEXT_LABELS` constant + `useLineClampDetect` hook + `UseLineClampDetectOptions` + `UseLineClampDetectResult` + `meta`. **Demo:** 4 sub-tabs (Default with long+short side-by-side proving toggle conditional / Custom maxLines 2 + 6 / Localized TR with `Daha fazla göster`/`Daha az göster` / Custom toggle via chevron icons through `renderToggle` slot in controlled mode). **a11y:** real `<button type="button">` (keyboard activation free) + `aria-expanded={expanded}` + `aria-controls={contentId}` (id from `useId`) ↔ `<p id={contentId}>`; `focus-visible:ring-2 focus-visible:ring-ring`. Custom `renderToggle` is consumer's responsibility — slot receives `isExpanded` + `setExpanded` so works in both controlled and uncontrolled modes; usage doc warns to preserve `aria-expanded` + `aria-label` on icon-only triggers. **Verification:** `pnpm tsc --noEmit` clean; `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm build` clean (compiled in 11.3s + tsc 11.4s; 35 routes prerendered including `/components/expandable-text-01`); `pnpm registry:build` produced `expandable-text-01.json` + `expandable-text-01-fixtures.json` artifacts. Procomp docs at [docs/procomps/expandable-text-01-procomp/](../docs/procomps/expandable-text-01-procomp/) — description + plan + guide all present. Registry now 52 items (26 base + 26 fixtures). Zero peer deps beyond React (cleanest dep footprint of any pro-comp shipped so far). Consumer install via `pnpm dlx shadcn@latest add @ilinxa/expandable-text-01` + `@ilinxa/expandable-text-01-fixtures`. **Not yet validated in browser:** hydration, ResizeObserver re-measurement on window resize, controlled mode persistence across hypothetical remount, custom renderToggle chevron rotation, Turkish character measurement with latin-ext font subset. **Social-posts-system arc:** 1/8 done; 7 pending — video-player-01, media-carousel-01, engagement-bar-01, comment-thread-01, post-card-01, story-rail-01, story-viewer-01. **Composes inside upcoming `comment-thread-01`** (next ship) for comment bodies.

- 2026-05-02 — **Sandbox system shipped — `/sandbox` index + tabbed shell + nav entry; `obsidian-graph` removed.** New top-level surface for Tier-3 assembled-page demos. Three architectural layers: (1) **Manifest** at [src/app/sandbox/_lib/manifest.ts](../src/app/sandbox/_lib/manifest.ts) — `SandboxMeta` shape (`slug` / `title` / `description` / `domain` / `status` / `componentsUsed: string[]` / `updatedAt`); `SANDBOXES` array + `getSandbox(slug)` + `getAllSandboxSlugs()` lookups. Sandboxes are NOT registry components and never enter `manifest.ts` / `registry.json` / `public/r/` — they're host code per Tier-3 definition. (2) **Index page** at [src/app/sandbox/page.tsx](../src/app/sandbox/page.tsx) — card grid mirroring `/components` index style (Card primitive + status Badge + domain pill + "Open →" affordance). (3) **Per-sandbox tabbed shell** at [src/app/sandbox/_components/sandbox-shell.tsx](../src/app/sandbox/_components/sandbox-shell.tsx) — client component; sticky bar at `top-14 z-30` (just below the 56px sticky site-header at z-40) with back-to-`/sandbox` link + sandbox title + status badge + `<TabsList>` (Demo / `<>` Code). Uses Radix Tabs via shadcn — only the active tab renders, so the heavy Demo doesn't mount when on Code tab. **Code tab docs blocks** at [_components/docs-blocks.tsx](../src/app/sandbox/_components/docs-blocks.tsx) — `DocsContainer` (max-w-4xl wrapper) / `DocsSection` (icon + title + description + body) / `ShellBlock` (mono code block) / `InstallBlock` (auto-builds `pnpm dlx shadcn@latest add @ilinxa/<slug>` for every consumed component + a fixtures variant) / `ComponentsUsedList` (resolves names from `getEntry(slug)` and links each back to `/components/<slug>`) / `CodeOverview` (file-tree-style table of paths + descriptions) / `DeveloperGuide` (prose-styled bulleted-list wrapper for assembly notes). Future sandboxes compose docs in ~30 lines by spreading these blocks. **`event-detail-page-01` migrated** — its `page.tsx` now imports the demo (`event-detail-page-01.tsx`) + sibling `docs.tsx` and passes both to `<SandboxShell meta={...} demo={...} docs={...} />`. The new `docs.tsx` ships full Tier-3 documentation: install, components used (7 links), 4-row code overview, 8-bullet developer guide covering hero-as-host-code, status helper-kernel reuse, `now` injection convention, prose-lg className override, sticky `top-20` reasoning, info-list-01 className override for muted requirements card, registration-card-01 independent state machine. **`obsidian-graph` sandbox removed** — was the original force-graph Tier-3 demo (a heavy WebGL Sigma render); the live `/components/force-graph` demo page already covers that surface, and the obsidian-style host UI hasn't shipped any reusable patterns to the registry. Single `rm -rf` of the folder; no other code references. **Site nav gains `Sandbox`** between `Components` and `Docs` in [src/components/site/site-header.tsx](../src/components/site/site-header.tsx) NAV array — same uppercase font-mono pill styling as siblings; auto-active when route starts with `/sandbox`. **Tab semantics:** Demo tab renders the assembled page TabsContent with no inner padding (full-bleed); Code tab wraps in DocsContainer's `max-w-4xl px-6 py-10`. The site-header sticky-overlap is intentional — both bars stack: site-header (z-40, top-0, h-14) + sandbox tab strip (z-30, top-14, ~h-12) for ~108px combined sticky chrome on `/sandbox/<slug>` routes. **Verification:** `.next/` cache wipe required between runs (Next 16's typed-routes validator caches a stale reference to the deleted `obsidian-graph/page.js`); after wipe, `pnpm tsc --noEmit` clean, `pnpm lint` clean (1 pre-existing rich-card warning, no new), `pnpm build` clean (compiled 12.0s + tsc 10.9s; 34 routes prerendered including `/sandbox` index + `/sandbox/event-detail-page-01`; `/sandbox/obsidian-graph` no longer in route list). **Files added (5):** `src/app/sandbox/_lib/manifest.ts`, `src/app/sandbox/_components/sandbox-shell.tsx`, `src/app/sandbox/_components/docs-blocks.tsx`, `src/app/sandbox/page.tsx`, `src/app/sandbox/event-detail-page-01/docs.tsx`. **Files modified (2):** `src/app/sandbox/event-detail-page-01/page.tsx` (refactored to use SandboxShell), `src/components/site/site-header.tsx` (NAV array). **Files deleted (1 folder, 2 files):** `src/app/sandbox/obsidian-graph/` (`page.tsx` + `obsidian-graph.tsx`). **Pattern locked for future sandboxes:** add to manifest → create sibling `<slug>/docs.tsx` → wrap `page.tsx` in `<SandboxShell>` → done. **Not yet validated in browser:** Radix Tabs lazy-mount transition on first switch (Demo → Code), DocsContainer prose-block rendering of the developer-guide list, /sandbox card-grid hover transitions, nav active-state highlight on `/sandbox/*`, sticky tab strip + sticky site-header z-stack with backdrop-blur compositing in dark mode, sticky-sidebar `top-20` clearing both stacked sticky bars on `/sandbox/event-detail-page-01` Demo tab.

- 2026-05-02 — **First Tier-3 host page shipped — `event-detail-page-01`.** [src/app/sandbox/event-detail-page-01/](../src/app/sandbox/event-detail-page-01/) — 3 files (event-fixture.ts + event-detail-page-01.tsx + page.tsx). Lives at `/sandbox/event-detail-page-01` per the obsidian-graph precedent. **Pre-implementation re-validation pass against the kasder `events/[id]/page.tsx` source** mapped every section to the existing kit and surfaced exactly one gap: the hero. `page-hero-news-01` is gradient-only (`bg-linear-to-br from-primary` hard-coded; no `backgroundImage` slot, no dark-overlay slot, no `verticalAlign="bottom"` for the kasder layout). Three options weighed: (A) inline the hero as Tier-3 host JSX, (B) build sealed `page-hero-event-01` sibling, (C) extend `page-hero-news-01` with image/overlay/align props. **Chose A** — Tier-3 IS host code per project graph-system-description.md §3.3 ("a page in `src/app/` that wires Tier 2 with Tier 1 panels into the full product surface; this is host code, not a registry component"). One-off image hero on a single demo doesn't yet pass the dynamicity-primacy bar of "plausible consumer override" — productizing requires a second user. If/when a workshop-detail or conference-landing page appears, lift to sealed `page-hero-event-01` then. Hero is ~50 lines of JSX with full-bleed `<img>` + `bg-linear-to-t from-black/85 via-black/55 to-black/20` overlay + `items-end` bottom alignment + reveal-up 0/60/120/180ms staggered cascade for back-link / badges / title / description (one orchestrated reveal per page, per Motion mandate). **Composition map:** main column → `progress-timeline-01` ("Zaman Çizelgesi") + `<section>` ("Etkinlik Hakkında" h2) wrapping `ArticleBodyViewer` with `prose-lg` override + `schedule-list-01` ("Program") + `people-grid-01` ("Konuşmacılar"). Sidebar (sticky `top-20` to clear the 56px sticky site-header) → `registration-card-01` ("Kayıt Durumu") + `info-list-01` comfortable ("Etkinlik Bilgileri") + `info-list-01` compact ("İletişim") + `info-list-01` compact with `className="bg-muted/50 border-transparent"` override ("Gerekli Belgeler" — the muted-background distinction kasder uses; cn()/tailwind-merge dedupes the bg-card → bg-muted/50 swap cleanly). **Status badge in hero** uses exported `getEventStatus(event, now) → EventStatus` + `EVENT_STATUS_CONFIG[status]` helper kernel from event-card-01 — compose status visuals WITHOUT rendering the card itself. Type + Featured badges are plain shadcn `<Badge>` primitive with `bg-white/10 text-white border-white/30` for the type variant. **Demo event** mirrors the kasder İstanbul Sürdürülebilir Şehir Konferansı fixture: 500 capacity, 423 registered (→ lastSpots state, CTA enabled, counter neutral since 77 spots > 10 urgent threshold), 3 speakers (Prof. Dr. Ahmet Yılmaz / Dr. Elif Kaya / Mehmet Demir, Unsplash photos), 7-row schedule with endTime ranges, 4 Turkish requirements, Plate-JSON body (4 nodes — 2 paragraphs + h3 + paragraph). All time-derived comps receive `now = new Date("2026-06-01T12:00:00Z")` for deterministic SSR (matches the convention from event-card-01 + progress-timeline-01 + registration-card-01 demos). Real interactive `onRegister` + `onShare` handlers wired with `console.log` placeholders. **Plan deviations during implementation:** **(1) Initial back link rendered as `<a href="/">`** — flagged by `@next/next/no-html-link-for-pages` ESLint rule (the page lives in `src/app/`, so internal navigation must use `next/link`). Swapped to `<Link>` import. The "registry components must never import `next/*`" rule does NOT apply to `src/app/` host code — different scope. **(2) `min-h-[520px]` IDE-flagged as canonical-class warning** — Tailwind v4 prefers `min-h-130` (the spacing-scale shorthand). Fixed inline. **(3) Imported `Share2` then never used** — registration-card-01 ships its own default share button when `onShare` is provided; no need for a host-level Share2 import. Removed. **Architectural pattern locked:** Tier-3 assembled pages live at `src/app/sandbox/<slug>/page.tsx` (precedent: obsidian-graph as force-graph's Tier-3). Per Tier-3 definition, NOT in the registry, NOT through the procomp gate, NOT in `src/registry/`, NOT in `manifest.ts`, NOT in `registry.json`. They're product surfaces that prove the kit composes. **Verification:** `pnpm tsc --noEmit` clean (no new errors); `pnpm lint` clean (1 pre-existing rich-card warning, no new); `pnpm build` clean (compiled in 17.3s, 34 routes prerendered including `/sandbox/event-detail-page-01`, was 33 before). **Not yet validated in browser:** hydration of the page assembly, sticky-sidebar behavior under the 56px sticky site-header (sticky `top-20` chosen to clear it), Plate `PlateStatic` viewer rendering with `prose-lg` className override, status-badge contrast over the dark image overlay across all 6 statuses, motion-safe reveal-up cascade on hero (4 staggered elements), Hemen Kayıt Ol click handler firing console.log, Paylaş button rendering as outline variant, info-list-01 muted-bg requirements section visual distinction from card-bg siblings.







