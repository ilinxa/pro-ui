# ilinxa-ui-pro — Status

> **Current snapshot.** This file is the *now*, not a changelog.
>
> **Both workstreams SHIPPED 2026-05-16.** rich-card-in-flow plan v3 GATE 2 closed (commit `f3108f5`) → Workstream A `flow-canvas-01@v0.2.1` SHIPPED (commits `9dbb06c` + `55c630e` + `ef50f0a` + `22e5955`) → Workstream B `rich-card-in-flow@v0.1.0` first ship SHIPPED (commits `19e3f42` + `4c185ef` + `3519a7c` + `86cdd05`). F-V2 smoke harness path-b passed after F-S1 fix-up cycle. GATE 3 spot-check verdict **Pass with follow-ups** (3 findings: F-01 closed pre-ship; F-02 + F-03 v0.2 candidates). 43 components total (was 42). Prior handoffs ([2026-05-16 stage 2 pause](HANDOFF-2026-05-16-rich-card-in-flow-stage2-pause.md), [2026-05-14 perf pause](HANDOFF-2026-05-14-flow-canvas-perf-pause.md), [2026-05-09 session pause](HANDOFF-2026-05-09-session-pause.md)) now historical.
>
> **History:**
> - Per-decision log going forward: [`.claude/decisions/`](decisions/) (one file per decision; YAML frontmatter + summary)
> - Pre-2026-05-09 bulk archive: [`.claude/STATUS-archive.md`](STATUS-archive.md) (frozen; do not extend)
> - Past handoff: [`.claude/HANDOFF-2026-05-09-session-pause.md`](HANDOFF-2026-05-09-session-pause.md) (frozen; superseded by the active handoff above)
>
> **Last updated:** 2026-05-16 (**flow-canvas-01 v0.2.2 SHIPPED — defer fireOnChange to microtask**: latent setState-during-render bug surfaced by rich-card-in-flow@v0.1.0's demo (first controlled-mode `data={canvas}` + `onChange={setCanvas}` consumer in the library). React 19 warned: "Cannot update RichCardInFlowDemo while rendering Canvas." Root cause: `use-canvas-data.ts` calls `fireOnChange` synchronously from 13 reducer-side-effect sites; xyflow emits initial `dimensions` changes during its own render-phase DOM measurement → consumer's setState fires during the host's render → warning. Fix: wrap `fireOnChange`'s body in `queueMicrotask`. Zero call-site changes; all 13 sites get the SAME deferred behavior by construction. Promotes the v0.3-deferred cleanup forward to v0.2.2 (the v0.2.0 plan F-V4 finding had originally deferred this on "no forcing function" grounds; there's a forcing function now). F-V4's "consistency over a one-off divergence" principle preserved — all sites uniformly deferred, no mixed bag. Capture-semantics preserved: `nodes`/`edges`/`vp` captured in closure of immutable arrays from `applyNodeChanges`/`applyEdgeChanges`/`xyAddEdge`. Latency cost: one microtask, imperceptible (React was batching the consumer setState into the next paint anyway). Single commit `022cee8` (use-canvas-data.ts body change + meta 0.2.1→0.2.2 + `pnpm registry:build` regen). Patch-bump exemption per readiness-review rule — internal-only bug fix, no public API touch, GATE 3 skipped. tsc + lint (2 pre-existing virtualizer warnings unchanged) + validate-meta-deps (43/43) + registry:build + `pnpm build` (43 component routes incl `/components/rich-card-in-flow`) all clean. Decision file: [`.claude/decisions/2026-05-16-flow-canvas-v0.2.2-microtask-defer.md`](decisions/2026-05-16-flow-canvas-v0.2.2-microtask-defer.md). Earlier today: **rich-card-in-flow v0.1.0 SHIPPED — Workstream B**: 43rd component; new sealed folder at `src/registry/components/data/rich-card-in-flow/` with 9 shipped files (index.ts + types.ts + 4 lib/*.ts + 3 parts/*.tsx) + demo.tsx + usage.tsx + dummy-data.ts + meta.ts (docs-site only). Canonical implementation of the popup-edit renderer convention from flow-canvas-01@v0.2.0 perf description Q33: read-only `RichCardViewer` renderer paints title + first 3 flat fields + nested-card outlines with their own ports + selectability; clicking fires `ctx.onEditRequest?.(subPath?)` → bubbles to `FlowCanvasProps.onEditRequest?.(nodeId, subPath?)` → consumer-owned dialog mounts `<RichCard editable>`. At most ONE rich-card editor instance mounted at any moment regardless of node count. `RichCardCanvasNode = NodeData & RichCardJsonNode` intersection (F-V6 lock — `NodeRenderer<TData extends NodeData>` constraint; precedent customJsonRenderer). subPath = rich-card `__rcid` (UUID identifier); consumer wiring uses imperative `RichCardHandle.focusCard(id)` via ref (F-02 lock — rich-card has no `initialFocusCardId` prop). Q6 constants locked: MAX_FLAT_FIELDS=3, MAX_NESTED_OUTLINES=4, NESTED_DEPTH=1, single-click trigger. F-V1 lock: outer is `<div role="group">` + per-element `<button>`s (NOT button-of-buttons). F-03 graceful degradation when `__rcid` missing on a subcard (click bubbles to title-strip → root edit; dev-mode warn). F-05+G1 `position: relative` chain at every DOM level (NodeShell → RichCardViewer outer → SubcardBlock) for xyflow handle anchoring — comments in source mark each level. Cross-registry deps `@ilinxa/rich-card@^0.4.0` + `@ilinxa/flow-canvas-01@^0.2.1` (workstream A's v0.2.1 ship). Five commits: B1+B2 `19e3f42` (parts + lib + types + index + meta + manifest), B3 `4c185ef` (demo + usage + dummy-data with agent-workflow fixture: 3 rich-card nodes + 1 custom-json audit-log node + connecting edges), B4 `3519a7c` (registry.json base + fixtures items + `pnpm registry:build` regen), B-smoke `86cdd05` (F-S1 cross-procomp import bug fix-up — see below), B-final (this commit; guide + review + tracking). **F-V2 smoke harness path-b surfaced + closed F-S1** — shadcn 4.6.0 path-rewriter has TWO bugs hitting same-category cross-procomp ships: (1) `index.ts` cross-procomp re-exports get mangled (`@/components/data/rich-card/types` with stray `data/`; `@/lib/update-node-data` with most of path stripped) → workaround: DROP the re-exports, consumers import directly from each procomp's barrel (per json-form v0.1.4 precedent); (2) same-category `<other-slug>/types` imports substitute CURRENT slug for target slug (NOT seen in json-form's cross-category forms→data; specific to same-category siblings) → workaround: RELATIVE imports for all cross-procomp paths in shipped source (bypass the alias rewriter; sibling-procomp relationships preserved at same depth in both producer + consumer trees). Bug 2 promote-to-cross-cutting candidate: if a future same-category cross-procomp ship trips it, escalate to F-cross-13. **GATE 3 spot-check Pass with follow-ups** ([reviews/2026-05-16-v0.1.0-spotcheck.md](../docs/procomps/rich-card-in-flow-procomp/reviews/2026-05-16-v0.1.0-spotcheck.md)) — rotating dim Public API; 3 findings (F-01 F-S1 closed pre-ship; F-02 isCardLike heuristic over-trigger v0.2 candidate; F-03 rapid open/close per-mount cost blocked on rich-card v0.5's `setTree` API). tsc + lint (2 pre-existing virtualizer warnings unchanged) + validate-meta-deps (43/43) + registry:build all clean. Stage 3 procomp guide authored at [`docs/procomps/rich-card-in-flow-procomp/rich-card-in-flow-procomp-guide.md`](../docs/procomps/rich-card-in-flow-procomp/rich-card-in-flow-procomp-guide.md) with 10 sections (architecture, quick start, canonical wiring, subPath model, multi-select, viewer limits, footguns, migration, contributor F-S1 notes, cross-refs). Decision file: [`.claude/decisions/2026-05-16-rich-card-in-flow-v0.1.0-first-ship.md`](decisions/2026-05-16-rich-card-in-flow-v0.1.0-first-ship.md). Earlier today: **flow-canvas-01 v0.2.1 SHIPPED — Workstream A of rich-card-in-flow GATE 2 plan**: additive `onEditRequest` API on `FlowCanvasProps` (`(nodeId, subPath?) => void`) + `RenderContext` (per-node bound; `(subPath?) => void`) + new exported `updateNodeData(canvas, nodeId, nextData): CanvasData` helper (Q10 lock). Renderers fire `ctx.onEditRequest?.(subPath?)` to request a popup-edit dialog open; the host bubbles to the canvas-level callback. F-V5 ref-mirror lock in `flow-canvas-01.tsx` (`onEditRequestRef` + stable wrapper whose identity flips only on wired/unwired transition — matches `use-canvas-data.ts` defensive posture for `onChange`/`onBeforeConnect`/etc.). NodeAdapter binds the host's `(nodeId, subPath)` to each node's id and exposes a `(subPath?) => void` to renderers; `ctx.onEditRequest` is `undefined` when consumer hasn't wired the canvas-level prop (so renderers can gate click affordances). Two commits: A1 `9dbb06c` code edits (5 files modified + 1 new lib file); A2 `55c630e` meta 0.2.0→0.2.1 + registry.json adds `lib/update-node-data.ts` as `type: registry:component` + `pnpm registry:build` regenerated `public/r/flow-canvas-01.json` (artifact carries the new file at the expected target). Patch-bump exemption per readiness-review rule (additive, non-breaking, no public-API-touch-of-existing) — **GATE 3 skipped**; v0.2.0 spotcheck verdict `Pass with follow-ups` carries forward. tsc + lint (2 pre-existing virtualizer warnings unchanged) + validate-meta-deps (42/42) all clean. Decision file at [`.claude/decisions/2026-05-16-flow-canvas-v0.2.1-on-edit-request.md`](decisions/2026-05-16-flow-canvas-v0.2.1-on-edit-request.md). Canonical first consumer of the `onEditRequest` convention is **rich-card-in-flow@v0.1.0** (Workstream B, remaining). Pre-impl plan review caught one Blocker (F-V6: `NodeRenderer<RichCardJsonNode>` fails the `TData extends NodeData` generic constraint because `__type` is required on NodeData but absent from RichCardJsonNode) — resolved in plan v3 by adding `type RichCardCanvasNode = NodeData & RichCardJsonNode` intersection in rich-card-in-flow's `types.ts` + barrel re-export, following the established `CustomJsonData = NodeData & { _label?: string }` precedent at [parts/custom-json-node.tsx:8](../src/registry/components/data/flow-canvas-01/parts/custom-json-node.tsx). Also dropped fragile `void {} as ...` v0.2 stub from §5.7 (YAGNI) + beefed up §10 enumerateSubcards-stays-private rationale (signature instability tied to F-04's v0.2 tightening path). Plan v3 status: signed off 2026-05-16 (GATE 2 closed, commit `f3108f5`). Earlier today: **rich-card-in-flow Stage 1 description signed off + Stage 2 plan drafted, approval-ready** — new procomp planning track for putting rich-card content into flow-canvas-01 nodes via the popup-edit renderer convention (locked in flow-canvas-01@v0.2.0 perf description Q33). Architecture: read-only `RichCardViewer` renderer in each flow-canvas node + consumer-owned dialog mounting `<RichCard editable>` + at-most-one editor instance mounted regardless of node count + shared `RichCardJsonNode` shape (no transformation layer). Stage 1 description signed off 2026-05-16 (commit `4a9b5a3`) with all 10 Qs locked: Q1 subcards have own ports + selectability + content edits via popup (subPath argument added to `onEditRequest` signature for subcard-level edit targeting), Q2 live save (every keystroke flows; rich-card owns undo), Q3 n8n-style multi-select supported in v0.1 (bulk-EDIT-via-dialog deferred to v0.2), Q4 sibling procomp (slug `rich-card-in-flow`; flow-canvas-01 stays rich-card-unaware per CLAUDE.md portability rule), Q5 REVISED — `onEditRequest` API ships as `flow-canvas-01@v0.2.1` additive patch (NOT v0.2.0 bundle — v0.2.0 already shipped without it), Q6 hardcoded constants v0.1 + consumer can register own NodeRenderer<RichCardJsonNode> for fully custom paint, Q7 soft-fail to neutral placeholder, Q8 consumer's choice (single-dialog recommended), Q9 already satisfied (rich-card at v0.4.1 beta), Q10 helper home picked at GATE 2 (recommend pairing with v0.2.1 patch). Stage 2 plan drafted with two coordinated workstreams: **A** flow-canvas-01@v0.2.1 patch (~1–2 hours — adds `onEditRequest` to both surfaces with optional `subPath` arg + ref-mirror plumbing in `flow-canvas-01.tsx` to defend against consumer callback identity churn + `lib/update-node-data.ts` helper); **B** rich-card-in-flow@v0.1.0 first ship (~4–6 hours — new sealed folder ~13 files; viewer renderer with subcard ports + selectability + click-to-focus; smoke harness path-b REQUIRED per readiness-review rule for v0.1.0 first ships; GATE 3 spot-check required with rotating dim Public API). Plan re-validated: 5 V-findings + 4 gap-scan fold-ins all resolved in place — F-V1 nested-button trap avoided via `<div role="group">` outer + per-element `<button>`s; F-V2 smoke harness required (not skippable); F-V3 dependencies.internal `["rich-card", "flow-canvas-01"]`; F-V4 root component wiring explicit; F-V5 ref-mirror onEditRequest matching `use-canvas-data.ts` defensive posture; G1 position-relative chain at every DOM level (NodeShell → RichCardViewer outer → SubcardBlock) — load-bearing for subcard handle positioning; G3 rapid open/close per-mount cost documented; G4 Radix `forceMount` defeats single-instance property (consumer footgun); G5 subcards NOT drag-extractable in v0.1 (one-tree-per-node rule per Q1). Key locks at impl time: subPath = `__rcid` (rich-card UUID; verified in source), imperative `RichCardHandle.focusCard(id)` via ref (NOT a hypothetical prop — verified rich-card has no `initialFocusPath`-style prop), `__rcid` missing → graceful degradation + dev warn, Object.entries walker for subcard enumeration with is-card-like heuristic, position-relative MUST be at every DOM level for subcard ports, single-click trigger. **User paused 2026-05-16 before approving plan** — handoff at [HANDOFF-2026-05-16-rich-card-in-flow-stage2-pause.md](HANDOFF-2026-05-16-rich-card-in-flow-stage2-pause.md). Next action on resume: user reviews + approves the plan, then Workstream A → A1/A2/A3 commits + push (v0.2.1 ships) → Workstream B (scaffold + B1/B2/B3/B4 + smoke harness + B-guide + B-review + B5 + push). Earlier today: **flow-canvas-01 v0.2.0** Tier 1 + Tier 2 perf bundle SHIPPED. Plan signed off 2026-05-16 (GATE 2 closed; 6 V-findings from re-validation pass resolved in place). Four commits: Tier 1 (`a6b3295` — `onlyRenderVisibleElements` default flipped `false → true` + `onChange` no longer fires per drag-tick, batches to drag-end-only via new `isDraggingRef` + position-only short-circuit in `onNodesChange` + new `onNodeDragStart`/`onNodeDragStop` hook callbacks wired to xyflow), Tier 2 (`ac00c5c` — `DefaultEdge`'s `useStore` selector gains narrow `portEqual` comparator (id/type/multi only — the three fields that drive the renderer); selection ring **visual** decouples from React rendering via new sealed-folder `flow-canvas-01.css` keyed on xyflow's `.react-flow__node.selected` wrapper class — React reconciliation on selection unchanged per F-V1 reframe, the win is visual resilience + cleaner `<NodeShell>` contract; new internal `lib/shallow.ts` 12-LOC zustand-shape helper (avoids leaking a transitive zustand dep to consumers; xyflow's nested zustand@4.5.7 doesn't surface; top-level zustand@5.0.12 via platejs); NodeShell drops `isSelected` prop + Tailwind v4 `*:` canonicalization for focus-visible), Guide §8 (`86d8920` — new top-level "Performance & scale" section in procomp-guide with 4 subsections: what v0.2.0 changed, custom-edge selection renderer-author rule (cite xyflow #4975), popup-edit renderer convention (per description Q33), measuring (pointer to protocol); §8 inserted between Footguns and Migration with §8–§11 renumbered to §9–§12 (no external section anchors broke — verified); guide header version bumped 0.1.0-alpha → 0.2.0-alpha; usage.tsx Performance bullet rewritten to reflect v0.2.0 + new "Performance & scale — renderer-author rules" section back-linking guide §8.2/§8.3), Ship (`0ec3fbd` — meta.ts 0.1.4 → 0.2.0 + updatedAt 2026-05-16; registry.json adds `flow-canvas-01.css` as `type: "registry:file"` (CSS exception per F-V3 + `engagement-heart-burst.css` precedent — CLAUDE.md "every file registry:component" wording overstates) and `lib/shallow.ts` as `registry:component`; `pnpm registry:build` regenerated `public/r/flow-canvas-01.json` + sibling fixtures + catalog; GATE 2 plan doc committed). Public API matrix: one observable default flip + one observable soft behavior change + zero breaking type changes. **GATE 3 spot-check verdict: Pass with follow-ups** ([reviews/2026-05-16-v0.2.0-spotcheck.md](../docs/procomps/flow-canvas-01-procomp/reviews/2026-05-16-v0.2.0-spotcheck.md)) — 3 findings, all bookkeeping (F-01 Med: formal post-Tier-1+2 protocol-compliant DevTools-trace measurement deferred per Path 1 ship choice; F-02 Low: smoke harness path-b not run; F-03 Low: usage.tsx "Deferred to v0.2" heading stale post-ship). All three are v0.2.1 candidates. Path 1 ship rationale: the 2026-05-14 baseline showed `xyflow + v0.1.4 bug-fix + culling-on` hits 60–70 FPS at N=5000 heavy — already exceeds Tier 1's `≥50 FPS at N=2000 light` success bar without any Tier 1 code; running a formal pre-baseline would have burned 15–30 min on a confirmation we already had directionally; plan §9 risks pre-authorized the choice. tsc + lint (2 pre-existing virtualizer warnings unchanged) + validate:meta-deps (42/42) + build (51 routes incl. `/sandbox/flow-stress`) + registry:build all clean. Re-validation methodology learning: six V-findings on the GATE 2 plan I authored, one a substantive misframing (NodeShell "saved re-render" claim was wrong because `NodeAdapter` recreates children every render → memo defeated → NodeShell still re-renders; actual win is CSS-driven visual + clarity, not React perf) + one a real visual-regression bug (`border-radius: inherit` would have squared off all renderer corners) — the other four were lock-downs of TBD items. `feedback_re_validation_pass_catches_real_issues.md` continues to hold. **42 components total** across 8 categories. Previously: 2026-05-14 — **flow-canvas-01 v0.1.4** custom-json handle-render bug fix — surfaced during v0.2 perf-tier pre-work measurement. The built-in `customJsonRenderer` (fallback for unknown `__type` and explicit `__type: "custom-json"`) **never rendered `<Handle>` elements** even when `data.ports` declared ports. xyflow's edge layer then called `getEdgePosition` every frame for handles that didn't exist in the DOM, logging "[React Flow]: Couldn't create edge for source handle id" and at N=200 × ~2 edges × 60 fps ≈ **24,000 failed lookups + console warnings per second** — that was the warning-spam CPU cost masquerading as a React reconciliation perf bug. Discovery: user spotted via screenshot (nodes had no port circles) + console flood during pan. Fix: added 4 `<PortsAt>` calls (one per side) to `CustomJsonNodeImpl`, plus a `relative` class on the outer wrapper. ~6 lines added. **Measured impact** on light stress fixture at vis-on: N=100 40-90 → 160; N=200 7-15 → 100-115; N=500 3-4 → 80-90; N=1000 unmeasured → 60-80 — **15-20× FPS improvement at the cliff**, console clean during pan. Pre-work artifacts also landed: v0.2.0 perf-tier description signed off (Q25–Q35 locked, Tier 1+2=v0.2.0 / Tier 3=v0.3.0 / Tier 4 = sibling procomp, NOT flow-canvas-01), measurement protocol doc (multi-machine recording, min FPS across as success-criterion), heavy synthetic stress fixture (`makeHeavyStressData` in dummy-data.ts) + non-published sandbox stress page at `src/app/sandbox/flow-stress/` with URL-param-driven N/fixture/visibility levers + FPS overlay (eyeball-only per protocol §9 — DevTools Performance traces are authoritative). Baseline measurements filed at `docs/procomps/flow-canvas-01-procomp/research/2026-05-14-baseline.md` including a failed `<details>/<summary>` patch attempt that was reverted before discovering the real bug. Methodology learnings captured: (1) FPS overlay can mislead — 1-sec rolling average hides frametime stutter, trust eyes + DevTools trace; (2) long dev sessions degrade browser + HMR state independent of code, restore via dev-server kill + node-process kill + incognito tab; (3) investigate visually-rendered output before guessing at React internals. **Tier ladder priority shifted by the fix:** Tier 1 success criterion (light ≥50 FPS at N=500) **already met** by v0.1.4 alone before any Tier 1 implementation; Tier 2 urgency reduced (light comfortably smooth at N=1000); Tier 3 needs fresh use-case justification; sibling-procomp threshold should rise from 2k to 10-20k. The v0.2 description stays sealed (signed off; loud changes only); the Stage 2 plan doc absorbs these updated priors. **42 components total** across 8 categories. Previously: 2026-05-13 — **json-form v0.1.4** path-b smoke-fix patch — pre-push smoke against the local-served `public/r/json-form.json` (served via `python -m http.server 8765` with the smoke consumer's `@ilinxa` namespace temporarily pointed at localhost) surfaced four real consumer-side tsc errors that producer-side tsc did not catch — exactly the F-cross-11 brittleness pattern the harness is for. **F-S1 (High):** cross-folder imports from `@/registry/components/data/article-body-01` (barrel) got rewritten by shadcn to `@/components/article-body-01/article-body-01` (a .tsx file), which doesn't export the symbols. Producer-side passed because tsc resolves the barrel; consumer-side failed because the rewrite is wrong. Fix: import from specific files (`/article-body-01` for the editor, `/types` for `ArticleBodyValue` + `ARTICLE_BODY_EMPTY_VALUE`). Also surfaced an inconsistent rewriter behavior — same source path resolves to a flattened consumer path from `parts/*.tsx` but keeps the `data/` segment when imported from `index.ts` or `lib/*.ts`. Workaround: dropped the `JSON_FORM_RICHTEXT_EMPTY_VALUE` re-export from the json-form barrel entirely (initial attempt with a `lib/richtext-helpers.ts` shim also hit the path-rewriter quirk). Consumers using richtext should import `ARTICLE_BODY_EMPTY_VALUE` directly from `@ilinxa/article-body-01`; documented in usage.tsx. **F-S2 (High):** `zodResolver(zodSchema)` overload resolution silently differs between producer and consumer trees even with identical pinned versions — producer matched the v4 overload, consumer matched neither. Fix: `zodResolver(zodSchema as never)` bypasses overload matching; runtime behavior identical. **F-S3 (Medium):** `field-select` non-combobox `onValueChange` callback signature in the installed shadcn Select is `(value: string | null) => void` — guard the null in `coerceOptionValue` call. **F-S4 (Medium):** `field-slider` used `onValueCommit` (radix-v3 name) but the installed shadcn Slider's prop is `onValueChange` only (no commit hook in this version) and the value type is `number | readonly number[]`. Fix: removed `onValueCommit` (onBlur moved to wrapper div), normalized value via a `firstNumber` helper. Pre-existing consumer-side errors in code-block (`TooltipProvider delayDuration`), pdf-viewer (peer deps not installed), and `ui/calendar.tsx` (older calendar version's ClassNames type) flagged as out-of-scope — they predate json-form's install and need their own tracker. Also caught: **harness drift** — consumer's `package.json` had `cmdk@^1.11.0` (phantom version; latest is 1.1.1) from a prior session, reset via `git checkout -- package.json pnpm-lock.yaml`. Producer-side tsc + lint + meta-deps (42/42) + build + registry:build clean post-fixes; consumer-side tsc has zero json-form errors. **Smoke F-01 from v0.1.0 spotcheck — CLOSED**. Earlier today: **json-form v0.1.3** "fully cleaned" patch — closes the four remaining v0.1.2-review findings (F-R4 through F-R7) plus the v0.1.0 leftover F-04 in one bump. **F-R4 (Medium):** `hasSubmitted` was on the main `JsonFormContextValue` memo — the first-submit flip invalidated context and re-rendered every field consumer. Fix: split into a narrow `JsonFormSubmittedContext` provided as a sibling prop on `<JsonFormProvider>`. New `useJsonFormHasSubmitted()` accessor exported; `useJsonFormContext()` continues to merge the flag in for compat. Only `<JsonFormErrorSummary>` actually subscribes to it now. **F-R5 (Low):** `JSON.stringify(values)` was the memo-dep key in `use-conditional` + `use-computed`; the catch-fallback would silently freeze the memo on circular refs. Fix: drop the value-derived key entirely — compute booleans inline (cheap, bounded by condition-tree depth) and memoize the returned object on the booleans. `use-computed` drops the outer useMemo since `field-computed.tsx`'s `value !== computed` effect-guard already handles stability. **F-R6 (Low):** richtext defaultValue ergonomics — re-exported `ARTICLE_BODY_EMPTY_VALUE` as `JSON_FORM_RICHTEXT_EMPTY_VALUE` from index.ts; usage.tsx value-shape table now points consumers at it. **F-R7 (Low):** dropped `'use client'` from `lib/default-registry.ts` (registry object is a plain ref-bag; the `'use client'` modules it imports carry the boundary). **v0.1.0 F-04 cleanup:** headless-demo `<JsonFormProvider value={{ strings: <hardcoded 18-key literal> }}>` swapped for `defaultJsonFormStrings` spread — teaches the canonical pattern. tsc + lint + meta-deps (42/42) + build + registry:build clean. GATE 3 skipped per the rule. Earlier today: **json-form v0.1.2** ARIA-correctness + `onValidationChange` patch — fresh review of the v0.1.1 ship surfaced two real findings that the v0.1.0 spotcheck missed (rotating dim was Public API, not A11y, so the breakage was off-axis). **F-R1 (High):** `label[htmlFor]` was silently failing for ~half the field types — Popover-wrapped controls (`field-date`/`datetime`/`date-range`, `field-select` combobox path) lost `id` because Radix `Popover.Root` is a context-only component with no DOM node; ARIA-roled groups (`field-radio-group`, `field-checkbox-group`, `field-rating`) couldn't bind because HTML `label[for]` only targets native form controls. Fix: new `FieldAriaProps` bridge passed via `FieldRendererArgs.ariaProps` — renderers attach `id` to native controls (label htmlFor binds) OR `aria-labelledby` to group roots. `Slot.Root` wrap dropped from `FieldWrapper`; label gains an explicit `id={labelId}` so groups can `aria-labelledby` reference it. 14 renderers updated. **F-R2 (High):** `onValidationChange` prop was wired but never called — `ChangeBridge` had `void onValidationChange` (no-op). Fix: `ChangeBridge` now subscribes to RHF `useFormState()` (isValid + errors), fires the callback on change with stable identity-key guard to avoid storms. **F-R3 (Medium):** `field-select` non-combobox path was coercing `option.value` to string and never coercing back — fixed via the same `coerceOptionValue` lookup `field-radio-group` already used. v0.1.0 GATE 3 finding F-R7 (use-client on default-registry) still deferred to v0.2 per the rule. tsc + lint + meta-deps (42/42) + build + registry:build clean. GATE 3 skipped per the rule — additive patch bumps don't trigger; v0.1.0 spotcheck verdict carries forward. Earlier today: **json-form v0.1.1** same-day patch bump — additive `richtext` field type wraps `<ArticleBodyEditor>` from `@ilinxa/article-body-01` via `React.lazy` (~165KB Plate bundle only loads when a schema contains a `richtext` field). Second component to use `dependencies.internal` (now `["code-block", "article-body-01"]`). `applyRequiredRichtext` Zod chain walks Plate's `{ type, children, text }` tree to distinguish "empty paragraph default" from real content. Path A from substrate decision — slot article-body-01 directly today, extract leaner `rich-text` procomp in v0.2 only if form-specific richtext needs diverge. tsc + lint + meta-deps (42/42) + build + registry:build clean. GATE 3 skipped per the rule — additive patch bumps don't trigger. v0.1.0 spot-check (Pass with follow-ups) carries forward. Earlier today: **json-form v0.1.0** SHIPPED — sixth component under the GATE-3 readiness-review rule. Schema-driven form renderer with 24 built-in field types across 5 families (text / choice / date-time / rich-composite / special); declarative validators block compiled to Zod v4 at mount; 11-operator Condition DSL plus function escape hatch covering `visibleWhen` / `enabledWhen` / `requiredWhen`; `computed` fields via pure interpolation OR function escape hatch (both deps-tracked); extensible renderer registry (`fieldRegistry` spread-and-extend + form-level `renderField` slot); standalone parts (`<JsonFormField>` / `<JsonFormSubmitButton>` / `<JsonFormResetButton>` / `<JsonFormErrorSummary>` / `<JsonFormHeader>` / `<JsonFormFieldWrapper>`) for fully-headless layouts via `<JsonFormProvider>` + `useJsonForm()` factory; 12-method imperative handle (submit / reset / setValue / getValue / setError / clearErrors / trigger / focus / isDirty / isValid / isSubmitting). Substrate: react-hook-form v7.75.0 + @hookform/resolvers v5.2.2 + zod v4.4.3 + lucide-react. Cross-registry dep on `@ilinxa/code-block` for the `code` field renderer (lazy-loaded via `React.lazy` — CodeMirror chunk only ships when a form contains a `code` field) — json-form is the FIRST component to actually use `dependencies.internal`. `validate-meta-deps.mjs` extended in earlier session to scan `@ilinxa/<slug>` + `@/registry/components/<cat>/<slug>` + raw-relative `../../<slug>` patterns; surfaced + fixed a phantom `video-player-01` declaration in `post-card-01` along the way. Sealed-folder shape: 42 files (8 top-level + 22 parts/ + 5 hooks/ + 7 lib/), comparable to code-block (42) and file-manager (32). FieldWrapper uses `Slot.Root` from radix-ui to forward `id` / `aria-required` / `aria-invalid` / `aria-describedby` onto whatever single element the renderer returns — keeps ARIA wiring centralized without an explicit `ariaProps` prop in `FieldRendererArgs`. Two consumer-invisible additive type refinements landed during the implementation pass: `JsonFormContextValue` gained a `fieldRegistry` member (so `<JsonFormField>` can resolve renderers without re-merging at every standalone-part call site), and `JsonFormProvider` / `useJsonFormContext` generics widened to `extends Record<string, any>` to match RHF's `FieldValues` constraint already baked into `JsonFormProps<TValues>`. `'form'` was dropped from shadcn deps because the implementation uses `Slot.Root` + RHF `Controller` directly rather than wrapping the shadcn `Form` primitive — same end-state, less indirection. Demo ships 7 tabs (Registration / Backend-driven / Conditional / Computed / Rich fields / Imperative / Custom registry); the Custom registry tab additionally embeds a fully-headless example using `useJsonForm()` + standalone parts inside a `<details>` block. tsc + lint + meta-deps (42/42) + build + registry:build all clean. Verdict **Pass with follow-ups** — 4 findings, all 🔸 Medium or 🔹 Low (F-01 path-b smoke pre-push; F-02 requiredWhen validation-timing docs; F-03 use-client directive on default-registry, deferred to v0.2 refactor; F-04 swap demo's literal strings for `defaultJsonFormStrings` spread). Active queue 5/7 — 3 remaining: rich-graph-2, chat-panel, notification-system. **42 components total** across 8 categories. Previously: 2026-05-12 — **Component detail page — Phase 1 shadcn parity** SHIPPED. Detail pages at `/components/<slug>` now render shadcn-style Installation block (Command/Manual tabs + pnpm/npm/yarn/bun PM tabs + copy via existing `useCopyToClipboard`), View-Code Dialog overlay on the Preview (button top-right, opens centered modal with full demo source via `<CodeBlock />`), and an inline Demo-source code-block between Preview and Usage with `maxLines=28` collapse + expand-to-modal. Implemented via new build-time generator `scripts/build-source-map.mjs` that emits a typed `sourceMap` keyed by slug; wired into `predev`/`prebuild`/`vercel-build`. Zero per-component authoring required — all 41 components picked up the new sections for free. Phases 2 (Examples convention + stat-card 9-tab + flow-canvas-01 4-tab extractions) and 3 (Composition tree + API Reference meta fields) deferred; full plan at `.claude/plans/create-a-comprehensive-plan-hidden-pebble.md`. DP-1 locked = hybrid + migration tracker at `docs/usage-migration-tracker.md` (41/41 unmigrated; authors flip each row as they next touch a component). Earlier this week: **flow-canvas-01 v0.1.3** second-round perf patch — after v0.1.2 made drag-tick callbacks stable, user pushed back: "better but very heavy, 200 is not too much." Remaining cost was paint-side, not React-side: `fitView` was hardcoded on `<ReactFlow>`, so every node sat on-screen at mount and `onlyRenderVisibleElements` had nothing to cull → the browser composited 200 full cards every pan/zoom frame. Plus `CustomJsonNode` ran `JSON.stringify` on every render even when collapsed, inside a CSS-truncated `<pre>` block with a `shadow-sm` paint. Fix: read `data?.viewport ?? defaultData?.viewport` via `useState(() => ...)` snapshot, pass to ReactFlow as `defaultViewport`, disable `fitView` when present; collapsed `CustomJsonNode` drops `<pre>` + JSON.stringify + shadow + header-separator (label-only); stress fixture now ships `viewport: { x: 40, y: 40, zoom: 0.9 }` so culling actually kicks in on first paint. Internal-only — `CanvasData.viewport` was already a public field; no new props. v0.1.2 earlier this session was the cascading-callback round — three xyflow anti-patterns the `xyflow-react-pro` skill calls out — `isValidConnection` rebuilding on every drag tick via `useCallback([xyNodes, xyEdges])`; `onNodesChange`/`onEdgesChange`/`onConnect`/`appendNode`/etc. in `use-canvas-data` closing over sibling state so their dep arrays churned on every state update; `<CanvasContextMenu>` receiving full re-mapped `nodes[]`/`edges[]` arrays per tick. All three rebuilds cascaded into ReactFlow's reconciliation, and `fitView` mounting all 200 nodes meant `onlyRenderVisibleElements` couldn't cull anything. Fix: ref-mirror sibling state + callback props in `use-canvas-data`, make every change handler `useCallback(..., [])` or `[fireOnChange]` only; same ref pattern for `isValidConnection`; new `getNodeById` / `getEdgeById` resolvers replace the array props into `CanvasContextMenu` (internal API change, not exported). Internal-only — no public-API touch, so per the readiness-review rule patch bumps skip GATE 3. tsc + lint + meta-deps all clean. Earlier in the session: **code-block v0.1.0** SHIPPED — fifth component under the GATE-3 readiness-review rule, first entry in the new **`code`** category. Language-agnostic code surface with **three modes in one component** — view (Shiki tokenization, dual-theme CSS variables, zero re-tokenize on theme switch, GitHub Light/Dark Default palettes), edit (CodeMirror 6 with custom HighlightStyle approximating GitHub colors via P2 substrate-shift after `@shikijs/codemirror` was found to not exist on npm), terminal (structured `TerminalLine[]` API with input/output/error kinds + prompt detection + optional macOS traffic-lights). Streaming-friendly via explicit `streaming` prop + rAF-batched re-tokenize + blinking tail cursor. Long-block collapse via `maxLines` + fade-out + "Show all" button. Line highlights, severity-icon annotations with tooltips, copy with clipboard-API + execCommand fallback, download via Blob URL, fullscreen expand modal, wrap toggle, filename pill, language label, full keyboard a11y. Filename → lang priority chain: `lang` > `filenameToLang()` > 30-entry built-in ext map > plaintext. Object-shape callbacks throughout. 42-file sealed folder + 2 registry items (base + fixtures). New `code` category at order 9 (between `media` and `auth`). Description + plan + spot-check review authored; both gates surfaced re-validation refinements (3 contradictions + 6 ambiguities + 3 decisions on GATE 1, 4 refinements + 1 decision on GATE 2, mid-implementation P2 substrate shift). Verdict `Pass with follow-ups` (5 findings, all v0.1.1+ or v0.2.0, none blocking). **41 components total** across 8 categories.)

---

## Components

43 components across 8 categories. Source of truth for per-component description / API / status: each component's `meta.ts` and procomp docs (`docs/procomps/<slug>-procomp/`). For the version snapshot: [`docs/component-versions.md`](../docs/component-versions.md). For per-component review state (Tier 1 reviewed / Tier 2 pending) + per-finding history: [`docs/reviews/sweep-tracker.md`](../docs/reviews/sweep-tracker.md).

| Slug | Category | Status | Version |
|------|----------|--------|---------|
| `data-table` | data | alpha | 0.1.1 |
| `rich-card` | data | **beta** | 0.4.1 |
| `kanban-board-01` | data | alpha | 0.3.0 |
| `flow-canvas-01` | data | alpha | 0.2.2 |
| `article-body-01` | data | alpha | 0.2.0 |
| `engagement-bar-01` | data | alpha | 0.1.2 |
| `post-card-01` | data | alpha | 0.1.1 |
| `comment-thread-01` | data | alpha | 0.1.0 |
| `article-meta-01` | data | alpha | 0.1.0 |
| `content-card-news-01` | data | alpha | 0.2.0 |
| `event-card-01` | data | alpha | 0.1.1 |
| `expandable-text-01` | data | alpha | 0.1.0 |
| `info-list-01` | data | alpha | 0.1.0 |
| `people-grid-01` | data | alpha | 0.1.0 |
| `progress-timeline-01` | data | alpha | 0.1.2 |
| `project-card-01` | data | alpha | 0.2.0 |
| `registration-card-01` | data | alpha | 0.1.1 |
| `schedule-list-01` | data | alpha | 0.1.0 |
| `story-rail-01` | data | alpha | 0.2.0 |
| `thumb-list-01` | data | alpha | 0.1.0 |
| `stat-card` | data | alpha | 0.1.1 |
| `rich-card-in-flow` | data | alpha | 0.1.0 |
| `workspace` | layout | alpha | 0.1.1 |
| `grid-layout-news-01` | layout | alpha | 0.2.0 |
| `markdown-editor` | forms | alpha | 0.1.1 |
| `properties-form` | forms | alpha | 0.1.1 |
| `json-form` | forms | alpha | 0.1.4 |
| `entity-picker` | forms | alpha | 0.1.1 |
| `category-cloud-01` | forms | alpha | 0.1.0 |
| `filter-bar-01` | forms | alpha | 0.1.0 |
| `filter-stack` | forms | alpha | 0.1.0 |
| `author-card-01` | marketing | alpha | 0.1.0 |
| `newsletter-card-01` | marketing | alpha | 0.1.0 |
| `page-hero-news-01` | marketing | alpha | 0.1.2 |
| `share-bar-01` | marketing | alpha | 0.1.0 |
| `media-carousel-01` | media | alpha | 0.1.2 |
| `story-viewer-01` | media | alpha | 0.1.1 |
| `video-player-01` | media | alpha | 0.1.2 |
| `pdf-viewer` | media | alpha | 0.1.3 |
| `file-tree` | navigation | alpha | 0.1.0 |
| `file-manager` | navigation | alpha | 0.1.0 |
| `code-block` | code | alpha | 0.1.0 |
| `detail-panel` | feedback | alpha | 0.1.1 |

> `force-graph` removed 2026-05-08 pending recreation; v0.2 source + procomp docs archived to [`docs/migrations/force-graph/`](../docs/migrations/force-graph/). v3 design + slug TBD.

---

## Active queue (session-open list, 2026-05-13)

User's procomp queue — 5 of 6 + 1 inserted (`code-block`) + 1 inserted (`json-form`) shipped, 3 remaining. Each goes through GATE 1 (description) → GATE 2 (plan) → implementation → GATE 3 (spot-check review).

1. ~~`pdf-viewer`~~ ✅ shipped 2026-05-10 (v0.1.0 → v0.1.3 same day)
2. ~~`file-tree`~~ ✅ shipped 2026-05-10 (v0.1.0; the data-substrate primitive)
3. ~~`file-manager`~~ ✅ shipped 2026-05-10 (v0.1.0; renamed from `folder-manager` to better reflect "files + folders"; composes `file-tree` via optional `sidebar` slot; ships shared `<FileClipboardProvider>` primitive)
4. ~~`code-block`~~ ✅ shipped 2026-05-11 (v0.1.0; inserted mid-session at user's request; new `code` category; substrate for chat-panel, markdown fenced blocks, rich-card code sections, virtual terminals; P2 substrate-shift after `@shikijs/codemirror` was found to not exist on npm)
5. ~~`json-form`~~ ✅ shipped 2026-05-13 (v0.1.0; inserted between code-block and rich-graph-2; first component to actually use `dependencies.internal` — cross-registry dep on `@ilinxa/code-block` lazy-loaded for the `code` field; 24 built-in field types; RHF v7 + zod v4 substrate; spotcheck Pass-with-follow-ups, 4 findings)
6. `rich-graph-2`
7. `chat-panel`
8. `notification-system`

## Roadmap (longer-term team-utility candidates)

Next candidates, ordered by team utility (separate from the active queue above; surface here so they're not forgotten):

1. ~~`data/stat-card`~~ ✅ shipped 2026-05-09
2. `feedback/empty-state` — icon + title + body + primary action.
3. `forms/multi-select` — combobox with tag chips (shadcn has Command, no real multi-select).
4. `layout/page-header` — title + breadcrumbs + actions slot.
5. `feedback/notification-feed` — grouped, time-bucketed, read/unread (overlaps with active-queue `notification-system` — reconcile when that one starts).
6. `navigation/command-palette` — cmd+k, grouped results.
7. `media/dropzone` — drag-drop + progress + previews.

**Active sweep work** — in-progress at `docs/reviews/sweep-tracker.md`. Tier 1 (9 components) reviewed at v0.1 across sessions 1-6; mid-sweep checkpoint Phases 1-6 closed in sessions 7-7d (this commit); Tier 2 (27 components) reviews scheduled across sessions 8-12; sweep close + rollup at session 13.

---

## Open decisions / TODOs

**Active — needs decision or work**

- **rich-card-in-flow v0.1.0 spot-check follow-ups (2 v0.2 candidates):** F-02 (Low) `isCardLike` heuristic in `lib/enumerate-subcards.ts` may over-trigger on consumer-defined RichCardJsonNode with incidental `ports` or `__rcorder` keys — tighten when rich-card v0.5 ships a canonical "is-card" predicate. F-03 (Low) rapid open/close per-mount cost: `key={editing.nodeId}` remounts Plate per click; blocked on rich-card v0.5's hypothetical `RichCardHandle.setTree(tree)` API. Both deferred to rich-card-in-flow@v0.2.0. Decision file: [`.claude/decisions/2026-05-16-rich-card-in-flow-v0.1.0-first-ship.md`](decisions/2026-05-16-rich-card-in-flow-v0.1.0-first-ship.md); spot-check: [`reviews/2026-05-16-v0.1.0-spotcheck.md`](../docs/procomps/rich-card-in-flow-procomp/reviews/2026-05-16-v0.1.0-spotcheck.md).
- **F-S1 same-category cross-procomp `/types` substitution bug (Watch):** shadcn 4.6.0 path rewriter substitutes the CURRENT slug for the target slug when a same-category sibling procomp imports `<other-slug>/types`. NOT seen in json-form (cross-category forms→data). Worked around in rich-card-in-flow via RELATIVE imports for all cross-procomp paths in shipped source. **If a future same-category cross-procomp ship trips it again, escalate to `docs/reviews/sweep-tracker.md` as F-cross-13** (it's currently a per-procomp finding; promotion requires a second instance).
- **flow-canvas-01 v0.2.0 spot-check follow-ups (F-01 Med, F-02 Low, F-03 Low; all v0.2.1 candidates):** GATE 3 spot-check ([reviews/2026-05-16-v0.2.0-spotcheck.md](../docs/procomps/flow-canvas-01-procomp/reviews/2026-05-16-v0.2.0-spotcheck.md)) verdict Pass with follow-ups. **F-01 Med:** formal protocol-compliant post-Tier-1+2 DevTools-trace measurement deferred per Path 1 ship choice — run the 4-cell matrix (light vis-on N=200, N=2000; heavy vis-on N=200, N=1000) per protocol §3 and file as `research/<date>-tier2-postship.md`. **F-02 Low:** smoke harness path-b not run for v0.2.0 — run against locally-served `public/r/flow-canvas-01.json` before or after push. **F-03 Low:** usage.tsx "Deferred to v0.2" heading stale post-v0.2.0 ship — rename + reorganize per actual deferral targets (Tier 3 = v0.3, minimap/undo-redo/marquee = future v0.2 non-perf candidates). Decision file at [`.claude/decisions/2026-05-16-flow-canvas-v0.2.0-perf-bundle.md`](decisions/2026-05-16-flow-canvas-v0.2.0-perf-bundle.md).
- **pdf-viewer worker default (F-01, v0.2.0):** Plan locked bundled-`?url` worker import for offline-friendliness. Turbopack rejected `new URL("bare-specifier", import.meta.url)` at build, so v0.1.0 ships with unpkg-CDN default + `workerSrc` prop override + `postinstall` recipe in guide. Bump-target v0.2.0: replace CDN default with bundled-or-postinstall path. Decision file at [`.claude/decisions/2026-05-10-pdf-viewer-v01-pipeline.md`](decisions/2026-05-10-pdf-viewer-v01-pipeline.md).
- **F-cross-12 (v0.2 candidate):** Positional-callback signatures across 5 components / 6 occurrences (kanban-board-01, grid-layout-news-01, content-card-news-01, project-card-01, story-rail-01). Breaking change; out of v0.1.x (non-breaking) scope. Library-wide v0.2 migration to object-shape callbacks with deprecation warnings emitted in v0.1.x as transition.
- ~~**F-cross-11 follow-up paths (b)/(c)**~~ **✅ Path (b) CLOSED 2026-05-09** — smoke harness extended with consumer-side `pnpm tsc --noEmit` post-install; first run surfaced + fixed a real bug (37 `index.ts` files re-exporting docs-site-only `meta`). Path (c) — tsconfig path realignment — remains deferred; with (b) actively guarding, (c)'s additional safety value is marginal. See [`.claude/decisions/2026-05-09-fcross11-path-b-smoke-tsc.md`](decisions/2026-05-09-fcross11-path-b-smoke-tsc.md).

**Informed defers — explicit trigger to revisit**

- **MDX for usage docs:** currently `usage.tsx`. **Trigger:** ~5 components reach prose-heavy guidance, OR a consumer needs MDX-specific features (codeblocks-with-render, embeds).
- **NPM publish artifacts:** no `tsup`/`rollup`, no `package.json` exports map. Distribution via shadcn-registry handles the team-internal use case. **Trigger:** external consumer onboards, OR shadcn-registry's update-friction (re-running `pnpm dlx shadcn add`) surfaces real pain. Heavyweights (rich-card 51 files, markdown-editor 28 files + 10 codemirror peer deps) are the most likely first trigger.
- **Test runner not wired.** `pnpm tsc --noEmit && pnpm lint` cover correctness today; demo-driven manual verification is the interactivity story. **Trigger:** first non-trivial bug in pure-function modules (workspace + rich-card + properties-form `lib/` directories). First test should be rich-card's `parse → serialize → parse` fixed-point round-trip property test.
- ~~**F-cross-04 (environmental)**~~ **✅ CLOSED 2026-05-09** — replaced `next/font/google` with `@fontsource-variable/*` self-hosted packages; `pnpm build` no longer requires network access. See [`.claude/decisions/2026-05-09-fcross04-self-host-fonts.md`](decisions/2026-05-09-fcross04-self-host-fonts.md).
- ~~**F-cross-12 (v0.2 callback migration)**~~ **✅ FULLY CLOSED 2026-05-09** — v0.2 cutover landed: all 6 positional callbacks removed, `<oldName>Args` renamed to canonical `<oldName>` (now object-shape). Components bumped: `grid-layout-news-01` 0.2.0, `content-card-news-01` 0.2.0, `project-card-01` 0.2.0, `story-rail-01` 0.2.0, `kanban-board-01` 0.3.0. v0.1.x transition (Args sibling props with deprecation warnings) was the bridge; now removed. See [`.claude/decisions/2026-05-09-fcross12-callback-migration-transition.md`](decisions/2026-05-09-fcross12-callback-migration-transition.md) + [`.claude/decisions/2026-05-09-fcross12-v02-cutover.md`](decisions/2026-05-09-fcross12-v02-cutover.md).

For closed entries (Phase 0 risk spike, chart palette, site nav, alpha/beta variants, footer version, public registry build, reserved meta fields, lime contrast pattern, F-cross-01 / F-cross-11, etc.), see the per-decision files in `.claude/decisions/` plus `STATUS-archive.md` (pre-2026-05-09 entries).

---

## Recent activity

The 5 most-recent decision files (or HANDOFF artifacts), most-recent first. Full list at [`.claude/decisions/`](decisions/).

- [2026-05-16 — flow-canvas-01 v0.2.2 SHIPPED — defer fireOnChange to microtask (setState-during-render fix)](decisions/2026-05-16-flow-canvas-v0.2.2-microtask-defer.md) (Latent setState-during-render bug surfaced by rich-card-in-flow@v0.1.0's demo on first dev-server load. React 19 warned about consumer's setCanvas being called during Canvas's render. Root cause: 13 reducer-side-effect sites in `use-canvas-data.ts` (the F-V4-acknowledged pattern, originally deferred to v0.3); xyflow's initial DOM-measurement dimensions emit triggers `onNodesChange` during render-phase → reducer fires consumer's `onChange` synchronously. Fix: single-site `queueMicrotask` wrap of `fireOnChange`'s body — zero call-site changes, all 13 sites uniformly deferred. F-V4's "consistency over divergence" principle preserved. Patch-bump exemption (internal-only, no public API touch); GATE 3 skipped, v0.2.0 verdict carries forward. Single commit `022cee8`. tsc + lint + validate-meta-deps (43/43) + build (43 routes) all clean.)
- [2026-05-16 — rich-card-in-flow v0.1.0 SHIPPED — Workstream B (43rd component)](decisions/2026-05-16-rich-card-in-flow-v0.1.0-first-ship.md) (Canonical implementation of the popup-edit renderer convention from flow-canvas-01@v0.2.0 perf description Q33. New sealed folder at `src/registry/components/data/rich-card-in-flow/` with 9 shipped files; cross-registry deps on `@ilinxa/rich-card@^0.4.0` + `@ilinxa/flow-canvas-01@^0.2.1` (Workstream A's same-day ship). `RichCardCanvasNode = NodeData & RichCardJsonNode` intersection type (F-V6 lock); subPath protocol carries clicked subcard's `__rcid`; consumer wiring uses imperative `RichCardHandle.focusCard(id)` via ref (F-02 lock). Title strip + first 3 flat fields + 4 subcard outlines with own ports + selectability; one level deep. F-V1 outer `<div role="group">` (NOT button-of-buttons); F-03 graceful degradation when `__rcid` missing; F-05+G1 `position: relative` chain load-bearing for xyflow handle anchoring. Five commits: B1+B2 `19e3f42` (parts/lib/types/index/meta/manifest), B3 `4c185ef` (demo with full Dialog + RichCard wiring + agent-workflow fixture), B4 `3519a7c` (registry.json base + fixtures + regen), B-smoke `86cdd05` (F-S1 fix-up — shadcn rewriter has TWO bugs: (1) `index.ts` cross-procomp re-exports get mangled → DROP them; (2) same-category `<other-slug>/types` imports substitute current slug for target → use RELATIVE paths for all cross-procomp imports in shipped source), B-final (this commit; guide + review + tracking). F-V2 smoke harness path-b PASSED after F-S1 fix-up cycle. GATE 3 spot-check **Pass with follow-ups** (rotating dim Public API; 3 findings — F-01 F-S1 closed pre-ship, F-02 + F-03 v0.2 candidates). Stage 3 procomp guide 10 sections incl. contributor F-S1 notes. tsc + lint + validate-meta-deps (43/43) + registry:build all clean. 43 components total.)
- [2026-05-16 — flow-canvas-01 v0.2.1 SHIPPED — Workstream A of rich-card-in-flow plan (onEditRequest API + updateNodeData helper)](decisions/2026-05-16-flow-canvas-v0.2.1-on-edit-request.md) (Two commits: A1 `9dbb06c` code edits — types.ts adds `RenderContext.onEditRequest?` + `FlowCanvasProps.onEditRequest?`; canvas-context.tsx + node-adapter.tsx thread + per-node-bind the callback; flow-canvas-01.tsx F-V5 ref-mirror lock so identity flips only on wired/unwired transition (matches use-canvas-data.ts defensive posture for onChange/etc.); new `lib/update-node-data.ts` immutable walk-and-replace helper (Q10 lock); index.ts re-exports it. A2 `55c630e` meta 0.2.0→0.2.1 + registry.json adds update-node-data.ts + `pnpm registry:build` regen — artifact verified at line 103 of `public/r/flow-canvas-01.json`. Patch-bump exemption per readiness-review rule: GATE 3 SKIPPED, v0.2.0 spotcheck verdict carries forward. tsc + lint (2 pre-existing virtualizer warnings unchanged) + validate-meta-deps (42/42) all clean. Plan v3 sign-off (commit `f3108f5`) caught Blocker F-V6 pre-impl: `NodeRenderer<RichCardJsonNode>` fails the `TData extends NodeData` constraint because `__type` is required on NodeData but absent from RichCardJsonNode → resolved via `type RichCardCanvasNode = NodeData & RichCardJsonNode` intersection in rich-card-in-flow's types.ts following the customJsonRenderer precedent. Canonical first consumer of the convention is rich-card-in-flow@v0.1.0 (Workstream B, remaining work).)
- [2026-05-16 — rich-card-in-flow Stage 1 description signed off (commit `4a9b5a3`) + Stage 2 plan drafted approval-ready, user paused before approving](HANDOFF-2026-05-16-rich-card-in-flow-stage2-pause.md) (new procomp planning track for putting rich-card content inside flow-canvas-01 nodes via the popup-edit renderer convention from flow-canvas-01@v0.2.0 perf description Q33. **Stage 1 description signed off** with all 10 Qs locked — most consequential: Q1 subcards have own ports + selectability + content edits via popup (added subPath arg to onEditRequest); Q4 sibling procomp keeps flow-canvas-01 rich-card-unaware per CLAUDE.md portability rule; Q5 REVISED — `onEditRequest` API ships as `flow-canvas-01@v0.2.1` additive patch (NOT v0.2.0 bundle as originally proposed — v0.2.0 already shipped); Q9 already satisfied (rich-card at v0.4.1 beta, far past the originally-anticipated v0.1 viewer milestone). **Stage 2 plan drafted + approval-ready** with two coordinated workstreams: A (flow-canvas-01@v0.2.1 patch ~1–2h — adds `onEditRequest` with optional subPath + ref-mirror plumbing + `updateNodeData` helper) + B (rich-card-in-flow@v0.1.0 first ship ~4–6h — sealed folder ~13 files; viewer + subcard block + dialog wiring; smoke harness REQUIRED; GATE 3 spot-check with Public API rotating dim). Plan resolved 5 V-findings (from initial + deep source verification) + 4 gap-scan fold-ins (G1-G5) in place: F-V1 nested-button trap (use `<div role="group">` outer + per-element `<button>`s); F-V2 smoke harness REQUIRED for v0.1.0 first ships per readiness-review rule; F-V3 dependencies.internal `["rich-card", "flow-canvas-01"]`; F-V4 root component wiring spelled out; F-V5 ref-mirror onEditRequest matching `use-canvas-data.ts` defensive posture; G1 position-relative MUST be at every DOM level (NodeShell → RichCardViewer outer → SubcardBlock) — load-bearing for subcard handle visual positioning since xyflow's `<Handle>` is position:absolute and anchors to nearest positioned ancestor; G3 rapid open/close per-mount cost documented; G4 Radix `forceMount` defeats "at most one editor mounted" property (consumer footgun); G5 subcards NOT drag-extractable v0.1 per Q1 lock. Key impl-time locks verified against actual source: subPath = `__rcid` (rich-card UUID identifier, verified in types.ts); imperative `RichCardHandle.focusCard(id)` via ref (verified rich-card has no `initialFocusPath` prop); `__rcid` missing → degrades gracefully + dev warn; Object.entries walker for subcard enumeration (rich-card uses open-shape, not fixed children[] array); `<RichCard editable={true}>` required (rich-card defaults to false, verified); single-click trigger. **User paused before approving the plan** — wants to resume in a fresh session. Next action: review + approve plan, then Workstream A.)
- [2026-05-16 — flow-canvas-01 v0.2.0; Tier 1 + Tier 2 perf bundle SHIPPED (4 commits)](decisions/2026-05-16-flow-canvas-v0.2.0-perf-bundle.md) (Tier 1 `a6b3295`: `onlyRenderVisibleElements` default flipped to `true` + `onChange` batches mid-drag, fires on drag-stop only via new `isDraggingRef` + position-only short-circuit + new `onNodeDragStart`/`onNodeDragStop` hook callbacks wired to xyflow. Tier 2 `ac00c5c`: `DefaultEdge`'s `useStore` selector gains narrow `portEqual` comparator (id/type/multi); selection ring **visual** decouples from React rendering via new sealed-folder `flow-canvas-01.css` keyed on xyflow's `.react-flow__node.selected` class (per F-V1 reframe — React reconciliation on selection unchanged, win is visual resilience + cleaner `<NodeShell>` contract); new internal `lib/shallow.ts` 12-LOC zustand-shape helper; NodeShell drops `isSelected` + Tailwind v4 `*:` canonicalization. Guide `86d8920`: new top-level §8 "Performance & scale" in procomp-guide (subsections: what changed, custom-edge selection rule citing xyflow #4975, popup-edit convention, measuring); §8–§11 renumbered to §9–§12 (no external anchors broke); usage.tsx Performance bullet rewritten + new "Performance & scale — renderer-author rules" section back-linking guide §8.2/§8.3. Ship `0ec3fbd`: meta 0.1.4→0.2.0; registry.json adds `flow-canvas-01.css` as `registry:file` (F-V3 CSS exception per engagement-heart-burst.css precedent) and `lib/shallow.ts` as `registry:component`; `pnpm registry:build` regenerated artifacts; GATE 2 plan doc committed (signed off 2026-05-16; six V-findings from re-validation resolved in place — F-V1 perf-claim reframe, F-V2 release-notes destination lock, F-V3 CSS file-type lock, F-V4 reducer-pattern consistency, F-V5 line-numbers reframe, F-V6 border-radius:inherit removed). Public API matrix: one default flip + one soft behavior change + zero breaking type changes. **GATE 3 spot-check: Pass with follow-ups** ([reviews/2026-05-16-v0.2.0-spotcheck.md](../docs/procomps/flow-canvas-01-procomp/reviews/2026-05-16-v0.2.0-spotcheck.md)) — 3 findings, all bookkeeping, all v0.2.1 candidates (F-01 Med: formal post-Tier-1+2 protocol-compliant measurement deferred per Path 1 ship choice; F-02 Low: smoke harness path-b not run; F-03 Low: usage.tsx "Deferred to v0.2" heading stale). Path 1 rationale: 2026-05-14 baseline showed `xyflow + v0.1.4 + culling-on` already hits 60-70 FPS at N=5000 heavy — exceeds Tier 1's success bar without any Tier 1 code; plan §9 risks pre-authorized. tsc + lint (2 pre-existing virtualizer warnings) + validate:meta-deps (42/42) + build (51 routes incl `/sandbox/flow-stress`) + registry:build all clean. Lessons: re-validation surfaces real bugs (`border-radius: inherit` would have squared off renderer corners; "saved re-render" framing was wrong because NodeAdapter recreates children per render so memo defeated); the reducer-side-effect pattern in `use-canvas-data.ts` is a known smell with 11 precedent sites — propagate consistency, clean up all at once in v0.3.)
For every prior session / decision before 2026-05-08: see [`STATUS-archive.md`](STATUS-archive.md).

---

## How to update this file

`STATUS.md` is the slim snapshot. Don't extend it with verbose entries.

| When something happens | Where it goes |
|---|---|
| Component ships / version bumps / status changes | Update the Components table row above + author a `.claude/decisions/<date>-<slug>.md` |
| Sweep phase closes / cross-cutting finding closes | Author a decision file; update sweep-tracker; add a "Recent activity" pointer above |
| New TODO / Open decision lands | Add a bullet in "Open decisions / TODOs" |
| Something old gets closed | Either move the bullet to the archive OR strike it inline if recent + relevant |

The "Recent activity" pointer list above stays at ~5 entries (most recent first). Older entries are still in `.claude/decisions/` — not removed; just not surfaced in this index.
