# ilinxa-ui-pro — Status

> Current snapshot of the project. Update when components ship, statuses change, or non-obvious decisions are made. This is the *now*, not a changelog.
>
> Last updated: **2026-04-29** (force-graph v0.1 + v0.2 plans signed off; system description consistency pass + decision #11 footnote applied; **Phase 0 risk-spike execution brief signed off** at [docs/procomps/force-graph-procomp/force-graph-phase-0-spike-brief.md](../docs/procomps/force-graph-procomp/force-graph-phase-0-spike-brief.md) — Day-1-ready for the spike author. Per-phase plan cascade state: v0.1 + v0.2 ✓ signed off; v0.6 plan unblocked but not started; v0.3 / v0.4 / v0.5 gated on Tier 1 plans (none of which are authored yet). **Phase 0 risk spike itself remains the bottleneck** — v0.1 implementation cannot begin until the 2-day GPU benchmark validates the WebGL pipeline at ≥30 fps on integrated GPU at 100k edges. [HANDOFF.md](HANDOFF.md) refreshed for the current pause point.)

## Components

| Slug | Category | Status | Version | Notes |
|------|----------|--------|---------|-------|
| `data-table` | data | alpha | 0.1.0 | Canonical template — generic, typed columns, no client state. Future tables compose on it. |
| `rich-card` | data | **beta** | 0.4.0 | JSON-driven recursive card-tree viewer + full structural editor + safety net. **Bumped to beta 2026-04-28** — feature-complete for current scope (v0.1–v0.4 all shipped), API stable, suitable for real use. Stable (1.0.0) gated on external consumer adoption + 30-day no-break window. Core surface: editing, drag-drop, bulk multi-select, permissions, custom predefined keys, virtualization, native search, validation hooks, undo/redo. v0.5 markdown adapter remains deferred indefinitely as a separate companion. Drag-drop reordering via `@dnd-kit` (2 scopes), bulk multi-select (shift-click + cmd-click + bulk toolbar), per-level/per-card/per-key permission matrix with predicate escape hatches, custom predefined-key registration, opt-in virtualization (`virtualize: 'auto'` enables at 500+ nodes), inline meta editing with custom renderers + audit trail, root-removal opt-in, promote-on-delete with configurable collision strategy, native data-model search (finds collapsed/virtualized/meta — auto-expands path). 12 v0.3 reducer actions, 5 v0.3 event types, 13 imperative handle methods, ~46 optional props. **One intentional breaking change in v0.3:** `onSelectionChange` signature shifts from `(id: string \| null) => void` to `(ids: readonly string[]) => void`. **Markdown intentionally dropped from rich-card itself.** Deps: `popover` + `separator` shadcn primitives, `lucide-react`, `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` + `@tanstack/react-virtual`. ~50 files. |
| `workspace` | layout | alpha | 0.1.0 | Splittable canvas: corner-drag split/merge, edge-drag resize, registry-driven content, responsive collapse + per-breakpoint depth cap. Largest component in the registry — 26 files, full procomp gate (description → plan → guide). |

## Roadmap

Next candidates, ordered by team utility:

1. `data/stat-card` — value + label + delta + sparkline. Universal in dashboards.
2. `feedback/empty-state` — icon + title + body + primary action.
3. `forms/multi-select` — combobox with tag chips (shadcn has Command, no real multi-select).
4. `layout/page-header` — title + breadcrumbs + actions slot.
5. `feedback/notification-feed` — grouped, time-bucketed, read/unread.
6. `navigation/command-palette` — cmd+k, grouped results.
7. `media/dropzone` — drag-drop + progress + previews.

**`rich-card` v0.2 follow-ups** (deferred from v0.1.0, by phase per [rich-card-procomp-plan.md](../docs/procomps/rich-card-procomp/rich-card-procomp-plan.md)): v0.2 inline editor (add/edit/remove fields/cards/predefined-key elements, granular change events, dirty tracking, explicit selection); v0.3 drag-drop (2 scopes), bulk ops, per-level/per-card/per-key permissions, custom predefined-key registration, virtualization if needed; v0.4 sync validation hooks + per-commit undo/redo; v0.5 (deferred indefinitely, separate companion) markdown ↔ JSON adapter. The v0.1 API surface is designed so each phase adds props rather than changing them.

**`workspace` v0.2 follow-ups** (deferred from v0.1.0): detach-to-OS-window for multi-monitor users; touch / pen gesture support; aligned-edge linked resize (multiple edges move together); DnD between areas; cross-breakpoint transition animation. The v0.1 API surface is designed so v0.2 doesn't break it.

## Open decisions / TODOs

- **Phase 0 risk spike PENDING — gates `force-graph` v0.1 implementation.** A 2-day technical-research block: prototype custom `DashedDirectedEdgeProgram` (Sigma WebGL edge program with solid+dashed × arrows × straight+curved); benchmark at 100k edges on integrated AND discrete GPUs; gate ≥30 fps on integrated GPU at idle camera. **Independent of the procomp gate** (per [system §10.1](../docs/systems/graph-system/graph-system-description.md#101-phase-0--risk-spike-2-days)) — runs as a separate work block, not crammed into a Claude session. **Execution brief at [docs/procomps/force-graph-procomp/force-graph-phase-0-spike-brief.md](../docs/procomps/force-graph-procomp/force-graph-phase-0-spike-brief.md)** — Day-1-ready, consolidates the locked program contract (per-edge attributes, no uniforms), 4-step implementation approach (start from `@sigma/edge-arrow`), benchmark methodology + test conditions (power profile / GPU isolation via `chrome://gpu` / Chrome stable to avoid false negatives), 2-day budget split, 4-tier contingency tree (A unified → B split-programs + targeted SVG for dashed+directed subset → C shader optimizations → D full SVG overlay matching description §3 lock). **If spike succeeds:** v0.1 + v0.2 plans are valid; implementation can begin. **If spike fails:** plans are invalidated and rewritten with the contingency tree from the brief §9. Document the result here when complete per the brief §10 protocol. Tier 1 plans (properties-form, detail-panel, filter-stack, entity-picker, markdown-editor) are unaffected by the spike outcome and can author independently.
- **Chart palette still neutral:** `--chart-1..5` are greyscale. Define a lime-leaning analogous palette (e.g. lime → emerald → teal → cyan → blue) when the first chart-heavy component ships.
- **Site nav has only "Components":** add Docs / Blog / GitHub when those sections exist. Mobile menu (sheet/drawer) is currently unnecessary at this nav size — revisit when 4+ items.
- **Reserved-but-unrendered meta fields:** `RegistryEntry.examples`, `ComponentMeta.thumbnail`, `ComponentMeta.subcategory`, `ComponentMeta.related` exist on the types but render nowhere yet. Decide whether to wire any (each adds detail-page surface) or remove them.
- **`alpha` and `beta` badges look identical:** the hard-coded mapping in [components/page.tsx:78-83](../src/app/components/page.tsx#L78) and [[slug]/page.tsx:56-62](../src/app/components/[slug]/page.tsx#L56) only assigns unique colors to `stable` (default) and `deprecated` (destructive); both `alpha` and `beta` render `secondary`. If we want `beta` distinguishable, that's a 2-line tweak in two pages.
- **Footer version is hardcoded** (`v0.1.0`) in `site-footer.tsx`. Lift to a single source (`package.json` or a constant) when version starts changing.
- **Lime contrast pattern is dark-on-lime.** Both modes use near-black `--primary-foreground`. Don't switch to white — contrast fails. If a use case really wants white text on a green action, use a darker green (e.g. `oklch(0.45 0.18 142)` forest) and pair white text — but that becomes a separate token, not `--primary`.
- **MDX for usage docs:** currently `usage.tsx`. Consider migrating to `usage.mdx` once we cross ~5 components with prose-heavy guidance.
- **Public registry build:** no `pnpm dlx shadcn build` config or `public/r/*.json` output yet. Defer until publish phase.
- **NPM publish artifacts:** no `tsup`/`rollup`, no `package.json` exports map. One-day job at publish time.
- **Test debt — `workspace`:** workspace ships v0.1.0 without unit tests because no test runner is wired in this repo. The pure modules in `src/registry/components/layout/workspace/lib/` (`tree.ts`, `reducer.ts`, `geometry.ts`) are written to be testable in isolation when Vitest lands. Verification for v0.1.0 is demo-driven (the dummy registry's `counter` component proves Q3 state preservation across split visually). Land Vitest as a separate STATUS decision before workspace ships v0.2.
- **Test debt — `rich-card`:** same posture. The pure modules in `src/registry/components/data/rich-card/lib/` (`parse.ts`, `serialize.ts`, `classify-key.ts`, `infer-type.ts`, `reducer.ts`) are pure functions and trivially testable when Vitest lands. The single highest-value test is a **round-trip property test** — random `RichCardJsonNode` → `parse` → `serialize` → `parse` → assert tree-equal — which would lock down the Q3 fixed-point contract. v0.1.0 verification is demo-driven via the three demos in [`dummy-data.ts`](../src/registry/components/data/rich-card/dummy-data.ts) and the manual "Read canonical JSON" button in [demo.tsx](../src/registry/components/data/rich-card/demo.tsx).

## Recent decisions (keep latest ~10)

- 2026-04-29 — **Phase 0 risk-spike execution brief signed off.** New auxiliary doc at [docs/procomps/force-graph-procomp/force-graph-phase-0-spike-brief.md](../docs/procomps/force-graph-procomp/force-graph-phase-0-spike-brief.md) — Day-1-ready, consolidates already-locked context (system §10.1 gate + v0.1 plan §8.2 program contract + spec §11.3 starting plan + description §8.5 #2 contingency tree origin) into 11 sections: the gate (≥30 fps integrated GPU at 100k edges, idle camera), `DashedDirectedEdgeProgram` contract (per-edge attributes only, no uniforms), 4-step implementation approach (start from `@sigma/edge-arrow`, add dashed → arrowSource → curveOffset), benchmark methodology + test conditions (power profile / GPU isolation via `chrome://gpu` / Chrome stable / hard-refresh between runs to avoid false negatives), 2-day budget split, scope fences (no node program, no hulls, no store wiring), 4-tier contingency tree (A unified → B split-programs + targeted SVG for dashed+directed subset → C shader optimizations → D full SVG overlay matching description §3 lock), STATUS-update protocol on completion. Validate pass surfaced 7 refinements, all applied: gate-condition wording (§1+§8 — `hideEdgesOnMove` is irrelevant at idle camera; §8 had wrongly called `: false` "the actual gate"), fabricated dash specifics removed (§3 — invented "6px/4px shadcn parity"), Bezier softened to "implementation choice" (§3 — v0.1 plan §8.2 only locks midpoint displacement, not curve construction), mix arithmetic fixed (§4 — direction percentages now sum to 100%), test-condition discipline added (§4 — power profile + GPU verification + browser fixed), Tier B subset handling clarified (§9 — targeted SVG for the small dashed+directed intersection, not "fall to Tier C"), Tier D scope aligned to description §3 lock (§9 — "all edges via SVG, ~5k visible ceiling," not the spec's narrower "dashed-directed edges only"). Spike itself remains PENDING — runs as 2 days of human GPU benchmarking outside any Claude session.

- 2026-04-29 — **`force-graph` v0.2 plan signed off (Stage 2, Phase 2 of 6).** 879 lines covering the interaction infrastructure phase (2 weeks focused): selection / hover / drag-to-pin / undo / redo / linking-mode infrastructure, plus the **compound API** (`<ForceGraph.Provider>` + `<ForceGraph.Canvas>`) per v0.1 plan Q-P0 lock — the single `<ForceGraph>` component continues working as a thin convenience wrapper around Provider+Canvas. UI slice fully activated (selection / hovered / linkingMode / multiEdgeExpanded / dragState); history slice fully activated (ring buffer of composite transactional entries with both `forwards` + `inverses` per Q-P6 lock; capacity 10–500, default 100, cannot be 0); cascade-on-delete branches now meaningful (v0.1 wired them as no-op scaffolding). Drag-to-pin produces ONE drag-coalesced history entry per drag (setNodePosition + maybe pinNode if newly pinned). Keyboard shortcuts canvas-focus only (Cmd/Ctrl+Z, Cmd/Ctrl+Shift+Z + Ctrl+Y, Esc). Refinements on re-validation: **Q-P3 revised** (Zustand v5 removed equality-fn parameter from `useStore`; v5 idiom is the `useShallow` wrapper); **Q-P10 wording fixed** (clarifying React `key` direction — to PREVENT data-change reset hosts stabilize the prop reference via `useMemo`, not `key`; `key` is for FORCED remount). 13 §16.5 plan-stage refinements covering pinned-state-is-layout-local-not-canonical, Provider context useMemo, single-component ref delegation, stale-history cleanup on delta-driven deletes, onSelectionChange initial-fire suppression, and history capture timing. v0.2 implementation runs sequentially after v0.1 lands. Commits: `2aa993b` (draft) + `2d5fac3` (sign-off).
- 2026-04-29 — **System description consistency pass + decision #11 footnote.** Applied the [v0.1 plan Q-P3](../docs/procomps/force-graph-procomp/force-graph-v0.1-plan.md) finding: decision #11 (Lucide icon atlas) footnoted to clarify the original "v0.1" wording predated the procomp decomposition; in the phased plan, atlas + `IconNodeProgram` ship together in **v0.5**, not v0.1. v0.1–v0.4 use Sigma's stock `NodeCircleProgram` (plain disc nodes). Plus: §8 introduces a "Note on plan references" mapping legacy `force-graph-procomp-plan.md` citations to per-phase plans (`force-graph-v0.{N}-plan.md`); §3.1 cleanup (forms category no longer "(new)"); §7.2 + §11.5 + §9 sub-doc map references aligned with per-phase plan structure. Force-graph description line 11 plan-file reference also fixed. **All 7 graph-system planning docs cross-validated** for internal/external consistency — no substantive cross-doc inconsistencies found between the 6 procomp descriptions. Commit: `8cb70f7`.
- 2026-04-28 — **`force-graph` v0.1 plan signed off (Stage 2, Phase 1 of 6).** 1040 lines covering the viewer-core foundation phase (3 weeks focused): origin-aware data model, two-layer state architecture (graphology imperative outside Zustand + Zustand reactive slices per [spec §5.1](../graph-visualizer-old.md)), full `GraphInput = GraphSnapshot \| GraphSource` contract from v0.1 (loadInitial required; subscribe + applyMutation optional; applyMutation type-supported but not exercised until v0.3), `validateSnapshot` algorithm (12 checks with structured errors + graceful degradation per decision #24), custom `DashedDirectedEdgeProgram` structure (Phase 0 spike outcome), FA2 worker integration, theme resolution via globals.css CSS variables. 11 Q-Ps resolved. **Q-P0 added on re-validation:** description §6.3 sibling-hook pattern (`useGraphSelector` from FilterStack/DetailPanel siblings) doesn't compile against single-component v0.1 API → resolved by deferring compound API to v0.2 (matches v0.1's read-only scope where siblings have minimal hook needs). Q-P5 + Q-P9 refined. **Phase 0 risk-spike pre-condition gate:** implementation cannot begin until `DashedDirectedEdgeProgram` benchmarks at ≥30 fps on integrated GPU at 100k edges; SVG-overlay fallback documented as contingency (see Open decisions item above). Commits: `a9d4eac` (draft) + `089955f` (sign-off).
- 2026-04-28 — **graph-system description sweep complete (5 Tier 1 + 1 Tier 2; per-phase plan-lock cascade unlocked).** All six procomp descriptions for the graph-system are signed off and locked. Tier 1: [properties-form](../docs/procomps/properties-form-procomp/properties-form-procomp-description.md) (schema-driven entity editor; mixed-permission rendering; build-from-scratch state mgmt; Q2+Q5 refined), [detail-panel](../docs/procomps/detail-panel-procomp/detail-panel-procomp-description.md) (compound `Header`/`Body`/`Actions` API; auto-reset mode on `selection.id` change; Q6 refined), [filter-stack](../docs/procomps/filter-stack-procomp/filter-stack-procomp-description.md) (4 built-in types incl. opt-in solo buttons; flat `__mode` suffix; Q2 reversed + Q7 refined), [entity-picker](../docs/procomps/entity-picker-procomp/entity-picker-procomp-description.md) (searchable kind-badged picker; single+multi via `mode` prop; no refinements), [markdown-editor](../docs/procomps/markdown-editor-procomp/markdown-editor-procomp-description.md) (CodeMirror 6 substrate per [decision #19](../docs/systems/graph-system/graph-system-description.md); slot-able toolbar; wikilink autocomplete + preview; Q1 reversed + Q4 + Q8 refined). Tier 2: [force-graph](../docs/procomps/force-graph-procomp/force-graph-procomp-description.md) — phased v0.1–v0.6 (~13.5w focused), Sigma + graphology + FA2-in-worker substrate, full `GraphInput = GraphSnapshot \| GraphSource` from v0.1, two-layer state model (graphology imperative + Zustand reactive per spec §5.1), origin-aware permission resolver (own-resolver-per-component decision #25), 300KB component-alone bundle ceiling (Tier 1 deps NOT included per decision #35), Phase 0 risk spike (DashedDirectedEdgeProgram, ≥30 fps gate at 100k edges on integrated GPU) runs BEFORE v0.1 with intermediate fallbacks; Q5+Q8+Q10 refined on re-validation. **Per-phase plan-lock cascade now unlocked**: `force-graph` v0.1/v0.2/v0.6 plans can author independently; v0.3 gated on properties-form + detail-panel plans; v0.4 on filter-stack plan; v0.5 on markdown-editor plan. Tier 1 plans can all author in parallel (component independence per decision #35). System-level Stage 2 (`graph-system-plan.md`) is authored after all per-procomp Stage 2 docs lock. Pattern observed: 5 of 6 descriptions had refinements caught on the re-validation pass — the draft → validate → re-validate → sign-off cadence consistently surfaces real issues (~1–3 refinements per procomp). Total commits this sweep: 12 (one draft + one sign-off per procomp).
- 2026-04-28 — **Codegen fix: `VALID_CATEGORIES` derived from `types.ts` at runtime.** Resolved the duplicate-source-of-truth issue between `src/registry/types.ts` `ComponentCategorySlug` union and `scripts/new-component.mjs` `VALID_CATEGORIES` array. Scaffolder now reads `types.ts` at startup and parses the union literals via regex. types.ts is single source of truth; categories.ts agreement enforced at compile time via `Record<ComponentCategorySlug, ...>`. Open-decision item removed from this STATUS.md. Commits: 260d035, 06a0892.
- 2026-04-28 — **Introduced `docs/systems/` planning tier with `graph-system` description.** New tier above `docs/procomps/` for multi-component product surfaces — composing several pro-components plus host code into one coherent experience. First system: `graph-system` (DB visualizer + personal KG + hybrid documenter). Description at [docs/systems/graph-system/graph-system-description.md](../docs/systems/graph-system/graph-system-description.md) locks 37 cross-cutting decisions and supersedes [graph-visualizer-old.md](../graph-visualizer-old.md) for cross-cutting concerns; original spec remains authoritative for `force-graph` internals. Decomposition: 5 generic Tier 1 pro-components (`properties-form`, `detail-panel`, `filter-stack`, `entity-picker`, `markdown-editor`) + 1 graph-specific Tier 2 (`force-graph`, phased v0.1–v0.6) + Tier 3 assembled-experience page at `src/app/systems/graph-system/page.tsx`. Adds new `forms` registry category. Sub-doc map and authoring order in §9 of the description. Tier 1 components are independently useful (filter-stack → data-table; detail-panel → rich-card; markdown-editor → future docs system). System-level Stage 2 (plan) is next, gated on per-procomp Stage 1 descriptions. New `docs/systems/README.md` documents the tier governance.
- 2026-04-28 — **Shipped `data/rich-card` v0.4.0 (alpha) — safety net.** Implementation against [v0.4 plan](../docs/procomps/rich-card-procomp/rich-card-procomp-plan-v0.4.md). 3 new files: `lib/validators.ts` (3-layer pipeline runner: per-action → master, with `safeCall` wrapping for thrown errors), `hooks/use-undo.ts` (canUndo / canRedo / undo / redo / clearHistory + default keyboard listener that respects active inputs), `parts/undo-toolbar.tsx` (sibling export). 5 modified: types (4 new types: `RichCardValidators`, `RichCardMasterValidator`, `RichCardValidationResponse`, `ValidationFailedEvent`; 7 new props; 5 new handle methods), reducer (3 state slots: `undoStack`/`redoStack`/`maxUndoDepth`; 4 new actions; `withUndoBookkeeping` wrapper that snapshots pre-state on every commit-action), rich-card (`gateValidation` closure threaded into all 14 dispatchers, `useUndo` integration, expanded handle), demo (`STRICT_VALIDATORS` example: priority must be 1–5 + locked cards cannot be removed; toast for validation failures; `<RichCardUndoToolbar>` wired). **Validators run sync-only (Q14 lock); `runHostValidators` catches throws and reports as `validator-error`; per-action layer fires first, then master.** **Undo restores tree + collapsed + selectedIds + focusedId; NOT search query (controlled by host) or drag-transient state. `version` does NOT decrement on undo — `isDirty()` stays true after undoing all changes (intentional; prevents data-loss footgun).** Bulk operations = single undo step (atomic dispatch). `replace-tree` clears history. Default `maxUndoDepth: 50` per Q14. Keyboard `Cmd+Z` / `Cmd+Shift+Z` / `Cmd+Y` bound by default; respects native textbox undo when an input is focused; opt out via `disableUndoShortcuts: true`. `tsc + lint + build` all clean (1 acceptable virtualizer warning carried from v0.3). 56 files (53 from v0.3 + 3 new). Index re-exports the v0.3 search bar AND the new v0.4 undo toolbar as sibling exports.
- 2026-04-28 — **Shipped `data/rich-card` v0.3.0 (alpha) — structural management layer.** Total update against [v0.3 plan](../docs/procomps/rich-card-procomp/rich-card-procomp-plan-v0.3.md) including all Option-B MUSTs and NICEs plus native data-model search (folded into v0.3 mid-plan). New surfaces: drag-drop via `@dnd-kit/core` + `@dnd-kit/sortable` (2 scopes; keyboard parity built-in); bulk multi-select with shift-click range + cmd-click toggle + `<BulkToolbar>` (delete / duplicate / toggle-lock / bulk-set-field); 7-layer permission resolver (predicate > meta-lock > byCard > byPredefinedKey > byFieldType > byLevel > default > global) with `__rcmeta.locked` cascade lock; mount-only `customPredefinedKeys` registration; opt-in virtualization via `@tanstack/react-virtual` (`virtualize: 'auto'` enables at 500+ nodes); inline meta editor with `metaRenderers` + audit-trail auto-stamp; configurable promote-on-delete with suffix / qualify / reject collision strategies; root-removal opt-in with `onRootRemoved` callback + `<EmptyTreePlaceholder>`; native data-model search via `lib/search.ts` finds matches in collapsed subtrees / virtualized cards / meta entries with auto-expand of match paths and `<MatchHighlight>` rendering. **One intentional breaking change:** `onSelectionChange(id)` → `onSelectionChange(ids[])` for multi-select. Imperative handle expanded with `setSelection`, `focusCard`, `addCardAt`, `removeCard`, `replaceRoot`, `getEffectivePermissions`, `findNext`, `findPrevious`, `scrollToMatch`, `clearSearch`. Plan-stage Q-Ps Q-P1–Q-P15 all baked in per recommendations. `tsc + lint + build` all clean (1 acceptable warning about `useVirtualizer` not being React-Compiler-memoizable). 50 files (36 from v0.2 + 14 new for v0.3 — 4 lib + 5 hooks + 8 parts; modifications across types / reducer / validate-edit / card / card-header / field-row / card-actions / meta-popover / meta-inline / rich-card / demo).
- 2026-04-28 — **Shipped `data/rich-card` v0.2.0 (alpha) — inline editor.** Second phase landed against [v0.2 plan](../docs/procomps/rich-card-procomp/rich-card-procomp-plan-v0.2.md). 11 new files (`lib/validate-edit.ts`; hooks: `use-edit-mode`, `use-dirty`, `use-selection`; parts: `inline-error`, `field-edit`, `field-add`, `card-title-edit`, `card-actions`, `predefined-edit`, `predefined-add-menu`); 9 modified (types, index, reducer, card, card-header, field-row, rich-card, demo, use-tree-keyboard). 36 files total. **Editor surface:** `editable: boolean` global gate (default `false`, backward-compat); single-click triggers inline edit on field values, keys, and card titles; hover-× removes; "+ field" / "+ block" / "+ child" affordances reveal in edit mode. **State extensions:** 12 new reducer actions; 3 new state slots (`selectedId`, `version`, `cleanVersion`); selection now distinct from focus (aria-selected reflects selection). **Events:** 9 granular handlers (`onFieldEdited`, `onCardAdded`, ...) + coarse `onChange(tree)` fired after every commit. **Imperative handle:** `isDirty()`, `markClean()`, `getSelectedId()` added. **Dirty tracking:** counter-based (every commit increments; `markClean` snapshots; v0.4 upgrades to structural-diff with undo). **Validation:** sync only — reserved-key / sibling-key / predefined-shape collisions block commit with inline error. **Two minor description deviations flagged in plan:** root deletion forbidden in v0.2 (description said "may allow with consumer config"); promote-on-delete deferred to v0.3 (description said "v0.2+"). **Tree-keyboard handler bypassed when editor active** so arrow keys reach the input. `tsc` + `lint` + `next build` all clean; static page at `/components/rich-card`.
