# ilinxa-ui-pro — Status

> Current snapshot of the project. Update when components ship, statuses change, or non-obvious decisions are made. This is the *now*, not a changelog.
>
> Last updated: **2026-04-28** (rich-card pro-component shipped v0.1.0 viewer)

## Components

| Slug | Category | Status | Version | Notes |
|------|----------|--------|---------|-------|
| `data-table` | data | alpha | 0.1.0 | Canonical template — generic, typed columns, no client state. Future tables compose on it. |
| `rich-card` | data | alpha | 0.2.0 | JSON-driven recursive card-tree viewer + **inline editor (v0.2 shipped)**. Click any field value or key to edit; type-aware inputs (text/number/checkbox/date); hover-× to remove; "+ field" / "+ block" / "+ child" affordances in edit mode. 12 reducer actions, 9 granular change events, sync validation, version-counter dirty tracking, click-driven single-select. v0.3 (planned) = dnd + permissions + multi-select; v0.4 = validation hooks + undo/redo; v0.5 (deferred companion) = markdown adapter. **Markdown intentionally dropped from rich-card itself.** Dependencies: `popover` + `separator` shadcn primitives, `lucide-react`. 36 files (1 root + 16 parts + 4 hooks + 6 lib + 7 anatomy + barrel + meta + dummy + demo + usage). |
| `workspace` | layout | alpha | 0.1.0 | Splittable canvas: corner-drag split/merge, edge-drag resize, registry-driven content, responsive collapse + per-breakpoint depth cap. Largest component in the registry — 26 files, full procomp gate (description → plan → guide). |

## Roadmap

Next candidates, ordered by team utility:

1. `data/stat-card` — value + label + delta + sparkline. Universal in dashboards.
2. `feedback/empty-state` — icon + title + body + primary action.
3. `forms/multi-select` — combobox with tag chips (shadcn has Command, no real multi-select).
4. `layout/page-header` — title + breadcrumbs + actions slot.
5. `feedback/notification-feed` — grouped, time-bucketed, read/unread.
6. `navigation/command-palette` — cmd+k, grouped results.
7. `media/dropzone` — drag-drop + progress + previews.

**`rich-card` v0.2 follow-ups** (deferred from v0.1.0, by phase per [rich-card-procomp-plan.md](../docs/procomps/rich-card-procomp/rich-card-procomp-plan.md)): v0.2 inline editor (add/edit/remove fields/cards/predefined-key elements, granular change events, dirty tracking, explicit selection); v0.3 drag-drop (2 scopes), bulk ops, per-level/per-card/per-key permissions, custom predefined-key registration, virtualization if needed; v0.4 sync validation hooks + per-commit undo/redo; v0.5 (deferred indefinitely, separate companion) markdown ↔ JSON adapter. The v0.1 API surface is designed so each phase adds props rather than changing them.

**`workspace` v0.2 follow-ups** (deferred from v0.1.0): detach-to-OS-window for multi-monitor users; touch / pen gesture support; aligned-edge linked resize (multiple edges move together); DnD between areas; cross-breakpoint transition animation. The v0.1 API surface is designed so v0.2 doesn't break it.

## Open decisions / TODOs

- **Chart palette still neutral:** `--chart-1..5` are greyscale. Define a lime-leaning analogous palette (e.g. lime → emerald → teal → cyan → blue) when the first chart-heavy component ships.
- **Site nav has only "Components":** add Docs / Blog / GitHub when those sections exist. Mobile menu (sheet/drawer) is currently unnecessary at this nav size — revisit when 4+ items.
- **Reserved-but-unrendered meta fields:** `RegistryEntry.examples`, `ComponentMeta.thumbnail`, `ComponentMeta.subcategory`, `ComponentMeta.related` exist on the types but render nowhere yet. Decide whether to wire any (each adds detail-page surface) or remove them.
- **`alpha` and `beta` badges look identical:** the hard-coded mapping in [components/page.tsx:78-83](../src/app/components/page.tsx#L78) and [[slug]/page.tsx:56-62](../src/app/components/[slug]/page.tsx#L56) only assigns unique colors to `stable` (default) and `deprecated` (destructive); both `alpha` and `beta` render `secondary`. If we want `beta` distinguishable, that's a 2-line tweak in two pages.
- **Duplicate `VALID_CATEGORIES` source:** [src/registry/types.ts:5-14](../src/registry/types.ts#L5) `ComponentCategorySlug` and [scripts/new-component.mjs:27-37](../scripts/new-component.mjs#L27) `VALID_CATEGORIES` are two hand-synced sources of truth for the same list. Adding/removing a category requires editing both. Future automation candidate (codegen the array from the type).
- **Footer version is hardcoded** (`v0.1.0`) in `site-footer.tsx`. Lift to a single source (`package.json` or a constant) when version starts changing.
- **Lime contrast pattern is dark-on-lime.** Both modes use near-black `--primary-foreground`. Don't switch to white — contrast fails. If a use case really wants white text on a green action, use a darker green (e.g. `oklch(0.45 0.18 142)` forest) and pair white text — but that becomes a separate token, not `--primary`.
- **MDX for usage docs:** currently `usage.tsx`. Consider migrating to `usage.mdx` once we cross ~5 components with prose-heavy guidance.
- **Public registry build:** no `pnpm dlx shadcn build` config or `public/r/*.json` output yet. Defer until publish phase.
- **NPM publish artifacts:** no `tsup`/`rollup`, no `package.json` exports map. One-day job at publish time.
- **Test debt — `workspace`:** workspace ships v0.1.0 without unit tests because no test runner is wired in this repo. The pure modules in `src/registry/components/layout/workspace/lib/` (`tree.ts`, `reducer.ts`, `geometry.ts`) are written to be testable in isolation when Vitest lands. Verification for v0.1.0 is demo-driven (the dummy registry's `counter` component proves Q3 state preservation across split visually). Land Vitest as a separate STATUS decision before workspace ships v0.2.
- **Test debt — `rich-card`:** same posture. The pure modules in `src/registry/components/data/rich-card/lib/` (`parse.ts`, `serialize.ts`, `classify-key.ts`, `infer-type.ts`, `reducer.ts`) are pure functions and trivially testable when Vitest lands. The single highest-value test is a **round-trip property test** — random `RichCardJsonNode` → `parse` → `serialize` → `parse` → assert tree-equal — which would lock down the Q3 fixed-point contract. v0.1.0 verification is demo-driven via the three demos in [`dummy-data.ts`](../src/registry/components/data/rich-card/dummy-data.ts) and the manual "Read canonical JSON" button in [demo.tsx](../src/registry/components/data/rich-card/demo.tsx).

## Recent decisions (keep latest ~10)

- 2026-04-28 — **Shipped `data/rich-card` v0.2.0 (alpha) — inline editor.** Second phase landed against [v0.2 plan](../docs/procomps/rich-card-procomp/rich-card-procomp-plan-v0.2.md). 11 new files (`lib/validate-edit.ts`; hooks: `use-edit-mode`, `use-dirty`, `use-selection`; parts: `inline-error`, `field-edit`, `field-add`, `card-title-edit`, `card-actions`, `predefined-edit`, `predefined-add-menu`); 9 modified (types, index, reducer, card, card-header, field-row, rich-card, demo, use-tree-keyboard). 36 files total. **Editor surface:** `editable: boolean` global gate (default `false`, backward-compat); single-click triggers inline edit on field values, keys, and card titles; hover-× removes; "+ field" / "+ block" / "+ child" affordances reveal in edit mode. **State extensions:** 12 new reducer actions; 3 new state slots (`selectedId`, `version`, `cleanVersion`); selection now distinct from focus (aria-selected reflects selection). **Events:** 9 granular handlers (`onFieldEdited`, `onCardAdded`, ...) + coarse `onChange(tree)` fired after every commit. **Imperative handle:** `isDirty()`, `markClean()`, `getSelectedId()` added. **Dirty tracking:** counter-based (every commit increments; `markClean` snapshots; v0.4 upgrades to structural-diff with undo). **Validation:** sync only — reserved-key / sibling-key / predefined-shape collisions block commit with inline error. **Two minor description deviations flagged in plan:** root deletion forbidden in v0.2 (description said "may allow with consumer config"); promote-on-delete deferred to v0.3 (description said "v0.2+"). **Tree-keyboard handler bypassed when editor active** so arrow keys reach the input. `tsc` + `lint` + `next build` all clean; static page at `/components/rich-card`.
- 2026-04-28 — **Shipped `data/rich-card` v0.1.0 (alpha)** — viewer-only phase. Second component through the procomp gate ([description](../docs/procomps/rich-card-procomp/rich-card-procomp-description.md) → [plan](../docs/procomps/rich-card-procomp/rich-card-procomp-plan.md) → guide pending). JSON-driven recursive card-tree viewer with five typed scalar field types, five predefined-key blocks, three meta presentation modes, full ARIA tree + keyboard nav, slot-based per-level styling, canonical JSON round-trip via imperative `getValue()` / `getTree()`. 26 files (1 root + 10 parts + 5 lib + 3 hooks + 7 anatomy). `tsc` + `lint` + `next build` all clean; statically renders at `/components/rich-card`. **Markdown intentionally dropped** — see [description-deviation banner](../docs/procomps/rich-card-procomp/rich-card-procomp-description.md#) (top of doc). **Plan-stage round-trip bug fixed mid-review:** added `parentKey` to internal `RichCardTree` so child property names (e.g. `section1` / `section2`) round-trip identically; flipped Q-P4 from "flatten arrays of objects" to "disallow + clear parse error" because the original recommendation silently broke Q3.
- 2026-04-28 — **Established the rich-card phased-release pattern.** First component to declare v0.1 → v0.5 phases up front in its description, with each phase getting its own plan revision. v0.5 (markdown adapter) is intentionally a separate companion module, not internal to rich-card — keeps the JSON model unconstrained by markdown's quirks.
- 2026-04-27 — **Shipped `layout/workspace` v0.1.0 (alpha).** First component to go through the full procomp gate: description ([docs/procomps/workspace-procomp/workspace-procomp-description.md](../docs/procomps/workspace-procomp/workspace-procomp-description.md)) → plan ([workspace-procomp-plan.md](../docs/procomps/workspace-procomp/workspace-procomp-plan.md)) → guide ([workspace-procomp-guide.md](../docs/procomps/workspace-procomp/workspace-procomp-guide.md)). Splittable canvas: corner-drag split/merge, edge-drag resize, registry-driven content, responsive collapse, per-breakpoint hard depth cap. 26 files (1 root + 9 parts + 6 hooks + 4 lib + 7 anatomy). Public API: `<Workspace>` + `useAreaContext()` hook + 9 type exports. State preservation contract: split keeps original area's instance, mounts new sibling fresh. v0.2 follow-ups (detach-window, touch, DnD, aligned-edge resize, x-breakpoint animation) deferred deliberately. `tsc` + `lint` + `next build` all clean.
- 2026-04-27 — **Established procomp planning gate.** Every new pro-component must pass through three stages — `description.md` (signed off), `plan.md` (signed off), `guide.md` (alongside implementation) — under [docs/procomps/<slug>-procomp/](../docs/procomps/). Gate documented in [.claude/CLAUDE.md](.claude/CLAUDE.md) as Workflow step 0 and in [docs/component-guide.md §2](../docs/component-guide.md#2-before-you-start). `workspace` was the first component to use the gate end-to-end.
- 2026-04-27 — **Authored [docs/component-guide.md](../docs/component-guide.md)** — comprehensive 18-section developer reference for building pro-components. Grounded in the actual codebase (every rule anchored to a specific file), includes the data-table walkthrough, an end-to-end stat-card paper example, anti-patterns with violation→why→fix, the visual-contract "renders where" table, and a 16-question pre-PR checklist. README and `.claude/CLAUDE.md` now link to it as the human reference; CLAUDE.md stays terse for Claude sessions.
- 2026-04-27 — Removed silent `prose prose-neutral dark:prose-invert` wrapper classes from [data-table/usage.tsx:3](../src/registry/components/data/data-table/usage.tsx#L3) and [_template/_template/usage.tsx:3](../src/registry/components/_template/_template/usage.tsx#L3). `@tailwindcss/typography` isn't installed so they were no-ops; visual rendering is identical, but the lint surface is cleaner and the guide can honestly say "don't use prose classes".
- 2026-04-27 — **Rebranded.** Aurora-cyan → **signal-lime** (`oklch(0.80 0.20 132)` light / `oklch(0.86 0.18 132)` dark). Warm-charcoal → **graphite-cool** in dark mode (`oklch(0.13 0.006 250)` base, `0.17` raised, `0.22` subtle). Light mode pure white → cool off-white (`oklch(0.975 0.003 250)`) with `--card`/`--popover`/`--sidebar` lifted to pure white so cards visibly elevate. Updated `--primary-foreground` to near-black in both modes (lime too bright for white text). Light borders also picked up the cool tint (`oklch(0.91 0.003 250)`). CLAUDE.md mandate updated to match — future sessions should not revert to aurora-cyan or warm-charcoal.
- 2026-04-27 — Built site chrome at `src/components/site/`: `theme-provider.tsx` (next-themes wrapper), `theme-toggle.tsx` (dropdown with light/dark/system options), `site-header.tsx` (sticky h-14 with `bg-background/70 backdrop-blur`, `bg-primary` square mark + Onest+mono wordmark, mono nav with active-route `bg-primary` dot prefix), `site-footer.tsx` (single-row mark + version + nav + copyright). Editorial dev-tool aesthetic — generous whitespace, mono accents, single sharp brand-accent (color sourced from design tokens, not hard-coded).
- 2026-04-27 — Defined `reveal-up` keyframe in `globals.css` with cubic-bezier(0.2,0.8,0.2,1) over 0.7s, prefers-reduced-motion guard, exposed as `.reveal-up` utility. Applied to landing page hero with 0/60/120/180ms staggered `animationDelay`.
- 2026-04-27 — Wired `ThemeProvider` (next-themes class strategy, `defaultTheme="system"`, `enableSystem`, `disableTransitionOnChange`) in `layout.tsx`. Layout now owns `<main>` so pages use `<div>` to avoid nesting. Added `suppressHydrationWarning` on `<html>` for next-themes.
- 2026-04-27 — Restructured project memory: rewrote `.claude/CLAUDE.md` with project-specific instructions, added this `.claude/STATUS.md` as the live tracker, trimmed auto-memory to motivation-only.
- 2026-04-27 — Added `pnpm new:component <category>/<slug>` scaffolder. Template lives at `src/registry/components/_template/_template/`. Script prints the 3-line manifest snippet to paste — no auto-edit of `manifest.ts`.
- 2026-04-26 — Scaffolded Next 16.2.4 + React 19.2.4 + Tailwind 4.2.4 + shadcn (Radix base, Nova preset, neutral) via official CLIs. No manual base-file edits.
- 2026-04-26 — Locked the registry pattern: components in `src/registry/components/<category>/<slug>/`, sealed folders, hand-edited `manifest.ts` (3 lines per component), auto-rendered docs at `/components/[slug]`.
