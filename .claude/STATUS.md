# ilinxa-ui-pro — Status

> **Current snapshot — the *now*, not a changelog.** For per-decision context, browse [`.claude/decisions/`](decisions/) (one file per decision, YAML-frontmatter queryable). For full historical record pre-2026-05-09, see [`.claude/STATUS-archive.md`](STATUS-archive.md) (frozen).
>
> **🎨 SHIPPED 2026-07-01 — gamification visual QA + hover pass (4 patch bumps + a shared Button-primitive fix; user-driven docs-page walkthrough).** The user walked the six gamification docs pages and flagged concrete polish; each fixed live against the dev server, then shipped as a batch. **Real component patch bumps** (visual-only → NO GATE 3 per the readiness rule): **team-trophy-shelf-01 v0.1.0→v0.1.1** (name-length-driven ragged grid → **fixed size-driven badge cells** `w-24`/`w-20` + **2-line `line-clamp-2 wrap-break-word`** labels w/ reserved `min-h-8` + seamless medallion hover; uniform cells ⇒ ~5/row not ragged 6) · **team-quest-log-01 v0.1.0→v0.1.1** (state-aware beat-circle hover on both interactive+static wrappers) · **team-progress-bar-01 v0.1.0→v0.1.1** + **cooperative-challenge-01 v0.1.1→v0.1.2** (progress fill **brightens + lime-glows** on hover). **Shared primitive fix — docs-site only, NO bump:** [`src/components/ui/button.tsx`](../src/components/ui/button.tsx) primary/green variant hovered **only as a link** (`[a]:hover:bg-primary/80`) → real `<button>` hover (`hover:bg-primary/90 + hover:shadow-primary/25`); our components ship shadcn's `button` (not ours) so **consumers unaffected**. **Docs-only demo tweaks (no bump):** 6× `mx-auto` demo centering · feedback-loop "Start" visible hand-off · task-choice demo-card hover. **🔑 Reusable:** (1) the Nova-preset Button primary variant only hovered as an anchor — a real gap; (2) a glow on the `Progress` **root** shows (its own box-shadow isn't clipped by `overflow-x-hidden`); (3) `brightness` lifts lightness not chroma ⇒ mandate-safe for lime. Gates GREEN: **tsc 0 · eslint(touched) 0 · meta-deps 63/63 · build 72 (no SSR err) · registry:build ✓** (4 artifacts spot-checked — hover classes present). Decision: [`2026-07-01-gamification-visual-hover-pass.md`](decisions/2026-07-01-gamification-visual-hover-pass.md).
>
> **📖 BUILT 2026-07-01 — `team-quest-log-01` v0.1.0: the 6th & FINAL gamification component (63rd procomp; E5 Team Narrative Framing). 🎮 THE `gamification-system` IS NOW 6/6 COMPLETE.** A light team narrative overlay, two surfaces: a **skippable quest-name editor** (title = `questName?.trim() || name`; clearing/saving blank reverts to the team name — **never forced**, no validation, no nag; a quiet "Name your quest" invitation on the default) + a **milestone-chapter timeline** (vertical rail; **done/current/upcoming** beats derived from `Milestone.done`; current = first not-done by order; all-done → no current; **unresolved `milestoneId` renders gracefully + dev-warns, never crashes**). Emits `narrative.chapter-viewed` **once per chapter per mount** on first in-view (one IntersectionObserver + fire-once Set + SSR guard + click fallback). Light shadcn-compound (**no `React.lazy`**): pure `lib/resolve-title`+`lib/derive-beats` + headless `TeamQuestLogRoot` (title/draft/beat-derivation + telemetry + a **`forwardRef` handle** `focusNameEditor`/`scrollToChapter`) + flat `TeamQuestNameEditor`/`TeamQuestChapters` + Tier-C `QuestTitle`/`QuestNameField`/`ChapterRail`/`ChapterBeat`/`EmptyNarrative` + assembly; timeline-only + name-only subsets fall out (the `input` primitive lives only in `QuestNameField`). Deps shadcn `input`/`button` + lucide; **zero `@ilinxa/*` (D-03)**. SSR-safe (pure title fn; deterministic UTC `doneAt`; IO in effect). All gates GREEN: **tsc 0 · ESLint(folder) 0 · meta-deps 63/63 · build 72 pages (no SSR err) · registry:build ✓** (11-file artifact, 0 forbidden; **D-03 grep audit clean**). **GATE 3 Pass-with-follow-ups** (rotating dim Robustness): F-01 ✅ **CLOSED** (production smoke below) + F-02 live walkthrough (Low) + F-03 horizontal-rail/blurbs→v2 (Low). Guide + review + decision authored. **✅ SHIPPED + PUSHED; combined production confirm smoke (components 5+6) CLEAN** — both `task-choice-control-01` + `team-quest-log-01` installed from the live `@ilinxa` registry into the base-nova/Base-UI consumer, consumer `tsc` **0 errors**. **Dev-server render check PASSED** — `pnpm dev` (localhost:3000), all 6 gamification detail pages `/components/<slug>` return **HTTP 200** with real content + **0 error markers**. **🎮 THE `gamification-system` IS FULLY DONE: 6/6 built + shipped + cross-backend smoke-verified + renders clean** (E3+E6 patched to v0.1.1 via the smoke; E4+E5 pre-push-Base-UI-clean + production-confirmed; E1+E2 smoked with the initial pack). `master`==`origin` at `5d2fc22`. **▶️ RESUME (next session) = the deferred shared `gamification-kit` (D-04): resolve/derive/diff/clamp helpers + event factories across all 6 now clearly share a shape — extract `resolveQuestTitle`/`deriveBeats`/`deriveChallenge`/`resolveTaskChoiceState`/`resolveMember` + the per-component event factories + shared `Team`/`Milestone`/`TeamMember` slices; also a Tier-host `gamification-system` page composing all six (system §7 "all together").** Review: [`2026-07-01-v0.1.0-spotcheck.md`](../docs/procomps/team-quest-log-01-procomp/reviews/2026-07-01-v0.1.0-spotcheck.md); decision: [`2026-07-01-team-quest-log-01-v0.1.0-first-ship.md`](decisions/2026-07-01-team-quest-log-01-v0.1.0-first-ship.md).
>
> **🙌 BUILT 2026-07-01 — `task-choice-control-01` v0.1.0: the 5th gamification component (62nd procomp; E4 Autonomy; the pack's SINGLE-UNIT exception).** A small, droppable control for one team task — an "open for anyone" `switch`, an "I'll take this" claim, and an assignee chip with a **neutral, no-penalty** Release + a searchable `popover`+`command` reassign picker. Renders 3 states (open / claimed / unassigned + the legal assigned-and-open edge) deterministically from a controlled `TaskChoiceState` slice + this team's members. **Never-forced made STRUCTURAL:** no `disabled`/`required`/`mustAssign` prop and no "you must" branch exists in the type system (choice is the default; `readOnly` opt-in only); **Release folds into `onAssigneeChange(undefined)`** (one chokepoint, no `onRelease`) with a `ghost`/muted treatment — never destructive/red, never a cold verb ("Drop"/"Abandon"), never a penalty glyph/motion; the `aria-live` sentence is neutral. **Single-unit (NOT a Root/compound):** flat à-la-carte parts `OpenForAnyoneToggle`/`ClaimButton`/`AssigneeChip` under a logic-free assembly, no context, no `hooks/` (only `pickerOpen` lives in the chip); pure `lib/state`+`lib/members` (exported resolvers). Deps: shadcn `switch`/`button`/`avatar`/`popover`/`command` + lucide; **zero `@ilinxa/*` (D-03)**. All gates GREEN: **tsc 0 · ESLint(folder) 0 · meta-deps 62/62 · build 71 pages (no SSR err) · registry:build ✓** (8-file artifact, 0 forbidden; **D-03 grep audit clean**). **🔑 Given the sibling's just-caught `getValueLabel` break, ran a PROACTIVE pre-push cross-backend smoke:** served `public/r` locally → `shadcn add` into the **base-nova/Base-UI** consumer (installed the `command`+`input-group` it lacked) → consumer `tsc` **0** — the popover/command/switch divergence surface held (mirrored the proven `field-author-picker` controlled pattern: PopoverTrigger direct, `(boolean)=>void` handlers, `CommandItem value/onSelect`). **GATE 3 Pass-with-follow-ups** (rotating dim Copy / never-forced): F-01 production round-trip confirmation (Med→Low, pre-push Base UI already clean) + F-02 live no-penalty walkthrough (Low). Guide + review + decision authored. **RESUME = commit/push → production confirm smoke → component 6 `team-quest-log-01` (E5, the final one, a compound) → then the deferred `gamification-kit` (D-04).** Review: [`2026-07-01-v0.1.0-spotcheck.md`](../docs/procomps/task-choice-control-01-procomp/reviews/2026-07-01-v0.1.0-spotcheck.md); decision: [`2026-07-01-task-choice-control-01-v0.1.0-first-ship.md`](decisions/2026-07-01-task-choice-control-01-v0.1.0-first-ship.md).
>
> **🤝 SHIPPED + PUSHED 2026-07-01 — `cooperative-challenge-01` v0.1.0→v0.1.1: the 4th gamification component (61st procomp; E3 Relatedness).** A safe-by-design cooperative team challenge card — one shared goal, a **collective** progress meter (`current / target`, never per-member), a **whole-team** reward chip (D-08), and a **penalty-free, team-level opt-in** where opted-out is a neutral, first-class "joinable" state (the **never-forced** rule made literal: Join is a prominent lime primary, Leave is one click with no confirm/guilt, a visible no-penalty hint sits in both states). On `done`, a lightweight inline "Completed together" ack (non-blocking, no modal; `aria-live` on the pill only) — the heavy celebration is E6's job, **not built here** (clean seam). Controlled-only opt-in (omit `onOptInChange` → read-only card). Ships as the planned **light shadcn-compound** (no `React.lazy`): pure `lib/derive` (+ centralized never-forced copy defaults) + `lib/events` + headless `CooperativeChallengeRoot` (controlled-echo + telemetry chokepoint, `challenge.opened` `firedRef`-guarded on `challenge.id`) + flat `Header`/`Progress`/`Reward`/`OptIn` + Tier-C `ChallengeProgressMeter`/`ChallengeRewardChip`/`OptInToggle`/`TeamMemberStack`/`Skeleton` + assembly. Deps: shadcn `progress`/`avatar`/`button`/`badge`/`skeleton` + `lucide-react`; **zero `@ilinxa/*` (D-03), zero new npm/primitive** → no F-cross-13 trigger. All gates GREEN: **tsc 0 · ESLint(folder) 0 · meta-deps 61/61 · build 70 pages (no SSR err) · registry:build ✓** (13-file artifact, 0 forbidden, fixtures resolve `@ilinxa/cooperative-challenge-01`). **GATE 3 Pass-with-follow-ups** (rotating dim Copy / never-forced). **🔑 The F-01 post-deploy smoke CAUGHT a real F-cross-13 divergence** — the meter used Radix-only **`getValueLabel`** on `Progress`; the **Base UI** consumer rejected it (`TS2322`); producer tsc was blind (producer's `progress` is Radix). **v0.1.1 fix:** drop `getValueLabel`/`max`, carry the "3 of 5" count via `aria-label` (the sibling `team-progress-bar-01`'s proven cross-backend pattern); re-smoke consumer tsc **0**. **Lesson: F-cross-13 fires on *how you use* an already-shipped primitive, not only on new ones — pass only the Radix↔Base-UI-common surface (`value`+`aria-label`).** F-02 live never-forced walkthrough carries (Low). Guide + review + decision authored. **RESUME = component 5 `task-choice-control-01` (E4; the single-unit exception — no Root/compound).** Review: [`2026-07-01-v0.1.0-spotcheck.md`](../docs/procomps/cooperative-challenge-01-procomp/reviews/2026-07-01-v0.1.0-spotcheck.md); decision: [`2026-07-01-cooperative-challenge-01-v0.1.0-first-ship.md`](decisions/2026-07-01-cooperative-challenge-01-v0.1.0-first-ship.md).
>
> **🚀 SHIPPED + PUSHED 2026-07-01 — `gamification-system` 3 of 6 BUILT + POST-DEPLOY SMOKE RUN + F-01 CLOSED; tree GREEN; `master`==`origin`.** Resumed from the GATES-1+2 planning lock, built the first three cooperative gamification pro-components (**`team-progress-bar-01` (E1) · `team-trophy-shelf-01` (E2) · `team-feedback-loop-01` (E6→v0.1.1)**), **committed + pushed** (SHA `571100b` — which also carried the previously-pending task-family `4c672a7` to origin), Vercel-deployed, and ran the **post-deploy 4-ship smoke** against the base-nova/Base-UI consumer pulling from the live registry. Each is a shadcn-compound honoring D-03/D-05/D-06/D-08/D-10/D-13/D-15/D-16, each GATE-3 Pass-with-follow-ups. Library is now **60 components across 9 categories.** **🔑 The 4-ship smoke earned its keep — it caught a real gap the producer build was blind to:** `team-feedback-loop-01`'s `canvas-confetti` ships **no bundled types**, so the consumer failed `tsc` with **TS7016** (producer tsc passed — it has `@types/canvas-confetti` locally). Fixed in **v0.1.1**: added `@types/canvas-confetti` to the registry item's **`devDependencies`** (shadcn v4 installs item devDeps into the consumer — schema-confirmed via the `shadcn-registry-pro` skill); re-smoke = consumer `tsc` **0 errors**, `@types` landed in consumer devDeps. **progress-bar + trophy-shelf smoked CLEAN (0 errors).** First `@types/*` package the library has shipped. **Recurring lessons carried:** `set-state-in-effect` lint ERRORS → setState-during-render (trophy diff) or `useSyncExternalStore` (feedback reduced-motion), never a `mounted`/matchMedia effect; PROVE every `React.lazy` boundary by grepping built chunks (trophy 1517B · confetti 10.6KB isolated); *producer compiling clean ≠ consumer compiling clean — only the cross-consumer smoke closes it.* **Per-component notes:** progress-bar (E1, light compound, dep `progress`, cooperative-only by construction, reduced-motion verified in built CSS) · trophy-shelf (E2, full compound + lazy `BadgeAwardOverlay` 1517B, `awardedAt` = earned/locked discriminator, D-16 on `animateAward`) · feedback-loop (E6, full compound + lazy `ConfettiBurst`, D-10 non-blocking structural: timer clamped `[200,999)` + pointer-events:none except skip, two trigger paths → one reducer). Decisions: [`.claude/decisions/2026-07-01-team-*`](decisions/); reviews under each procomp's `reviews/`. **▶️ RESUME = build component 4 `cooperative-challenge-01` (E3), then `task-choice-control-01` (E4), `team-quest-log-01` (E5) — each has NO new dep/primitive → then revisit the deferred shared `gamification-kit` (D-04; resolve/diff/clamp + event factories are the extraction candidates).** Handoff (planning): [`HANDOFF-2026-07-01-gamification-system-gates-1-2-locked.md`](HANDOFF-2026-07-01-gamification-system-gates-1-2-locked.md).
>
> **🔒 SESSION-CLOSE LOCK 2026-07-01 — `gamification-system` GATES 1 + 2 COMPLETE for all 6 components; D-01 category plumbed; first component scaffolded; NO code implemented yet (planning-locked); tree GREEN (tsc 0); ALL UNCOMMITTED.** New **cooperative-only, team-scoped gamification layer** (a `system`, like graph-system) extracted from the thesis "Gamification Design Specification" → **6 pro-components, one per SDT element** in a new `gamification` category: `team-progress-bar-01` (E1) · `team-trophy-shelf-01` (E2) · `cooperative-challenge-01` (E3) · `task-choice-control-01` (E4) · `team-quest-log-01` (E5) · `team-feedback-loop-01` (E6). **GATE 1** (system description + elements catalogue + 6 component descriptions) + **GATE 2** (6 implementation plans) authored (parallel subagents from a shared brief, cross-checked in one context), **deep-adversarially reviewed TWICE**, and signed off; the shared contract is **16 locked decisions D-01…D-16** in [`docs/systems/gamification-system/gamification-system-description.md`](../docs/systems/gamification-system/gamification-system-description.md) §8. **Key architecture (user-chosen "contract now, extract kit later"):** components are **INDEPENDENT at the registry level (D-03 — each owns its `types.ts` slice, `registryDependencies: []`), NOT a shared foundation package up front (D-04)** — that conflicts with graph-system #25/#35 + re-arms the cross-procomp type-import bug class; a shared `gamification-kit` is extracted only after 2-3 prove it; "all together" = a future Tier-host page. Each is a shadcn-compound EXCEPT `task-choice-control-01` (single-unit, no Root). **New cross-component decisions: D-15** (team-prop: text-rendering → `team` object subset, identity-only → scalar `teamId`) + **D-16** (celebration ownership: trophy-shelf vs feedback-loop never trigger each other). **F-cross-13 reframed** (risk = the primitive's divergence-proneness, NOT "new to the library"; only feedback-loop's confetti npm is genuinely new). **D-01 DONE:** `gamification` added to [`types.ts`](../src/registry/types.ts) + [`categories.ts`](../src/registry/categories.ts) (order 11); `team-progress-bar-01` **scaffolded** (`pnpm new:component`, template stubs, NOT in manifest, NOT implemented). **⚠️ ALL UNCOMMITTED** (D-01 edits + scaffold + all planning/handoff/decision/memory docs) — not committed per convention. **RESUME = implement component-by-component in build order (team-progress-bar-01 FIRST — scaffolded, fill stubs per its plan), each: implement → manifest + registry.json (base + `-fixtures`) → tsc/lint/build/meta-deps → GATE 3 spot-check → ship.** Handoff: [`HANDOFF-2026-07-01-gamification-system-gates-1-2-locked.md`](HANDOFF-2026-07-01-gamification-system-gates-1-2-locked.md). Decision: [`2026-07-01-gamification-system-gates-1-2.md`](decisions/2026-07-01-gamification-system-gates-1-2.md).
>
> **🔒 SESSION-CLOSE LOCK 2026-06-23 — task-family clipboard UNIFY + gantt v0.5.0 calendar-parity (4 components; built + gated + deep-reviewed + COMMITTED, push pending). Resume = push → post-deploy smoke. Handoff:** [`HANDOFF-2026-06-23-task-family-clipboard-unify.md`](HANDOFF-2026-06-23-task-family-clipboard-unify.md). Closed the gap the user flagged: gantt lacked calendar's double-click-create + right-click clipboard, AND the family had **two incompatible clipboard formats** (todo-rich-card `json-io` bare-item MIME vs calendar `ilinxa/task` text/plain envelope → cross-paste silently no-op'd). Fix: **(Phase 1)** hoisted ONE unified envelope to **`todo-rich-card/lib/clipboard.ts`** (dual-transport MIME+text/plain, omnivorous read with legacy bare-item back-compat) + migrated rich-card's own copy/paste onto it → **todo-rich-card v0.4.0** (json-io's 4 clipboard fns removed). **(Phase 2)** calendar repointed its 4 import sites + **public re-export** to the shared module (non-breaking; local `clipboard.ts` deleted) → **calendar-01 v0.2.3**. **(Phase 3)** **gantt-timeline-01 v0.5.0** (the headline): double-click empty-row → **quick-composer** (`parts/gantt-quick-composer.tsx`, plain panel, name+status, `quickCompose`/`renderQuickComposer`) · **copy/cut/paste** (document ClipboardEvent listeners + menu Copy/Cut, paste = sibling-of-selection dates-preserved) · **Priority submenu** (`changePriority` mutate+onChange, no typed field event — parity w/ calendar). **(Phase 4)** **todo-tree v0.3.0**: clipboard (document Ctrl+C/X/V over selection + handle `copyItems`/`cutItems`/`pasteItems`) + `priorityOptions` prop → editor card. **kanban stays OUT** (data-agnostic `KanbanItem`, no todo-rich-card dep). **Zero new deps** anywhere (clipboard pure; composer reuses input/button; Priority reuses ContextMenuSub). Gates GREEN: **tsc 0 · lint 0 (touched folders; repo-baseline warnings pre-existing) · meta-deps 57/57 · build 66/66 · registry:build ✓** (artifacts verified: clipboard ships in todo-rich-card, removed from calendar, gantt ships the composer, all import the shared module). Smoke: **envelope round-trip logic 7/7** (incl. back-compat + nested-subtree preservation) + rewriter resolution evidenced by producer-tsc exercising the identical deep relative import (`../../todo-rich-card/lib/clipboard`) + depth-1 precedent; **live `shadcn add` consumer-tsc + browser cross-surface round-trip = post-deploy (4-ship pattern)**. **3× GATE 3 spotcheck reviews authored (Pass-with-follow-ups). DEEP ADVERSARIAL RE-REVIEW (3 parallel agents) caught + FIXED 1 would-have-blocked issue + 3 real correctness fixes:** (1) 🚫 **todo-tree `registry.json` omitted `@ilinxa/todo-rich-card`** despite 5 runtime clipboard imports — consumer install would break (gantt+calendar had it; tree was the odd one out) → added + artifact verified; (2) rich-card copy serialized **stale `item.children`** (3 sites) → `denormalize(node)`; (3) back-compat fence too loose (foreign `{id,name}` false-positived) → tightened to full `id/name/status/active/setAt`; (4) todo-tree `pasteItems()` handle default mismatched its keyboard path → aligned to focused row. **KEY LESSON: meta `internal:[]` ≠ registry.json `registryDependencies` — a cross-procomp runtime import needs the `@ilinxa/<dep>` entry.** Re-gated clean. **RESUME: commit + push; post-deploy live smoke.** Decision: [`2026-06-23-task-family-clipboard-unify-and-gantt-parity.md`](decisions/2026-06-23-task-family-clipboard-unify-and-gantt-parity.md).
>
> **🔚 SESSION CLOSE 2026-06-23 — both task surfaces fully closed, nothing in-flight.** gantt-timeline-01 **v0.4.0** + calendar-01 **v0.2.1** SHIPPED + PUSHED + DEPLOYED; `master`==`origin` (0/0); all owed F-01/F-02 verifications CLOSED via the now-working dockerized virtual browser. Only Low/future follow-ups carry (F-03 modal focus-trap · F-04 perf + clipboard-hoist epic · resize-the-group). **Resume guide:** [`HANDOFF-2026-06-23-gantt-v0.4.0-calendar-v0.2.1-closed.md`](HANDOFF-2026-06-23-gantt-v0.4.0-calendar-v0.2.1-closed.md).
>
> **✅ SHIPPED + PUSHED 2026-06-23 — `calendar-01` v0.2.0: the EDITABLE calendar** (`data`; 57th procomp, now editable). The opt-in editing layer over v0.1.0, additive (`editable` default off → read-only). **Stages 1–4 + ship:** **month** (@dnd-kit drag-move + native day-resize) · **week/day time-grid** (native-pointer move/resize/draw + all-day spanning band) · **inspector** (`showInspector`) + **modal detail editor overlay** (so Edit/Enter work without the inspector) + **inline rename** · **quick-composer** (plain fixed panel, NOT a Radix Popover) · **right-click menu** (Edit/Rename/Status/Priority/**Copy/Cut**/Delete) · **live resize preview** · **full keyboard editing** (focus event → ←/→ move · Shift+←/→ (+↑/↓ grid) resize · Del · Enter edit · F2 rename; day-cell Enter → composer) · **cross-surface copy/cut/paste** over a shared `ilinxa/task` clipboard envelope (carries tasks to gantt/kanban/tree; paste-target decides all-day vs timed) · **F-04 height-responsive month overflow**. Pure `lib/edit-mutations`/`edit-permissions`/`clipboard` + `hooks/use-calendar-edit` (single chokepoint, **no internal data state**, controlled-echo). **Stage-4 deviation (user-approved, plan §14c):** single-view assembly makes cross-view drag impossible → **copy/paste IS the cross-surface mechanism** (deferred: external HTML5 drop tray (`onExternalDrop` reserved+inert), agenda-row drag, conversion-drag). **2 intentional non-byte-identical-to-v1 read-only spots (opt-out-able):** (A) status-driven color (`colorBy="urgency"` restores v1) · (B) height-responsive overflow (`maxEventsPerCell` restores a fixed cap). New deps: `context-menu`+`input` (shadcn) + `@dnd-kit/core`+`@dnd-kit/utilities` (npm). Gates green: **tsc 0 · lint 0 · meta-deps 57/57 · build 66/66 (no SSR err) · registry:build ✓ (32-file artifact)**. **GATE 3 Pass-with-follow-ups** ([`2026-06-23-v0.2.0-spotcheck`](../docs/procomps/calendar-01-procomp/reviews/2026-06-23-v0.2.0-spotcheck.md)) — reviewed by **3 AI-assisted adversarial passes** (correctness/consistency/design-a11y); 1 real bug (dead Edit/Enter under inspector) + ~9 polish items **fixed pre-ship**. **✅ v0.2.1 (2026-06-23) CLOSED F-01 + F-02** via the dockerized virtual browser (`virtualbrowser-stealth-browser-1`, after reviving its dead Xvfb display): **F-01** live walkthrough run against Production — renders + context menu + selection→inspector editor + Week time-grid + F-04 overflow all verified, no visual bugs. **F-02** Base UI (`base-nova`) consumer-tsc smoke caught **3 real cross-backend errors** (`ToggleGroup` is a multi-value model on Base UI: `value: string[]` + `onValueChange(string[], details)`; `TooltipProvider delayDuration` is Radix-only) → **fixed in v0.2.1**: view switcher → plain-button segmented control (**`toggle-group` dep dropped**), tooltip uses the default delay. Base UI consumer-tsc **0 errors** post-fix (pre-deploy). **Carried Low:** F-03 modal focus-trap · F-04 live-preview perf + clipboard hoist→todo-rich-card. GATE1/2: [`description-v0.2.0`](../docs/procomps/calendar-01-procomp/calendar-01-procomp-description-v0.2.0.md) + [`plan-v0.2.0`](../docs/procomps/calendar-01-procomp/calendar-01-procomp-plan-v0.2.0.md). Decisions: [`v0.2.0-editing-layer`](decisions/2026-06-23-calendar-01-v0.2.0-editing-layer.md) · [`v0.2.1-cross-backend-fix`](decisions/2026-06-23-calendar-01-v0.2.1-cross-backend-fix.md).
>
> **✅ SHIPPED + PUSHED 2026-06-22 — `gantt-timeline-01` v0.4.0: Draw-mode toggle + right-click portal fix** (56th procomp; `data`). Finished a set of uncommitted WIP fixes the user flagged, shipped properly as a minor. **(1) Draw-vs-pan conflict:** an empty-row drag used to always draw-create (fighting pan). Now drawing is an explicit **Draw** toggle in the editable toolbar (`Pencil`, `aria-pressed`) — **off (default) → empty-row drag pans; on → draws a sibling** (crosshair). Bars/edges/brackets still move/resize/group-move directly. The press pipeline was rewritten from immediate-classify to **deferred-classify**: `onPointerDown` records a `Pending` (+ `classifyTarget`), the first move past 4px picks one gesture (vertical→scroll · horizontal→resize/move/group-move/draw/pan), a sub-threshold release is a click→select. Strictly more robust; `!editable` classifies as `void` → byte-identical v1. **(2) Right-click items did nothing:** the Radix `ContextMenu` portals to `document.body` but stays a React child of the canvas, so its pointer/key events bubbled into the body gesture + gutter keyboard handlers and swallowed the menu item's own `pointerup` (+ double-triggered gutter nav/delete). Fixed with a **DOM-descendant guard** (`!e.currentTarget.contains(e.target) → return`) on body `onPointerDown`/`onKeyDown` + gutter `onKeyDown`. **Minor, not patch:** adds public API (`GanttContextValue.drawMode`/`setDrawMode`) + flips a default — the v0.2.1/v0.3.1 patch precedents added zero public API. Path: **v0.4.0 minor + GATE 3 spot-check, no retroactive GATE 1/2 docs** (fix-driven, narrow — rationale in the decision file). **No new files/deps/shadcn primitive** (Draw reuses `Pencil` from the declared `lucide-react`) → F-cross-13 smoke trigger doesn't fire. GATE 3 spot-check **Pass-with-follow-ups** (rotating dim **Robustness**). Gates green: **tsc 0 · lint 0 err · meta-deps 57/57 · build 66/66 (no SSR err) · registry:build ✓** (artifact spot-checked: `drawMode`/`setDrawMode`/`classifyTarget`/`resolvePending`/`Pencil` + v0.4 description present; no demo/usage/meta). **✅ F-01 CLOSED (2026-06-23):** live walkthrough run against Production via the dockerized virtual browser — the **Draw toggle** flips `aria-pressed` + active variant + crosshair cursor (draw-vs-pan), and the **right-click menu opens** with a real "Add task below" click **creating a new task** (the swallowed-pointerup bug is gone). F-02 N/A (no new primitive); F-03/F-04 Low. **Pushed to `master` separately from the concurrent calendar-01 WIP (another session owns calendar; left untouched).** Decision: [`2026-06-22-gantt-timeline-01-v0.4.0-draw-mode-and-rightclick-fix.md`](decisions/2026-06-22-gantt-timeline-01-v0.4.0-draw-mode-and-rightclick-fix.md); GATE 3: [`2026-06-22-v0.4.0-spotcheck.md`](../docs/procomps/gantt-timeline-01-procomp/reviews/2026-06-22-v0.4.0-spotcheck.md).
>
> **✅ COMMITTED — push pending 2026-06-22 — `calendar-01` v0.1.0: the read-only four-view calendar** (57th procomp; `data`; feat `7ba0dd6`). The **fifth task-family surface** — the date-grid sibling of `gantt-timeline-01`, consuming the same canonical `TodoItem[]` (no adapter). **Month** (multi-day spanning bars + chips + "+N more" overflow) · **week/day** hour time-grids (all-day band + lane-packed timed blocks + now-line) · **agenda** (day-grouped list). **Three-layer all-day/timed/milestone classification** (consumer predicate → date-only strings parsed **floating-local**, no TZ off-by-one → span heuristic; **no fork of `TodoItem`**). Color imported from todo-rich-card `RAMPS`; SSR-safe `now` (seed + rAF, not setState-in-effect); finite-date guards (gantt G2 lesson). shadcn-**compound**: 6 pure lib modules + `Calendar01Root` + flat view parts + 11 Tier-C primitives + the `Calendar01` assembly (a month-only subset drops the time-grid; full-card tooltip lazy-loads todo-rich-card). v0.2 editing surface **declared but inert**. GATE 1 + GATE 2 user-approved (each **deep-reviewed for consistency** via an independent verifier — desc fixed 2-ext + 5-int drifts incl. a real TZ off-by-one in the first-pass recommendation; plan fixed 3-ext + 2-int). **GATE 3 spotcheck Pass-with-follow-ups** (rotating dim Robustness). Gates green: **tsc 0 · lint 0 (folder) · meta-deps 57/57 · build 66/66 pages (no SSR err) · registry:build (23-file artifact)**. **⏭️ OPEN:** F-01 (Med) live visual walkthrough OWED (headless env — no browser; a dev server is up on :3001) · F-02 (Med) post-deploy cross-backend smoke — new `toggle-group`/`popover`/`tooltip`/`calendar` primitives re-arm F-cross-13, the 4-ship pattern + a likely v0.1.1 expected · F-03/F-04 Low (week/day all-day band per-day chips; month overflow fixed cap). **⚠️ PUSH PENDING** — staged by **explicit path** amid **unrelated concurrent gantt v0.4.0 draw-mode WIP** in the tree (left untouched/unstaged); deploy not yet pushed (awaiting confirm). Decision: [`2026-06-22-calendar-01-v0.1.0-first-ship.md`](decisions/2026-06-22-calendar-01-v0.1.0-first-ship.md).
>
> **🔒 PUSHED 2026-06-21 — `gantt-timeline-01` v0.3.0: group-move** (56th procomp; `data`; feat `7d67a31` + docs lock). Drag a **WBS summary bracket → rigidly shift the whole subtree** by one snapped delta (Alt = free) — the capability the user intuited in the v0.2.1 "bracket doesn't track" investigation (which correctly closed as WBS semantics, not a bug). **Additive over the v0.2.x edit layer + the v0.2.1 pan seam, zero rewrite:** one new `groupmove` gesture arm, one pure `shiftSubtree` mutation, one `moveSubtree` dispatcher. **Activation auto-when-movable (D22):** bracket-drag shifts when `editable` + the group is movable, else **falls back to v0.2.1 pan** (read-only + non-movable paths byte-identical). **Atomic permission (D23):** the summary + **every descendant leaf** must pass `can("move")`; one locked leaf vetoes the group + fires `onPermissionDenied`. **No new event/permission type** — reuses the `drag` rule + per-leaf `onFieldEdited`/`onTaskReschedule` + one `onChange` (= one undo step; controlled-echo, no internal data state). **New public surface:** `SummaryBar.groupMovable` prop + `GanttTimelineHandle.shiftTaskGroup(id, deltaMs)`. **No new files / deps / shadcn primitive** (so the F-cross-13 smoke trigger doesn't fire). GATE 1 + GATE 2 user-approved (two consistency passes fixed 3 description + 3 plan drifts vs source); GATE 3 spotcheck **Pass-with-follow-ups** (rotating dim Public API). Gates green: **tsc 0 · lint 0 err · meta-deps 56/56 · build 65/65 · registry:build ✓** (artifact spot-checked — all 8 group-move tokens present). **⏭️ OPEN (F-01, Medium): the live drag-walkthrough is OWED** — verified by code-path reasoning + green gates, but the browser drag-walkthrough (this project's de-facto gate) was not run in-environment. **RESUME = run the post-deploy walkthrough** (drag a movable bracket; lock a leaf → confirm pan; collapsed-summary shift). Other follow-ups Low: F-02 per-leaf ghost preview · F-03 memoize `canGroupMove` · resize-the-group (v0.3.x). v0.3.0 decision: [`2026-06-21-gantt-timeline-01-v0.3.0-group-move.md`](decisions/2026-06-21-gantt-timeline-01-v0.3.0-group-move.md).
>
> **🔒 PUSHED 2026-06-20/21 — `gantt-timeline-01` v0.2.1: the EDITABLE Gantt** *(superseded by v0.3.0 above)* (56th procomp; `data`; v0.1.0 `144afd7` → v0.2.0 editing `db2e2d5` → v0.2.1 polish `abb2aaf`). The "Phase 2" the user asked for: the editing layer, **additive over v1's drag seam, zero rewrite**. Opt in via `editable` (default **off** → byte-identical v1 read-only). Surfaces: **drag-to-reschedule · edge-resize · milestone-drag · draw-on-canvas + gutter-＋ create · delete · inline rename · gutter reparent/reorder (@dnd-kit three-zone drops) · detail edit (lazy `<TodoRichCard editable>` in a custom overlay) · right-click context-menu**. Controlled-echo: edits fire **todo-rich-card-shaped events** (`onItemAdded`/`Removed`/`Moved`/`onFieldEdited`/`onStatusChanged` + kept `onTaskReschedule`) + `onChange(TodoItem[])`; **no internal data state** (undo/redo is consumer-free). **Permissions reuse `TodoPermissions` from todo-rich-card** — the gantt is the 3rd consumer after card + tree (`evalGanttPermission` mirrors todo-tree); `locked` blocks all; summary bars non-manipulable. **New code:** `lib/edit-mutations` + `lib/edit-permissions` (pure, Vitest-ready) + `hooks/use-gantt-edit` + `parts/gantt-context-menu` + `parts/gantt-edit-popover` (24-file base). **New deps:** shadcn `context-menu` + `input`; npm `@dnd-kit/core` (popover **dropped** → custom overlay, a strict dep reduction). GATE 1 + GATE 2 (both user-approved; context-menu kept in v2 per user) + GATE 3 **Pass-with-follow-ups** (rotating dim Robustness; F-02 reorder off-by-one **fixed pre-ship**). Gates green: **tsc 0 · lint 0 err · meta-deps 56/56 · build ✓ (65/65 pages, no SSR err) · registry:build ✓**. **F-01 post-deploy cross-backend smoke CLEAN** — installed v0.2.0 into the base-nova/**Base UI** consumer (pulls `context-menu`/`input`/`@dnd-kit/core`/`todo-rich-card`); consumer `tsc` had **0 gantt / todo-rich-card errors** — the new `context-menu` `asChild` compiled clean cross-backend, **no F-cross-13 regression on first ship**. **v0.2.1 (`abb2aaf`, 2026-06-21):** investigated a user report that the summary bracket "doesn't track" a moved child — **confirmed CORRECT (WBS: bracket = children min→max; the moved child owns one edge, another child holds the other; verified by repro at the px level)**; shipped a polish so **dragging on a summary bracket pans instead of spawning a stray task** (logic-only patch — no new primitive/API/dep, so no consumer smoke required per the patch rule; artifact redeploys). **⏭️ OPEN (user deciding at session close): group-move** — drag a summary bracket → shift the whole subtree by one delta (the behavior the user intuited; v0.2.x feature — summaries are read-only today per D20). **RESUME = the group-move decision is the open thread; otherwise nothing pending.** Other Low follow-ups: F-03 virt-suspend-on-drag · F-04 draw-create-gate-at-start · marquee/bulk; carried v0.1.0 (chart `height`, context-split perf, Vitest — new pure libs now test-ready). v0.1.0 first-ship record: [`2026-06-20-gantt-timeline-01-v0.1.0-first-ship.md`](decisions/2026-06-20-gantt-timeline-01-v0.1.0-first-ship.md). v0.2.0 decision: [`2026-06-20-gantt-timeline-01-v0.2.0-editing-layer.md`](decisions/2026-06-20-gantt-timeline-01-v0.2.0-editing-layer.md).
>
> **🔒 LOCKED 2026-06-18 — consumer-issue-report fixes SHIPPED + PUSHED + post-deploy smoke CLEAN (kanban-board-01 v0.4.0 · todo-rich-card v0.2.1 · todo-tree v0.2.0).** An external consumer's 35-item report was **independently validated (all 35 real)** and fully fixed. Hybrid scope (built R4 controlled mode + R3 kanban edit-flow; honest-trimmed R5/R7/K5/TT8; deferred tests). 4 commits `a946c6f` / `9eff784` / `a1bc6e0` / `ec77cfc` on `master`. **F-cross-11 path-b smoke (base-nova/Base-UI consumer, live registry) is CLEAN for all three** — but it caught a REAL regression my own **R10** fix introduced (narrowing the status `Select` `onValueChange` to `(next: string)` broke Base UI, whose Select passes `(string|null, eventDetails)`); patched in **todo-rich-card v0.2.1** (`ec77cfc`), re-smoke 0 errors. **Lesson: a consumer-report finding analyzed against ONE backend (Radix) can be wrong for the other (Base UI); only the cross-backend consumer-tsc smoke catches it.** Decision: [`2026-06-18-three-procomp-review-fixes.md`](decisions/2026-06-18-three-procomp-review-fixes.md); GATE 3 reviews under each procomp's `reviews/`. **RESUME = nothing pending here** (open follow-ups all Low / future-minor: v0.3 editors/SSR/gated-handle/inline-rename; Vitest defer). *Prior-session open item still standing:* **blackboard-01 v0.1.0** F-01 post-deploy consumer smoke (tip `96ebd5b`; [decision](decisions/2026-06-18-blackboard-01-v0.1.0-first-ship.md)).
>
> **🔼 PUSHED 2026-06-05 (tip `92d5eec`):** v0.2.0 carousel-into-post integration + a manual-test **bug sweep** are LIVE — **carousel v0.1.2** (browse FileList bug: snapshot files before resetting input.value), **content-composer v0.2.1** (FSM `start`-on-mount so Next/Save/Publish work — latent since v0.1.x; Publish last-step-only + Save-every-step; visible lifecycle errors), **media-editor v0.1.3** (crop controls in-flow so bottom handles reach). Post-deploy **F-01 re-smoke CONFIRMED CLEAN** (composer + carousel 0 consumer-tsc errors; 21 unrelated *pre-existing* base-nova-Slider / Radix primitive-divergence errors in media-editor/json-form/story-composer/ui/code-block — not regressions). See Recent activity. **Base context:** [`HANDOFF-2026-06-05-carousel-v0.1.1-and-content-composer-v0.2.0.md`](HANDOFF-2026-06-05-carousel-v0.1.1-and-content-composer-v0.2.0.md) (the integration + 3-agent review). Concurrent in-flight: cms-panel-01 GATE 1 [`HANDOFF-2026-05-25-...`](HANDOFF-2026-05-25-cms-panel-01-gate-1-awaiting-signoff.md) unchanged. Open → v0.3 post backend: multi-blob upload + `post-content-item` adapter + durable local persistence.
>
> **Last big snapshot trim:** 2026-05-25 (this trim, restoring the lean-snapshot convention; ~41K tokens → ~8K). Prior trim: 2026-05-09 (F-cross-02 split — Recent decisions log moved to per-decision files; pre-2026-05-09 bulk archive frozen).

---

## Library tiers

Four-tier model formalized 2026-05-25. Charter: [`docs/library-tiers-charter.md`](../docs/library-tiers-charter.md). Rule: [`.claude/rules/readiness-review.md`](rules/readiness-review.md).

| Tier | Shipped | Distribution | Folder |
|---|---|---|---|
| **pro-component** | 63 | runtime (`registry:component`) | `src/registry/components/` |
| **pro-section** | 0 (charter locked, tooling Phase B) | runtime default | `src/registry/sections/` *(Phase B)* |
| **pro-page** | 0 (charter locked, tooling Phase B) | scaffold-fork (`registry:block`) | `src/registry/pages/` *(Phase B)* |
| **pro-panel** | 0 (charter locked, tooling Phase B) | scaffold-fork meta-block | `src/registry/panels/` *(Phase B)* |

Phase A landed 2026-05-25 (charter + rule restructure + tier dir READMEs + STATUS block). Phase B (scaffolders + registry infra + per-tier categories + meta-deps lint extension) ships alongside the first pilot in each tier. Phase C is pilots in order: section → page → panel (no panel-first; composition risk compounds).

---

## Components

63 components across 9 categories (63rd: `team-quest-log-01`, 2026-07-01 — 6th & final `gamification` component; the pack is complete). Source of truth for per-component description / API / status: each component's `meta.ts` and procomp docs (`docs/procomps/<slug>-procomp/`). For the version snapshot: [`docs/component-versions.md`](../docs/component-versions.md). For per-component review state + per-finding history: [`docs/reviews/sweep-tracker.md`](../docs/reviews/sweep-tracker.md).

| Slug | Category | Status | Version |
|------|----------|--------|---------|
| `data-table` | data | alpha | 0.1.1 |
| `rich-card` | data | **beta** | 0.4.3 |
| `kanban-board-01` | data | alpha | 0.4.1 |
| `flow-canvas-01` | data | alpha | 0.2.5 |
| `article-body-01` | data | alpha | 0.2.2 |
| `engagement-bar-01` | data | alpha | 0.3.2 |
| `post-card-01` | data | alpha | 0.3.2 |
| `comment-thread-01` | data | alpha | 0.2.1 |
| `article-meta-01` | data | alpha | 0.1.0 |
| `content-card-news-01` | data | alpha | 0.3.0 |
| `event-card-01` | data | alpha | 0.1.1 |
| `expandable-text-01` | data | alpha | 0.1.0 |
| `info-list-01` | data | alpha | 0.1.0 |
| `people-grid-01` | data | alpha | 0.1.0 |
| `progress-timeline-01` | data | alpha | 0.1.2 |
| `project-card-01` | data | alpha | 0.2.0 |
| `registration-card-01` | data | alpha | 0.1.1 |
| `schedule-list-01` | data | alpha | 0.1.0 |
| `story-rail-01` | data | alpha | 0.2.1 |
| `thumb-list-01` | data | alpha | 0.1.0 |
| `stat-card` | data | alpha | 0.1.1 |
| `rich-card-in-flow` | data | alpha | 0.2.0 |
| `todo-rich-card` | data | alpha | 0.4.0 |
| `todo-tree` | data | alpha | 0.3.0 |
| `blackboard-01` | data | alpha | 0.1.0 |
| `gantt-timeline-01` | data | alpha | 0.5.0 |
| `calendar-01` | data | alpha | 0.2.3 |
| `workspace` | layout | alpha | 0.1.3 |
| `grid-layout-news-01` | layout | alpha | 0.2.0 |
| `markdown-editor` | forms | alpha | 0.1.1 |
| `properties-form` | forms | alpha | 0.1.1 |
| `json-form` | forms | alpha | 0.2.5 |
| `registration-form-01` | forms | alpha | 0.1.1 |
| `pricing-table-01` | marketing | alpha | 0.1.0 |
| `entity-picker` | forms | alpha | 0.1.1 |
| `category-cloud-01` | forms | alpha | 0.1.0 |
| `filter-bar-01` | forms | alpha | 0.1.0 |
| `filter-stack` | forms | alpha | 0.1.0 |
| `author-card-01` | marketing | alpha | 0.1.0 |
| `newsletter-card-01` | marketing | alpha | 0.1.0 |
| `page-hero-news-01` | marketing | alpha | 0.1.2 |
| `share-bar-01` | marketing | alpha | 0.1.0 |
| `media-carousel-01` | media | alpha | 0.1.3 |
| `story-viewer-01` | media | alpha | 0.4.4 |
| `story-composer-01` | media | alpha | 0.2.1 |
| `media-editor-01` | media | alpha | 0.1.3 |
| `content-composer-01` | media | alpha | 0.2.1 |
| `media-carousel-editor-01` | media | alpha | 0.1.2 |
| `video-player-01` | media | alpha | 0.1.2 |
| `pdf-viewer` | media | alpha | 0.1.3 |
| `media-library-01` | media | alpha | 0.1.1 |
| `file-tree` | navigation | alpha | 0.1.0 |
| `file-manager` | navigation | alpha | 0.1.0 |
| `rich-sidebar` | navigation | alpha | 0.3.0 |
| `account-switcher-01` | navigation | alpha | 0.1.0 |
| `code-block` | code | alpha | 0.1.1 |
| `detail-panel` | feedback | alpha | 0.1.1 |
| `team-progress-bar-01` | gamification | alpha | 0.1.0 |
| `team-trophy-shelf-01` | gamification | alpha | 0.1.0 |
| `team-feedback-loop-01` | gamification | alpha | 0.1.1 |
| `cooperative-challenge-01` | gamification | alpha | 0.1.1 |
| `task-choice-control-01` | gamification | alpha | 0.1.0 |
| `team-quest-log-01` | gamification | alpha | 0.1.0 |

> `force-graph` removed 2026-05-08 pending recreation; v0.2 source + procomp docs archived to [`docs/migrations/force-graph/`](../docs/migrations/force-graph/). v3 design + slug TBD.

---

## Active queue (session-open list, 2026-05-13)

User's procomp queue — 7 of 10 shipped, 3 remaining. Each goes through GATE 1 (description) → GATE 2 (plan) → implementation → GATE 3 (spot-check review).

1. ~~`pdf-viewer`~~ ✅ shipped 2026-05-10
2. ~~`file-tree`~~ ✅ shipped 2026-05-10
3. ~~`file-manager`~~ ✅ shipped 2026-05-10
4. ~~`code-block`~~ ✅ shipped 2026-05-11
5. ~~`json-form`~~ ✅ shipped 2026-05-13 (now v0.2.5)
6. ~~`todo-rich-card`~~ ✅ shipped 2026-05-20 (now v0.1.1)
7. ~~`todo-tree`~~ ✅ shipped 2026-05-21 (now v0.1.3)
8. `rich-graph-2`
9. `chat-panel`
10. `notification-system`

Sibling procomps queued (no GATE 1 yet): `todo-rich-card-in-flow` (flow-canvas-01 adapter), `bottom-tab-bar-01` (shares NavBadge + NavItem schema with rich-sidebar via F-S1 relative imports), `related-articles-ribbon-01` (queued 2026-06-02 alongside content-card-news-01 v0.3.0 description sign-off — sibling that consumers stack *under* a news card on detail pages; deferred out of content-card-news-01 scope so the card stays a teaser primitive).

**Side workstreams — recently closed:** `rich-sidebar` v0.1 → v0.3 (3 ships across 2026-05-22 / 23), `account-switcher-01` v0.1.0 (49th component, 2026-05-23), `registration-form-01` + `pricing-table-01` (CMS conversion batch, 2026-05-22), `workspace` v0.1.2 → v0.1.3 + v0.2.0 GATE 1 (2026-05-24 / 25). See Recent activity below or `.claude/decisions/`.

---

## Roadmap (longer-term team-utility candidates)

Next candidates, ordered by team utility (separate from the active queue above):

1. ~~`data/stat-card`~~ ✅ shipped 2026-05-09
2. `feedback/empty-state` — icon + title + body + primary action.
3. `forms/multi-select` — combobox with tag chips (shadcn has Command, no real multi-select).
4. `layout/page-header` — title + breadcrumbs + actions slot.
5. `feedback/notification-feed` — grouped, time-bucketed, read/unread (overlaps with active-queue `notification-system` — reconcile when that one starts).
6. `navigation/command-palette` — cmd+k, grouped results.
7. `media/dropzone` — drag-drop + progress + previews.

**Active sweep work** — in-progress at [`docs/reviews/sweep-tracker.md`](../docs/reviews/sweep-tracker.md). Tier 1 (9 components) reviewed at v0.1 across sessions 1-6; Phases 1-6 closed in sessions 7-7d; Tier 2 (27 components) reviews scheduled across sessions 8-12.

---

## Open decisions / TODOs

**Active — needs decision or work**

- **🟡 consumer-integration-note fixes — COMMITTED, NOT PUSHED (kanban-board-01 v0.4.1 · todo-rich-card v0.3.0).** A second external consumer note (usage-driven, after the 2026-06-18 code-review note) on the same two components. All findings validated against current source first — **B6 caught as wrong vs our code** (surface already a token; skipped). kanban v0.4.1 (patch): drop-anywhere + wheel-scroll + body-portaled overlay + wider columns (A1–A5). todo-rich-card v0.3.0 (minor): status dropdown (`statusEditable`) + status tones (`tone`/`icon`) + priority/labels meta row + two-line header + shell-drag adapter (B1–B8, C1). All gates green. **RESUME = push to master + post-deploy F-cross-11 cross-backend consumer-tsc smoke (B3 `asChild` is the R10 risk class) + manual DnD smoke (A4/A5).** Decision: [`2026-06-19-consumer-note-kanban-v0.4.1-todo-rich-card-v0.3.0.md`](decisions/2026-06-19-consumer-note-kanban-v0.4.1-todo-rich-card-v0.3.0.md); GATE 3: [todo-rich-card](../docs/procomps/todo-rich-card-procomp/reviews/2026-06-19-v0.3.0-spotcheck.md) (Pass w/ follow-ups) / [kanban](../docs/procomps/kanban-board-01-procomp/reviews/2026-06-19-v0.4.1-spotcheck.md).
- **blackboard-01 v0.1.0 — GATE 3 follow-ups (4 open).** F-01 (Med, v0.1.1) post-deploy consumer-install smoke (button/textarea Base-UI divergence + `@fontsource` install). F-02 (Low, user-owned) visual + interaction walkthrough (no X server in this env). F-03 (Low, v0.1.1) opportunistic prune of absorbed optimistic/appended extras (display already dedups; buffer capped at 300). F-04 (Low, v0.2) in-composer live mention-highlight overlay. Decision: [`2026-06-18-blackboard-01-v0.1.0-first-ship.md`](decisions/2026-06-18-blackboard-01-v0.1.0-first-ship.md). **SHIPPED + PUSHED to master 2026-06-18.**
- **media-editor-01 v0.1.0 + story-composer-01 v0.2.0 — GATE 3 follow-ups (8 open / 2 closed-in-review).** v0.1.1: F-01 (Med) wrap mutations in `history.execute()` to make undo/redo functional. v0.2.1: F-01 (Med) `editorBackground` prop forwarding (needs MediaEditor01 `background?` prop expansion) + F-04 (Low) story-composer planning docs v0.2.0 amendment. v0.3.0 coordinated cohort: F-02 media-editor (add `setMode` to handle) + F-03 story-composer (drop loadState workaround) + F-03 media-editor (drop `StoryComposer01Labels` shim, refactor parts onto `MediaEditor01Labels`). Post-push: F-04 media-editor + F-05 story-composer 5-tab visual regression walkthrough (user-owned). Decision: [`2026-06-02-media-editor-01-v0.1.0-extraction-and-story-composer-01-v0.2.0.md`](decisions/2026-06-02-media-editor-01-v0.1.0-extraction-and-story-composer-01-v0.2.0.md).
- **F-cross-13** — shadcn primitive Radix→Base UI divergence (Medium). Path (a) shipped per-procomp (defensive callback contravariance + drop divergent prop names; established 2026-05-17 in rcif v0.2.0). Path (b) — producer-primitive refresh — standalone hygiene task, affects every procomp using shadcn primitives with non-trivial props. Tracker: [`docs/reviews/sweep-tracker.md` F-cross-13](../docs/reviews/sweep-tracker.md). Defensive-callback pattern is the **new default for all future procomp authors**.
- **F-S1 cross-procomp `/types` substitution** (Watch). shadcn 4.6.0 path rewriter substitutes the current slug for the target slug when a same-category sibling imports `<other-slug>/types`. Worked around via RELATIVE imports for all cross-procomp paths in shipped source. Promotion criterion: a second same-category cross-procomp ship that trips the bug despite the relative-paths lock.
- **rich-card-in-flow v0.1 + v0.2 spot-check follow-ups** → v0.3.0 plan (4 Low candidates: `isCardLike` tightening + Plate per-mount cost + per-field ports + custom port-type registration). Decisions: [v0.1.0](decisions/2026-05-16-rich-card-in-flow-v0.1.0-first-ship.md), [v0.2.0](decisions/2026-05-17-rich-card-in-flow-v0.2.0-port-editor.md).
- **flow-canvas-01 v0.2.0 spot-check follow-ups** → v0.2.1 candidates (F-01 Med post-Tier-1+2 DevTools-trace measurement; F-02 Low path-b smoke; F-03 Low usage.tsx stale "Deferred to v0.2" heading). Decision: [2026-05-16-flow-canvas-v0.2.0-perf-bundle](decisions/2026-05-16-flow-canvas-v0.2.0-perf-bundle.md).
- **pdf-viewer worker default** (F-01) → v0.2.0. Replace unpkg-CDN default with bundled-or-postinstall path; Turbopack rejected bare-specifier `new URL()` so v0.1.0 ships CDN default + `workerSrc` override. Decision: [2026-05-10-pdf-viewer-v01-pipeline](decisions/2026-05-10-pdf-viewer-v01-pipeline.md).

**Informed defers — explicit trigger to revisit**

- **MDX for usage docs** — currently `usage.tsx`. Trigger: ~5 components reach prose-heavy guidance, OR a consumer needs MDX-specific features (embeds, codeblocks-with-render).
- **NPM publish artifacts** — no `tsup`/`rollup`, no exports map. shadcn-registry handles team-internal use. Trigger: external consumer onboards, OR registry update-friction surfaces real pain.
- **Test runner not wired** — `tsc --noEmit` + `lint` + demo-driven manual verification cover today. Trigger: first non-trivial bug in pure-function `lib/` modules (workspace + rich-card + properties-form). First test should be rich-card's `parse → serialize → parse` fixed-point property test.

Closed entries (F-cross-01/04/11-pathB/12, Phase 0 risk spike, chart palette, site nav, alpha/beta variants, footer version, public registry build, reserved meta fields, lime contrast pattern, rcif p-llm-system-in warning, etc.) live in [`.claude/decisions/`](decisions/) + [`STATUS-archive.md`](STATUS-archive.md) (pre-2026-05-09).

---

## Recent activity

The 5 most-recent decision files, most-recent first. Full per-decision log at [`.claude/decisions/`](decisions/).

- **🎨 2026-07-01 — gamification visual QA + hover pass: 4 patch bumps + Button-primitive hover fix (SHIPPED)** ([decision](decisions/2026-07-01-gamification-visual-hover-pass.md)) — User-driven docs-page walkthrough of the 6/6 pack. **trophy-shelf v0.1.1** (name-length-ragged grid → fixed size-driven badge cells `w-24`/`w-20` + 2-line `line-clamp-2` labels + seamless medallion hover), **quest-log v0.1.1** (state-aware beat-circle hover on interactive+static wrappers), **progress-bar v0.1.1** + **cooperative-challenge v0.1.2** (fill brightens + lime glow on hover). Shared **`button.tsx` primary variant fix** — hovered only as an anchor (`[a]:hover:`) → real `<button>` hover; **docs-site only** (our components ship shadcn's `button`, not ours) → consumers unaffected, no bump. Docs-only demo tweaks: `mx-auto` centering ×6 · feedback-loop Start hand-off visible · task-choice demo-card hover. Patch bumps → no GATE 3. Gates: tsc 0 / eslint(touched) 0 / meta-deps 63/63 / build 72 / registry:build ✓ (4 artifacts spot-checked).
- **📖 2026-07-01 — team-quest-log-01 v0.1.0: 6th & FINAL gamification component (63rd procomp; E5; 🎮 pack 6/6 COMPLETE; BUILT + gated)** ([decision](decisions/2026-07-01-team-quest-log-01-v0.1.0-first-ship.md), [GATE 3](../docs/procomps/team-quest-log-01-procomp/reviews/2026-07-01-v0.1.0-spotcheck.md)) — A light team narrative overlay: a **skippable** quest-name editor (`questName?.trim() || name`; blank-save reverts — never forced) + a milestone-chapter timeline (vertical rail; done/current/upcoming from `Milestone.done`; unresolved milestoneId renders gracefully + dev-warns). `narrative.chapter-viewed` fire-once per mount (one IO + Set + SSR guard + click fallback). Light compound (no `React.lazy`): `lib/resolve-title`+`lib/derive-beats` + headless Root (title/draft/beats + telemetry + `forwardRef` handle focusNameEditor/scrollToChapter) + flat NameEditor/Chapters + Tier-C QuestTitle/QuestNameField/ChapterRail/ChapterBeat/EmptyNarrative + assembly. Deps shadcn input/button + lucide; zero `@ilinxa/*` (D-03); SSR-safe. Gates: tsc 0 / lint 0 / meta-deps 63/63 / build 72 pages / registry:build ✓ (11-file artifact; D-03 grep clean). GATE 3 Pass-w-follow-ups (rotating Robustness): F-01 post-deploy smoke (Med) · F-02 walkthrough / F-03 v2 (Low). **RESUME = the deferred `gamification-kit` (D-04) + a Tier-host page composing all six.**
- **🙌 2026-07-01 — task-choice-control-01 v0.1.0: 5th gamification component (62nd procomp; E4 Autonomy; single-unit exception; BUILT + gated + pre-push Base UI smoke CLEAN)** ([decision](decisions/2026-07-01-task-choice-control-01-v0.1.0-first-ship.md), [GATE 3](../docs/procomps/task-choice-control-01-procomp/reviews/2026-07-01-v0.1.0-spotcheck.md)) — A droppable autonomy control for one team task: "open for anyone" `switch` + "I'll take this" claim + assignee chip with a **neutral no-penalty** Release + a `popover`+`command` reassign picker. 3 states (open/claimed/unassigned + legal edge) from a controlled slice + team members. **Never-forced STRUCTURAL** (no mandatory state in the type system; `readOnly` opt-in only); Release = `onAssigneeChange(undefined)`, ghost/muted, never destructive/cold-verb/penalty-motion; neutral `aria-live`. **Single-unit (no Root/context/hooks)**: flat parts `OpenForAnyoneToggle`/`ClaimButton`/`AssigneeChip` + logic-free assembly + pure `lib/state`+`lib/members`. Deps shadcn switch/button/avatar/popover/command + lucide; zero `@ilinxa/*` (D-03). Gates: tsc 0 / lint 0 / meta-deps 62/62 / build 71 pages / registry:build ✓ (8-file artifact). **Proactive pre-push Base UI smoke CLEAN** (given the sibling's getValueLabel break) — popover/command/switch held via the proven author-picker pattern. GATE 3 Pass-w-follow-ups (rotating Copy/never-forced): F-01 production confirm (Med→Low) · F-02 walkthrough (Low). **RESUME = component 6 `team-quest-log-01` (E5, final, compound).**
- **🤝 2026-07-01 — cooperative-challenge-01 v0.1.0: 4th gamification component (61st procomp; E3 Relatedness; BUILT + gated)** ([decision](decisions/2026-07-01-cooperative-challenge-01-v0.1.0-first-ship.md), [GATE 3](../docs/procomps/cooperative-challenge-01-procomp/reviews/2026-07-01-v0.1.0-spotcheck.md)) — A safe-by-design cooperative team challenge card: one shared goal, a **collective** progress meter (never per-member), a **whole-team** reward, and a **penalty-free** opt-in where opted-out is a neutral, first-class "joinable" state (**never-forced** made literal: Join is a prominent lime primary, Leave is one click no-confirm-no-guilt, a visible no-penalty hint in both states). `done` → a lightweight inline "Completed together" ack (non-blocking, no modal, `aria-live` on the pill only) — heavy celebration deferred to E6 (clean seam, not imported). Controlled-only opt-in (omit `onOptInChange` → read-only card). Light shadcn-compound (no `React.lazy`): `lib/derive`(+copy defaults)/`lib/events` + headless Root (controlled-echo + `challenge.opened` `firedRef`-guarded telemetry) + flat Header/Progress/Reward/OptIn + Tier-C Meter/RewardChip/OptInToggle/MemberStack/Skeleton + assembly. Deps shadcn `progress`/`avatar`/`button`/`badge`/`skeleton` + lucide; **zero `@ilinxa/*` (D-03), zero new npm/primitive**. Gates: tsc 0 / lint 0 / meta-deps 61/61 / build 70 pages / registry:build ✓ (13-file artifact). GATE 3 Pass-w-follow-ups (rotating Copy/never-forced): F-01 post-deploy smoke (Med, expected clean) · F-02 never-forced walkthrough / F-03 meter aria-valuenow=pct (Low). **RESUME = component 5 `task-choice-control-01` (E4, single-unit exception).**

- **🎉 2026-07-01 — team-feedback-loop-01 v0.1.0→v0.1.1: 3rd gamification component (60th procomp; new `canvas-confetti` dep; SHIPPED + PUSHED `571100b`; F-01 4-ship smoke RAN + CLOSED)** ([decision](decisions/2026-07-01-team-feedback-loop-01-v0.1.0-first-ship.md), [GATE 3](../docs/procomps/team-feedback-loop-01-procomp/reviews/2026-07-01-v0.1.0-spotcheck.md)) — E6 (Competence): a host-triggered NON-BLOCKING feedback layer — brief (<1s) skippable celebration + gentle next-task nudge. Full compound: `lib/clamp` + headless Root (single-chokepoint reducer both trigger paths funnel into + clamped timer + reduced-motion + imperative handle) + flat Celebration/Nudge + Tier-C CelebrationOverlay/NextTaskNudge/**React.lazy ConfettiBurst** (proven 10.6KB isolated chunk). **D-10 enforced structurally** (pointer-events:none except skip, no focus-trap, timer clamped [200,999)); **two trigger paths → one reducer → newest-wins**; reduced-motion static branch via **useSyncExternalStore** (dodges set-state-in-effect). NEW dep `canvas-confetti ^1.9.4` (opt-in, lazy) + `button` + lucide; zero `@ilinxa/*`; team-subject copy only; D-16 documented. **F-01 4-ship smoke CAUGHT a real gap: `canvas-confetti` ships no types → consumer TS7016 (producer tsc was blind — has `@types` locally) → v0.1.1 adds `@types/canvas-confetti` to the registry item's `devDependencies` (shadcn installs item devDeps); re-smoke consumer tsc 0.** Gates: tsc 0 / lint 0 / meta-deps 60/60 / build / registry:build ✓. GATE 3 Pass-w-follow-ups: F-01 ✅ closed (v0.1.1) · F-02 D-10 walkthrough / F-03 coalesce→v0.2 (Low). **RESUME = component 4 `cooperative-challenge-01`.**
- *(team-trophy-shelf-01 E2 + team-progress-bar-01 E1 recent-activity entries trimmed 2026-07-01 to hold the list at ~5 — full detail in their decision files under [`.claude/decisions/2026-07-01-team-*`](decisions/) + the Components table + the top lock.)*
- **🟡 2026-06-23 — task-family clipboard UNIFY + gantt v0.5.0 calendar-parity (4 components; built+gated, push pending)** ([decision](decisions/2026-06-23-task-family-clipboard-unify-and-gantt-parity.md), GATE 3: [gantt v0.5.0](../docs/procomps/gantt-timeline-01-procomp/reviews/2026-06-23-v0.5.0-spotcheck.md) · [todo-rich-card v0.4.0](../docs/procomps/todo-rich-card-procomp/reviews/2026-06-23-v0.4.0-spotcheck.md) · [todo-tree v0.3.0](../docs/procomps/todo-tree-procomp/reviews/2026-06-23-v0.3.0-spotcheck.md)) — Closed the user-flagged gantt-vs-calendar gap AND **resolved the long-carried "clipboard-hoist epic"**: the family had two incompatible clipboard formats (rich-card bare-item MIME vs calendar `ilinxa/task` envelope → cross-paste silently no-op'd). **Hoisted ONE unified envelope to `todo-rich-card/lib/clipboard.ts`** (dual-transport, omnivorous read + legacy back-compat) → rich-card **v0.4.0**; calendar repointed (public re-export preserved) → **v0.2.3**; gantt gained double-click-create + quick-composer + copy/cut/paste + Priority submenu → **v0.5.0**; todo-tree gained clipboard (kbd+handle) + `priorityOptions` → **v0.3.0**. kanban stays out (data-agnostic). Zero new deps. Gates green (tsc 0 / lint 0 touched / meta-deps 57/57 / build 66/66 / registry:build ✓); envelope round-trip 7/7; live consumer + browser round-trip = post-deploy. 3× GATE 3 Pass-with-follow-ups. **RESUME: commit + push; post-deploy smoke.**
- **🔌 2026-06-23 — calendar-01 v0.2.2: port-feedback fixes (external node-base-v0 fork report; SHIPPED-pending-push)** ([decision](decisions/2026-06-23-calendar-01-v0.2.2-port-feedback-fixes.md), [GATE 3](../docs/procomps/calendar-01-procomp/reviews/2026-06-23-v0.2.2-spotcheck.md)) — The fork team filed a port-readiness report (4 correctness / 1 API / 2 design / 5 polish), same process as the gantt port. **Every finding re-validated first-hand against source before acting.** **A1 (the one consequential fix):** the inspector / modal detail editor echoed via `onChange` only — the embedded `<TodoRichCard>`'s granular events were dropped — so a consumer persisting via `onFieldEdited` silently missed all modal edits + every priority change. `applyEditedSubtree` now diffs prev↔next and emits `onFieldEdited` per changed `TodoEditableField` (no double-fire; the card's own events stay unwired); `priority` documented as `onChange`-only (it is **not** a `TodoEditableField` — the cross-procomp type extension was deliberately rejected); `TodoEditableField` re-exported from the calendar barrel. **Plus validated polish:** P1 (milestone mislabeled "All day" in agenda — `allDay=true` hit the all-day branch first; reordered + `◇ <time>`), P2 (`periodLabel` dead `sameYear` ternary), P3 (skeleton 35→42), P4 (`initials()` whitespace → `"?"`), C3 (`effectiveEnd` propagate `dateOnly` — inert tidy), C4 (delete dead `occurrencesInRange`). **Deliberately NOT changed: C2** — report code-accurate but the defect framing is disputed; a timed event ending at midnight has zero extent on the next day and is already excluded by the time-grid, so the proposed `allDay`-gate would regress Month↔week/day consistency. **C1 (DST intra-day geometry) + D1/D2/P5 carried to follow-ups F-02..F-05 (→ v0.3.0).** No new files / deps / primitive. Gates: tsc 0 / lint calendar-clean (81-err baseline all `navigation/*`) / meta-deps 57/57 / build 66/66 / registry:build ✓ (artifact spot-checked). GATE 3 Pass-with-follow-ups (rotating dim Public API). **SHIPPED + PUSHED (`1854b79`). F-01 CLOSED post-deploy 2026-06-23:** Base UI (`base-nova`) consumer-tsc on the live v0.2.2 artifact = **0 errors**; Production walkthrough clean (demo mounts, no runtime error) + **P1 fix visibly live** (agenda renders `◇ 5:00 PM` for the milestone, not "All day"). Remaining F-02..F-05 (C1 DST + D1/D2/P5) → v0.3.0.
- **✅ 2026-06-23 — closed the owed verifications for BOTH task surfaces (gantt v0.4.0 F-01 + calendar v0.2.0 F-01/F-02 → calendar v0.2.1)** ([calendar v0.2.1 decision](decisions/2026-06-23-calendar-01-v0.2.1-cross-backend-fix.md)) — Revived the dockerized stealth-browser (`virtualbrowser-stealth-browser-1` had a dead Xvfb display → restart) and used it to run both owed **live walkthroughs against Production**. **gantt v0.4.0 F-01:** Draw toggle flips `aria-pressed`+crosshair (draw-vs-pan); right-click menu opens + a real "Add task below" click creates a task (swallowed-pointerup fix proven). **calendar v0.2.0 F-01:** renders + context menu + selection→inspector editor + Week time-grid + F-04 overflow all clean. **calendar F-02 caught 3 REAL Base UI errors** (the F-cross-13 the smoke exists for): `ToggleGroup` is a multi-value model on Base UI (`value: string[]`, `onValueChange(string[], details)`) + `TooltipProvider delayDuration` is Radix-only — **all latent since v0.1.0** (smoke deferred then). **Fixed → calendar v0.2.1** (patch): view switcher `ToggleGroup` → plain-button segmented control (the gantt/blackboard "plain buttons shrink the F-cross-13 surface" rule; **`toggle-group` dep dropped**) + drop `delayDuration` (default delay). Base UI consumer-tsc **0 errors** post-fix. **Post-deploy re-smoke against the live v0.2.1 registry: ✅ CLEAN** (fresh `shadcn add` install 0 + consumer-tsc 0 errors — the 4-ship loop closed: v0.2.0 → smoke 3 errors → v0.2.1 → re-smoke 0). Gates: tsc 0 / lint 0 / meta-deps 57/57 / registry:build ✓. **Nothing pending** except F-03/F-04 Low (modal focus-trap; perf + clipboard-hoist epic).
- **🗓️ 2026-06-23 — calendar-01 v0.2.0: the EDITABLE calendar (Stages 1–4 + ship; SHIPPED + PUSHED)** ([decision](decisions/2026-06-23-calendar-01-v0.2.0-editing-layer.md), [GATE 3](../docs/procomps/calendar-01-procomp/reviews/2026-06-23-v0.2.0-spotcheck.md), [plan §14c](../docs/procomps/calendar-01-procomp/calendar-01-procomp-plan-v0.2.0.md)) — Opt-in editing over v0.1.0 (`editable` default off → read-only). Month @dnd-kit drag + native day-resize · week/day native-pointer move/resize/draw + all-day spanning band · inspector + **modal detail-editor overlay** (so Edit/Enter work without the inspector) + inline rename · quick-composer (plain panel, not Radix Popover) · right-click menu (Edit/Rename/Status/Priority/**Copy/Cut**/Delete) · live resize preview · **full keyboard editing** (focus event → ←/→ move, Shift+←/→ (+↑/↓ grid) resize, Del, Enter edit, F2 rename; day-cell Enter → composer) · **cross-surface copy/cut/paste** over a shared `ilinxa/task` clipboard envelope (carries tasks to gantt/kanban/tree; paste-target decides all-day vs timed) · **F-04 height-responsive month overflow**. Pure `lib/edit-mutations`/`edit-permissions`/`clipboard` + `hooks/use-calendar-edit` (single chokepoint, no internal data state, controlled-echo). **Stage-4 deviation (user-approved, plan §14c):** single-view assembly ⇒ cross-view drag impossible ⇒ **copy/paste IS the cross-surface mechanism**; deferred external HTML5 drop tray (`onExternalDrop` reserved+inert) / agenda drag / conversion-drag. **2 intentional non-byte-identical-v1 read-only spots (opt-out):** status-driven color (`colorBy="urgency"` restores) + responsive overflow (`maxEventsPerCell` restores). New deps `context-menu`+`input` (shadcn) + `@dnd-kit/core`+`@dnd-kit/utilities` (npm). Gates: tsc 0 / lint 0 / meta-deps 57/57 / build 66/66 (no SSR err) / registry:build (32-file artifact). **GATE 3 Pass-with-follow-ups; reviewed by 3 AI-assisted adversarial passes (correctness/consistency/design-a11y) — 1 real bug (dead Edit/Enter under inspector) + ~9 polish items fixed pre-ship.** Branch reconciled earlier (cal commits rebased onto origin gantt `84ef1ea`, dup gantt dropped). **F-01 + F-02 CLOSED 2026-06-23 → v0.2.1** (live walkthrough clean; cross-backend smoke caught 3 real Base UI errors, fixed — `toggle-group` dep dropped + `delayDuration` dropped; see the closure entry above). F-03 modal focus-trap · F-04 perf + clipboard hoist→todo-rich-card carry. **v0.2.0 SHIPPED + PUSHED to master; v0.2.1 pending push.**
- **✏️ 2026-06-22 — gantt-timeline-01 v0.4.0: Draw-mode toggle + right-click portal fix (finished uncommitted WIP fixes as a minor; SHIPPED + PUSHED)** ([decision](decisions/2026-06-22-gantt-timeline-01-v0.4.0-draw-mode-and-rightclick-fix.md), [GATE 3](../docs/procomps/gantt-timeline-01-procomp/reviews/2026-06-22-v0.4.0-spotcheck.md)) — Two bug fixes the user flagged. **(1) Draw-vs-pan conflict** → drawing is now an explicit **Draw** toggle (editable toolbar; `Pencil`/`aria-pressed`): off (default) empty-row drag pans, on draws a sibling (crosshair). The press pipeline was rewritten immediate-classify → **deferred-classify** (`onPointerDown` records a `Pending` + `classifyTarget`; first move past 4px picks one gesture: vertical→scroll, horizontal→resize/move/group-move/draw/pan; sub-threshold release → click-select). Strictly more robust; `!editable` → `void` → byte-identical v1. **(2) Right-click items did nothing** → the Radix `ContextMenu` portals to `document.body` but stays a React child, so its events bubbled into the gesture/keyboard handlers and swallowed the menu-item `pointerup` (+ double-triggered gutter nav/delete); fixed with a **DOM-descendant guard** (`!currentTarget.contains(target)→return`) on body `onPointerDown`/`onKeyDown` + gutter `onKeyDown`. **Minor (not patch)** — adds `GanttContextValue.drawMode`/`setDrawMode` + flips a default (patch precedents added zero public API). **No new files/deps/primitive** (Draw reuses `Pencil`). GATE 3 spot-check Pass-with-follow-ups (rotating dim **Robustness**). Gates: tsc 0 / lint 0 err / meta-deps 57/57 / build 66/66 / registry:build ✓ (artifact verified). **F-01 CLOSED 2026-06-23** (live walkthrough run against Production via the revived dockerized virtual browser — Draw toggle + right-click "Add task below" both proven; see the closure entry above). F-02 N/A (no new primitive); F-03/F-04 Low. Pushed separately from the concurrent calendar-01 WIP (another session owns calendar; untouched).
- **🗓️ 2026-06-22 — calendar-01 v0.1.0 first ship: read-only four-view calendar over TodoItem[] (57th procomp; date-grid sibling of gantt; feat `7ba0dd6` — COMMITTED, PUSH PENDING)** ([decision](decisions/2026-06-22-calendar-01-v0.1.0-first-ship.md), [GATE 3](../docs/procomps/calendar-01-procomp/reviews/2026-06-22-v0.1.0-spotcheck.md)) — Month (multi-day spanning bars + chips + "+N more") / week+day hour time-grids (all-day band + lane-packed timed blocks + now-line) / agenda. **Three-layer all-day/timed/milestone classify** (consumer predicate → date-only strings parsed floating-local, no TZ off-by-one → span heuristic; **no fork of `TodoItem`** — "do it like Google" = the date-vs-dateTime mechanism on the existing ISO fields). Color from todo-rich-card `RAMPS`; SSR-safe `now` (seed + rAF, dodges `set-state-in-effect`); finite-date guards (gantt G2 lesson). shadcn-compound: 6 pure lib modules (classify/color/date-range/occurrences/segments/lane-pack) + `Calendar01Root` + flat view parts + 11 Tier-C primitives + the assembly; a month-only subset drops the time-grid; the full-card tooltip lazy-loads todo-rich-card. GATE 1 + GATE 2 each **deep-reviewed via an independent verifier** (desc 2-ext + 5-int, plan 3-ext + 2-int drifts fixed). Gates: tsc 0 / lint 0 (folder) / meta-deps 57-57 / build 66-66 (no SSR err) / registry:build (23-file artifact). GATE 3 Pass-with-follow-ups (rotating Robustness). **Open:** F-01 (Med) live walkthrough OWED (headless env, no browser; dev server up on :3001) · F-02 (Med) post-deploy cross-backend smoke (new `toggle-group`/`popover`/`tooltip`/`calendar` → 4-ship pattern + likely v0.1.1) · F-03/F-04 Low. Built amid **unrelated concurrent gantt v0.4.0 draw-mode WIP** — staged by explicit path, WIP left untouched. **PUSH PENDING user confirm.**
- **🩹 2026-06-22 — gantt-timeline-01 v0.3.1: external-review hardening (10 validated findings; SHIPPED + PUSHED `8235917`, gates green)** ([decision](decisions/2026-06-22-gantt-timeline-01-v0.3.1-review-hardening.md)) — An external read-only review filed 10 findings; **all 10 validated real against source** (2 explanation-level caveats noted: G2's `summarySpan` "NaNs the parent bracket" claim is wrong — NaN comparisons silently skip; G10's trigger is near-theoretical since the popover self-unmounts when the item vanishes). Non-breaking → patch, skips GATE 3 per the readiness rule. **G1 (High — the one real defect):** the gutter advertised a keyboard-navigable WAI-ARIA tree, but vertical scroll is one-way (body→gutter `translateY`) and the gutter is `overflow-hidden` + virtualized, so ArrowDown/End past the visible window focused a row not in the DOM → focus no-op + silent dead-end. Fix: on `focusedId` change, scroll the body to bring the row's index into view, then focus across a few rAFs while the virtualizer re-renders (+ alias the bundled `bodyScrollRef` to a local so the React-Compiler immutability rule permits the `scrollTop` write — the body's own proven pattern). **G9 (a11y):** `aria-setsize`/`aria-posinset` added (two new `GanttRow` fields from `flatten`; flat virtualized-tree pattern, no `role="group"`). **G2 (robustness):** finite-date guards — body renders no bar (label-only) for NaN geometry; `setWindow`/`shiftSubtree`/keyboard-arrow no longer `new Date(NaN).toISOString()`-throw on commit. **G3 (consistency):** drag-snap for month/quarter/week now lands on real calendar boundaries via `startOfUnit`/`addUnit` (was average-ms rounding that drifted off the axis ticks); numeric/off snap unchanged. **G4:** phantom `onFieldEdited("expireAt")` on a start-edge resize fixed — gate each field event on the patch key actually being present + compare by `Date.parse` instant. **G5** clear hover tooltip in the wheel handler. **G6** "Add task below" gated on the parent's create rule (was the item's own → showed-then-no-op). **G7** clear `renamingId` on delete. **G8** rAF-coalesce the body `ResizeObserver` (scrollbar-appearance loop). **G10** `applyEditedSubtree` bails + closes when the edited id is gone (was echoing the unchanged forest = silent edit loss). Gates: tsc 0 / gantt-folder lint 0 err / meta-deps 56/56 / build 65/65 / registry:build ✓. **Open F-01 (carried from v0.3.0, Medium): live drag-walkthrough still OWED post-deploy** — static review found these 10; the gesture surface itself remains browser-unproven. **SHIPPED + PUSHED to master (`8235917`); Vercel auto-deploys.**
- **🖍️ 2026-06-18 — blackboard-01 v0.1.0 SHIPPED (55th procomp; collaborative handwritten-note chalkboard; GATE 1→2→3 single session)** ([decision](decisions/2026-06-18-blackboard-01-v0.1.0-first-ship.md), [GATE 3](../docs/procomps/blackboard-01-procomp/reviews/2026-06-18-v0.1.0-spotcheck.md), [guide](../docs/procomps/blackboard-01-procomp/blackboard-01-procomp-guide.md)) — Dark-navy chalkboard widget: a team writes **handwritten notes** that stream vertically, each in a chosen ink color + chalk width + handwriting font; **auto-save** (debounced, no Save button) + optimistic post/reconcile + retry; **scroll-up lazy load** (10/page, scroll-anchored jump-free prepend); inline **author-on-hover**; **pin** to a sticky row; **`@mentions`** over a roster (+ `@you` emphasis); handwritten **chalk-red unread** marker; **themeable** board (color/custom image); **minimal composer that reveals on double-click** (`composerMode` default `"double-click"`; Esc/✕ to dismiss; `"always"` docks it). **Built shadcn-compound** (rule): headless `BlackboardRoot` + 7 flat context parts + 9 context-free Tier-C primitives + `Blackboard01` assembly; **drop the composer → free read-only board** (demo "Lighter" tab proves it). **Composes NO other procomps** (only `button`+`textarea`) → zero cross-procomp rewriter risk — a first since the composition cluster. **3 scoped design-mandate exceptions** (user-requested, GATE-3 design-dim verified): handwriting font on note text + unread number only (chrome stays Onest), board dark in both themes (own `--bb-*` tokens), chalk-red unread `oklch(0.66 0.19 22)` chroma ≤0.20; **signal-lime stays the interactive accent**. **Hybrid fonts**: 4 bundled `@fontsource` faces (Kalam w/ real 300/400/700 + Caveat/Patrick Hand/Shadows Into Light) self-loaded by Root via `--bb-font-*`, overridable via `fonts` prop; "chalk width" = real weight where available, faux text-stroke otherwise. 30 base files + 1 fixtures. Gates: tsc 0 / lint (scoped) 0 / meta-deps 55-55 / build (55 routes) / registry:build (30 files, no demo/usage/meta) / SSR smoke 200 + content + no dev-log errors. **3 reusable lessons:** (1) **meta-deps import-regex** swallows side-effect imports that precede a `from` import in the same file → put the `from` import FIRST (phantom-npm fix); (2) **`react-hooks/refs`** flags ref-reads taken off a bundled context value during render → destructure ctx into locals + attach refs only in `ref=` prop form (prior media-editor lesson, re-confirmed); (3) reconciling optimistic state vs controlled props → derive display via `useMemo` dedup, NOT a prune effect (avoids `set-state-in-effect`; cap the local buffer). v0.1 scope calls: live in-textarea mention highlight → v0.2 (tokens render on posted notes); pickers are plain `Button`s (no `toggle-group`) + bg-editor inline (no Popover) to shrink the F-cross-13 surface. GATE 3 Pass-with-follow-ups (F-01 post-deploy smoke v0.1.1 · F-02 visual walkthrough user · F-03 extra-prune v0.1.1 · F-04 live mention-highlight v0.2). **SHIPPED + PUSHED to master 2026-06-18** (tip in the decision file).
- **🗂️ 2026-06-09 — media-library-01 v0.1.0 SHIPPED (54th procomp; Google-Drive-style media library; GATE 1→2→3 single session)** ([decision](decisions/2026-06-09-media-library-01-v0.1.0-first-ship.md), [GATE 3](../docs/procomps/media-library-01-procomp/reviews/2026-06-09-v0.1.0-spotcheck.md), [guide](../docs/procomps/media-library-01-procomp/media-library-01-procomp-guide.md)) — Composition-first Drive shell: folders + files, lazy load, drag-drop upload (optimistic + progress + retry), drag-to-move (self/cycle-validated), right-click menus, and a **multi-type preview dispatcher** (image/video/pdf/code/json/text/markdown) in **both** a side pane and a full-screen lightbox. **Owns the shell, delegates every file render** to the shipped viewers (`pdf-viewer`/`code-block`/`markdown-editor`/`video-player-01`, all `React.lazy` code-split) + a folder `file-tree` — **no shipped component modified**. **Built shadcn-compound** per user directive: headless `<MediaLibraryRoot>` + flat à-la-carte parts + one `<MediaLibrary01>` assembly; drop a part → tree-shake its viewer's weight. **Key re-validation catch:** `file-tree.tsx` has no tail type-export band → `FsNode` **declared locally** (not imported) + structural typing at the `<FileTree nodes>` boundary (avoids the F-01 `/types`-mangle + keeps file-tree untouched). 31 files; 5 internal + 6 shadcn registryDeps; zero new npm packages (@dnd-kit/core + date-fns already present). Gates: tsc 0 / lint clean / meta-deps 54-54 / build (54 slugs) / registry:build (31 files, no demo/usage/meta). GATE 3 Pass-with-follow-ups. **Browser walkthrough (F-06) DONE — found + fixed 7 runtime issues none of tsc/lint/build caught:** dnd-kit SSR hydration id (`useId` on DndContext) · preview error boundary · PDF `pdfWorkerSrc` + react-pdf `onError` fallback · sidebar root node · sidebar drag-move (`file-tree onMove → moveTo`) · false-"(empty)" folders (show full tree) · cards crushed by preview pane (width-aware `auto-fill` grids) + grid-drag hardening (`pointerWithin` + event-data ids). **NEW RULE:** [`compound-component-structure.md`](rules/compound-component-structure.md) — big/multi-part components MUST ship shadcn-compound (headless Root + flat parts + logic-free assembly + lazy heavy deps); referenced from CLAUDE.md Rules. **v0.1.1 (2026-06-10): F-01 consumer smoke PASSED** (base-nova/Base-UI consumer, 0 consumer-tsc errors) — found+fixed 4 cross-procomp install bugs the dev gates missed. **Precise shadcn-rewriter rules (reusable):** rewriter rewrites STATIC `import` (strips category) but NOT relative paths, NOT dynamic `import()`, NOT `export … from` re-exports. → same-cat cross-procomp = relative; cross-cat static = `@/registry/...` `import`; cross-cat LAZY = local re-export wrapper (`import X; export default X`) + `lazy(() => import("./wrapper"))`. Plus F-cross-13 context-menu `variant`→`className`. Added 2 wrapper files (registry 31→33). Remaining open: F-02 grid virtualization · F-03 copy/duplicate · F-04 sidebar lazy-expand · F-05 markdown light-path (all v0.2). **PUSHED v0.1.0 `bc56461` + v0.1.1 to master 2026-06-10.**
- **🎮 2026-06-06 — live JSON playgrounds (docs feature) + rich-card v0.4.3 SSR fix** ([decision](decisions/2026-06-06-config-playgrounds-and-rich-card-ssr-fix.md), handoff [`HANDOFF-2026-06-06-playgrounds-and-rich-card-ssr-fix.md`](HANDOFF-2026-06-06-playgrounds-and-rich-card-ssr-fix.md)) — **COMMITTED-NOT-PUSHED (tip `f8a734b`, 2 commits ahead, tree clean, prod build ✓).** User asked for a "write JSON config → live render" surface on the content-composer-01 detail page; generalized into a **reusable `<JsonPlayground<T>>`** (`src/app/components/[slug]/_components/json-playground.tsx`): split-pane mono editor + live zod validation + Submit (gated on valid) / Format + an error boundary + a generic result strip. Four thin wrappers + per-type validators (`_lib/*-schema.ts`): **content-composer-01** (`ComposerConfig`; a registered "playground" adapter + fake uploader make Publish/Save assemble a visible item), **json-form** (`FormSchema`), **rich-card** (`RichCardJsonNode`, editable), **flow-canvas-01** (`CanvasData`; module-scope `card` renderer + built-in custom-JSON fallback; fixed-height frame). `page.tsx` routes per slug via a `PLAYGROUNDS` map. **Key impl lesson: validators run zod for ERROR MESSAGES but return the ORIGINAL `JSON.parse` result** (not zod's output) so unmodelled config keys (`rows`/`options`/`validators`/extra slotConfig) survive at full fidelity. CodeMirror IS already a dep, but the user chose a styled mono textarea. **rich-card v0.4.3** — the rich-card playground page surfaced a **pre-existing SSR hydration bug**: `lib/parse.ts` minted auto-ids for id-less cards with `crypto.randomUUID()` at render-time state-init → SSR ≠ client ids → `data-rcid`/`aria-labelledby` mismatch. Fixed with deterministic `rc-auto-<sanitized-path>` (server + client parse the same input → identical ids; verified stable across requests). Runtime edits still mint random ids (client-only, safe). **Lesson: any id minted during render/parse must be deterministic (path-derived / useId) — random UUIDs at SSR always hydration-mismatch.** Candidate next playgrounds (reusable component makes them ~10 lines): the data-driven tier (media-carousel-01, pricing-table-01, kanban-board-01, tree/list family). All gates green incl prod build. **Resume = browser-verify rich-card + flow-canvas playgrounds → push.**
- **🐞 2026-06-05 — manual-test bug sweep: media-carousel-editor-01 v0.1.2 + content-composer-01 v0.2.1 + media-editor-01 v0.1.3** — First real click-through of the composer surfaced FOUR bugs that tsc/lint/build/smoke all missed (re-proving "the visual walkthrough is the de-facto gate"). **(4) media-editor v0.1.3 — crop bottom-handles unreachable:** in crop mode the bottom control bar (Apply/Cancel + Crop/Filters/Adjust tabs) floated `absolute bottom-0` over the full-bleed canvas, covering the crop frame's bottom drag-handles so they couldn't be grabbed (+ masked the image bottom). Fix: when `activeTool === "crop"` the bottom controls drop INTO FLOW below the canvas (the flex-1 canvas region shrinks to reserve the space); other tools (sliders/inputs, no canvas-edge handles) keep the space-saving overlay. **Lesson: a floating-overlay control bar is fine over a passive canvas, but a tool with draggable canvas-EDGE handles (crop) needs the controls in-flow so the edges stay reachable.** Also affects the carousel's photo-edit panel (same editor). The Post-tab phase/browse/button bugs: **(1) carousel v0.1.2 — browse-path FileList bug:** Browse opened the picker but selecting files added nothing, no error. `parts/media-dropzone.tsx` `onChange` captured the live `e.target.files` **FileList reference**, then ran `e.target.value = ""` **before** calling `onFiles` — resetting the input empties that FileList in place, so `filesToItems`' `Array.from(fileList)` saw `[]` → silent no-add (drag-drop unaffected; uses `dataTransfer.files`). Fix: snapshot to `Array.from(...)` BEFORE the reset; widen `onFiles` to `File[] | FileList`. **Lesson: extract/clone a file input's selection (array or the `File` itself) BEFORE resetting `value` — `media-editor-01` got it right via `files?.[0]`; capturing the raw FileList reference is the trap.** **(2) content-composer v0.2.1 — phase stuck at "idle" (LATENT since v0.1.x):** Next / Save / Publish all silently no-op'd. The FSM inits to `"idle"` and only `start` → `"editing"`, but `dispatchPhase({type:"start"})` was **never called anywhere** → `goToStep` early-returns (`phase !== "editing"`) and every lifecycle action stays idle. Fix: a one-time mount effect dispatching `start` (idempotent). Affected ALL content types — never caught because nobody reached Next (media wouldn't add pre-fix; GATE 3 spotcheck didn't click-through step-nav). **Lesson: an FSM with an explicit `start`/init transition needs a mount effect to fire it — a reducer that defines the transition but never dispatches it freezes silently; manual click-through is the only gate that catches it.** **(3) content-composer v0.2.1 — button placement + invisible publish errors:** Publish/Save showed on every step (should be: Save draft on every step, Publish/Schedule on the LAST step only — `placedArms` filtered by `isLastStep`, footer reordered so Save sits left of Next). And lifecycle errors (e.g. the deferred `post-content-item` "no adapter") went only to an sr-only region → buttons looked dead. Fix: visible dismissable error alert in `composer-shell.tsx` (`error`/`onDismissError` props) + `lifecycleError` state set in the save/publish/schedule catches, cleared on nav. Post publish stays the honest "no adapter" message (deferred to v0.3 per user choice). Both patch bumps (no GATE 3). Gates: tsc 0 / lint 81-22 baseline / meta-deps 53-53 / registry:build ✓.
- **✅ 2026-06-05 — media-carousel-editor-01 v0.1.1 (review-hardening) + content-composer-01 v0.2.0 (carousel wired into the post step)** ([decision](decisions/2026-06-05-media-carousel-editor-01-v0.1.1-and-content-composer-01-v0.2.0.md), [carousel review](../docs/procomps/media-carousel-editor-01-procomp/reviews/2026-06-05-v0.1.0-spotcheck.md), [content-composer review](../docs/procomps/content-composer-01-procomp/reviews/2026-06-05-v0.2.0-spotcheck.md), handoff [`HANDOFF-2026-06-05-carousel-v0.1.1-and-content-composer-v0.2.0.md`](HANDOFF-2026-06-05-carousel-v0.1.1-and-content-composer-v0.2.0.md)) — Same day as the v0.1.0 first ship. **Integration:** post content type authors multi-media via an additive `mediaCarouselSlot` (substrate bridge + shell `CarouselLiveCacheContext`); news untouched. Multi-blob upload-at-publish deferred with the post backend adapter (authoring live; publish = existing "no adapter" error). **Review-hardening:** user asked to confirm "perfectly professional" → ran a **3-agent adversarial review** (code / UX+a11y / architecture) = **24 real findings** (honest verdict: NOT yet perfect). Re-validated all, fixed blockers+highs+most mediums. **Headline (all 3 agents): silent local-media data loss on step-nav** → live-items cache + `revokeOnUnmount={false}` (lossless within a session; cross-reload rides with upload backend). Plus: orphan-URL revoke (controlled-swap leak), synchronous-itemsRef + cap-in-addItems (intake race), loading state, in-UI error/max region, `aria-live`+dnd announcements, export busy/error surface, **re-edit double-overlay** (`initialSource` only when no `editorState`), video a11y dead-end→static caption, 24px hit targets, video poster (`playsInline`+seek), `getIsDirty` baseline, `assertNever` gate guards, exported `useCarouselState`, dead `"max-items"` kind removed. **F-01 caught AGAIN by the local-registry smoke** — the rewriter resolves a barrel import to the `.tsx`, which lacked a tail type band → added the band + `.tsx`-path cross-procomp imports; re-smoke 0 errors. **Lesson (twice-proven): a composed procomp MUST tail-re-export its public types from its `.tsx`; a barrel import isn't rewriter-safe.** Gates: tsc 0 / lint 81-22 baseline / meta-deps 53-53 / build 62 pages / smoke 0. Both GATE 3 Pass-with-follow-ups. Open → v0.3 post backend: multi-blob upload + adapter + durable persistence.
- **🛠️ 2026-06-20 — gantt-timeline-01 v0.1.0 BUILT (uncommitted; 56th procomp; completes the task-management set — `calendar-01` is next)** ([decision](decisions/2026-06-20-gantt-timeline-01-v0.1.0-first-ship.md), [GATE 3](../docs/procomps/gantt-timeline-01-procomp/reviews/2026-06-20-v0.1.0-spotcheck.md), [guide](../docs/procomps/gantt-timeline-01-procomp/gantt-timeline-01-procomp-guide.md)) — Read-only Gantt over the canonical `TodoItem[]`; shadcn compound (`GanttTimelineRoot` + flat Toolbar/Axis/Gutter/Body + Tier-C primitives + `GanttTimeline01` assembly); continuous-zoom two-tier axis + **pan/swipe/zoom canvas** (dominant-lock drag + momentum + boundary-resistance + pinch + non-passive ⌘/ctrl-wheel); filled status/urgency bars (ramp **imported** from todo-rich-card); collapsible WBS summary rows; milestone diamonds; SSR-safe `now`; WAI-ARIA tree gutter; `@tanstack/react-virtual`; lazy full-card tooltip. User supplied a GATE-1 draft written against a *different repo* → reconciled (paths/imports/tokens/facts). GATE 1 + GATE 2 reviewed for consistency (9 + 3 fixes), GATE 3 **Pass-with-follow-ups** (rotating dim = Performance). Gates green: tsc 0 · lint 0 err · meta-deps 0/0/0 · build ✓ (65/65) · registry:build ✓ (19-file base + fixtures; artifact spot-check clean). **RESUME = commit + push → F-01 post-deploy consumer smoke.** Lessons: cross-procomp import from a sub-folder needs `../../<slug>` (not `../`); momentum rAF self-ref → hoisted `function` decl; context-bundled refs false-positive `react-hooks/refs` (file-suppress per blackboard-01); arrows = tree-nav NOT pan (a11y collision caught at GATE 2); continuous-zoom ⊥ row-virtualization (orthogonal axes).
- **✅ 2026-06-05 — media-carousel-editor-01 v0.1.0 SHIPPED (53rd procomp; Instagram-feed multi-media composer; GATE 1→2→3 single session)** ([decision](decisions/2026-06-05-media-carousel-editor-01-v0.1.0-first-ship.md), [GATE 3](../docs/procomps/media-carousel-editor-01-procomp/reviews/2026-06-05-v0.1.0-spotcheck.md), handoff [`HANDOFF-2026-06-05-media-carousel-editor-01-v0.1.0-shipped.md`](HANDOFF-2026-06-05-media-carousel-editor-01-v0.1.0-shipped.md)) — Origin: user manual-testing content-composer-01's post-media step found the "Upload dropzone (Phase B retrofit)" **stub** (which lives in media-editor-01, surfaced via the composer's 1:1 passthrough) and asked for a real droppable+browse zone (one or more files), a thumbnail rail + main preview, drag-to-reorder, and one shared edit panel — explicitly **not** separating photo/video. **Load-bearing decision: build a NEW procomp that drives ONE shared `media-editor-01` instance — do NOT expand media-editor-01 to multi-item** (it's shipped + shared; story-composer-01 depends on single-item; the diff leaves media-editor-01 **untouched**). A single editor mounts only while editing, keyed per item (never N → can't trip the multi-instance guard). The **"no photo/video tabs"** the user wanted falls out of mounting in **edit-only mode** (`enabledModes={[]}` + `initialSource` → no capture surface); intake is file-based (MIME decides kind). 14 sealed files: root + 5 parts (media-dropzone / preview-rail / rail-thumb / main-preview / edit-panel) + 2 hooks (use-carousel-state + sealed use-controllable-state) + 4 lib (file-intake / validate-media-file / aspect / clamp-sources) + types/index. `@dnd-kit` rail reorder (existing dep). Controlled/uncontrolled `MediaCarouselItem[]` + imperative handle; edits flatten on apply (preview always publish-ready); `export()` pull-only; shared aspect from item 1; maxItems 10; video Edit disabled v0.1 (preview/reorder/remove). User correction during GATE 1 improved the design (one shared panel, not N editors). Impl-time plan amendments: ValidationError not re-exported by media-editor-01's `.tsx` → carousel defines own `MediaCarouselError`; selection made *derived* (no setState-in-effect). **The local-registry smoke caught a real F-cross-13** (producer `radix-ui` Tooltip `asChild` breaks `base-nova` Base-UI consumers using `render`) → switched the lone tooltip (video hint) to native `title`, dropped the `tooltip` dep; re-smoke 0 consumer-tsc errors; cross-procomp import rewrote cleanly via the `.tsx` entry (no `/types` mangling — F-01 mitigation held). Gates: tsc 0 / lint 81-22 baseline / meta-deps 53-53 / build ✓ (62 pages) / registry:build ✓ (14 base + 1 fixtures; no demo/usage/meta). GATE 3 Pass-with-follow-ups (F-01 resolved in-review; F-02 edit-panel runtime verify · F-03 video poster · F-04 hard aspect-crop · F-05 maxItems sub-frame race — all Low). **Follow-on gate (not started): content-composer-01 post-slot integration (breaking v0.2).** Lesson: prefer native affordances over the Tooltip primitive for trivial hints in distributed components; the local-registry smoke is the gate that catches the radix/Base-UI split pre-deploy.
- **✅ 2026-06-04 — content-composer-01 v0.1.0 SHIPPED through GATE 3 (52nd procomp; the SHELL media-editor-01 was extracted for; C4→C18 from the C3 pause; 18 local commits UNPUSHED — push user-gated)** ([decision](decisions/2026-06-04-content-composer-01-v0.1.0-first-ship.md), [GATE 3](../docs/procomps/content-composer-01-procomp/reviews/2026-06-04-v0.1.0-spotcheck.md), [guide](../docs/procomps/content-composer-01-procomp/content-composer-01-procomp-guide.md), handoff [`HANDOFF-2026-06-04-content-composer-01-v0.1.0-shipped-pre-push.md`](HANDOFF-2026-06-04-content-composer-01-v0.1.0-shipped-pre-push.md)) — Drained the full C4→C18 chain from the C3 foundation pause. The multi-step content-authoring **shell** (category `media`): one JSON `ComposerConfig` per content type drives the steps; each step's slot is rendered by a substrate behind a **uniform `SlotHandle`** — `metadataFields`→json-form (controlled `values` + custom `tags`/`author-picker` field renderers + hydration seam), `bodySlot`→article-body-01/Plate (lazy) or `<Textarea>`, `mediaSlot`→media-editor-01 (1:1 dials, `mediaSources` clamp, pull-only export). Shell owns step nav (forward-gated/backward-free), blocking gates, autosave, draft→publish→schedule FSM, lazy upload-on-publish, per-type adapter (`ComposerDraft` ↔ `ContentCardItem`). **News ships first** (adapter registered under `news-content-item`; 5 steps); **post** is modeled-but-deferred (clamp proof `["upload","library"]`→`["upload"]`; backend behind media-editor v0.2). **Architecture refinements over the plan (each a correctness win):** (1) **draft-level dirty** (`draft !== savedDraft`) not handle-aggregate — single correct signal under single-step mounting AND structurally averts the #1 Plate-autosave-loop trap; (2) **value-based gating** against the always-current draft (news has 3 metadataFields steps + only the active step mounts → the plan's slot-kind-keyed handle gate would read stale handles); active metadata step still gets full json-form `trigger()+isValid()`; (3) **media export pull-only at step-leave** (hero step unmounted by publish; blob captured to a `Map<stepId,Blob>`, uploaded lazily) — `SlotHandle` gained an optional media-only `export?`; (4) **substrate mounts are dedicated `*-substrate.tsx` components** (render() is a plain fn → hooks need a component); custom field renderers extract the hook body into an uppercase component. Commits `a97fe0a..8e1bff4` (14) on top of the 4 prior gate/foundation. Gates: tsc 0 / lint 81-22 baseline / meta-deps 52-52 / build ✓ (61 pages) / registry:build ✓ (33 base + 1 fixtures; no demo/usage/meta). GATE 3 **Pass with follow-ups** — 5 Low/Med deferred: F-01 (Med, v0.1.1) post-deploy consumer-tsc smoke (2nd inter-procomp install → expect F-cross-13 sub-traps, 4-ship pattern) · F-02 non-active-step metadata gate is required-presence-only · F-03 (v0.1.1) inline body-image upload unwired (ExportMetadata mismatch) · F-04 (v0.2) `export?` leaks into the uniform `SlotHandle` · F-05 (v0.2) video re-edit blob re-attach (news photo-only). **Push is user-gated** (per the C3-pause handoff). Prior gate-1-2 decision: [`2026-06-04-...-gate-1-2-and-foundation.md`](decisions/2026-06-04-content-composer-01-gate-1-2-and-foundation.md).
- **✅ 2026-06-03 — media-editor-01 v0.1.2 — GATE 3 follow-up batch SHIPPED + PUSHED (commit `95b21ae`)** ([decision](decisions/2026-06-03-media-editor-01-v0.1.2-followup-fixes.md), [v0.1.2 addendum on the v0.1.1 review](../docs/procomps/media-editor-01-procomp/reviews/2026-06-03-v0.1.1-spotcheck.md), handoff [`HANDOFF-2026-06-03-media-editor-01-v0.1.2-gate3-followups.md`](HANDOFF-2026-06-03-media-editor-01-v0.1.2-gate3-followups.md)) — Resumed the v0.1.1 spotcheck's v0.1.2-tagged follow-ups. **F-01** (⚠️): `onModeChange` + `onEditAction` were declared-but-dead → wired. `onModeChange` fires on every mode transition (effect-driven). `onEditAction` emits the navigation + lifecycle subset — `mode-change` / `tool-open` / `tool-close` (effects) + `reset` (central `performReset`); fine-grained content-mutation actions documented as v0.2 (union already complete for exhaustive switches). **F-02** (⚠️): `renderPermissionDenied` threaded into `EditorCamera`, replacing the built-in prompt for the **denied** state only. **F-06** (🔸): pan-zoom keyboard moved from `window` to the `targetRef` element; `EditorCanvas` made focusable (`tabIndex` + `role` + aria + focus-visible ring) so keys are focus-scoped to the canvas. **F-04** (🔹): bottom-overlay JSX re-indented (two uniform `-4` blocks; anchor-verified whitespace-only script since the repo has no formatter). Housekeeping: dropped the `as unknown as` touch cast by broadening `handleStageMouseDown` to `KonvaEventObject<MouseEvent \| TouchEvent>`. **Patch bump (v0.1.1 → v0.1.2)** — declared surface made functional + internal hook refinement, no public-API add/remove → no new GATE 3 (readiness-rule exemption). `registry:build` also corrected a **stale committed `story-composer-01.json`** (prior session's SC-08 dead-code removal hadn't been re-built into the artifact). All gates green: tsc 0 / lint 81-22 baseline / meta-deps 51-51 / registry:build / `pnpm build`. Lesson: render props that pass ref-reading callbacks trip `react-hooks/refs` even though the ref is only read on consumer invocation — hoist the body + scope-suppress (the `<CameraPermissionPrompt onUsePicker>` prop form isn't flagged, confirming the false positive). Scoping `onEditAction` to the effect-derivable navigation/lifecycle subset (vs all ~16 kinds) is the right patch-altitude call — wiring every kind is v0.2 per-mutation work. Still open → v0.2: imperative capture handle, C17 labels flattening, library media source, fine-grained `onEditAction` events.
- **✅ 2026-06-03 — media-editor-01 v0.1.1 + story-composer-01 v0.2.1 SHIPPED + PUSHED (walkthrough fixes + IG chrome model + 4-agent deep review)** ([decision](decisions/2026-06-03-media-editor-01-v0.1.1-walkthrough-fixes.md), [GATE 3 v0.1.1](../docs/procomps/media-editor-01-procomp/reviews/2026-06-03-v0.1.1-spotcheck.md), handoff [`HANDOFF-2026-06-03-media-editor-01-v0.1.1-story-composer-01-v0.2.1-SHIPPED.md`](HANDOFF-2026-06-03-media-editor-01-v0.1.1-story-composer-01-v0.2.1-SHIPPED.md)) — Three arcs in one day. **(1) Walkthrough-fix sweep** F-06…F-13 (F-12 done this cycle): edit-only validator gate, capture-surface layout, dialog a11y + centering + viewport-relative aspect sizing, takePhoto object-cover crop, native-res crop Apply + full crop/drawing/stickers/filters/text wiring, gesture-gated camera. **(2) User-driven visual iteration → Instagram chrome model:** single-pointer drag-to-pan (hit-test yields to overlays); mode tabs capture-only → back-to-capture arrow in edit stage (✕ hidden, replaced by Back via `showClose`; Publish hidden during photo/video capture via `showPublish`); bottom edit tools overlay a full-bleed canvas (scrim, click-through); container-query-sized shutter/controls; dialog/inline min/max size **clamp** so the surface can't collapse. **(3) 4-agent deep review** (code/wrapper/docs/registry) → registry clean; fixed pan-start jump + shouldStartPan type + honest handle-deferral comments; removed dead code in story-composer (unreachable `composer-shell.tsx` + registry entry, `done` overlay arm, no-op effect, redundant casts, orphaned imports); **caught + fixed a meta-deps regression** (removing composer-shell orphaned the `dialog` dep → dropped from meta + registry, resolves transitively via media-editor-01); full doc alignment (both metas, both guides, description, usage ×2, registry descriptions) + new v0.1.1 review file. Commits `2030b08` (media-editor v0.1.1) + `3dd9bdd` (story-composer v0.2.1) + docs. All gates green: tsc 0 / lint 81-22 baseline / meta-deps 51-51 / registry:build / `pnpm build`. Verdict Pass-with-follow-ups. Open: v0.1.2 (wire/remove onModeChange+onEditAction, thread renderPermissionDenied, focus-scope pan keyboard, formatter pass) + v0.2 (imperative capture handle, C17 labels flattening, library media source). Key lessons: visual walkthrough is the de-facto gate (every chrome issue was invisible to tsc/lint/build); clamp the aspect *driver* dimension to floor size without breaking ratio; container-query (`cqw`) not `vw` for controls inside a dialog; full-bleed canvas + overlaid controls (mirror the camera shutter) beats a shrunken card; re-run meta-deps after removing any shipped file (file removal cascades to orphaned deps + imports).
- **✅ 2026-06-02 — media-editor-01 v0.1.0 EXTRACTION + story-composer-01 v0.2.0 WRAPPER SHIPPED PRE-PUSH (51st procomp; cross-procomp registry-dep convention established)** ([decision](decisions/2026-06-02-media-editor-01-v0.1.0-extraction-and-story-composer-01-v0.2.0.md), [GATE 3 media-editor-01](../docs/procomps/media-editor-01-procomp/reviews/2026-06-02-v0.1.0-spotcheck.md), [GATE 3 story-composer-01](../docs/procomps/story-composer-01-procomp/reviews/2026-06-02-v0.2.0-spotcheck.md), handoff [`HANDOFF-2026-06-02-media-editor-01-v0.1.0-and-story-composer-01-v0.2.0-CLOSED.md`](HANDOFF-2026-06-02-media-editor-01-v0.1.0-and-story-composer-01-v0.2.0-CLOSED.md)) — 32-commit chain across Phase A (extraction C1–C15 + R1–R4 backfill) / Phase B (wrapper refactor C16–C19) / Phase C (GATE 3 reviews + close C20–C22). **media-editor-01 v0.1.0 alpha** is the reusable Konva-based capture + edit surface lifted out of story-composer-01 v0.1.5: 38 sealed-folder files + 1 fixtures item; 4 capability dials (enabledModes / enabledTools / mediaSources / aspect); 22-method imperative handle; InitialSource intake with 5 SourceError kinds; polymorphic export() with format dispatch + video perf-shortcut; multi-instance + empty-state footgun guards; inline/dialog/auto presentation with aspect-derived dimensions; full v0.1.5 UI parity restored in R1-R4 backfill (EditorCamera + 5 tool panels + DiscardConfirmDialog + TextOnlyCanvas + undo/redo). **story-composer-01 v0.2.0 alpha** is the 245-line thin wrapper (was 1145 lines in v0.1.5): public API preserved 100% (73-name C2 snapshot resolves through v0.2.0 barrel via tsc-level probe; all 32 v0.1.5 props accepted (5 publish-related handled by wrapper-internal publish flow; 26 forwarded to MediaEditor01 1:1 or transformed; 1 documented semantic gap — editorBackground not forwarded, F-01 v0.2.1)); 14-method handle wraps editorRef + adds publish/exportBlob; useStoryComposerState refactored to compose useMediaEditorState + augment with publishStatus/uploadProgress/publishError as strict-superset return-shape; @ilinxa/media-editor-01 declared as registryDependency (FIRST inter-procomp install path in the library); konva + react-konva drop from npm deps (resolve transitively). All Q-Ps locked at GATE 1 (aspect="free", enabledModes all-on, jpeg q=0.9, CORS-fires-onInitialSourceError, multi-instance dev-warn). Both GATE 3 reviews verdict Pass-with-follow-ups (8 open / 2 closed-in-review across both procomps). Coordinated v0.3.0 cohort: media-editor F-02 (add setMode to handle) + story-composer F-03 (drop loadState workaround) + media-editor F-03 (drop StoryComposer01Labels shim). v0.2.1 cohort: story-composer F-01 editorBackground + story-composer F-04 planning docs amendment + media-editor F-01 history.execute() wrapping. **Biggest executional lesson:** planning docs need to enumerate UI mount points alongside type/state surfaces — Phase A's plan optimistically assumed UI was complete at C8; C15 review caught the gap; R1–R4 backfill added ~22 pieces of state and mounted all the parts (~4 commits, ~1500 lines). Pre-push tip `d5eaf27`; final tip recorded at push.
- **✅ 2026-06-02 — content-card-news-01 v0.3.0 SHIPPED (A+ backend-shaped editorial features + 8-commit post-ship visual-review polish arc; matches post-card-01 v0.3.2 trait set; 20 commits total)** ([decision](decisions/2026-06-02-content-card-news-01-v0.3.0-backend-shaped-editorial-features.md), [GATE 3 review](../docs/procomps/content-card-news-01-procomp/reviews/2026-06-02-v0.3.0-spotcheck.md)) — Strictly additive on v0.2 (31 new optional `ContentCardItem` fields + ~50 new props + ~49 labels; zero v0.2.x consumer breakage). 14 A+ traits land: `viewerMode: "editor"|"viewer"` opt-in two-mode toggle + 19-capability `ContentCardPermissions` matrix + `canPerformAction(action, item)` universal predicate + moderator section + 16 mutation handlers + kebab system with role-aware defaults + paywall gate (excerpt+media; CTA polymorphic via `linkComponent` when `ctaHref` set) + sensitive content gate (media-only; contentWarnings list) + editorial badge stack (frozen priority: Breaking→Live→Exclusive→Featured→Pinned→Sponsored→status) + quoted-article recursive mini-card with strip helper + light engagement counts + 11-method imperative handle + per-variant feature matrix enforced at part level (small variant trims to highest-priority badge + sensitive gate only). 13 new files (2 lib + 11 parts including shared NewsKebab); 12 edited including all 5 variant parts + root component with local-mirror + types (117→866 lines) + index (28 sub-exports) + meta + registry.json (10→24 files) + dummy-data (+7 fixtures: breaking/paywall/sensitive/draft/sponsor/quoted/editor's-pick) + demo (+5 new tabs: Editor mode / Paywall / Sensitive / Quoted / Engagement) + guide.md (v0.3 section with 8 usage patterns) + format-default.ts (toDate accepts number). **User amendments folded:** (1) engagement-bar-01 detail-page composition pattern via `renderEngagementCounts` slot — engagement-bar-01 NOT a peer dep; engagement data shape on ContentCardItem matches engagement-bar-01's counts shape for drop-in slot composition (description §6.2); (2) `related-articles-ribbon-01` queued as sibling component for detail-page layouts. F-S1 lock applied for cross-procomp imports (`CommentMenuItem` via RELATIVE `../comment-thread-01/types`). F-cross-13 defensive `<DropdownMenuTrigger>` direct-child pattern in NewsKebab. All gates green: tsc + lint + meta-deps + registry:build (24 files match disk roster, manual audit clean per 2026-05-30 BLOCKER pattern) + pnpm build (59 static pages). Backward-compat drop-in test verified. GATE 3 Pass-with-follow-ups: F-01 post-push smoke (Med, v0.3.1 expected per `project_smoke_harness` 4-ship pattern) + F-02 Live badge/LiveUpdateLine double pulsing dot (Low, v0.3.1) + F-03 working-as-designed soft-compat note + F-04 openKebab handle stub (Low, v0.4). **Refinement locked:** `defaultContentCardKebabActions` uses single object-args signature instead of post-card-01's positional 8-arg form (Q-P29 refinement) — non-breaking expansion for future bumps. **Post-ship visual-review polish arc (8 commits between GATE 3 and push):** `02a255f` small sensitive-gate compact mode (drops heading + warnings + shrinks 44px button → 24px Show pill; fits 96×96 thumb); `b5bb2fa` small article `items-start` defense against grid-row stretch + sensitive demo `grid-cols-2` → `space-y-6` (medium + small wildly different heights); `7000a21` quoted-article inner variant medium → small + all gates suppressed (paywall/sensitive) + flatten chrome via className + quoted demo `space-y-6`; `b9c5447` **paywall scope correction (load-bearing)** — initial impl wrapped entire `paywallContent` so `paywall.preview` overlapped badges + whole card blurred; restructured so gate wraps just media block, `paywall.preview` substitutes for `item.excerpt` per Q-PG (title + author + footer stay visible); all 4 variants refactored (featured className="absolute inset-0" + content overlay z-20; large/medium wrap just media in flow; list wraps excerpt); `3ddbb7a` medium badge stack `left-2` → `right-2 max-w-[75%]` (was colliding with category chip at `left-4`); `558954c` quoted-article inner `disableBadgesRender` (EXCLUSIVE badge was overlapping title in compact preview); `444b9db` badge uniform shape `BADGE_BASE` const + saturation hierarchy (vivid Breaking/Live → accent Exclusive/Featured → subtle Pinned/Sponsored/Status); `9eab176` **badge placement split** — `NewsBadges` gains `group: "all" | "state" | "curation"`, medium variant renders TWO instances (state at top-right overlay + curation as kicker above title per newspaper convention). All 8 commits strictly additive — zero v0.2.x consumer break. Plan + description amended with Q-P32/Q-P33/Q-P34/Q-P34b/Q-P42b/Q-P42c locked refinements + Q-PG addendum + Q-PC2 split. GATE 3 review file appended with the post-ship findings-closed section. Unpushed; awaits master push + Vercel auto-deploy.
- **✅ 2026-06-01 — story-composer-01 v0.1.5 patch + doc-alignment sweep SHIPPED** ([decision](decisions/2026-06-01-story-composer-01-v0.1.5-photo-preview-and-arrow-keys.md), handoff [`HANDOFF-2026-06-01-story-composer-01-v0.1.5-session-close.md`](HANDOFF-2026-06-01-story-composer-01-v0.1.5-session-close.md)) — Two commits on top of the v0.1.0→v0.1.4 same-day arc. **`721fa75`** runtime patch: (1) photo preview no longer flashes black between shutter and editor — Suspense fallback now renders `<img src={draft.url}>` instead of a black div; matching underlay `<img>` inside ComposerEditor bridges the gap until Konva's `useImage` + Stage init resolve (gated on `!image` so applied filters don't bleed-through); deeper canvas-direct path deferred to v0.2; (2) keyboard arrow pan direction inverted in `usePanZoom` — flipped to moving-the-image semantics so ArrowRight moves image right (no public API change); (3) `demo.tsx` swapped all 5 tabs onto a shared `demoUploader` (fakes 1.5s success returning `URL.createObjectURL(blob)`) since `SAMPLE_UPLOAD_URL` (= `https://example.com/upload/story`, a non-POST endpoint) was making every Publish hit the working error path. **`b647ca4`** doc-alignment sweep: caught 8 drift points (guide banner v0.1.0→v0.1.5; install count 35→37; "Edit tools all enabled by default" → table with Default column flagging Crop opt-in since v0.1.4; NEW pan + pinch-zoom section in guide.md AND usage.tsx since v0.1.3 was undocumented; meta.ts features wording + new pan-zoom bullet; registry.json description matched; usage.tsx Photo line + new pan-zoom section). Patch-bump, no GATE 3 required. Tsc + lint + validate:meta-deps + registry:build all clean. Registry roster manually audited (37 base + 1 fixtures aligned with disk).
- **✅ 2026-06-01 — story-composer-01 v0.1.0 → v0.1.4 SHIPPED (50th procomp; full ship + 4 patch arc in one day; story-system trilogy closes)** ([decision](decisions/2026-06-01-story-composer-01-v0.1.0-first-ship.md)) — Greenfield ship via user request "create new story panel … directly opens camera … gallery … text editor … brightness/contrast/saturation … save/publish". 15-commit chain landed + 1 v0.1.1 smoke patch (`fb18b90`); smoke harness path-b consumer-tsc now ✅ green (36/36 files install + 0 consumer errors). 15-commit chain (C1 scaffold + C2 types + C3 shell/state/mode-pill + C4 camera + C5 video record/trim + C6 Konva editor with SSR-safe React.lazy boundary + C7 adjust/filters + C8 text + C9 stickers + C10 draw + history + retro-wire + C11 crop + C12 publish/uploader/composite-video bake-in + C13 text-only mode + a11y polish + guide + C14 registry roster + manual audit + C15 GATE 3 spotcheck Pass-with-follow-ups). 40 disk files, 36 base + 1 fixtures via registry, 2 new peer deps (`konva ^10.3.0` + `react-konva ^19.2.4`), 6 shadcn deps (alert-dialog/button/dialog/popover/slider/toggle-group). Full Instagram parity per user lock — three capture modes (photo + video + text), six edit tools (text/draw/stickers/filters/adjust/crop with 36 built-in SVG-data-URL emoji stickers + 10 Instagram-style preset filters with pre-rendered thumbnails), undo/redo on every command + Ctrl/Cmd+Z bindings, mobile-fullscreen / desktop 400×711 9:16 modal, XHR FormData uploader + signed-URL escape hatch, discard-confirm guard, live-region SR announcer, 15-method imperative handle. Q-Ps locked to recommendations 10/10. F-cross-13 defensive pre-wires applied (Popover direct trigger, no asChild). SSR boundary solved via React.lazy + Suspense (registry rule bans `next/dynamic`). Performance was the rotating-dim spot-check — surfaced 2 v0.1.x perf follow-ups (F-01 adjust-slider re-cache thrashing → RAF-throttle; F-02 drawing-stroke array-clone signal → counter ref). Review pre-push fixes landed: F-03 expanded index.ts barrel from 1 component + 24 types to also include 5 parts + 3 hooks + 5 lib helpers + 4 default tokens + 2 helper types = 14 newly-promised exports; F-04 STATUS.md catalog 49→50 + Components table row + this Recent activity pointer; F-05 `pnpm build` confirmed clean (59 static pages, RSC + React.lazy + Konva boundary holds). **v0.1.1 patch (`fb18b90`)** — first-pass smoke surfaced 3 F-cross-13 sub-traps against older consumer shadcn primitives: `radix-ui` namespace import not portable → use shadcn's PopoverTrigger directly (no asChild); `showCloseButton` prop is producer-side-only → CSS-hide via `[&_[data-slot=dialog-close]]:hidden`; `size="icon-sm"` producer-only Button variant → `size="icon" + size-8 className`. Re-smoke against deployed v0.1.1 ✅ green (36/36 files install + 0 consumer-tsc errors). Fourth consecutive ship following the established `ship → smoke → patch → re-smoke clean` pattern. **v0.1.2 patch (`e46778a`)** — user-reported Radix a11y console warning ("Missing Description or aria-describedby for DialogContent"). Added required `ariaDescription` prop on ComposerShell + sr-only `<DialogDescription>` + new `composerDescription` label key with a default explaining the 3 modes + close affordance. **v0.1.3 patch (`75acbba`)** — user request: "we must be able to pan and scale the visual content (2-finger touch + mouse/keyboard for PC)". New `usePanZoom` hook + Konva Stage scale/position wired. Touch 2-finger pinch+pan (single-finger drag still goes to text/sticker/drawing as before); desktop Ctrl/Cmd+wheel zoom anchored to cursor + arrow/+/-/0 keyboard. Min 1× / max 4×. Disabled during drawing (pointer-pipeline conflict) + cropping (DOM overlay can't follow stage transform). Reset-zoom CTA top-left when not at identity. Drawing-coord fix: `stage.getPointerPosition()` → `stage.getRelativePointerPosition()` so strokes stick at any zoom. **v0.1.4 patch (`c64df7a`)** — 3 user-reported issues fixed atomically: **(a)** PC Ctrl+wheel was zooming the whole browser — dropped Ctrl requirement; plain wheel over canvas zooms via native non-passive listener (React's `onWheel` is passive in modern browsers so `preventDefault` was a no-op); new required `targetRef` option on `usePanZoom`. **(b)** Crop tool removed from default `enabledTools` — stories are 9:16-locked by platform convention; consumers explicitly opt in for post-style composers. **(c)** Camera flicker root cause: photo mode was requesting mic permission (`audio: true` always), and if user denied mic while granting camera, `getUserMedia` rejected with `NotAllowedError` → status flipped to "denied" → auto-retry effect re-fired → infinite loop. Two fixes: renamed `recordAudio` option → `requestAudio` with default flipped `true` → `false`; only video mode requests mic via `requestAudio = mode === "video" && recordAudio` derivation. AND bounded the auto-retry effect via `autoRetryAttemptedRef` (fires at most once per granted-state transition; resets when status reaches "ready"). Mount effect deps on `requestAudio` so photo↔video re-acquires with the correct constraint. Final v0.1.4 verification: tsc + lint + meta-deps + registry:build + `pnpm build` (59 static pages) all clean. 20 commits total in single-day arc (15 chain + 5 patches). Closes the story-system trilogy: rail (discovery) + viewer (consumption) + composer (creation).
- **✅ 2026-05-30 SESSION CLOSE — story-viewer-01 v0.4.0 → v0.4.4 cube/swipe/readiness/docs arc (5 versions, single day)** ([decision](decisions/2026-05-30-story-viewer-01-v0.4.x-cube-swipe-arc.md)) — Continuation of the morning's v0.3.9 close. **v0.4.0** Instagram-canonical 3D cube transition between stories — pure CSS (Tailwind v4 `perspective-distant` + `@container` + `transform-3d` + `backface-hidden` + `translateZ(50cqw)` inline), no JS width measurement, no framer-motion. New hook `useCubeTransition` (internal) + new part `StoryCubeFace` (static ghost render). Mid-render `setState` pattern detects story-level cursor changes in the SAME React commit — no 1-frame flash. Angle driven via CSS variable + callback ref (no React renders per frame). New opt-outs `disableStoryTransition` + `storyTransitionDurationMs`. **v0.4.1** finger-following swipe gesture — pointer drag drives angle 1:1 at half-width=90°, prev+next ghosts mounted during drag, commit threshold 30% width OR 0.5 px/ms velocity, ×0.25 boundary resistance at first/last story. Conflicts handled: long-press cancelled on drag intent, tap-zone clicks suppressed via `swipeJustEnded` flag + `onClickCapture`. Mobile-fullscreen sizing fixed via Tailwind v4 `!` important suffix (shadcn's `sm:max-w-sm` was floating the modal at 384px on intermediate widths). Bundled docs page `overflow-x-hidden` + `wrap-break-word` for long feature bullets. Stability fixes: infinite-loop in `useEffect` dep on full `cube` object (destructured to stable `forceIdle` ref); tap-zones `aria-hidden` + focused-child (mousedown preventDefault). **v0.4.2** cube-engagement scale-jump fix — one-line CSS (prefix rotator with `translateZ(-50cqw)`) so front face lands at world z=0 (natural perspective plane) at idle — scale 1.0× at engagement (no jump from idle), shrinks DOWN to ≈0.857× as it rotates to ∓90°. **v0.4.3** full-component readiness review — 5 drift findings closed: 🚫 BLOCKER `registry.json` missing the 2 v0.4.0 cube files (would have broken consumer installs), `meta.context` + `registry.json.description` stale ("framer-motion v0.2 gate"), feature-count drift (1→7 vs actual 9 slots; 8 vs actual 12 opt-outs), `index.ts` barrel missing 17 public-API types, `usage.tsx` documented only v0.1. **v0.4.4** docs + demo alignment — `guide.md` major refresh (drop framer-motion claim from "5 rules"; rewrite engagement section for v0.3.5 collapsed-column + kebab-in-header; drop bookmark; extend slot table to 9 + opt-out table to 12; add v0.3.0/v0.3.1/v0.3.8/v0.3.9/v0.4.0/v0.4.1 sections; retitle "What's NOT in v0.1" → "Still out of scope (as of v0.4)" with a "now shipped" subsection); `demo.tsx` Multi-story tab renamed "Cube + swipe" with explainer + `disableStoryTransition` checkbox + `storyTransitionDurationMs` slider. Older entries trimmed per the lean-snapshot convention (kept ~5 most-recent). Recently trimmed but still meaningful: 2026-05-30 story-viewer-01 v0.4.0 → v0.4.4 cube/swipe/readiness/docs arc ([decision](decisions/2026-05-30-story-viewer-01-v0.4.x-cube-swipe-arc.md)); 2026-05-29 story-viewer-01 v0.2.0 → v0.3.9 rapid-iteration arc ([decision](decisions/2026-05-29-story-viewer-01-v0.3.x-arc-comments-share-link-drawer-heart-toggle.md)) + v0.3.0 comments-panel ship ([decision](decisions/2026-05-29-story-viewer-01-v0.3.0-comments-panel-and-bookmark-removal.md)); 2026-05-28 post-card-01 v0.3.0 + comment-thread-01 v0.2.0 ships ([decision](decisions/2026-05-28-ilx-3-and-ilx-4-moderator-and-edited-comment.md)); cms-panel-01 GATE 1 in-flight (surfaced via the active handoff banner above); library tier system charter ([decision](decisions/2026-05-25-library-tier-system-charter.md)). All entries since 2026-05-09 still live at [`.claude/decisions/`](decisions/) — every shipped component, gate closure, or substantive decision has its own dated file. Slug-grep is the search interface.

---

## How to update this file

`STATUS.md` is the slim snapshot. **Don't extend it with verbose entries.** Per-decision detail lives in `.claude/decisions/<date>-<slug>.md` (queryable by YAML frontmatter); STATUS.md is the index + the snapshot. The 2026-05-09 + 2026-05-25 trims were both restorations of this rule, not exceptions.

| When something happens | Where it goes |
|---|---|
| Component ships / version bumps / status changes | Update the Components table row + author a `.claude/decisions/<date>-<slug>.md` file |
| Sweep phase closes / cross-cutting finding closes | Author a decision file; update [`docs/reviews/sweep-tracker.md`](../docs/reviews/sweep-tracker.md); add a one-line "Recent activity" pointer above (keep ~5 most-recent only) |
| New TODO / Open decision lands | Add a one-line bullet in "Open decisions / TODOs" with a decision/tracker link — do NOT inline the decision content |
| Something old gets closed | Either strike inline if recent + relevant, OR drop the line entirely (the decision file is the source of truth) |
| Intro banner urge | **Resist.** No more banner-blockquote stack at the top. Banners ARE the changelog; they belong in decision files + Recent activity, not the snapshot |

The "Recent activity" pointer list stays at ~5 entries (most recent first). Older entries are still in `.claude/decisions/` — not removed, just not surfaced in this index. If you need to extend Recent activity past 7 entries, trim back to 5 instead.
