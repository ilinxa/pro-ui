# Readiness Loop config — ilinxa-ui-pro

<!-- Per-repo mechanics for the feature-readiness-loop skill. Every command is copy-paste
     runnable from the repo root. Update this file whenever a command, port, doc location, or
     flake changes — that update is part of that change's R6. -->

## meta
- config_version: 1.0.0
- updated: 2026-08-11
- repo: ilinxa-ui-pro (Next.js 16 docs site + shadcn registry; the library lives in `src/registry/`)
- workspace: single package, pnpm 10.x

## gates
| # | Gate | Command | Blocking | Green looks like |
|---|---|---|---|---|
| 1 | typecheck | `pnpm tsc --noEmit` | yes | exit 0 |
| 2 | lint | `pnpm lint` | yes | 0 errors; exactly the 9 known warnings (virtualizer compiler-skips ×4, exhaustive-deps ×3, unused var ×1, unused disable ×1) — a 10th warning is a finding |
| 3 | meta-deps | `pnpm validate:meta-deps` | yes | exit 0 (incl. reverse-npm) |
| 4 | registry validators | `pnpm validate:default-registry-whitelist; pnpm validate:registry-json; pnpm validate:naming` | yes | exit 0 each |
| 5 | doc validators | `pnpm validate:doc-drift; pnpm validate:doc-budget` | yes | exit 0 each — **doc-drift also gates at R6** after docs edits |
| 6 | registry build | `pnpm registry:build` | yes | all validators + `shadcn build` → `public/r/*.json` regenerated, exit 0 |
| 7 | app build | `pnpm build` | yes | Next build exit 0, all routes compiled |

## test-tiers
- **No unit-test runner exists** (informed defer; trigger = first pure-lib bug — see STATUS.md).
  R3 = gates 1–7 above; **R5 carries the proof burden** for behavior.
- package-scoped (R2 fast feedback): `pnpm tsc --noEmit` + targeted validator for the touched area
- full suite (R3): gates 1–7 in order
- e2e (R5): consumer smoke harness at `e:/tmp/ilinxa-smoke-consumer/` — real `pnpm dlx shadcn add`
  installs + consumer `tsc` (0 errors bar), both backends (Radix + Base UI variants); docs-site
  browser verification via stealth-browser MCP

## env-prep
1. Never clear the Turbopack cache while `pnpm dev` runs — stop server → clear → restart (RocksDB lock corruption otherwise).
2. Smoke installs: after EVERY `shadcn add`, re-align consumer `package.json` to producer truth (CLI 4.6.0 shuffles version specs across names — silent corruption).
3. For registry e2e against the deployed site: never poll `/r/*.json` at <60s intervals (Vercel bot-mitigation → 403). Prefer serving `public/r/` locally (e.g. `npx serve public` or file-copy) for spike/pilot loops.
4. Virtual browser: Docker container `virtualbrowser-stealth-browser-1` must be running; "Missing X server" → restart the container.

## docs-map
- plan docs: `docs/plans/<slug>-plan.md` ← readiness-loop state machines (new dir, established for P3). Master phase plan: `docs/production-readiness-plan.md`.
- ADRs: `.claude/decisions/<YYYY-MM-DD>-<slug>.md` — YAML frontmatter per `.claude/decisions/README.md`; this repo's ADR equivalent. Contract/convention changes require one (R1 draft, R6 land).
- changelog: none — STATUS.md "Recent activity" (5 one-liners) + decision files serve the role.
- architecture docs: `docs/component-guide.md` · `docs/library-tiers-charter.md` · `.claude/rules/*.md` · `docs/naming-canon.md`
- per-commit audit rule: run gate 5 (doc validators) after doc edits; commit bodies carry real gate numbers.
- improvement log: `.claude/improvement-log.md` (created at first R7 retro)
- other doc surfaces: `.claude/STATUS.md` (snapshot — one-line rows, 14KB budget) · `llms.txt`/`README` catalog (GENERATED by `build:llms` — never hand-edit) · `registry.json` (hand-maintained roster — diff folder vs `files[]` before every minor push) · `.claude/skills/shadcn-registry-pro/` (update when registry conventions change) · `.claude/CLAUDE.md` (12KB budget; edit only via `configuring-project-memory` skill)

## runtime-verification (R5)
- surfaces: docs site (web) · registry artifacts (`public/r/*.json`) · consumer installs (CLI)
- how to run: `pnpm dev` → http://localhost:3000 (docs site); production check on the Vercel deployment after push
- fixtures/accounts: none (public site); component demos ship their own `dummy-data.ts`
- instrument: stealth-browser MCP for browser flows; real `pnpm dlx shadcn@latest add` from the smoke consumer for install flows; `pnpm tsc --noEmit` inside the consumer as the compile bar
- "observed" means: component mounts + documented interaction patterns exercised in a real browser (green gates ≠ verification — project precedent); install e2e = add → consumer tsc 0 → render. Negative paths are mandatory (e.g. base-without-feature must compile AND render).

## conventions
- commits: conventional-style `type(scope): summary` — scope vocabulary includes phase tags (`feat(P3): …`, `fix(F-cross-13): …`, `docs(plan): …`). Body carries real gate numbers. Claude co-author trailer per harness rules.
- branches: work lands on `master` directly; **push = deploy = installable**, so GATE 3 review must pass BEFORE push.
- commit timing: base commit at end of R6 (code+tests+docs) per skill default; push deferred to R7 after GATE 3 verdict is recorded. Never amend a pushed commit.
- review fixes: fold into the unlanded base commit; on published work, separate `fix(<scope>):` commits.
- standards docs: `.claude/rules/readiness-review.md` (GATE 3 — this repo's R4 has a REQUIRED review-file artifact at `docs/reviews/` or the artifact's `reviews/` folder, verdict ≥ Pass-with-follow-ups) · `.claude/rules/compound-component-structure.md` · `.claude/rules/subagent-model-policy.md`
- repo-specific verification rules: read-the-source for any version/slug/path claim (naming canon renamed 52 slugs 2026-08-11 — older docs/memories carry dead slugs); registry code never imports `next/*`.

## known-flakes
- Consumer `pnpm add` fails oddly mid-smoke → shadcn CLI 4.6.0 corrupted consumer package.json on an earlier add → re-align versions to producer truth, retry. **RESOLVED on CLI 4.17.0** (P4.2 matrix 2026-08-12, 6 clean diffs) — applies only when pinning old CLIs.
- Doc validator fails with phantom slugs / generator thrashes on unchanged inputs → a Windows tool rewrote a doc file with CRLF (Edit-tool does this sometimes; core.autocrlf=true) → both scripts now `\r\n`-normalize on read (P4 F-1/F-4); if a NEW script compares or regex-scans doc files, normalize `\r\n` first.
- CLI rejects components.json as "Invalid configuration" → UTF-8 BOM (PowerShell 5.1 `Set-Content -Encoding utf8` writes BOM) → write config files with the Write tool or `[IO.File]::WriteAllText` with BOM-less UTF8.
- Consumer tsc fails on `@base-ui/react` / `@radix-ui/*` / `class-variance-authority` after installs → the CLI does NOT add installed primitives' own npm deps → `pnpm add` them post-add (cascades as fake implicit-any errors in registry code — fix deps first, re-judge).
- Container browser (`shadow-browser`, NOT the old `virtualbrowser-…` name) can't reach a host dev server → per-port Windows-firewall block (no elevation available) and/or port 3000 occupied on 0.0.0.0 by an unrelated host service → run `next dev -H 0.0.0.0 -p <free port>`; if still blocked, host-side Playwright in the scratchpad is the proven R5 instrument (`next.config.ts` already carries `allowedDevOrigins`).
- 403s fetching deployed `/r/*.json` → Vercel bot-mitigation on fast polling → wait 60s+ or use local `public/r/` copy.
- Dev server dies with RocksDB/lock errors → Turbopack cache cleared while server live → stop, clear, restart.
- Glob/ripgrep timeouts at repo root → node_modules scan on Windows → scope patterns to `src/`, `docs/`, `scripts/`.
- `validate:meta-deps` misses an import → side-effect/`import x =` shapes → keep `from`-imports first in registry files.

## agent-orchestration rules (earned 2026-08-11 P3 + 2026-08-12 P4 loops)
- **Agent briefs must mandate FOREGROUND-ONLY execution** — "never background or monitor a
  command; use long timeouts (300s+) instead". P4: two agents stalled 3× by backgrounding
  installs/gates and stopping to "wait" — each stall costs a coordinator round-trip, and a
  harness restart orphans backgrounded work.
- **R5 / design audits run against `pnpm build` + `next start`, NEVER the dev server** — the
  Turbopack dev compile-worker farm (~17 node procs / ~2.3GB) plus multi-route Playwright plus
  parallel consumer installs nearly crashed the machine (P4). Don't overlap heavy browser
  audits with install-agent waves.
- **Parallel agents share ONE working tree: git state-changing commands (stash/checkout/reset) are FORBIDDEN in agent briefs.** Two agents ran `git stash` mid-P3; one scooped ~90 files of concurrent work and restored via unconditional checkout (recovered, but a real clobber window existed). Put the prohibition in every implementer prompt.
- Coordinator owns registry.json / package.json / scripts / types.ts; implementers emit JSON blocks via handoff files; coordinator merges + re-runs the central battery (P1/P1.5 method — held up well).
- Size estimates from source bytes must model JSON-wrap overhead (~+13% built artifact vs raw source) before committing to KB bars.

## cost-constraints
- **Subagent model policy (HARD RULE, `.claude/rules/subagent-model-policy.md`):** Sonnet 5[1m] or Opus 4.8[1m] only; **NEVER Opus 5** (ignores project rules). In harnesses exposing only aliases: `sonnet` is acceptable (→ Sonnet 5); do NOT use `opus` alias when it may resolve to Opus 5.
- No scheduled/cron agents in this project (user directive).
- Adversarial verify passes are never downgraded to cheap tiers; bulk mechanical work may use cheap tiers only with coordinator re-verification.

## parked
- P1 follow-up cohorts (review §0 ledger: composer re-export-on-re-edit, media-editor text-export webfonts, §6/§7 remainders) → P3.4 fix-on-touch channel.
- cms-panel-01 GATE 1 awaiting sign-off + 10 open questions (May handoff) — resumes post-P1 by explicit user call.
- Informed defers: MDX usage docs · NPM publish artifacts · unit-test runner (first test = card-tree parse→serialize fixed-point).
- User actions: DNS `ui.ilinxa.com` + shadcn directory PR (user is deciding domain/hosting; VPS migration opinion recorded 2026-08-11 in conversation — decision doc deferred until user decides).
- CLAUDE.md trigger line for this skill (customization §5) — land during a CLAUDE.md-touching R6 via `configuring-project-memory` skill; 12KB budget check required.
