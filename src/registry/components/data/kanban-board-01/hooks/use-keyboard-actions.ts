"use client";

/**
 * Reserved for future keyboard shortcuts (delete, escape, etc.) at the board level.
 * Item-level keyboard handling lives in `parts/item-shell.tsx` (Enter → click).
 * dnd-kit's KeyboardSensor handles Space/Arrow/Escape during keyboard DnD.
 */
export function useKeyboardActions() {
  // Intentionally empty in v0.1.
}
