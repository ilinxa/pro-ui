"use client";

// B1 placeholder — B3 replaces this with the full <FlowCanvas> + <Dialog> +
// <RichCard editable> wiring per plan §5.10.
export default function RichCardInFlowDemo() {
  return (
    <div className="rounded-md border border-dashed border-border p-6 text-sm text-muted-foreground">
      Demo coming in B3 — full canvas + dialog wiring per plan §5.10.
    </div>
  );
}
