# Spot-check — `gantt-timeline-01` v0.5.0

> GATE 3 readiness review for the v0.5.0 calendar-parity minor (double-click-create +
> quick-composer + cross-surface clipboard + Priority submenu). AI-assisted self-review.

---

## Header

| Field | Value |
|---|---|
| **Component** | `gantt-timeline-01` |
| **Category** | `data` |
| **Version under review** | `0.5.0` |
| **Status at review time** | `alpha` |
| **Reviewer** | AI-assisted (Claude) |
| **Date** | 2026-06-23 |
| **Git SHA** | uncommitted (built + gated) |
| **Scope** | `spot-check` |
| **Trigger** | `public-API-touching minor bump` |
| **Verdict** | **Pass with follow-ups** |

---

## Dimensions covered

### Fixed core

| Dim | Name | Verified? | Notes |
|-----|------|-----------|-------|
| 1 | Procomp planning docs | partial | meta.ts description + 3 feature lines updated; description/plan/guide v0.5.0 deltas pending (F-02). |
| 9 | Registry distribution | yes | `parts/gantt-quick-composer.tsx` added to `registry.json`; artifact rebuilt + spot-checked (composer ships, clipboard imports resolve to `../../todo-rich-card/lib/clipboard`); no demo/usage/meta shipped. |
| 10 | Meta + manifest sync | yes | meta v0.5.0 + updatedAt 2026-06-23; STATUS.md row → 0.5.0; no new deps (input/button/context-menu already declared). |
| 12 | Verification | yes (producer) | tsc 0 · lint 0 (only pre-existing TanStack warning) · meta-deps 57/57 · build 66/66 · registry:build ✓. Live consumer smoke + browser round-trip = post-deploy (F-01). |

### Rotating dimension

- **Chosen:** Composition / clipboard-interop integrity
- **Why:** v0.5.0's headline is cross-procomp interop (shared envelope from todo-rich-card) + a new composer mounted in the Root — the highest-risk surface.
- **Verified?** yes
- **Notes:** Composer is a flat-exported Tier-B part reading context (compound-rule compliant); mounted by the Root next to the edit popover (assembly stays logic-free). Clipboard listeners are document-level + gated (focus / not-over-text / not-busy), mirroring calendar-root exactly. Paste preserves dates + re-ids (no collision). Envelope round-trip logic verified 7/7 (shared module). Double-click ignores `[data-itemid]`/`[data-summaryid]` so bar-edit (openEditor) and empty-row-create don't double-fire.

---

## Smoke result

| Slug | Install | TSC | Time (ms) | Notes |
|------|---------|-----|-----------|-------|
| `gantt-timeline-01` | post-deploy | post-deploy | — | Live `shadcn add` consumer-tsc deferred to post-deploy (4-ship). Producer-tsc exercises the identical deep relative import; depth-1 `../todo-rich-card` precedent shipped. |

---

## Findings (max 5)

#### F-01 — Post-deploy live smoke owed
- **Severity:** 🔹 Low
- **Dimension:** Verification
- **Location:** n/a (process)
- **Observed:** Live `shadcn add` consumer-tsc (rewriter) + browser cross-surface round-trip not run in-env (no clean network consumer). Producer-tsc + envelope round-trip (7/7) + artifact inspection + depth-1 precedent give strong static evidence.
- **Suggested fix:** Run the post-deploy consumer smoke + browser round-trip (copy in gantt → paste in calendar/rich-card/tree) per the 4-ship pattern; patch to v0.5.1 if the rewriter mishandles the deep relative import.

#### F-02 — Procomp doc deltas pending
- **Severity:** 🔹 Low
- **Dimension:** Planning docs
- **Location:** `docs/procomps/gantt-timeline-01-procomp/`
- **Observed:** meta.ts documents v0.5.0; the description/plan/guide v0.5.0 deltas (composer, clipboard, Priority) not yet authored.
- **Suggested fix:** Author the guide delta (quick-composer + copy/paste + Priority usage) before/with push; v0.5.x doc follow-up.

#### F-03 — Composer status-pick parity vs calendar
- **Severity:** 🔹 Low
- **Dimension:** Composition integrity
- **Location:** `parts/gantt-quick-composer.tsx`
- **Observed:** gantt's composer gained a native status `<select>`; calendar's composer is title-only. Minor cross-surface composer inconsistency.
- **Suggested fix:** Either add a status pick to calendar's composer or accept the divergence (gantt benefits from it); align in a follow-up.

---

## Verdict

**Verdict:** **Pass with follow-ups**

**Justification:** All producer gates green; new surface is compound-rule compliant and mirrors the proven calendar patterns; clipboard interop verified at the logic level. Only Low follow-ups (post-deploy smoke + doc sync + composer parity) remain.

---

## Follow-up actions

| Action | Linked finding | Owner | Target version | Status |
|---|---|---|---|---|
| Post-deploy consumer smoke + browser round-trip | F-01 | author | v0.5.1 if issues | Open |
| Author description/plan/guide v0.5.0 deltas | F-02 | author | v0.5.x | Open |
| Reconcile composer status-pick parity | F-03 | author | v0.5.x | Open |

---

## Addendum — deep adversarial re-review (2026-06-23)

A second adversarial pass confirmed gantt v0.5.0 is **production-grade clean — zero blockers, zero real bugs**. Specifically verified:

- **No double-reassign:** `lib/edit-mutations.ts#addItem` inserts verbatim (does NOT re-id), so `pasteTasks`'s single `reassignTaskIds` is correct; the incrementing `at` index lands pasted siblings in source order.
- **Clipboard effect leak-free + correctly gated** (owns/overText/busy; overlays covered by `busy()` not `owns()`); double-click can't double-fire with bar-edit; `changePriority` correctly omits `onFieldEdited` (`"priority"` is not a `TodoEditableField`); compound-rule compliant (flat-exported composer, Root-mounted, no new dep — confirmed gantt meta adds no `select`/`popover`).
- **Open Low follow-ups (non-blocking, deferred):** (a) document-path `cut` on a non-deletable item writes the clipboard then no-ops the delete (a stealth copy) — non-destructive and **consistent with calendar/tree's same pattern**, so left for a family-wide hardening follow-up; (b) Priority submenu shows label only (no `color` swatch) — polish, v0.5.1.

Re-gated: **tsc 0 · lint 0 (only pre-existing TanStack warning) · meta-deps 57/57 · build 66/66**.

## Sign-off

- [x] Verdict is set above
- [ ] Smoke row copied from latest run (post-deploy)
- [ ] Tracker row updated at `docs/reviews/sweep-tracker.md`
- [x] N/A (not Needs-revision/Block)

**Reviewer signature:** AI-assisted (Claude) — 2026-06-23
