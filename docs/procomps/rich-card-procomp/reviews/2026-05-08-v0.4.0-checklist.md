# Review checklist — `rich-card` v0.4.0

> Filled 2026-05-08 (sweep session 2). Companion report: [`2026-05-08-v0.4.0-review.md`](./2026-05-08-v0.4.0-review.md). Findings referenced by `F-NN` from the report.

---

## Header

- **Component:** `rich-card`
- **Category:** `data`
- **Version under review:** `0.4.0`
- **Reviewer:** Claude (sweep session 2)
- **Date:** 2026-05-08
- **Git SHA:** `7263286`
- **Scope:** full
- **Reference:** [review-guide.md](../../../reviews/review-guide.md)

---

## 1. Procomp planning docs

- [x] description.md exists, signed off
- [x] **plan.md exists** — actually 4 versioned plan files (`-plan.md`, `-plan-v0.2.md`, `-plan-v0.3.md`, `-plan-v0.4.md`); each phase has its own plan
- [x] guide.md exists, current
- [x] description §5 archetypes match what `demo.tsx` exercises
- [x] description out-of-scope list still accurate (markdown adapter remains v0.5 deferred indefinitely)
- [ ] **plan-stage decisions still honored in code** — see **F-01**: plan-v0.3.md §6 specifies virtualization wiring; the `useTreeVirtualizer` hook was authored but never wired into the render path
- [x] guide reads consumer-shaped, not implementer-shaped (50KB consumer guide is comprehensive)
- [ ] **guide code snippets compile against current public API** — N/A (guide.md not re-read in detail this session; spot-check OK)
- [x] v0.4 components have explicit "v0.4 update" sections — yes, plan-v0.4.md is the v0.4-specific plan
- [x] N/A — greenfield component (no migration origin)

**Notes:** Phased planning is the gold standard. F-01 surfaced via plan-vs-code drift on virtualization — the plan's wiring instruction was authored as a hook but never imported.

---

## 2. Public API (`<slug>.tsx` props + `types.ts`)

- [x] Openness vs sealedness — slots / callbacks / polymorphic / public helpers extensive (53 props, 21 imperative methods, 14 validators, 2 sibling exports)
- [x] Component renders meaningfully with zero props (1 required: `defaultValue`)
- [x] Discriminated unions for variants (`defaultCollapsed: 'all' | 'none' | (level) => boolean`, `metaPresentation: 'hidden' | 'inline' | 'popover'`, `dndScopes: { sameLevel?, crossLevel? }`, ValidationResponse `{ ok: true } | { ok: false; errors }`)
- [x] `onBefore*` / interceptor chains where applicable — 3-layer validator pipeline replaces this need
- [x] Public helpers exported (`PREDEFINED_KEYS`, `RESERVED_KEYS`, `RichCardSearchBar`, `RichCardUndoToolbar`)
- [x] No `any` in public types; generics where they buy real safety (`CustomPredefinedKey` is generic-friendly, validator events are typed)
- [x] Stable-identity rules documented for callback props (validators object should be stable; documented in plan-v0.4)
- [x] Naming consistent: verbs for callbacks, nouns for slots, sibling toolbar exports (`RichCardSearchBar`, `RichCardUndoToolbar`)
- [x] Future-proof: v0.5 markdown adapter is explicitly deferred to a separate companion module; no API change required
- [x] Optional props have documented defaults (53 props, all defaults in plan)
- [ ] **Published prop is wired** — see **F-01**: `virtualize` prop is in types but not destructured or used by the root component

**Notes:** Substantial API surface (53 props) but each is justified per phase. F-01 is the only API integrity finding.

---

## 3. Component code (sealed folder)

- [x] Folder shape conforms to `data-table` + `lib/` deviation (justified; same as workspace + kanban)
- [x] Imports limited to allowlist (verified: only `react`, `@/components/ui/popover`, `@/lib/utils`, `@dnd-kit/*`, `@tanstack/react-virtual`, `lucide-react`. No `next/*`.)
- [x] No commented-out blocks or stale `// removed` placeholders
- [ ] **No half-finished implementations** — see **F-06**: `hooks/use-virtualizer.ts` is dead code; correlated with F-01
- [x] Comments only when WHY is non-obvious (`gateValidation` has a tree-treeToJsonNode comment; event-queue ref pattern is documented)
- [x] No premature abstraction — file decomposition matches feature surface
- [x] `useCallback` / `useMemo` used where stable identity matters (resolveLevelStyle, dispatchers, validators, sortableIds, all properly memoized)
- [x] Module-scope constants (DEFAULT_LEVEL_STYLES, RESERVED_KEYS, PREDEFINED_KEYS, ESTIMATED_CARD_HEIGHT)
- [x] `parts/` decomposition purposeful (26 files, each with a clear responsibility — bulk-toolbar, card-actions, card-header, card-title-edit, card, drag-handle, drop-indicator, empty-tree, field-add, field-edit, field-row, inline-error, match-highlight, meta-edit, meta-inline, meta-popover, permission-tooltip, predefined-add-menu, predefined-codearea, predefined-edit, predefined-image, predefined-list, predefined-quote, predefined-table, search-bar, undo-toolbar)
- [x] No swallowed errors (logParseErrors logs to console; validator errors fire onValidationFailed)
- [x] `'use client'` only where actually needed (root + parts + hooks; `lib/` is pure)
- [ ] **Root `<slug>.tsx` size** — 42KB / ~1100 lines. Larger than ideal but justified by the breadth of features (DnD + validators + undo + search + selection + permissions + audit + meta). Plan §3 explicitly documented this; not a finding per se, but flag for v0.5+ consideration.

**Notes:** F-06 surfaced here. Otherwise the code is dense but well-organized. The 1100-line root component is a known size compromise; phased planning kept it from being worse.

---

## 4. Demo + usage

- [x] Demo covers v0.1 viewer + v0.2 inline editor + v0.3 DnD/multi-select/permissions + v0.4 validators + undo (all toggleable in the demo UI)
- [x] Demo dataset is realistic — `RICH_DEMO` exercises every predefined key + nested children + meta + audit trail
- [x] Demo exercises v0.4 features (validators toggle, RichCardUndoToolbar wired in)
- [ ] **Demo header text** — see **F-05**: says "v0.3 demo" but component is v0.4
- [x] Usage page reads like docs (when to use, basic example, reserved keys, predefined keys, field value typing, children, state model, accessibility)
- [x] `dummy-data.ts` exported via fixtures registry item
- [ ] Both light and dark mode look intentional — **deferred** (browser validation pending)
- [x] No console errors / warnings during demo run — **inferred from clean tsc + parse error logging path; not browser-confirmed**
- [x] No lorem ipsum
- [x] `usage.tsx` is not a copy of `demo.tsx`
- [ ] **Usage import paths** — see **F-04**: uses producer-side `@/registry/components/data/rich-card` (same pattern as kanban F-04 — promote to cross-cutting)

**Notes:** F-04 + F-05 found here. Demo coverage of all four phases is impressive.

---

## 5. Dependencies

- [ ] **`meta.ts.shadcnDependencies` matches imports** — see **F-03**: `separator` declared but never imported
- [ ] **`meta.ts.dependencies` matches every external npm import** — see **F-02**: dnd-kit pins are wrong (`^11.x` vs producer's `^10.0.0`); `^N.x` is non-standard
- [ ] **`registry.json.registryDependencies` mirrors `meta.ts`** — partial; would need re-running `pnpm registry:build` to verify after F-03 fix
- [ ] **No unused deps** — `separator` is unused (F-03)
- [x] Each external package's weight is justified
- [ ] **Version pins explicit** — see **F-02**: `^N.x` notation is non-standard
- [x] License compatible (all MIT)
- [x] N/A — no internal sibling-component deps (`internal: []`)

**Notes:** F-02 + F-03 surfaced here. Dependency declaration is the weakest area.

---

## 6. Design system

- [x] Fonts: Onest / JetBrains Mono only (verified — no font imports in registry code)
- [x] Accent: signal-lime via `--primary` semantic token (used in card hover states + buttons)
- [x] Light bg: `--background` not pure-white; raised surfaces use `--card`
- [x] Dark bg: graphite-cool inherited from project tokens
- [x] Tailwind v4 syntax (verified — no v3 leftovers in any reviewed file)
- [x] No forbidden patterns: pure-white page bg, purple-on-white, neon lime
- [x] N/A — rich-card doesn't introduce reveals
- [x] Spacing rhythm consistent
- [x] **Light + dark visual parity** in shipped code — rich-card.tsx + parts/ use semantic tokens cleanly throughout
- [x] N/A — no component-scoped CSS
- [ ] **Hardcoded colors in demo** — see **F-07**: demo's "dirty" indicator uses `bg-amber-500/15`, `text-amber-600`, `dark:text-amber-400`. Demo-only, doesn't ship.

**Notes:** F-07 only — and it's docs-only. Shipped component is clean.

---

## 7. Accessibility

- [x] Keyboard nav: full ARIA tree contract (arrow keys, home/end, expand/collapse via right/left)
- [x] No keyboard traps (escape exits edit mode; tab cycles cleanly)
- [x] ARIA roles + labels: `role="tree"`, `role="treeitem"`, `aria-level`, `aria-expanded`, `aria-multiselectable`, `aria-roledescription`
- [x] Live regions: `<InlineError>` uses `aria-live="polite"` (per plan §8); validation failures announced
- [x] Focus rings visible (`focus-visible:ring-ring`)
- [x] Focus management: `focusCard(id)` imperative method; popovers handle their own
- [x] Reduced-motion respected (Tailwind v4 motion-safe defaults)
- [x] Color contrast — semantic tokens
- [x] Screen-reader pass — drag start/end via dnd-kit announcer; validation errors announced inline
- [x] Disabled states via `aria-disabled` on undo/redo buttons (canUndo / canRedo)
- [x] Undo/redo keyboard shortcuts respect input focus (don't intercept inside textbox; per use-undo.ts:42-66)

**Notes:** A11y wiring is thorough. Strongest of the components reviewed so far.

---

## 8. Performance

- [x] React.memo not explicitly used; React Compiler auto-memo + dispatch-by-version pattern
- [x] Stable identity for props crossing memo boundaries (dispatchers, validators, config — all `useMemo`)
- [x] Module-scope constants (DEFAULT_LEVEL_STYLES, ESTIMATED_CARD_HEIGHT, OVERSCAN, RESERVED_KEYS, PREDEFINED_KEYS)
- [x] N/A — `useReducer` over immutable tree
- [ ] **Virtualization** — see **F-01**: claimed in API + meta but **not wired**. The `virtualize` prop has zero runtime effect.
- [x] No O(n²) walks per render (findCard, findAncestorIds, findParentId are O(n) tree walks; called sparingly)
- [ ] Stress demo for high-N — N/A (virtualization not actually wired; the meta claim of ">500 nodes opt-in virtualization" is unmet)
- [x] No render loops
- [x] No `console.log` in render path (only `console.warn` for parse errors, behind logParseErrors)
- [ ] Bundle weight not measured (build failed on F-cross-04)

**Notes:** Performance budget for ≤500 nodes is realistic without virtualization (per plan §12). Issue is the published claim that virtualization handles >500 — which it doesn't because F-01.

---

## 9. Registry distribution

- [x] Two items in `registry.json`: base + `rich-card-fixtures` (verified)
- [x] Locked target convention: `type: "registry:component"`, `target: "components/rich-card/<sub-path>"` for every file
- [x] Excluded files: `demo.tsx`, `usage.tsx`, `meta.ts` not in `files`
- [x] `pnpm registry:build` regenerates `public/r/rich-card.json` cleanly
- [x] **Smoke install passes** — `pnpm dlx shadcn@latest add @ilinxa/rich-card --yes` succeeded in 12.3s
- [x] N/A — no internal sibling-component deps
- [x] `description` matches `meta.ts.description` and doc-site card
- [ ] `pnpm vercel-build` not run (F-cross-04 env block)

**Notes:** Distribution is correct. Despite F-02's wrong `^11.x` dnd-kit pin, the install succeeded — likely because the consumer's package.json prefers existing kanban-pinned `^10.0.0` and shadcn doesn't enforce strict version conflicts.

---

## 10. Meta + manifest sync

- [x] `meta.ts.version: "0.4.0"` reflects reality
- [x] `meta.ts.status: "beta"` honest — only past-beta-gate component in the library
- [x] `meta.ts.tags` searchable (rich-card, tree, outline, json, viewer, editor, drag-drop, permissions, search, structured-content, data)
- [ ] **`meta.ts.description` overstates implementation** — see F-01: claims "virtualization, native search, validation hooks, and undo/redo" but virtualization is not wired
- [ ] **`meta.ts.features` overstates** — see F-01: feature list says "Opt-in virtualization for trees >500 nodes (auto-threshold)"
- [x] Registered in `manifest.ts.REGISTRY` (3 imports verified earlier)
- [x] `STATUS.md` Components table row matches v0.4.0
- [x] `STATUS.md` Recent decisions has v0.4 entry
- [x] `docs/component-versions.md` snapshot in sync (lists rich-card 0.4.0 beta)
- [x] `meta.ts.updatedAt: "2026-04-28"` reasonable

**Notes:** F-01 implications surface here — the description and features list both overstate.

---

## 11. UI copy / text

- [x] Empty states: `EmptyTreePlaceholder` is a real sentence with optional onAddRoot button
- [x] Error / warn messages actionable (`logParseErrors` cites path + message; validator errors include code + message)
- [x] Button labels verb-first ("Undo", "Redo", "Add field", "Remove", "Edit")
- [x] Tooltips terse, non-redundant
- [x] Placeholder text explains format
- [x] No lorem ipsum in prominent copy
- [x] Pluralization correct
- [x] Sentence case for in-product UI
- [x] Brand names (TanStack Virtual, dnd-kit) correctly cased in code
- [x] No "TODO copy"
- [x] No "Click here"
- [ ] **Demo header text version-stale** — see **F-05**: "v0.3 demo" should be "v0.4 demo"

**Notes:** F-05 only.

---

## 12. Verification

- [x] `pnpm tsc --noEmit` clean
- [ ] **`pnpm lint`** — 1 warning that this review re-classifies from "pre-existing" to **F-06 (rich-card-specific dead code)**
- [ ] **`pnpm build`** failed on F-cross-04 (env, not rich-card)
- [x] `pnpm registry:build` clean
- [ ] Docs-site detail page renders at `/components/rich-card` — **deferred** (browser validation)
- [ ] Browser validation: light + dark, narrow + wide, all interactive paths — **deferred**
- [x] Smoke install in tmp consumer **passes** (12.3s, session 1 run)
- [x] Console clean during install

**Pre-existing warning re-classified:**
```
E:\…\rich-card\hooks\use-virtualizer.ts:22:23 — TanStack Virtual incompatible-library
(F-06: dead-code file; correlate with F-01 virtualization decision)
```

**Notes:** Verifications are clean; the "pre-existing warning" was an unaudited debt.

---

## 13. Migration provenance (if applicable)

- [x] **N/A — greenfield component** (no `> Migration origin` line in description)

---

## 14. Cross-component coherence

- [x] Conditional DnDContext mounting pattern matches kanban (DnD short-circuited when not enabled)
- [x] Imperative-handle pattern matches kanban (and other components — `useImperativeHandle` with comprehensive method surface)
- [x] Naming: `rich-card` is bare-slug (canonical) — appropriate since no `rich-card-02` exists
- [x] Public-helper export naming follows convention (sibling exports `RichCardSearchBar`, `RichCardUndoToolbar`)
- [x] N/A — no component-scoped CSS additions
- [x] Sealed-folder shape matches data-table + lib/ deviation (justified)
- [x] Category assignment correct (data — primary purpose is structured-content display + manipulation)
- [x] Status convention (beta) — only past-beta-gate component; documented gates for stable promotion
- [ ] **Cross-component dep version drift** — see **F-02**: rich-card claims `@dnd-kit/sortable: ^11.x`; kanban uses `^10.0.0`; producer pkg.json has `^10.0.0`. Cross-component coherence broken.
- [ ] **Cross-component usage import-path drift** — see **F-04**: rich-card + kanban both use producer-side path; affirmed as systemic. Recommend cross-cutting fix.

**Notes:** F-02 (dep drift) + F-04 (import-path drift) surface as cross-component issues during this review. Worth library-level attention.

---

## Tally

- **Total items:** 133 (across 14 dimensions)
- **Ticked:** 109
- **N/A (with reason):** 8
- **Failed (becomes a finding):** 16 across 7 unique findings (F-01 through F-07)

> Companion narrative report: [`2026-05-08-v0.4.0-review.md`](./2026-05-08-v0.4.0-review.md)
