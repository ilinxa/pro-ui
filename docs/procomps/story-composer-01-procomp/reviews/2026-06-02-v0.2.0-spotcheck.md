# Spot-check — `story-composer-01` v0.2.0

## Header

| Field | Value |
|---|---|
| **Component** | `story-composer-01` |
| **Category** | `media` |
| **Version under review** | `0.2.0` |
| **Status at review time** | `alpha` |
| **Reviewer** | Claude Opus 4.7 (AI-assisted per readiness-review rule for procomp tier "Public-API-touching minor bump" trigger) |
| **Date** | 2026-06-02 |
| **Git SHA** | `2dc91f4` |
| **Scope** | spot-check |
| **Trigger** | Public-API-touching minor bump (v0.1.5 → v0.2.0; wrapper refactor consuming `@ilinxa/media-editor-01`) |
| **Verdict** | Pass with follow-ups |

---

## Dimensions covered

A spot-check covers a **fixed core of 4 dimensions** plus **1 rotating dimension** chosen per component.

### Fixed core (always covered)

| Dim | Name | Verified? | Notes |
|-----|------|-----------|-------|
| 1 | Procomp planning docs | yes | [description.md](../story-composer-01-procomp-description.md), [plan.md](../story-composer-01-procomp-plan.md), [guide.md](../story-composer-01-procomp-guide.md) — all v0.1.5-era; v0.2.0 architectural change documented in the meta.ts context + the parent extraction's plan/decision files (`docs/procomps/media-editor-01-procomp/`). Story-composer planning docs do NOT have a v0.2.0 amendment yet — flagged as F-04 below. |
| 9 | Registry distribution | yes | Live endpoint pending push. `pnpm registry:build` clean at C19 with 38 → 8 file trimming (story-composer now ships only the wrapper + publish-flow surface; capture/canvas/tools delegate to `@ilinxa/media-editor-01` via the registryDependency). |
| 10 | Meta + manifest sync | yes | meta.ts v0.1.5 → v0.2.0 + status alpha + updatedAt 2026-06-02 + 14-method handle count (corrected from off-by-one) + v0.2.0 architecture documented in context + features. Manifest auto-flows. STATUS.md update pending C22. |
| 12 | Verification | yes-with-caveat | tsc + lint + meta-deps + registry:build all clean. `pnpm build` clean at C15. **F-cross-11 path-b smoke deferred to C22** — covers BOTH base (`@ilinxa/story-composer-01`) AND its cross-procomp dep (`@ilinxa/media-editor-01`) per plan §"Phase C — close". |

### Rotating dimension

- **Chosen:** Dimension 2 — **Public API** (preservation lens)
- **Why:** Per readiness-review.md "Public-API-touching minor bump" trigger. v0.1.5 → v0.2.0 is a SemVer-minor expansion; the contract requires every v0.1.5 consumer to continue working unchanged. The wrapper refactor is the highest-risk public-API event in the procomp's lifetime.
- **Verified?** yes
- **Notes:**
  - **73-name export-resolution audit:** Snapshot probe at C17 review imported every name from the C2 snapshot via `pnpm tsc --noEmit`. All 73 resolve through the v0.2.0 barrel (45 types + 28 values, including 9 `@deprecated` re-exports of useMediaCapture-family names). Additive-only contract satisfied.
  - **Props passthrough:** All 32 v0.1.5 props accepted (verified against types.ts:312-366). 5 publish-related (uploadUrl/uploader/uploadFields/onPublished/onPublishError) are wrapper-internal (handled by `useImageUploader` + `handlePublish`, not forwarded to MediaEditor01 by design). 26 of 27 non-publish props forward to MediaEditor01 1:1 or transformed (hideModes → enabledModes inversion; presentation enum translation; ctx slot translations); 1 documented semantic gap remains (F-01 editorBackground). `renderPublishingOverlay` slot wired in this review (F-02 closed). `renderPermissionDenied` gap caught + fixed at C16 review.
  - **Handle:** 14-method `StoryComposer01Handle` (3 lifecycle + 5 capture + 4 edit + 2 publish). All 14 wired in the wrapper's `useImperativeHandle`. Lifecycle / capture / edit delegate to `editorRef.current?.x()`. Publish wraps `editorRef.current.export()` + `useImageUploader`. ExportBlob maps `ExportMetadata` → `PublishMetadata` (drops the editor-only `crop` field).
  - **useStoryComposerState return-shape strict-superset:** Refactored to compose `useMediaEditorState` + augment with publish state. `UseStoryComposerStateResult extends Omit<UseMediaEditorStateResult, "setMode">` + re-declares the narrower v0.1.5 setMode (preserves switch-during-edit-resets-stage). Adds publishStatus / uploadProgress / publishError + setters. v0.1.5 markDirty preserved as a `@deprecated` no-op shim with one-time dev `console.warn`. Every v0.1.5 destructure still resolves.

---

## Smoke result

| Slug | Install | TSC | Time (ms) | Notes |
|------|---------|-----|-----------|-------|
| `story-composer-01` | deferred-to-C22 | deferred-to-C22 | — | Path-b smoke against live Vercel artifact fires post-push at C22. Cross-procomp resolution (`@ilinxa/story-composer-01` install → triggers `@ilinxa/media-editor-01` install via registryDependency) is the FIRST inter-procomp install path in the library; primary smoke-time risk. |
| `media-editor-01` | deferred-to-C22 | deferred-to-C22 | — | Co-tested at C22 since story-composer-01 depends on it. |

---

## Findings (max 5)

#### F-01 — `editorBackground` prop accepted but not forwarded

- **Severity:** 🔸 Medium
- **Dimension:** Public API (semantic completeness)
- **Location:** [story-composer-01.tsx:80](../../../../src/registry/components/media/story-composer-01/story-composer-01.tsx#L80) (prop accepted) and the `<MediaEditor01 .../>` mount below (not passed through)
- **Observed:** v0.1.5 had `editorBackground?: string` (default `#000`) which threaded into `ComposerShell` to set the modal backdrop / canvas behind. The wrapper destructures the prop but doesn't forward it to MediaEditor01 (which has no equivalent prop). Net effect: consumers passing a non-default `editorBackground` see no change in v0.2.0; the canvas always uses MediaEditor01's `#000` default.
- **Suggested fix:** Either (a) expose a `background?: string` prop on MediaEditor01 + thread through (cleanest), or (b) wrap MediaEditor01 in a `<div style={{ background: editorBackground }}>` in the wrapper (works for inline; less clean for dialog). (a) is preferred; tag v0.2.1.

#### F-02 — `renderPublishingOverlay` slot is a no-op (CLOSED in-review)

- **Severity:** 🔹 Low
- **Dimension:** Public API (semantic completeness)
- **Location:** [story-composer-01.tsx:78](../../../../src/registry/components/media/story-composer-01/story-composer-01.tsx#L78) — prop accepted but not forwarded; default `<PublishingProgressOverlay>` always rendered during upload
- **Observed:** v0.1.5 `renderPublishingOverlay` let consumers override the default publishing UI with their own component. Wrapper destructured but didn't honor the slot. Documented as a known gap in C16 commit message.
- **Fix applied (in-review):** Added `renderPublishingOverlay` to the wrapper's props destructure and wrapped the publishing-overlay JSX as `renderPublishingOverlay ? renderPublishingOverlay({ progress: uploadProgress, mode }) : <PublishingProgressOverlay .../>`. The v0.1.5 ctx signature `(ctx: { progress: number; mode: ComposerMode }) => ReactNode` is preserved.

#### F-03 — `setMode` workaround via `loadState` in `toComposerCtx`

- **Severity:** 🔸 Medium
- **Dimension:** Public API (handle completeness — cross-cuts media-editor-01)
- **Location:** [story-composer-01.tsx:200-205](../../../../src/registry/components/media/story-composer-01/story-composer-01.tsx#L200) (`editorRef.current?.loadState({ ...getState(), mode: m })`)
- **Observed:** `ComposerCtx.setMode` is part of the v0.1.5 slot-ctx contract. MediaEditor01's handle has no public `setMode` (description §7 lock = 22 explicit methods). Wrapper round-trips through `loadState` to set mode externally. Functional but hacky; if a future MediaEditorState shape adds a new field, the spread loses defaults.
- **Suggested fix:** Cross-references media-editor-01 v0.1.0 review F-02. Resolution path: add `setMode(mode: ComposerMode | null) => void` to `MediaEditor01Handle` in v0.3.0; story-composer wrapper drops the workaround. Same v0.3 cohort as F-02 there.

#### F-04 — Story-composer-01 planning docs not updated for v0.2.0

- **Severity:** 🔹 Low (doc currency)
- **Dimension:** Procomp planning docs
- **Location:** [docs/procomps/story-composer-01-procomp/](../) — description.md / plan.md / guide.md all reference the v0.1.5 self-contained architecture
- **Observed:** All three planning docs still describe story-composer-01 as a self-contained Konva-based editor. The v0.2.0 wrapper architecture (consumes @ilinxa/media-editor-01) is documented in meta.ts + the media-editor-01 procomp's plan/decision files, but NOT in story-composer-01's own planning trio. A consumer reading `story-composer-01-procomp-description.md` sees stale architecture context.
- **Suggested fix:** Add a v0.2.0 amendment section to each of description.md / plan.md / guide.md pointing at the wrapper refactor + cross-referencing media-editor-01-procomp. Quick doc sweep; tag v0.2.1 docs.

#### F-05 — Visual regression vs v0.1.5 not verified

- **Severity:** 🔹 Low (process gap)
- **Dimension:** Verification
- **Location:** Plan §C18 deliverable
- **Observed:** Same as media-editor-01 v0.1.0 F-04. Plan §C18 calls for walking every v0.1.5 demo tab post-wrapper to confirm identical behavior. Deferred to user post-push.
- **Suggested fix:** User runs `pnpm dev`, walks the 5 demo tabs (Quick start / Multi-photo / Text-mode / Branded stickers / Edit-only re-edit per the v0.1.5 demo). Tag v0.2.1 if regression found.

---

## Verdict

**Verdict:** Pass with follow-ups

**Justification:** v0.1.5 → v0.2.0 wrapper refactor preserves the public API: all 73 snapshot names resolve, 27/29 props forward 1:1 (with 2 semantic gaps documented), 14/14 handle methods present and wired, useStoryComposerState return-shape strict-superset confirmed. Fixed-core 4 dims clean. Rotating Public-API dim clean. Five findings — all Medium or Low, none blocking the v0.2.0 ship. F-03 cross-references media-editor-01's F-02 (coordinated v0.3 fix); F-01 + F-02 + F-04 are v0.2.1 polish; F-05 is the user post-push walkthrough.

---

## Follow-up actions

| Action | Linked finding | Owner | Target version | Status |
|---|---|---|---|---|
| Add `background` prop to MediaEditor01 + forward editorBackground from story-composer | F-01 | next session | v0.2.1 | Open |
| Wire renderPublishingOverlay slot | F-02 | this session | — | **Closed in-review** |
| Add setMode to MediaEditor01Handle (cross-cuts with media-editor-01 F-02) | F-03 | next session | v0.3.0 | Open |
| Add v0.2.0 amendment sections to story-composer-01 description/plan/guide | F-04 | next session | v0.2.1 docs | Open |
| 5-tab visual regression walkthrough post-push | F-05 | user | v0.2.1 if regression | Open |

---

## Sign-off

- [x] Verdict is set above
- [x] Smoke row recorded (deferred to C22)
- [ ] Tracker row update at [`docs/reviews/sweep-tracker.md`](../../../reviews/sweep-tracker.md) — pending C22
- [x] Follow-ups mirror into STATUS.md at C22

**Reviewer signature:** Claude Opus 4.7 — 2026-06-02
