# Loop state — `blackboard` (U-loop)

## Status

| Field | Value |
|---|---|
| **Mode** | U-loop (change class: **patch**) |
| **Current stage** | CLOSED — shipped as v0.2.1 (U7 complete 2026-08-12) |
| **Sign-off policy** | patch class → architect proceeds, recorded (rigor ladder); pilot run under user's 2026-08-12 end-to-end delegation |
| **Version target** | v0.2.0 → v0.2.1 |
| **Model roster** | architect implements directly (diff ≪ 150-line orchestration bar) |
| **Started / updated** | 2026-08-12 / 2026-08-12 |

## Stage checklist

- [x] U0 Intake — change = owed follow-up **F-03** from `reviews/2026-06-18-v0.1.0-spotcheck.md` (opportunistic prune of absorbed optimistic/appended extras; original target v0.1.1, component now 0.2.0 → lands as **0.2.1**). Verified still open: `hooks/use-blackboard-state.ts:110-114` comment carries the IOU; no prune exists in any `setExtras` caller. Class = patch (internal hygiene, zero public-API touch). **Blast radius:** no dependents (`registry.json` grep: only blackboard's own items; no cross-component imports), no feature slices, no interop contracts. Docs: meta version, STATUS row, component-versions (generated), F-03 follow-up row status. Fix-on-touch ledger consulted: F-04 (composer mention overlay, v0.2 feature) NOT taken — feature-class, separate loop; popover-var audit n/a (no popover).
- [x] U1 Change contract — planning-doc check: description/plan/guide untouched by an internal buffer prune (no-change, recorded). **Invariants delta:** existing I-old-1 display membership unchanged (merged = notes ∪ extras dedup — prune only removes entries whose id ∈ notes, which `merged` already filters); I-old-2 `retryPost` still finds failed optimistic notes (failed notes are never in `notes`, never pruned); I-new-1 after a consumer absorbs an optimistic/appended note into `notes`, the next `setExtras`-writing interaction (post / appendNote / loadOlder) drops the absorbed entry from the buffer; I-old-3 no new effects (the `react-hooks/set-state-in-effect` constraint that motivated the deferral). Bump: patch → 0.2.1. Sign-off: architect, recorded.
- [x] U2 Implement — `pruneAbsorbed` pure helper (identity-stable when no-op) + prune in `post` / `appendNote` / `loadOlder`; dep arrays extended (`post` + imperative handle now include `notes`); `merged` comment IOU replaced; meta 0.2.0→0.2.1, updatedAt 2026-08-12. `pnpm tsc --noEmit` exit 0. Accepted nuance: `appendNote` duplicate-id early-return skips a prune opportunity (harmless — next writer prunes).
- [ ] U3 Gate battery
- [x] U4 Self-check (patch ladder) — invariants delta held under attack: retryPost safe (failed notes carry temp ids never in `notes`); display membership unchanged (prune ⊆ what `merged` already filtered); no new effects; dep arrays extended truthfully. Accepted nuance: `appendNote` dup-id early-return defers one prune opportunity (harmless).
- [x] U5 Runtime & smoke — live 3/3 on production build (detail page renders · docked composer reachable · posted note appears through the patched optimistic path); consumer install (Base UI backend) of the 0.2.1 artifact + consumer tsc 0. No dependents to co-smoke (U0).
- [x] U6 Docs sync — planning trio: no-change (internal hygiene); F-03 row in `reviews/2026-06-18-v0.1.0-spotcheck.md` marked Done; component-versions regenerated by registry:build; no review file required (patch class per rigor ladder).
- [x] U7 Ship — `fix(blackboard)` commit (SHA recorded in STATUS/decision file at session close); post-deploy artifact spot-check owner: assistant, same session.

## Reproduction (U2 pre-fix, replayed at U5)

Controlled host posts a note → optimistic entry enters `extras` → host echoes the real note into
`notes` → absorbed entry remains in `extras` indefinitely (observable via buffer growth; display
correct). After fix: next post/append/loadOlder prunes it.

## Gate battery (U3) — run 2026-08-12, shared with empty-state C4 (folder-disjoint diffs; see that state doc for full numbers)

tsc exit 0 · lint 0 errors/9 known warnings · meta-deps 64/64 clean · registry validators 0 high
(6 pre-existing warns, none blackboard) · doc validators green post-regen · registry:build green
(blackboard.json regenerated at 0.2.1) · Next build exit 0.

## Findings table (U4)

(pending)

## Runtime & smoke evidence (U5)

(pending)

## Parked

- F-04 in-composer mention-highlight overlay — stays owned, target v0.3.0 (feature class, own loop).

## Pre-mortem (U7)

If this breaks a consumer, it breaks because a host relied on absorbed extras persisting in the
buffer (e.g. reading them via appendNote dedup timing) — mitigated: prune only removes ids already
present in `notes`, which the display layer already treated as absorbed.
