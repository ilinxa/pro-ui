# Review checklist — `properties-form` v0.1.0

> Filled 2026-05-08 (sweep session 6). Companion report: [`2026-05-08-v0.1.0-review.md`](./2026-05-08-v0.1.0-review.md).

## Header
- **Reviewer:** Claude (sweep session 6) · **Date:** 2026-05-08 · **Git SHA:** `b5ad5d6`
- **Scope:** full (Tier 1) · **Trigger:** sweep — pre-public Tier 1 review

## Per-dimension summary

| Dim | Name | Status | Notes |
|---|---|---|---|
| 1 | Procomp planning docs | ✓→⚠ | description (10 locked Qs + 4 §8.5 tightenings) + plan (584 lines, 10 locked Q-Ps + 9 plan-stage refinements) thorough. **F-02**: `guide.md` MISSING (F-cross-01 carrier; 2 of 3 Tier 1 holdouts confirmed; entity-picker paired here closes the list). **F-05**: Plan §8.1 footer "File count: 22" but list has 25 entries (and source has 25) |
| 2 | Public API | ✓ | 9 props (5 required-by-mode + 4 optional ergonomics); 9 type exports including handle. Generic `<T extends Record<string, unknown>>` preserved through React 19 ref-as-prop pattern (function with `ref` prop, not `forwardRef`). Discriminated `FieldType` + `FieldPermission` unions. SubmitResult shape exposed |
| 3 | Component code | ✓→⚠ | 25 files match plan §8.1 exactly. Sealed-folder. Mount-time prop-mirror refs (line 58-74) for callback freshness. `submitId` token suppresses stale dispatch (plan §13.5 #4). Schema-instability dev-warning (lines 286-306). **F-01**: 8 `process.env.NODE_ENV` occurrences across 4 files (densest F-cross-08 carrier; now 3/9 Tier 1) |
| 4 | Demo + usage | ✓→🔸 | 5-tab demo per success #9. `demo.tsx` uses Badge + Button + Tabs (only Button is in shipped parts via submit-actions.tsx; Badge + Tabs are demo-only — see F-03). **F-04**: `usage.tsx:18-19` producer-side import path (F-cross-06 — now 7/7 systemic) |
| 5 | Dependencies | ⚠ | 6 shadcn primitives actually used in shipped files (button, input, select, switch, textarea, tooltip). **F-03 (High)**: `meta.shadcn` declares 8 (over-declares `tabs` + `badge` — demo-only) and `meta.npm` declares phantom `radix-ui: ^1.4.3` (no source imports it). registry.json `registryDependencies` correctly minimal at 6. **F-06**: meta vs registry drift |
| 6 | Design system | ✓ | Semantic tokens via shadcn primitives; no hardcoded colors |
| 7 | Accessibility | ✓ | `<label htmlFor>` association via id factory; `aria-required` + `aria-invalid` + `aria-describedby` wired through FieldRendererProps; `aria-busy` on form during pending; first-error focus on submit failure (line 173); ErrorSummary at form top with anchor links via focusField. Hidden fields omitted from DOM (per plan §5.3) |
| 8 | Performance | ✓ | React.memo'd field-rows (per plan §11.1); permission resolver memoized in field-row.tsx; format-value pure; submit spinner uses single setTimeout cleared on cleanup. Schema-stability dev-warning fires on >5 successive unequal-schema renders (plan §11.1.1) |
| 9 | Registry distribution | ✓→⚠ | 22 source files in `registry.json` with locked `target: "components/properties-form/<sub>"` convention; `properties-form-fixtures` ships only `dummy-data.ts`; `demo.tsx` / `usage.tsx` / `meta.ts` correctly excluded. **F-cross-09 carrier**: session 5 fresh smoke ✗ fail (4514ms; "try shadcn@4.6.0") — upstream CLI regression |
| 10 | Meta + manifest sync | ⚠ | version + status accurate; tags reflect graph-system + form. `related: ["detail-panel", "rich-card"]` — populated but **F-07 (Low)**: missing entity-picker + filter-stack + markdown-editor (forms siblings). **F-03 + F-06** (above): meta over-declares deps |
| 11 | UI copy | ✓ | "Save"/"Cancel" defaults; submitLabel/cancelLabel props for override; field labels via schema; permissionReason tooltip on read-only fields |
| 12 | Verification | ✓ | tsc + lint + registry:build all clean (1 pre-existing rich-card warning, no new). `pnpm build` blocked on F-cross-04 |
| 13 | Migration provenance | n/a | Greenfield component (no `> Migration origin:` line) |
| 14 | Cross-component coherence | ⚠ | Mirrors entity-picker / filter-stack / markdown-editor on generic-typing posture; mirrors filter-stack on schema/items reference-stability footgun. F-cross-08 confirmed 3/9; F-cross-06 confirmed 7/7; F-cross-01 closes 2 of 3 Tier 1 carriers (markdown-editor + properties-form). Phantom `radix-ui` now 2 of 3 forms components (entity-picker paired-review confirms 3/3) |

## Findings (full detail in report)

- **F-01 (High)** — F-cross-08 carrier — 8 `process.env.NODE_ENV` occurrences across 4 files (now 3/9 Tier 1)
- **F-02 (High)** — F-cross-01 carrier — `guide.md` missing (2 of 3 Tier 1 holdouts confirmed)
- **F-03 (High)** — `meta.ts` over-declares deps (phantom `radix-ui` + demo-only `tabs`/`badge`)
- **F-04 (Medium)** — F-cross-06 carrier — `usage.tsx:18-19` import-path drift (7/7 systemic)
- **F-05 (Medium)** — Plan §8.1 footer "File count: 22" but list/source has 25
- **F-06 (Medium)** — `meta.shadcn` (8 entries) drifts from `registry.json.registryDependencies` (6 entries)
- **F-07 (Low)** — `meta.related` could include entity-picker + filter-stack + markdown-editor

## Tally

- Total dimensions covered: 14 (13 active + 1 N/A)
- Failed → finding: 7 (3 High, 3 Medium, 1 Low)
- Verdict: **Pass with follow-ups**

## Notes

Properties-form is the highest plan-vs-code fidelity Tier 1 component reviewed (only F-05 is a structural drift; everything else is deliberate ecosystem patterns). 25-file plan §8.1 lands exactly. The phantom-radix-ui finding (F-03) and meta-vs-registry drift (F-06) are now confirmed across 2 of 3 forms components — pattern is systemic and best resolved with `pnpm validate:meta-deps` lint at session 7's mid-checkpoint.

Pairs naturally with entity-picker for session 6: both forms-tier, both F-cross-01 + F-cross-06 + F-cross-08 + phantom-radix-ui carriers. After this paired review, **F-cross-01's Tier 1 list is fully addressed** (3 of 3 confirmed).
