# Review checklist — `workspace` v0.1.0

> Filled 2026-05-08 (sweep session 5). Companion report: [`2026-05-08-v0.1.0-review.md`](./2026-05-08-v0.1.0-review.md).

## Header
- **Reviewer:** Claude (sweep session 5) · **Date:** 2026-05-08 · **Git SHA:** `d7b1409`
- **Scope:** full (Tier 1) · **Trigger:** sweep — pre-public Tier 1 review

## Per-dimension summary

| Dim | Name | Status | Notes |
|---|---|---|---|
| 1 | Procomp planning docs | ✓→⚠ | description (12 locked Qs) + plan (6 locked Q-Ps, 26-file structure) + guide all present and reference-grade. **F-02, F-04, F-05 surface plan-vs-code drift**: Alt+Enter shortcut not wired; plan §9 over-states `separator` dep; plan §2 enumerates 6 reducer actions but code has 7 (`focus` undocumented) |
| 2 | Public API | ✓ | 5 public types + 1 helper + 1 hook (`useAreaContext`) per plan §2; `WorkspaceProps` lands every locked field. Defaults applied via `DEFAULT_*` constants. Discriminated `AreaTree` (`leaf` / `split`). Optional props all have documented defaults |
| 3 | Component code | ✓ | 26 files = plan §4 exactly. Sealed-folder layout with `parts/` + `hooks/` + `lib/` (Q-P2 `lib/` deviation justified). Zero `next/*`, zero `process.env`, zero app contexts. Reducer + tree ops pure. **F-01** flags one behavior bug (adaptive flatten loses leaves) but architecture is sound |
| 4 | Demo + usage | ✓→🔸 | Demo registers 4 sample components (Notes, Clock, Counter, Data Table); Counter exercises Q3 state-preservation contract; ClockPanel uses `useAreaContext()` to verify Q-P1. **F-03**: `usage.tsx:16,43` use producer-side `@/registry/components/layout/workspace` (F-cross-06 — now 5/5 systemic) |
| 5 | Dependencies | ✓ | 3 shadcn primitives in `meta.ts` (`dropdown-menu` / `scroll-area` / `tabs`) all imported from source. `lucide-react: ^1.11.0` matches producer `package.json` exactly — no F-cross-07 drift. **F-04**: plan §9 listed `separator` (4th primitive) but it's unused; meta correctly omits |
| 6 | Design system | ✓ | Semantic Tailwind tokens (`bg-card`, `border-border`, `text-foreground`, `ring-primary`); no hardcoded colors. Onest sans / JetBrains Mono mono inherited via globals.css |
| 7 | Accessibility | ✓ | `role="application"` + `aria-label` on root; areas `role="region"` + `tabIndex={0}` + dynamic `aria-label`; dividers `role="separator"` + `aria-orientation` + `aria-valuenow`; corner handles `role="button"` + `aria-label="Split or merge area"` + `aria-disabled` when at-cap. Alt+Shift+Arrow resize wired (workspace.tsx:277-321). **F-02 caveat**: Alt+Enter from plan §5.3 not wired — chevron-button menu provides functional parity but not the documented shortcut |
| 8 | Performance | ✓ | Flat-list architecture with absolute positioning per plan §3.1. Memoized `flatten(tree, breakpoint, cap)` keyed by reference. `ResizeObserver` only on root. Drag state in `useRef` to avoid re-renders mid-drag. Edge-drag uses `requestAnimationFrame` flush. **F-06 (Low)**: `inertLogged` is module-level not per-instance |
| 9 | Registry distribution | ✓→⚠ | 26 source files in `registry.json` with locked `target: "components/workspace/<sub>"` convention; `workspace-fixtures` companion ships only `dummy-data.ts`; `demo.tsx` / `usage.tsx` / `meta.ts` correctly excluded. Session 1 smoke: ✓ pass (9.8s). **F-cross-09 carrier**: session 5 fresh smoke ✗ fail (4.3s; "try shadcn@4.6.0") — upstream CLI regression |
| 10 | Meta + manifest sync | ✓→🔹 | version + status accurate; createdAt + updatedAt 2026-04-27. **F-07 (Low)**: `related: []` empty — should reference data-table + kanban-board-01 + flow-canvas-01 (renderer-registry pattern siblings) |
| 11 | UI copy | ✓ | Component-not-registered fallback rendered with helpful prose ("Use the dropdown to pick one from the registry."); area aria-labels surface component names; toolbar item labels have lowercase casing pattern matching shadcn primitives |
| 12 | Verification | ✓ | tsc + lint clean; registry:build clean. `pnpm build` blocked on F-cross-04 (Google Fonts env, not workspace) |
| 13 | Migration provenance | n/a | Greenfield component (no `> Migration origin:` line) |
| 14 | Cross-component coherence | ✓→⚠ | Renderer-registry pattern matches kanban-board-01 + flow-canvas-01 (3 of 4 host components). **F-03 / F-cross-06** carrier (5/5). Plan-vs-code drift "deviations are loud, not silent" recurs across F-02 + F-04 + F-05 — recurring sweep theme |

## Findings (full detail in report)

- **F-01 (High)** — Adaptive flatten silently discards content past cap (`lib/tree.ts:139-142` `collapseToFirstLeaf`)
- **F-02 (High)** — Plan-locked `Alt+Enter` keyboard shortcut not wired; chevron-menu pattern undocumented
- **F-03 (Medium)** — F-cross-06 carrier — `usage.tsx:16,43` import-path drift (5/5 systemic)
- **F-04 (Medium)** — Plan §9 over-states deps (`separator` listed but not used)
- **F-05 (Medium)** — Reducer adds 7th `focus` action not in plan §2
- **F-06 (Low)** — `inertLogged` module-level, not per-instance
- **F-07 (Low)** — `meta.related: []` empty; missing renderer-registry pattern siblings

## Tally

- Total dimensions covered: 14 (13 active + 1 N/A)
- Failed → finding: 7 (2 High, 3 Medium, 2 Low)
- Verdict: **Pass with follow-ups**

## Notes

Workspace is the most ambitious component reviewed in the sweep so far (26 files; 7-action reducer; flat-list-over-binary-tree architecture). Planning docs are reference-grade — the description/plan/guide trio is the most thorough in the sweep. The implementation lands the headline contracts cleanly; findings target a specific behavior bug (F-01 — adaptive flatten loses leaves) and several plan-vs-code drift sites that suggest the review process should add an explicit "diff plan to code" pass.

The session-5 fresh smoke run was high-leverage — surfaced **F-cross-09** (shadcn CLI regression: 16 unexpected-fails vs session 1's 5). Recommend pinning smoke harness to `shadcn@4.6.0` until the new CLI's namespaced-registry resolution is understood.
