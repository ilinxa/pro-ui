# media-editor-01 — v0.1.1 readiness spotcheck

| Field | Value |
|---|---|
| **Slug** | `media-editor-01` |
| **Tier** | pro-component |
| **Version** | 0.1.1 (patch on 0.1.0; touches `usePanZoom` public option surface) |
| **Reviewer** | AI-assisted (4-agent parallel audit) + author synthesis |
| **Date** | 2026-06-03 |
| **Git SHA (working tree base)** | `8b1030b` (uncommitted v0.1.1 patch) |
| **Scope** | The post-walkthrough UX sweep (F-06…F-13, prior file) **plus** this session's visual fixes (drag-to-pan, Instagram chrome model, full-bleed canvas + overlaid controls, container-query capture controls, min/max size clamp) **plus** a deep internal/external consistency + doc-alignment audit of `media-editor-01` and its `story-composer-01` wrapper. |
| **Trigger** | Public-API-touching minor sweep + explicit deep-review request |
| **Verdict** | ✅ **Pass with follow-ups** |

> Companion: `story-composer-01` was audited in the same pass (it wraps this
> component). Its findings are folded into this file (prefixed `SC-`) since the
> two ship together; its own review remains the
> [`2026-06-02-v0.2.0-spotcheck.md`](../../story-composer-01-procomp/reviews/2026-06-02-v0.2.0-spotcheck.md)
> file, addended for v0.2.1.

---

## Dimensions

### 1. Planning docs (fixed) — ✅ Pass
Description, plan, guide present. Guide refreshed this cycle: v0.1.1 banner, new
**Capture vs edit chrome** + **Pan & zoom** sections, viewport-relative dialog
sizing table (replacing the stale fixed-px table), capture-handle deferral note,
and a rewritten **Known gaps** (the old "capture UI / tool panels / undo-redo are
stubs" claims were materially false — all are wired). Description browser-matrix
pan row updated to include drag-pan. meta `features[]` / `description` / `context`
updated for the chrome model + drag-pan + container-query controls + min-size.

### 2. Registry distribution (fixed) — ✅ Pass
Live convention verified by the distribution audit: base item (38 source files) +
`media-editor-01-fixtures` sibling; every file `type: registry:component`,
`target: components/media-editor-01/<sub-path>`; no `demo.tsx` / `usage.tsx` /
`meta.ts` shipped. **All 10 session-modified files are listed**, so consumers get
the fixes. (Wrapper side: dead `composer-shell.tsx` was removed from the folder
**and** `registry.json` — see SC-01.)

### 3. Meta + manifest sync (fixed) — ✅ Pass
`version` → `0.1.1`, `updatedAt` → `2026-06-03`. `validate:meta-deps` clean.
Wrapper `story-composer-01` bumped `0.2.0 → 0.2.1` (real code delta:
`showClose`/`showPublish` + dead-code removal) with its registry-dep on
`@ilinxa/media-editor-01` intact.

### 4. Verification (fixed) — ✅ Pass
`tsc` clean (0 errors), `lint` at the 81/22 baseline (no new findings),
`validate:meta-deps` 51/51, `pnpm build` green (verified this session).

### 5. Rotating — Public-API honesty (chosen: the audit's highest-risk class) — ⚠️ Pass with follow-ups
The audit's most consequential cluster is declared-but-unimplemented surface.
Addressed honestly this cycle (comments + docs); the implementation work is
tracked below.

---

## Findings

| ID | Sev | Area | Status | Note |
|---|---|---|---|---|
| F-03 | 🔸 | `usePanZoom.shouldStartPan` public type was `(e: PointerEvent)` while the canvas passes `{clientX,clientY}` | **Fixed** | Narrowed option type to `{ clientX; clientY }`. |
| F-05 | 🔸 | Single-pointer pan jumped ~4px on the first frame after the threshold | **Fixed** | Anchor `lastX/Y` to the threshold-crossing point. |
| F-07 | 🔸 | Stale "C6 skeleton" scaffolding comments + `NOT_IMPLEMENTED_MARKER` referencing a non-existent plan phase | **Fixed** | Rewrote the JSDoc + marker to an honest "imperative capture deferred to v0.2" message. |
| F-08 | 🔹 | Bottom-overlay JSX is uniformly under-indented (~4 sp) under the new overlay wrapper | **Noted → v0.1.2** | Compiles + reads fine; no formatter is configured in the repo, so a manual 230-line re-indent isn't worth the structural-edit risk this cycle. Batch with a one-time formatter pass. |
| SC-01 | 🔸 | `composer-shell.tsx` was dead (unimported, unexported) yet shipped via `registry.json` | **Fixed** | Removed file + registry entry + stale meta comment. |
| SC-02 | 🔸 | `PublishingProgressOverlay` `status:"done"` arm was unreachable | **Fixed** | Removed the dead arm; narrowed the status union; dropped the unused `Check` import. |
| SC-08 | 🔹 | Empty no-op effect in `use-story-composer-state` | **Fixed** | Removed. |
| SC-05/06 | 🔹 | Redundant casts (`ctx.stage as ComposerStage`, `getState() ?? {}` + triple cast in `setMode`) | **Fixed** | Dropped. |
| **F-01** | ⚠️ | `onModeChange` / `onEditAction` props declared but never emitted | **Follow-up → v0.1.2** | Wire (or remove) the analytics callbacks. Documented in guide Known-gaps. Owner: author. |
| **F-02** | ⚠️ | `renderPermissionDenied` slot declared but `CameraPermissionPrompt` is always used | **Follow-up → v0.1.2** | Thread the slot into `EditorCamera`. Documented. Owner: author. |
| **F-07b** | 🔸 | 5 imperative *capture* handle methods (`takePhoto`/`startRecording`/`stopRecording`/`switchCamera`/`importFromGallery`) dev-warn + no-op | **Follow-up → v0.2** | Genuine architecture work (orchestrator↔camera control). Documented honestly in handle JSDoc + guide; in-UI capture is the supported path. Owner: author. |
| **F-06** | 🔸 | `usePanZoom` arrow/`+`/`-`/`0` keys bound at `window` scope (hijack risk when unfocused) | **Follow-up → v0.1.2** | Needs a focus-scope design (targetRef is the canvas, not the whole editor). Documented. Owner: author. |
| **SC-03/04** | ⚠️ | `labels` coupling: `media-editor-01` carries `Required<StoryComposer01Labels>` as an `@internal` shim and consumes the public `labels` prop as the flat shape — nested keys silently ignored | **Follow-up → v0.2 (C17)** | The label-flattening refactor. Mode/tool/Back strings are correct today; nested overrides are the gap. Documented in guide Known-gaps. Owner: author. |
| F-09…F-15, SC-07/09/10 | 🔹 | Minor (transformer hit-test edge case, `as unknown as` touch cast, filter `as any`, `ModeTogglePill`/`EditorToolbar` second-source, `editorBackground` ignored, naming/comment nits) | **Noted** | Low-impact; batch into v0.1.2/v0.2 housekeeping. |

---

## Verdict — Pass with follow-ups

All four fixed dimensions pass; verification gates are green. The rotating
dimension (public-API honesty) passes **with follow-ups**: the unimplemented
surface is now either fixed, or honestly documented (handle JSDoc, guide
Known-gaps) with a bump target. No blocker; nothing breaks tsc/lint/build. The
component renders correctly across capture + edit stages at the dialog and inline
presentations.

**Open follow-ups (owner: author):**
- v0.1.2 — wire/remove `onModeChange` + `onEditAction`; thread `renderPermissionDenied`; focus-scope the pan-zoom keyboard listener; minor housekeeping batch.
- v0.2 — implement the 5 imperative capture handle methods; land the C17 `labels` flattening (closes SC-03/04); add the device-library media source.

Cross-cutting candidates promoted to the sweep tracker: none (all are
component-local). The `labels` coupling (SC-03/04) is tracked as the standing C17
item, not a new cross-finding.
