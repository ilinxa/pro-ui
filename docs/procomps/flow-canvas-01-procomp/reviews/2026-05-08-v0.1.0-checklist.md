# Review checklist — `flow-canvas-01` v0.1.0

> Filled 2026-05-08 (sweep session 3). Companion report: [`2026-05-08-v0.1.0-review.md`](./2026-05-08-v0.1.0-review.md). Findings referenced by `F-NN` from the report.

---

## Header

- **Component:** `flow-canvas-01`
- **Category:** `data`
- **Version under review:** `0.1.0`
- **Reviewer:** Claude (sweep session 3)
- **Date:** 2026-05-08
- **Git SHA:** `7acfb9b`
- **Scope:** full (solo, was paired with force-graph)
- **Reference:** [review-guide.md](../../../reviews/review-guide.md), `.claude/skills/xyflow-react-pro/`

---

## 1. Procomp planning docs

- [x] description.md exists, signed off
- [x] plan.md exists, signed off
- [x] guide.md exists, current
- [x] description §5 archetypes (5) match what `demo.tsx` exercises
- [x] description out-of-scope list still accurate
- [ ] **plan-stage decisions still honored** — see **F-05**: 4 consolidations from plan §4 not annotated (3 menu files merged, 4 hooks moved/inlined, edge-adapter eliminated, controls.tsx omitted)
- [x] guide reads consumer-shaped, not implementer-shaped (32KB consumer guide)
- [x] guide code snippets compile against current public API (spot-checked basic example)
- [x] N/A — initial-ship component (no v0.x update sections needed yet)
- [x] N/A — greenfield (no migration origin)

**Notes:** F-05 surfaced. Phased-planning quality is high; just missing post-impl annotations.

---

## 2. Public API

- [x] Openness vs sealedness — 17 public types + 11 helpers/components
- [x] Component renders meaningfully with zero props
- [x] Discriminated unions for variants
- [x] `onBefore*` interceptor chains: `onBeforeDrop`, `onBeforeConnect`
- [x] Public helpers exported (full list in `index.ts`)
- [x] No `any` in public types; generics where appropriate (`NodeRenderer<TData extends NodeData>`)
- [x] Stable-identity rules documented; merged via useMemo in root
- [x] Naming consistent
- [x] Future-proof: edge-type registry shape allows polymorphism without API breaks
- [x] Optional props have documented defaults

**Notes:** Excellent open API. 3 types added during impl beyond description's 14 (FlowCanvasBackgroundConfig, FlowCanvasExportHandle, PortsAtProps).

---

## 3. Component code

- [x] Folder shape conforms to data-table + `lib/` deviation
- [x] Imports limited to allowlist (verified)
- [x] No commented-out blocks or stale `// removed` placeholders
- [x] No half-finished `// TODO later` without tracked follow-up
- [x] Comments only when WHY is non-obvious (xyflow-react-pro footgun fix well-commented)
- [x] No premature abstraction
- [x] `useCallback` / `useMemo` properly placed
- [x] **Module-scope `NODE_TYPES` / `EDGE_TYPES`** — xyflow-react-pro footgun fix correctly applied ✓
- [x] `parts/` decomposition purposeful
- [x] No swallowed errors
- [x] `'use client'` only where needed
- [ ] **`process.env.NODE_ENV` use (4 files)** — see **F-03**: violates description §6 #10
- [x] Root `<slug>.tsx` reasonably sized (84 lines)

**Notes:** F-03 surfaced.

---

## 4. Demo + usage

- [x] Demo covers description §5's 5 archetypes
- [x] Demo dataset is realistic (`FLOW_CANVAS_RICH` + `makeStressData` 200-node)
- [x] Demo exercises extensibility
- [x] Module-scope renderer definitions in demo.tsx
- [x] Usage page reads like docs
- [ ] N/A on fixtures registry shipping until F-01 fix
- [ ] Both light and dark mode look intentional — **deferred** (browser validation)
- [x] No console errors in code path
- [x] No lorem ipsum
- [x] `usage.tsx` is not a copy of `demo.tsx`
- [ ] **Usage import-path drift** — needs spot-check vs F-04 of kanban / rich-card pattern; not audited this session

**Notes:** Demo coverage matches plan exactly.

---

## 5. Dependencies

- [ ] **`meta.ts.shadcnDependencies` matches imports** — see **F-02**: declares `[]`, actual `["button", "context-menu", "dialog", "textarea"]`
- [ ] **`meta.ts.dependencies.npm` matches imports** — see **F-02**: missing `lucide-react`
- [ ] **`registry.json.registryDependencies`** — N/A (component not in registry.json — F-01)
- [ ] **No unused deps** — declared `@xyflow/react` is used; missing decls per F-02
- [x] Each external package's weight is justified
- [x] Version pins explicit (`@xyflow/react: ^12.10.2` — standard semver)
- [x] License compatible (xyflow MIT — no Pro license required for `hideAttribution`)
- [x] N/A — no internal sibling-component deps in shipped code

**Notes:** F-02 is the headline.

---

## 6. Design system

- [x] Fonts (no font imports in registry code)
- [x] Accent: `--primary` semantic token
- [x] Light/dark bgs use semantic tokens
- [x] Tailwind v4 syntax
- [x] No forbidden patterns
- [x] N/A — no reveals
- [x] Spacing rhythm consistent
- [x] Light + dark visual parity in shipped code
- [ ] **Component-scoped CSS uses namespace pattern** — partial: plan §3.10 mentioned `--xy-controls-*` overrides but `controls.tsx` wrapper not authored (F-05 sub-issue); audit deferred

**Notes:** Shipped code clean. Open question on `--xy-*` overrides for xyflow's built-in controls.

---

## 7. Accessibility

- [x] Keyboard nav: pan/zoom/select/delete via xyflow + `tabIndex={0}` on canvas root
- [x] No keyboard traps
- [x] ARIA: `role="region"`, `aria-label="Flow canvas"` default
- [x] Live regions: xyflow's announcer handles drag/connect events
- [x] Focus rings visible (`focus-visible:ring-2 focus-visible:ring-ring/50`)
- [x] Focus management
- [x] Reduced-motion respected
- [x] Color contrast — semantic tokens
- [x] Screen-reader pass — xyflow's accessibility announcements
- [x] Disabled states perceivable (interactive flag)
- [x] Backspace / Delete keys (via `deleteKeyCode`)

**Notes:** A11y wiring is solid.

---

## 8. Performance

- [x] **Module-scope `NODE_TYPES` / `EDGE_TYPES`** — footgun fix correctly applied
- [x] Stable identity for props crossing memo boundaries
- [x] Module-scope constants (NODE_TYPES, EDGE_TYPES, defaultPortTypes)
- [x] No `useStore(s => s)` pattern (xyflow has its own store)
- [x] **Virtualization**: `onlyRenderVisibleElements` toggle exposed
- [x] No O(n²) walks per render
- [x] Stress demo exists (`makeStressData` 200-node fixture)
- [x] No render loops
- [x] No `console.log` in render path
- [ ] Bundle weight not measured (build failed on F-cross-04)

**Notes:** Performance discipline is exemplary.

---

## 9. Registry distribution

- [ ] **Two items in `registry.json`** — see **F-01**: NEITHER item exists
- [ ] N/A — locked target convention can't be verified
- [ ] N/A — exclusion of `demo.tsx`/`usage.tsx`/`meta.ts` can't be verified
- [x] **`pnpm registry:build`** correctly skips this slug
- [ ] **Smoke install** — see **F-01**: returns 404; correctly classified as `expected-fail`
- [x] N/A — no internal sibling deps until F-01 fix
- [x] `description` matches `meta.ts.description` and `manifest.ts` registration
- [ ] `pnpm vercel-build` not run (F-cross-04)

**Notes:** F-01 is the headline.

---

## 10. Meta + manifest sync

- [x] `meta.ts.version: "0.1.0"` reflects reality
- [x] `meta.ts.status: "alpha"` honest
- [x] `meta.ts.tags` searchable
- [x] `meta.ts.description` matches doc-site card
- [x] Registered in `manifest.ts.REGISTRY` (3 imports verified)
- [ ] **`STATUS.md` Components table row file count** — see **F-06**: claims 39 files; actual is 30
- [ ] **`STATUS.md` Recent decisions** — needs entry for v0.1 review (added at sign-off)
- [x] `docs/component-versions.md` snapshot in sync
- [x] `meta.ts.updatedAt: "2026-05-06"` reasonable
- [ ] **`meta.ts.related` includes `force-graph`** — see **F-04**: stale (removed this session)

**Notes:** F-04 + F-06 surface here.

---

## 11. UI copy / text

- [x] Empty states present (custom-JSON collapsed view with `{ ... }` summary)
- [x] Error / warn messages actionable
- [x] Button labels verb-first
- [x] Tooltips terse (`title="Drag out to extract — Alt+drag to move"`)
- [x] No lorem ipsum in prominent copy
- [x] Pluralization correct
- [x] Sentence case
- [x] Brand names spelled correctly
- [x] No "TODO copy"
- [x] No "Click here"

**Notes:** Copy is clean.

---

## 12. Verification

- [x] `pnpm tsc --noEmit` clean
- [ ] **`pnpm lint`** — 1 pre-existing rich-card warning (F-06 of session 2's rich-card review); no flow-canvas-01-specific warnings
- [ ] **`pnpm build`** failed on F-cross-04 (env, not flow-canvas)
- [x] `pnpm registry:build` clean (correctly skips this slug per F-01)
- [ ] Docs-site detail page renders at `/components/flow-canvas-01` — **deferred**
- [ ] Browser validation — **deferred**
- [ ] **Smoke install** — `expected-fail` by design (per F-01)
- [x] Console clean during code-path inspection

---

## 13. Migration provenance

- [x] **N/A — greenfield component**

---

## 14. Cross-component coherence

- [x] Renderer-registry pattern consistent with workspace + kanban-board-01
- [x] Naming: `flow-canvas-01` uses `-01` suffix per family-variant convention
- [x] Public-helper export naming consistent
- [x] N/A — `--xy-*` overrides documented in plan §3.10 but not audited
- [x] Sealed-folder shape matches data-table + lib/ deviation
- [x] Category assignment correct
- [x] Status convention (alpha) consistent
- [ ] **Cross-component dep version drift** — `lucide-react` not declared (F-02); compare to kanban/rich-card which do declare
- [ ] **Edge-adapter abstraction** — F-05: plan called for `edge-adapter.tsx`; actual inlines. Future second-edge-renderer will need the abstraction; v0.2.

**Notes:** Renderer-registry pattern now confirmed across 4 components.

---

## Tally

- **Total items:** 133 (across 14 dimensions)
- **Ticked:** 100
- **N/A (with reason):** 9
- **Failed (becomes a finding):** 24 across 7 unique findings (F-01 through F-07)

> Companion narrative report: [`2026-05-08-v0.1.0-review.md`](./2026-05-08-v0.1.0-review.md)
