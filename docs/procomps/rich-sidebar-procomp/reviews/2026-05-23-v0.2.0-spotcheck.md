# Spot-check — `rich-sidebar` v0.2.0

## Header

| Field | Value |
|---|---|
| **Component** | `rich-sidebar` |
| **Category** | `navigation` |
| **Version under review** | `0.2.0` |
| **Status at review time** | `alpha` |
| **Reviewer** | Author (self-review, AI-assisted per rule §"Self-review vs peer review") |
| **Date** | 2026-05-23 |
| **Git SHA** | `fc68fed` (tip after C5; this commit is C6 ship — registry artifacts + STATUS + this file) |
| **Scope** | `spot-check` |
| **Trigger** | `v0.1.x → v0.2.0 minor bump touching public API` (mandatory GATE 3 per `.claude/rules/component-readiness-review.md`; patch-bump exemption does NOT apply) |
| **Verdict** | **Pass with follow-ups** |

---

## Dimensions covered

### Fixed core

| Dim | Name | Verified? | Notes |
|-----|------|-----------|-------|
| 1 | Procomp planning docs | **yes** | Description addendum (~310 lines, GATE 1 closed 2026-05-23) + plan addendum (~470 lines incl. re-validation log, GATE 2 closed 2026-05-23) both present + cross-referenced. Locks L41–L52 + 5 PQs threaded through plan §16. Two re-validation passes ran (GATE 1: addendum + cross-consistency; GATE 2: 4 findings → Set-based dedup + bypassFiltering at both levels + explicit defaults + R16 reframe). |
| 9 | Registry distribution | **yes** | `registry.json` updated for rich-sidebar (added `lib/href-resolver.ts` + `hooks/use-filtered-nav-sections.ts` to the files list = 36 ship files total, up from 34). `pnpm registry:build` produces `public/r/rich-sidebar.json` + `-fixtures.json` clean. Locked target convention `components/rich-sidebar/<sub-path>` preserved for new files. |
| 10 | Meta + manifest sync | **yes** | `meta.ts` v0.2.0 / `status: alpha` / `updatedAt: 2026-05-23` / `related: [account-switcher-01, todo-tree, todo-rich-card]`. Features list grew from 14 to 20 (6 new v0.2 entries). STATUS.md row will bump 0.1.1 → 0.2.0 in this commit. `validate:meta-deps` 49/49 clean — no new shadcn or npm peers added (v0.2 is internal-deps-only). |
| 12 | Verification | **yes** | `pnpm tsc --noEmit` clean. `pnpm lint` clean for all rich-sidebar files. `pnpm validate:meta-deps` 49/49 clean. `pnpm build` clean (49 component paths still prerender). `pnpm registry:build` regenerates artifacts including the 2 new file references. Path-b consumer-tsc smoke deferred to post-deploy per project pattern. |

### Rotating dimension

- **Chosen:** **Public API** (additive surface delta + locks L41–L52 + Finding 4's `bypassFiltering` scope correctness).
- **Why:** v0.2 is an additive minor bump that touches the public API in 6 ways (topSlot prop, 2 href props, 2 gate props, 1 bypass prop) + adds 2 new exports (`NavContext` type + `useFilteredNavSections` hook) + adds 2 NavItem fields (`ownerOnly`, `minMembers`). The contract must be (a) zero-breaking for v0.1.x consumers and (b) honor every locked semantic — especially Finding 4's "bypassFiltering applies at BOTH section + item levels" structural fix that surfaced during GATE 2 re-validation.
- **Verified?** **yes**
- **Notes:**
  - **Zero breaking** — `deriveVisibleEntries` extension uses destructure-with-defaults (`isOwner = false`, `currentMaxMembers = Infinity`, `bypassFiltering = false` per Finding 5). v0.1 callers (which don't pass these) get byte-identical behavior. Verified at [`lib/derive-visible-entries.ts:36-44`](../../../src/registry/components/navigation/rich-sidebar/lib/derive-visible-entries.ts).
  - **L41 topSlot** — verified at [`rich-sidebar.tsx`](../../../src/registry/components/navigation/rich-sidebar/rich-sidebar.tsx) inside `renderInnerChrome`. Renders ABOVE the brand row when truthy; short-circuits to `null` otherwise (no padding ghost). PQ3 honored: no role, no aria-label on the wrapper.
  - **L42/L43 href resolution** — verified at [`lib/href-resolver.ts`](../../../src/registry/components/navigation/rich-sidebar/lib/href-resolver.ts). Set-based dedup per Finding 3 (duplicate placeholders trigger ONE replaceAll each). 3-level precedence (callback → substitution → as-is). Dev-warn NODE_ENV-gated.
  - **L44/L45/L46 three-gate intersection** — verified at [`lib/derive-visible-entries.ts`](../../../src/registry/components/navigation/rich-sidebar/lib/derive-visible-entries.ts) `passesItemGates()` helper. `permission ∩ ownerOnly ∩ minMembers`; only `permission` populates the diagnostic struct (other gates don't carry diff-firing semantic).
  - **Finding 4 — bypassFiltering at BOTH levels** — verified: section branch at [`derive-visible-entries.ts`](../../../src/registry/components/navigation/rich-sidebar/lib/derive-visible-entries.ts) wraps section perm gate in `if (!bypassFiltering)`; item branch uses the shared `passesItemGates` which also short-circuits on `bypassFiltering`. The "section disappears, items remain" semantic inconsistency is structurally impossible.
  - **L47 useFilteredNavSections** — verified at [`hooks/use-filtered-nav-sections.ts`](../../../src/registry/components/navigation/rich-sidebar/hooks/use-filtered-nav-sections.ts). Items-only return per PQ1. Memoized over 5 inputs per Q16 + R14 mitigation. Documented consumer-side useMemo guidance for Set construction (R16 from Finding 6).
  - **L48 NavContext re-export** — type-only export verified in `index.ts`. No `useNavContext` hook shipped per the same lock.

---

## Smoke result

| Slug | Install | TSC | Time (ms) | Notes |
|------|---------|-----|-----------|-------|
| `rich-sidebar` | (deferred) | (deferred) | — | Path-b consumer-tsc smoke deferred to post-deploy per project pattern (F-02). Live Vercel artifact regen will run on push. |

Smoke harness will run against the published artifact within a session of push. Same-day patch budget reserved for v0.2.1 if F-cross-13 surfaces — but no new shadcn primitives in v0.2.0 (only existing v0.1 surface: Tooltip + Sheet + DropdownMenu, all already defensively guarded from v0.1.0 ship), so the risk is low.

---

## Findings (4)

#### F-01 — Path-b consumer-tsc smoke deferred to post-deploy

- **Severity:** 🔸 Medium
- **Dimension:** Verification
- **Location:** Project verification protocol
- **Observed:** Path-b smoke runs against the live Vercel artifact, which requires push first. Same pattern as the last 5 ships (account-switcher-01 v0.1.0, rich-sidebar v0.1.0/v0.1.1, todo-tree v0.1.1, todo-rich-card v0.1.1).
- **Suggested fix:** Run smoke immediately after the push lands on Vercel. v0.2.1 patch reserved if any F-cross-13 hit surfaces.

#### F-02 — Demo's V02 sections render below a 4th demo block; not at the top

- **Severity:** 🔹 Low
- **Dimension:** Public API (consumer comprehension)
- **Location:** [`demo.tsx`](../../../src/registry/components/navigation/rich-sidebar/demo.tsx) (V02MultiContextDemo + V02HeadlessFilterDemo appended at the end)
- **Observed:** v0.2 demos are below the v0.1 demos. Consumers landing on the page see the older surface first. Acceptable for now — preserving v0.1 demo order is the conservative choice for an additive bump — but a future v0.3 ship could reorganize so the latest surface leads.
- **Suggested fix:** Reorder demos at next major polish pass; not a v0.2.1 blocker.

#### F-03 — `useFilteredNavSections` JSDoc could call out the cross-procomp composition pattern explicitly

- **Severity:** 🔹 Low
- **Dimension:** Public API (docs)
- **Location:** [`hooks/use-filtered-nav-sections.ts`](../../../src/registry/components/navigation/rich-sidebar/hooks/use-filtered-nav-sections.ts) JSDoc
- **Observed:** The hook docs mention "NOT coupled to <RichSidebar>" but don't show a quick "render your own UI with this" example. The detail panel of the docs site usage.tsx + procomp guide cover the pattern, but inline JSDoc could too.
- **Suggested fix:** Add 4-line `@example` block to the hook's JSDoc. Pure docs polish; v0.2.1 batch.

#### F-04 — V02MultiContextDemo casts switcher items via `as never` ✅ CLOSED in-review

- **Severity:** 🔹 Low
- **Dimension:** Public API (demo code hygiene)
- **Location:** [`demo.tsx`](../../../src/registry/components/navigation/rich-sidebar/demo.tsx) (now resolved)
- **Observed:** The `V02_SWITCHER_ITEMS` array was declared with `as const`, narrowing the type too strictly; cast was `as never` workaround.
- **Fix applied:** Replaced `as const` with explicit `ReadonlyArray<SwitcherItem>` type annotation on the constant + dropped the `as never` cast. Imported `SwitcherItem` type from the sibling procomp. Verified tsc + lint clean.

---

## Verdict justification

**Pass with follow-ups.**

All locks L41–L52 verified threading correctly through the implementation. The 6 GATE 2 re-validation findings landed in code at the expected sites:
- Finding 3 (Set-based dedup) ✅ in `lib/href-resolver.ts`
- Finding 4 (bypassFiltering at both levels) ✅ in `lib/derive-visible-entries.ts`
- Finding 5 (explicit defaults) ✅ in `lib/derive-visible-entries.ts:36-44`
- Finding 6 (R16 reframe to Set ref instability) — documented in the hook's JSDoc

Zero breaking changes. v0.1.x callers compile + run unchanged (verified via `pnpm build` re-running production prerender with 49 component paths all green; rich-sidebar still in the static list).

4 findings surfaced — all non-blocking; F-04 closed in-review:
- F-01 is the project's established defer pattern, not a real omission
- F-02 + F-03 are docs polish (v0.2.1 batch)
- F-04 ✅ closed in-review (1-line type-annotation fix applied before commit)

No 🚫 Blockers, no ⚠️ High findings.

---

## Follow-up assignments

| Finding | Owner | Bump target |
|---|---|---|
| F-01 (path-b smoke) | author | post-deploy same-session; v0.2.1 patch reserved if F-cross-13 hits |
| F-02 (demo order) | author | next v0.x major polish; not scheduled |
| F-03 (JSDoc @example) | author | v0.2.1 docs batch |
| F-04 (`as never` → `as ReadonlyArray<SwitcherItem>`) | author | ✅ closed in-review |
