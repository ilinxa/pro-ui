# Review checklist — `entity-picker` v0.1.0

> Filled 2026-05-08 (sweep session 6). Companion report: [`2026-05-08-v0.1.0-review.md`](./2026-05-08-v0.1.0-review.md).

## Header
- **Reviewer:** Claude (sweep session 6) · **Date:** 2026-05-08 · **Git SHA:** `b5ad5d6`
- **Scope:** full (Tier 1) · **Trigger:** sweep — pre-public Tier 1 review

## Per-dimension summary

| Dim | Name | Status | Notes |
|---|---|---|---|
| 1 | Procomp planning docs | ✓→⚠ | description (10 locked Qs, no refinements on re-validation) + plan (772 lines, 10 locked Q-Ps + 6 validate-pass refinements + 12 plan-stage refinements) — most carefully validated Tier 1 plan in the sweep. **F-02**: `guide.md` MISSING (F-cross-01 carrier; closes 3 of 3 Tier 1 carrier list). **F-05**: plan §6.3 documents `useRef`-callback-ref pattern but implementation uses `useState`-as-callback-ref (deliberate per in-code comment, undocumented in plan) |
| 2 | Public API | ✓ | Mode-aware value typing via TS function overloads (Q-P1 lock); 14 type exports including `EntityLike`, both prop variants (Single/Multi), all 3 slot contexts, handle. Generic `<T extends EntityLike>` preserved through React 19 ref-as-prop. `id?: string` validate-pass refinement for properties-form integration |
| 3 | Component code | ✓→🔸 | 18 files match plan §7.1 exactly. `parts/selected-indicator.tsx` correctly inlined into result-row per plan §12.5 #12. `<div role="button">` trigger element type per Q-P8 + §6.1 (avoids nested-interactive HTML). Selection equality dedup via `singleSelectionEqual` / `multiSelectionEqual` (Q-P4). Try/catch around match predicate (line 192-205). **F-01**: 3 `process.env.NODE_ENV` occurrences — but **plan §12.5 #5 explicitly LOCKS this approach** as deliberate; severity High because it's the canonical evidence for relaxing F-cross-08's underlying rule, not because the component is wrong |
| 4 | Demo + usage | ✓→🔸 | 7-tab demo per success #10. `demo.tsx` uses Badge + Button + Tabs (only Badge is in shipped parts; Button + Tabs are demo-only — see F-03). **F-04**: `usage.tsx:17` producer-side import path (F-cross-06 — now **8/8 systemic** Tier 1) |
| 5 | Dependencies | ⚠ | 3 shadcn primitives actually used in shipped files (badge, command, popover) per plan §10.2. **F-03 (High)**: `meta.shadcn` declares 5 (over-declares `button` + `tabs` — demo-only); `meta.npm` declares phantom `radix-ui: ^1.4.3` (no source imports it). registry.json `registryDependencies` correctly minimal at 3. cmdk peer ✓ correctly declared |
| 6 | Design system | ✓ | Semantic tokens via shadcn primitives; KindMeta supports `var(--*)` CSS variables AND OKLCH literals (plan §3.1) |
| 7 | Accessibility | ✓ | WAI-ARIA 1.2 combobox pattern: `role="combobox"` (cmdk) + `role="listbox"` + `aria-expanded` + `aria-multiselectable` + `aria-haspopup="listbox"` (trigger). cmdk handles ↑/↓/Enter/Esc natively. `<div role="button">` trigger with explicit click + keyboard handlers; chip remove buttons remain valid `<button>` children. `id` prop wires `<label htmlFor>` for properties-form integration |
| 8 | Performance | ✓ | `useItemsById` memoized Map for O(1) cmdk filter lookup (plan §10.1); cmdk handles its own keystroke debouncing internally; popover content unmounted between opens; query resets on close (line 70-76). No virtualization (plan §3 — v0.2 for >500 items) |
| 9 | Registry distribution | ✓ | 16 source files in `registry.json` with locked `target: "components/entity-picker/<sub>"` convention; `entity-picker-fixtures` ships only `dummy-data.ts`. **Notable**: smoke install ✓ pass (8581ms) in session 5 fresh run despite F-cross-09 — entity-picker is in the F-cross-09-immune subset (useful signal for session 7 investigation) |
| 10 | Meta + manifest sync | ⚠ | version + status accurate; tags reflect graph-system + combobox. `related: ["properties-form", "filter-stack"]` — populated but **F-06 (Low)**: missing detail-panel + markdown-editor (markdown-editor uses parallel kind-badged-picker pattern in its wikilink autocomplete). **F-03** (above): meta over-declares deps |
| 11 | UI copy | ✓ | "Search…" placeholder default; "No results" / "Nothing to pick from" empty-state copy locked per Q-P5; renderEmpty slot for hosts wanting query-string copy. Backspace-on-empty-search-removes-last-chip (Q-P3) — standard combobox-with-tags pattern |
| 12 | Verification | ✓ | tsc + lint + registry:build all clean. `pnpm build` blocked on F-cross-04 |
| 13 | Migration provenance | n/a | Greenfield component (no `> Migration origin:` line) |
| 14 | Cross-component coherence | ⚠ | Mirrors properties-form / filter-stack / markdown-editor on generic-typing posture (Q-P10 ref-as-prop pattern); mirrors filter-stack reference-stability footgun pattern. **Phantom `radix-ui` now confirmed 3/3 forms components — fully systemic** within forms tier. F-cross-06 closes Tier 1 at 8/8. F-cross-01 closes Tier 1 at 3/3. F-cross-08 affirmed 4/9 with this carrier. Plan §12.5 #5 is the definitive evidence for relaxing F-cross-08's rule |

## Findings (full detail in report)

- **F-01 (High)** — F-cross-08 carrier — 3 `process.env.NODE_ENV` occurrences (intentional per plan §12.5 #5; now 4/9 Tier 1; the canonical evidence for relaxing F-cross-08's rule sweep-wide)
- **F-02 (High)** — F-cross-01 carrier — `guide.md` missing (closes Tier 1 carrier list at 3/3)
- **F-03 (High)** — `meta.ts` over-declares deps (phantom `radix-ui` + demo-only `button`/`tabs`)
- **F-04 (Medium)** — F-cross-06 carrier — `usage.tsx:17` import-path drift (8/8 systemic — fully Tier 1)
- **F-05 (Medium)** — Plan §6.3 documents `useRef` callback ref but implementation uses `useState`-as-callback-ref (deliberate but undocumented in plan)
- **F-06 (Low)** — `meta.related` could include detail-panel + markdown-editor

## Tally

- Total dimensions covered: 14 (13 active + 1 N/A)
- Failed → finding: 6 (3 High, 2 Medium, 1 Low)
- Verdict: **Pass with follow-ups**

## Notes

Entity-picker closes Tier 1 at 9/9 with this paired review. The component is the most carefully-validated Tier 1 plan implementation; every discipline shift (trigger element type to `<div role="button">`, dual-ref pattern via Slot composition, `useState`-as-callback-ref refinement, `<Check>` inlining into result-row dropping `parts/selected-indicator.tsx`) is either explicitly locked in plan §12.5 refinements or has clear in-code justification (only F-05's `useState` deviation is undocumented in plan).

The phantom-`radix-ui` finding now confirms 3/3 forms components — fully systemic. The pattern is best resolved sweep-wide via `pnpm validate:meta-deps` lint at session 7. Entity-picker plan §12.5 #5's explicit lock of `process.env.NODE_ENV` as the dev-warn-gate mechanism is the definitive evidence for relaxing F-cross-08's underlying rule (description §6 #10 "no `process.env`").

Notable smoke datapoint: entity-picker passes (8581ms) while workspace + markdown-editor + properties-form fail — suggests F-cross-09's CLI regression is selective. Useful signal for session 7 investigation.
