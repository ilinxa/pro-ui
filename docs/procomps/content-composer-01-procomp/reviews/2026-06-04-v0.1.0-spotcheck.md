# Spot-check — `content-composer-01` v0.1.0

> GATE 3 readiness review (per [`.claude/rules/readiness-review.md`](../../../../.claude/rules/readiness-review.md)). First-ship spot-check: 4 fixed dims + 1 rotating (**composition integrity**, locked for this shell per the plan §GATE-3).

---

## Header

| Field | Value |
|---|---|
| **Component** | `content-composer-01` |
| **Category** | `media` |
| **Version under review** | `0.1.0` |
| **Status at review time** | `alpha` |
| **Reviewer** | Claude (AI-assisted; composition surface warrants it) |
| **Date** | 2026-06-04 |
| **Git SHA** | `a45b379` (C16; review = working tree pre-C18) |
| **Scope** | `spot-check` |
| **Trigger** | first-ship v0.1.0 |
| **Verdict** | **Pass with follow-ups** |

---

## Dimensions covered

### Fixed core

| Dim | Name | Verified? | Notes |
|-----|------|-----------|-------|
| 1 | Procomp planning docs | yes | description + plan (GATE 1 + 2 signed off) + guide (authored C17, this commit) all present + current. Plan §file-map count reconciled at C16 (28→33 base). |
| 9 | Registry distribution | yes | `registry.json` base (33 `registry:component` files, `target: components/content-composer-01/<sub-path>`) + `content-composer-01-fixtures` (dummy-data.ts). `registry:build` artifact spot-checked: 33 files, 11 registryDependencies (3 `@ilinxa/*` + 8 shadcn URLs), **no demo/usage/meta**. Live endpoint resolves post-deploy (C18). |
| 10 | Meta + manifest sync | yes | meta v0.1.0 alpha; shadcn deps (8) match shipped imports; `validate:meta-deps` 52/52 clean; manifest entry present (C1). STATUS.md row lands C18. |
| 12 | Verification | yes (smoke → F-01) | tsc 0 · lint 81/22 baseline (no content-composer findings) · meta-deps 52/52 · `pnpm build` ✓ (61 static pages — full composer with lazy Plate + media-editor + json-form prerenders) · `registry:build` ✓. **Consumer-tsc smoke is C18** (see F-01). |

### Rotating dimension — Composition integrity (LOCKED)

- **Chosen:** Composition integrity.
- **Why:** this shell composes three heterogeneous substrates (json-form / Plate / media-editor) behind one uniform `SlotHandle`; prop-flow correctness, no leaked internals, and correct dirty/validity aggregation across the asymmetric slots are the load-bearing risk.
- **Verified?** yes.
- **Notes:**
  - **Prop flow:** the root threads `{ slotConfig, value, onChange, ctx, handleRef }` to each substrate via `SlotMount`; `onChange` wraps back into the discriminated `ComposerStepValue` at the root. One documented dispatch-boundary cast at the `SlotMount` call site (`SlotHandle` is invariant in `TValue` via `loadValue`) — runtime-correct, localized. ✅
  - **No prop-drilling hacks:** state is fully lifted to the root; substrates are controlled; `useComposerState` owns the draft + FSM. ✅
  - **Dirty aggregation:** refined to **draft-level** (`draft !== savedDraft`) rather than the plan's handle-aggregate — single correct signal under single-step mounting AND structurally averts the Plate autosave-loop (#1 trap). The body baseline (`use-body-dirty`) stays only for the headless `SlotHandle.getIsDirty` contract. ✅
  - **Gate correctness:** value-based gating against the always-current draft (per-mutation `onChange`) — required because news has 3 metadataFields steps and only the active one mounts; the plan's handle-keyed-by-slot publish-all would read stale handles. The active metadata step still gets full json-form validation. ✅ (see F-02 for the non-active-step nuance)
  - **Substrate boundary:** the uniform `SlotHandle` carries an optional media-only `export?` (see F-04) — a minor leak, documented + duck-typed.

---

## Smoke result

| Slug | Install | TSC | Time (ms) | Notes |
|------|---------|-----|-----------|-------|
| `content-composer-01` | **pass** | **fail (34 own + 18 upstream)** | — | Ran 2026-06-04 post-deploy. Install ✅. Consumer-tsc fail = the F-cross-11 brittleness signal: 34 own-file errors, all the cross-procomp `/types` rewriter mangling (F-01, → v0.1.1); 18 upstream errors are pre-existing F-cross-13 in json-form/media-editor/etc. (not this component's scope). |

---

## Findings (5)

#### F-01 — Consumer-tsc smoke CONFIRMED FAIL: cross-procomp `/types` rewriter mangling (F-S1 at scale)

- **Severity:** ⚠️ High (consumer-breaking) — **promoted from Medium after the smoke ran**
- **Dimension:** Verification
- **Location:** every cross-procomp `@/registry/components/<...>/types` import (`types.ts`, `lib/gates.ts`, `lib/hydration.ts`, `lib/clamp-media-sources.ts`, `lib/upload.ts`, `configs/*.config.ts`, `parts/json-form-substrate.tsx`, `parts/media-substrate.tsx`)
- **Observed (smoke 2026-06-04, deployed artifact live attempt 1):** install ✅ (all registryDependencies resolved). Consumer-tsc: **34 content-composer errors** — the shadcn 4.6.0 path rewriter collapsed EVERY cross-procomp `/types` import (`json-form/types`, `media-editor-01/types`, `content-card-news-01/types`) to `@/components/article-body-01/types` → `TS2305 has no exported member` for FormSchema / MediaEditorState / ContentCardItem / … This is the F-S1 `/types` bug, hit hard because the composer imports types from THREE procomp `/types` files. **The `.tsx`-path import (ArticleBodyValue from `article-body-01.tsx`) and the deep-lib imports (`condition-evaluator`/`path`/`define-field-renderer`) resolved cleanly — only `/types` is mangled.** (Separately, 18 PRE-EXISTING upstream errors in json-form/media-editor/story-composer/code-block — Base-UI `render`/`showCloseButton`/`size="xs"` divergence + implicit-any — are those components' own F-cross-13 items, NOT content-composer's scope.)
- **Suggested fix (v0.1.1):** route every cross-procomp TYPE import through the source procomp's `.tsx` component-file path (the rewriter-preserved path; field-richtext precedent). Requires additive tail `export type {…}` on `json-form.tsx` (FormSchema/Condition/ConditionOrFn/FieldDefinition/FieldOptionsResolver/FieldRenderer/JsonFormHandle/FieldConfig), `media-editor-01.tsx` (MediaEditor01Props/Handle/State/ExportMetadata/InitialSource/ComposerMode/EditTool/MediaSource/AspectRatio), `content-card-news-01.tsx` (ContentCardItem/ContentStatus/NewsArticleAuthor/NewsPublisher/NewsVisibility/ContentSensitivity/ContentPaywall) — then switch the composer's imports from `<slug>/types` to `<slug>/<slug>`. Re-smoke to 0 content-composer errors. Owner: author. Target: **v0.1.1** (the established ship→smoke→patch→re-smoke pattern; touches 3 source procomps' artifacts).
- **✅ RESOLVED in v0.1.1 (`0c55016`):** all of the above applied. **PLUS** a second, unanticipated half: `content-card-news-01` (deliberately omitted from registryDependencies as a "type-only, erased" dep per the plan) had to be ADDED as a registryDependency — consumer-tsc needs the module installed to resolve the types, and the rewriter left its path un-mapped (kept `data/`, file missing) precisely because it wasn't installed. After the fix a LOCAL-registry re-smoke (served `public/r`, repointed the consumer, `shadcn add` + consumer-tsc) gives **0 content-composer errors** (was 34). **Lesson: a cross-procomp TYPE dependency still needs (a) imports via the `.tsx` path AND (b) a registryDependency entry — "types are erased at build" does NOT mean "skip the registryDependency"; consumer-tsc resolution requires the module.**

#### F-02 — Non-active-step metadata gate is required-presence only

- **Severity:** 🔹 Low
- **Dimension:** Composition integrity
- **Location:** `lib/gates.ts:evaluateMetadataValue` / `evaluateStep`
- **Observed:** for the ACTIVE metadata step the gate calls `handle.validate()` (full json-form trigger + isValid — catches pattern/custom validators). For NON-active steps (publish-all over unmounted steps) it falls back to a required-presence check over stored values. A field that is present-but-pattern-invalid on a non-active step could pass the publish gate (it was fully validated when the user last left it forward, so this is narrow).
- **Suggested fix:** for full parity, validate non-active metadata values against the json-form Zod schema headlessly (reach `json-form/lib/schema-compiler`), or mount all steps hidden (rejected — media camera-permission risk). Owner: author. Target: v0.1.x (if a config surfaces it).

#### F-03 — Inline body-image upload unwired

- **Severity:** 🔹 Low
- **Dimension:** Composition integrity
- **Location:** `parts/body-substrate-plate.tsx` (`onImageUpload` optional, unwired)
- **Observed:** the composer `uploader` is `(blob, ExportMetadata) => …` (media-export-specific). Fabricating an `ExportMetadata` (textOverlays/stickers/adjustments/crop) for an inline article image is semantically wrong, so Plate falls back to its URL-prompt image insertion in v0.1.
- **Suggested fix:** add a dedicated `bodyImageUploader?: (file) => Promise<{ src }>` prop and thread it. Owner: author. Target: v0.1.2 (rolled from v0.1.1 — v0.1.1 was spent on the F-01 smoke fix).

#### F-04 — `export?` leaks into the uniform `SlotHandle`

- **Severity:** 🔹 Low
- **Dimension:** Composition integrity
- **Location:** `types.ts:SlotHandle.export?`
- **Observed:** the otherwise-uniform `SlotHandle<TValue>` carries an optional media-only `export?` so the shell can pull the hero blob at publish. The shell duck-types it; other slots omit it. It's a minor abstraction leak into the generic interface.
- **Suggested fix:** introduce a `MediaSlotHandle extends SlotHandle<MediaSlotValue>` subtype and narrow at the media call site. Owner: author. Target: v0.2.0.

#### F-05 — Video re-edit blob re-attach deferred

- **Severity:** 🔹 Low
- **Dimension:** Composition integrity
- **Location:** `parts/media-substrate.tsx:loadValue`
- **Observed:** `loadValue` restores photo via `editorState.imageSrc` (videoBlob nulled). A VIDEO draft would restore with an empty source — the locked invariant (re-attach the blob from the shell Map / re-fetch from `exportedUrl` before `loadState`) is not yet wired. News v0.1 is photo-only, so this is unexercised.
- **Suggested fix:** wire video-blob re-attach alongside the first video config. Owner: author. Target: v0.2.0 (with the video config / post ship).

---

## Verdict

**Verdict:** **Pass with follow-ups**

**Justification:** All four fixed dims pass; composition integrity is sound (clean state lifting, correct value-based gating, draft-level dirty that structurally avoids the autosave-loop trap). The five findings are all Low/Medium and deferred — none blocks the news first-ship. The one Medium (F-01) is the expected consumer-tsc smoke, tracked for C18 per the established 4-ship pattern.

---

## Follow-up actions

| Action | Linked finding | Owner | Target version | Status |
|---|---|---|---|---|
| Run path-b consumer-tsc smoke; patch F-cross-13 sub-traps | F-01 | author | v0.1.1 | ✅ Closed (`0c55016`) |
| Headless full json-form validation for non-active metadata steps | F-02 | author | v0.1.x | Open |
| Dedicated body-image uploader prop | F-03 | author | v0.1.2 | Open (rolled from v0.1.1 — v0.1.1 was the F-01 smoke fix) |
| `MediaSlotHandle` subtype (drop `export?` from the generic handle) | F-04 | author | v0.2.0 | Open |
| Video re-edit blob re-attach | F-05 | author | v0.2.0 | Open |

---

## Sign-off

- [x] Verdict is set above
- [x] Smoke row recorded (pending C18, tracked as F-01)
- [ ] Tracker row updated at `docs/reviews/sweep-tracker.md` (C18)
- [x] No Needs-revision/Block → no STATUS mirror required (follow-ups land in the decision file at C18)

**Reviewer signature:** Claude (AI-assisted) — 2026-06-04
