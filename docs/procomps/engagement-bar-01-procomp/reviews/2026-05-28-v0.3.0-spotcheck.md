# Spot-check — `engagement-bar-01` v0.3.0

> Tier-2 procomp spotcheck. Shared fixed core (4 dims) + rotating Public API (per Q-PP-9 lock). Time-box: 25–35 min.

---

## Header

| Field | Value |
|---|---|
| **Component** | `engagement-bar-01` |
| **Category** | `data` |
| **Tier** | `pro-component` |
| **Version under review** | `0.3.0` |
| **Status at review time** | `alpha` |
| **Reviewer** | Claude (self-review — per readiness-review rule §"Tier-scaled review mode" — pro-component v0.1.0 first ship / minor-bump additive expansion accepts self-review) |
| **Date** | 2026-05-28 |
| **Git SHA** | working tree (post-fa2b53a; not yet committed) |
| **Scope** | `spot-check` |
| **Trigger** | `public-api-touching minor bump` (additive expansion — new union arm + new prop + new delta variants + new state fields + new reducer action + new handle methods + new labels) |
| **Driver** | [`docs/consumer_order/ilinxa-proui-improvement-spec.md`](../../../consumer_order/ilinxa-proui-improvement-spec.md) (ILX-1 HIGH + ILX-2 MEDIUM from `social-moduls-python` backend team) |
| **Verdict** | **Pass with follow-ups** (post-push smoke + 1 documented host-responsibility caveat) |

---

## Dimensions covered

### Fixed core (Dims 1–4)

| Dim | Name | Verified? | Notes |
|-----|------|-----------|-------|
| **1** | Planning docs (description / plan / guide) | yes | [`engagement-bar-01-procomp-description-v0.3.0.md`](../engagement-bar-01-procomp-description-v0.3.0.md) — GATE 1 signed off 2026-05-28 with 3 Q-Ps locked (Q-P1=(b) single `kinds` array / Q-P2=(c) `clearOnTap` default `true` / Q-P3=(a) allow like+reaction coexist). [`engagement-bar-01-procomp-plan-v0.3.0.md`](../engagement-bar-01-procomp-plan-v0.3.0.md) — GATE 2 with 10 Q-PPs locked + re-validated to absorb 6 findings (F-01 Q-P2 self-contradiction / F-02 Slot trap on picker `anchor` / F-03 `onCountClick` split semantics / F-04 optimistic count merge rule / F-05 demo state shape / F-06 neutral-icon override confusion). Guide [`engagement-bar-01-procomp-guide.md`](../engagement-bar-01-procomp-guide.md) extended with new "Recipe 11 — FB-style multi-kind reactions" + updated Limitations + v0.4+ candidates section. |
| **2** | Registry distribution | yes | Local `pnpm registry:build` regenerated `public/r/engagement-bar-01.json` cleanly — 18 files (was 16; +2 reaction-picker + reaction-action). All files retain locked target convention: `type: "registry:component"`, `target: "components/engagement-bar-01/parts/<file>"`. `registryDependencies` correctly extended `["button", "avatar", "input"]` → `["button", "avatar", "input", "popover"]` (the picker's `<Popover>` dep). No `demo.tsx` / `usage.tsx` / `meta.ts` in artifact. Live endpoint `https://ilinxa-proui.vercel.app/r/engagement-bar-01.json` will resolve to v0.3.0 post-deploy. |
| **3** | Meta + manifest sync | yes | [`meta.ts`](../../../../src/registry/components/data/engagement-bar-01/meta.ts) — version `0.2.1 → 0.3.0` ✓; `updatedAt: "2026-05-28"` ✓; description expanded to mention reaction + reactors preview + long-press ✓; 5 new feature entries (reaction kind / ReactionPicker / ReactionAction / reactionsPreview slot / Defense 2) ✓; "reactions" tag added ✓; `dependencies.shadcn: ["avatar", "button", "input", "popover"]` ✓. Stale-feature-bullet sweep applied during C7 re-validation (H1/H2/H3 fixed — added "reaction" to action-list bullet, extended handle-methods bullet, updated 8-key → 12-key labels bullet). STATUS.md row update is the user's responsibility post-deploy. |
| **4** | Verification | yes | `pnpm tsc --noEmit` clean repo-wide (verified after every C# commit). `pnpm validate:meta-deps` 49/49 clean. `pnpm registry:build` clean (49 slugs built). `pnpm lint` — pre-existing baseline (file-manager + file-tree + rich-sidebar — 21 errors unrelated to engagement-bar-01; baseline carry from master `fa2b53a`). **Smoke harness against live Vercel deferred** to post-commit/post-deploy (cannot smoke working-tree changes — Vercel registry pulls from master). Pattern matches v0.2.0 spotcheck precedent. Manual interaction in 3-browser matrix (Chromium / Firefox / Safari) is the post-push gate covered in the GATE 3 follow-up. |

### Rotating dimension — **Public API** (per Q-PP-9)

**Chosen:** Dim 4 — Public API.
**Why:** v0.3.0 is the largest additive surface in the procomp's history (new union arm + new prop + 3 deltas + 3 state fields + new reducer action + 2 handle methods + 4 labels + 1 new exported type). Matches v0.2.0's rotating choice for consistency.

**Verified ✓** with the audit trail below.

#### Public API additions

| Addition | Where | Notes |
|---|---|---|
| `EngagementReactionKind` interface | [`types.ts`](../../../../src/registry/components/data/engagement-bar-01/types.ts) lines 14-25 | `{ key, icon, label, count, color? }` — single source of truth per Q-P1 lock. Exported via barrel ✓ (A1 finding caught + fixed during C1 re-validation). |
| `kind: "reaction"` union arm | `types.ts` lines 78-109 | `kinds[]` + `totalCount` + `viewerReaction?` + `onSelect?` + `onCountClick?` + `clearOnTap?` + `align?`. JSDoc documents Q-P3 hybrid lock. |
| `EngagementDelta` — 3 new variants | `types.ts` lines 139-148 | `reaction-changed` (server-replace, viewer optional) + `reactor-added` + `reactor-removed` (pass-through). Parallel to existing `liker-added/removed`. |
| `EngagementState` — 3 new nullable fields | `types.ts` lines 236-247 | `reactionCounts: Record<string,number>` \| null + `reactionTotalCount: number` \| null + `viewerReaction: string` \| null. Source-of-truth rule documented inline per Q-PP-4. |
| `EngagementLocalAction` — `reaction-select` | `types.ts` line 253 | `{ reactionKind: string` \| `null }`. |
| `EngagementBarLabels` — 4 new keys | `types.ts` lines 170-177 | `react` / `removeReaction` / `openReactionsPanel` / `reactionPickerLabel` (+ defaults in `DEFAULT_ENGAGEMENT_BAR_LABELS`). |
| `EngagementBar01Handle` — 2 new methods | `types.ts` lines 214-223 | `triggerReaction: (kind: string` \| `null) => void` (catalog-validated, Defense 1 microtask defer) + `getCurrentReaction: () => string` \| `null`. |
| `EngagementBar01Props` — `reactionsPreview` | `types.ts` line 200 | Parallel to `likersPreview`; unconditional when provided; both render in order. |

#### Zero v0.2.x breakage verified

Every existing v0.2.x export carries unchanged:
- `EngagementBar01`, `EngagementHeartBurst`, `engagementReducer`, `useEngagementState`, `deriveStateFromActions`, `formatEngagementCount`, `LikersStrip`, `ShareMenu` — components/functions unchanged
- All v0.2.x types (`EngagementBar01Props`, `EngagementBar01Handle`, `EngagementAction`, `EngagementDelta`, `EngagementState`, `EngagementLocalAction`, `EngagementLikeUser`, `EngagementLikerProfile`, `EngagementBarLabels`, etc.) — STRUCTURAL EXTENSIONS only; existing consumers compile unchanged
- `DEFAULT_ENGAGEMENT_BAR_LABELS` — 4 new defaults added; existing 8 keys present

**Consumer-side mechanical fixes** (documented in plan §3.2): any exhaustive `switch (action.kind)` or `switch (delta.kind)` in consumer code now requires the new arms covered (TS-flagged at compile time; trivial to fix with `default:` or explicit case + no-op).

#### Q-PP locks compliance audit

| Lock | Implementation site | Status |
|---|---|---|
| Q-P1 — single `kinds` array catalog | `types.ts` + picker reads `kinds` directly + demo `REACTION_KINDS` | ✓ |
| Q-P2 — `clearOnTap?: boolean` default `true` | `reaction-action.tsx` lines 100-110 (tap matrix) | ✓ |
| Q-P3 — like + reaction coexist | Demo "Hybrid" tab + no enforcement in main switch | ✓ |
| Q-PP-1 — Popover not DropdownMenu/Tooltip; F-02 restructure (ReactionAction owns popover) | `reaction-action.tsx` lines 205-218 | ✓ |
| Q-PP-2 — 350ms long-press + 10px tolerance | `reaction-action.tsx` lines 20-21 (constants) + 58-92 | ✓ (E7 setPointerCapture fix applied) |
| Q-PP-3 — Defenses 1+2 not 3 | Defense 1 via `queueMicrotask` in `triggerReaction` + tap handlers; Defense 2 via sync effect in [`use-engagement-state.ts`](../../../../src/registry/components/data/engagement-bar-01/hooks/use-engagement-state.ts) lines 355-379 | ✓ (B2 finding caught + fixed during C2 re-validation) |
| Q-PP-4 — source-of-truth rule for counts | `mergedCounts` useMemo in `reaction-action.tsx` lines 135-141 + reducer init in [`use-engagement-state.ts`](../../../../src/registry/components/data/engagement-bar-01/hooks/use-engagement-state.ts) lines 66-77 | ✓ |
| Q-PP-5 — icon/count 2-zone split | `reaction-action.tsx` lines 162-203 (mirrors like-action) | ✓ (F-03 finding caught + fixed during plan re-validation) |
| Q-PP-6 — picker layout | [`reaction-picker.tsx`](../../../../src/registry/components/data/engagement-bar-01/parts/reaction-picker.tsx) lines 56-104 | ✓ (D1 always-apply-color fix during C3 re-validation) |
| Q-PP-7 — `reactionsPreview` in all 3 variants | [`engagement-bar-01.tsx`](../../../../src/registry/components/data/engagement-bar-01/engagement-bar-01.tsx) lines 152-155 + 207-210 | ✓ |
| Q-PP-8 — F-cross-13 audit | Popover `open`/`onOpenChange` signatures stable across Radix/Base UI variants | ✓ |
| Q-PP-9 — rotating dim = Public API | This review | ✓ |
| Q-PP-10 — popover in shadcn deps | `meta.ts` + `registry.json` | ✓ |

---

## Re-validation findings absorbed during implementation

Tracked here for audit completeness — these were ALL caught + fixed BEFORE this spotcheck. Pattern follows the [`feedback_re_validation_pass_catches_real_issues`](../../../../C:/Users/AsiaData/.claude/projects/e--2026-ilinxaDOC-ilinxa-ui-pro/memory/feedback_re_validation_pass_catches_real_issues.md) memory.

### Plan-doc re-validation (6 findings)

| # | Sev | Description | Resolution |
|---|---|---|---|
| F-01 | 🚫 HIGH | Q-P2 self-contradiction (`clearOnTap=false` "no-op" vs C4 "open picker") | Locked open-picker as canonical fallback. Test plan row updated. |
| F-02 | 🚫 HIGH | F-cross-13 Slot trap on picker `anchor` ReactNode prop | Restructured: ReactionAction owns popover; plain `<button>` under asChild. |
| F-03 | ⚠️ MEDIUM | `onCountClick` split semantics undefined | Locked Q-PP-5 to mirror `like-action.tsx`'s icon/count 2-zone DOM. |
| F-04 | ⚠️ MEDIUM | Optimistic count merge rule missing | Locked Q-PP-4 source-of-truth: `state.reactionCounts[k.key] ?? k.count`. |
| F-05 | 🔹 LOW | Demo state shape unspecified | Locked: rely on internal mirror; demo `onSelect = console.log`. |
| F-06 | 🔹 LOW | Q-PP-5 neutral-icon override sentence confusing | Locked: hardcode lucide `<Smile>`; no host override. |

### C-step implementation re-validations

| # | Sev | Step | Description | Resolution |
|---|---|---|---|---|
| A1 | ⚠️ MEDIUM | C1 | `EngagementReactionKind` not exported from barrel | Added to `index.ts` re-export block. |
| A2 | 🔹 LOW | C1 | `triggerReaction` stub `() => {}` doesn't accept declared param | Rewrote `(_kind: string \| null) => {}`. |
| B2 | ⚠️ MEDIUM | C2 | Defense 2 (structural resync guard) not implemented per Q-PP-3 lock | Added 12-LOC sync effect at end of `useEngagementState`. |
| D1 | 🔸 MEDIUM | C3 | `kind.color` only applied when selected; plan says always | Dropped `isSelected &&` guard. |
| E1 | 🔹 LOW | C4 | Type alias `ReactionAction` shadows component name | Renamed to `ReactionActionData`. |
| E7 | ⚠️ MEDIUM | C4 | `setPointerCapture` not used → long-press tracking unreliable on touch | Added `e.currentTarget.setPointerCapture(e.pointerId)` in pointerdown. |
| E8 | 🔹 LOW | C4 | Manual `aria-haspopup` + `aria-expanded` may duplicate Radix's auto-set | Dropped both; kept `aria-pressed`. |
| E10 | 🔸 MEDIUM | C4 | Non-split variant added extra wrapping `<div>` instead of merging `actionClassName` into button | Refactored to match like-action's DOM convention. |
| G1 | ⚠️ MEDIUM | C6 | Demo `REACTION_ACTIONS` + `HYBRID_ACTIONS` passed `viewerReaction` → controlled mode → user picks didn't visually update | Dropped `viewerReaction` from both → uncontrolled → internal mirror drives visuals. |
| H1/H2/H3 | 🔹 LOW | C7 | Stale feature bullets in `meta.ts` | Updated action-list / handle-methods / labels-count bullets. |

**Total: 16 substantive findings caught + fixed before this spotcheck.** Matches the memory's "3-5 per Stage 2 plan + 1-3 per implementation step" expectation (well above the rubber-stamp threshold).

---

## Smoke result

**Local-equivalent verification used in lieu of live smoke:**

| Slug | `pnpm registry:build` | `pnpm tsc --noEmit` | `pnpm validate:meta-deps` | Notes |
|------|----------------------|---------------------|----------------------------|-------|
| `engagement-bar-01` | pass | pass | pass | Local `public/r/engagement-bar-01.json` regenerated with 18 files; new entries `parts/reaction-picker.tsx` + `parts/reaction-action.tsx` present with correct targets. |
| All other 48 slugs | pass | pass | pass | No cross-procomp breakage from the widened `EngagementAction` / `EngagementDelta` / `EngagementState` unions. |

**Live smoke (`pnpm dlx shadcn add @ilinxa/engagement-bar-01`)** will exercise the published artifacts after commit + Vercel deploy. Required for GATE 3 *closure* per the readiness-review rule — this spotcheck records the plan: rerun live smoke once changes deploy. Tracked as `Follow-up F-1`.

---

## Findings (this spotcheck)

### 🔹 F-1 — Post-push live smoke deferred

| Field | Value |
|---|---|
| **Severity** | 🔹 LOW (procedural; not a Blocker) |
| **Location** | `e:/tmp/ilinxa-smoke-consumer/` |
| **Description** | Live smoke against the Vercel-hosted registry artifact cannot run before push + Vercel deploy completes. Current `public/r/engagement-bar-01.json` is local-only. |
| **Owner** | Author |
| **Bump target** | Same-cycle (post-push verification gate; no version bump needed) |
| **Action** | After `git push origin master`, run `cd e:/tmp/ilinxa-smoke-consumer && pnpm dlx shadcn@4.6.0 add @ilinxa/engagement-bar-01 && pnpm tsc --noEmit`. Expect: install success + consumer-tsc clean. If failure, surface as a v0.3.1 patch. |

### 🔹 F-2 — Controlled `viewerReaction` + realtime delta race documented

| Field | Value |
|---|---|
| **Severity** | 🔹 LOW (documented host-responsibility; no code change needed) |
| **Location** | Guide §Limitations + plan §C-1 re-validation note |
| **Description** | When the host has `viewerReaction` controlled AND a server `reaction-changed` delta arrives carrying a new viewer kind, the bar updates internal state but the effective overlay shows the host's still-stale controlled value briefly. Defense 2 catches up on the next render once the host syncs. Host MUST subscribe to deltas and reflect in their state. |
| **Owner** | (documentation only) |
| **Bump target** | n/a — accepted host responsibility |
| **Action** | Guide line 343 updated with the explicit caveat. No code change. |

### 🔹 F-3 — Browser-test matrix not yet run

| Field | Value |
|---|---|
| **Severity** | 🔹 LOW (procedural — pre-push manual verification gate) |
| **Location** | dev server `/components/engagement-bar-01` |
| **Description** | Manual interaction verification across Chromium / Firefox / Safari (long-press timing, picker keyboard nav, focus management on Remove click, hybrid demo independent interaction) is the next step before push. tsc + lint + meta-deps + registry-build all green; logical correctness verified via the test plan rows in §4.3 of plan. |
| **Owner** | Author |
| **Bump target** | Same-cycle (pre-push gate) |
| **Action** | Start dev server, exercise the Reactions + Hybrid demo tabs in all 3 browsers, verify the 17 test plan rows. |

---

## Verdict

**Verdict:** **Pass with follow-ups** (F-1 + F-3 = procedural pre-/post-push gates; F-2 = documentation-only acknowledged host responsibility).

**Justification:** All Q-Ps + Q-PPs locked + verified. 16 re-validation findings caught + fixed during plan + implementation; zero residual code issues. Zero v0.2.x breakage. Public API additions are clean discriminated-union extensions + slot additions + handle method additions. Defenses 1+2 implemented per Q-PP-3 (Defense 3 N/A — discrete events). F-cross-13 audit passes (Popover signatures stable Radix↔Base UI). Demo proves both reaction-only and hybrid (Q-P3) flows. Guide extended with Recipe 11 + updated Limitations + v0.4+ candidates. tsc + meta-deps + registry-build all clean.

**Closure path:**
1. F-3 — Browser-matrix manual interaction pass (pre-push).
2. Commit + push to master.
3. F-1 — Live smoke against deployed Vercel artifact (post-push).
4. STATUS.md update + decision file authored.

If F-3 surfaces a substantive bug → bump verdict to `Needs revision` + patch in C9 follow-up.

---

**Authored:** 2026-05-28 · **Targets:** engagement-bar-01 v0.2.1 → v0.3.0 · **Procomp tier**
