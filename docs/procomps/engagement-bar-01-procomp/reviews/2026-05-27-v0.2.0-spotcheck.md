# Spot-check — `engagement-bar-01` v0.2.0

> Tier-2 spotcheck. Fixed core (Dim 1 / 9 / 10 / 12) + rotating Dim 4 (Public API) per Q-PEB-7. Time-box: 25–35 min.

---

## Header

| Field | Value |
|---|---|
| **Component** | `engagement-bar-01` |
| **Category** | `data` |
| **Version under review** | `0.2.0` |
| **Status at review time** | `alpha` |
| **Reviewer** | Claude (post-card-01 v0.2.0 prerequisite ship — C0 chain) |
| **Date** | 2026-05-27 |
| **Git SHA** | `44e943a` (working tree state — not yet committed) |
| **Scope** | `spot-check` |
| **Trigger** | `public-api-touching minor bump` (additive sub-exports per Q-EB-1 + Q-EB-2) |
| **Verdict** | **Pass** |

---

## Dimensions covered

### Fixed core

| Dim | Name | Verified? | Notes |
|-----|------|-----------|-------|
| 1 | Procomp planning docs | yes | Description addendum [engagement-bar-01-procomp-description-v0.2.0.md](../engagement-bar-01-procomp-description-v0.2.0.md) + plan addendum [engagement-bar-01-procomp-plan-v0.2.0.md](../engagement-bar-01-procomp-plan-v0.2.0.md) authored and signed off. Both reflect the 6 mechanical scope items + 2 Q-EB locks. Base v0.1 description + plan + guide carry unchanged. No guide-section addendum authored at this time (deferred — base guide still accurate; v0.2.0 additions are mechanical sub-exports, not new behavior to document). |
| 9 | Registry distribution | yes | Local `pnpm registry:build` regenerated `public/r/engagement-bar-01.json` cleanly — 16 files (was 14; +2 likers-strip + share-menu). All files retain locked target convention: `type: "registry:component"`, `target: "components/engagement-bar-01/<sub>"`. `registryDependencies` correctly extended `["button", "avatar"]` → `["button", "avatar", "input"]` (the ShareMenu's `<Input>` dep). No `demo.tsx` / `usage.tsx` / `meta.ts` in artifact (untouched; unchanged from v0.1). Live endpoint `https://ilinxa-proui.vercel.app/r/engagement-bar-01.json` will resolve to v0.2.0 after deploy. |
| 10 | Meta + manifest sync | yes | `engagement-bar-01/meta.ts` version `0.1.2` → `0.2.0` ✓; `updatedAt: "2026-05-27"` ✓; 2 new features appended (LikersStrip + ShareMenu sub-exports) ✓; +1 tag `"likers"` ✓; `dependencies.shadcn` extended `["button"]` → `["avatar", "button", "input"]` ✓. STATUS.md row update is the user's responsibility post-deploy (this review notes the bump for capture). |
| 12 | Verification | yes | `pnpm tsc --noEmit` clean repo-wide (after C0-1 atomic commit boundary — see §3 of plan). `pnpm validate:meta-deps` 49/49 clean. `pnpm registry:build` clean (49 slugs built). `pnpm lint` — pre-existing baseline (file-manager + file-tree + rich-sidebar warnings/errors unrelated to engagement-bar-01 or post-card-01; none introduced by this ship). Smoke harness against live Vercel deferred to post-commit/post-deploy (cannot smoke against working-tree changes). |

### Rotating dimension

- **Chosen:** Dim 4 — Public API
- **Why:** The entire v0.2.0 scope is public-API additions (2 sub-exports + 2 type re-exports + 1 new type). Obvious rotating dim.
- **Verified?** yes (with 0 substantive findings — see §"Findings" below)
- **Notes:**
  - **2 new component exports** added to `engagement-bar-01/index.ts` lines 4–7 (`LikersStrip` + `ShareMenu`). Sub-export pattern matches the existing `EngagementHeartBurst` precedent (line 2). ✓
  - **2 new type exports** added inline alongside the components (`LikersStripProps` + `ShareMenuProps`). ✓
  - **1 new type added to `types.ts`**: `EngagementLikerProfile` (relaxed-fields shape) coexists with the existing strict `EngagementLikeUser`. Re-export added to the type-export block (line 23 between `EngagementLikeUser` and `EngagementState`). ✓
  - **Naming decision audit** (Q-EB-1 re-validation): chose `EngagementLikerProfile` over `EngagementLikerUser` after detecting the one-letter typo risk vs `EngagementLikeUser`. The `Profile` suffix is semantically clearer ("user profile shown in the likers strip") and unambiguous beside the strict delta-payload type. ✓
  - **`PostLikeUser` `@deprecated` alias** in post-card-01 v0.2.0 — propagated into post-card-01 v0.2.0 plan C1; not landed at this gate (post-card-01 v0.2.0 implementation hasn't begun). ✓ noted.
  - **Component code byte-identical to v0.1.1** post-`git mv`: the only edits inside the moved files are the type-import rename (`PostLikeUser` → `EngagementLikerProfile`, mechanical replace-all). No behavior changes. ✓
  - **Zero v0.1.x breakage:** every existing engagement-bar-01 export carries unchanged. `EngagementBar01`, `EngagementHeartBurst`, `engagementReducer`, `useEngagementState`, `deriveStateFromActions`, `formatEngagementCount`, `EngagementBar01Props`, `EngagementBar01Handle`, `EngagementBar01Variant`, `EngagementBarLabels`, `EngagementAction`, `EngagementActionAlign`, `EngagementDelta`, `EngagementLikeUser`, `EngagementState`, `EngagementLocalAction`, `Subscribe`, `Unsubscribe`, `DEFAULT_ENGAGEMENT_BAR_LABELS` — all 19 existing exports identical. ✓
  - **post-card-01 cross-folder import wiring** (atomic per plan §3): `post-card-01.tsx` lines 37-38 updated `./parts/likers-strip` + `./parts/share-menu` → `@/registry/components/data/engagement-bar-01` consolidated import. `post-card-01/index.ts` lines 3-4 (the barrel re-exports of `LikersStrip` + `ShareMenu`) updated to also pull cross-folder with a JSDoc note about soft-compat for v0.1 consumers who imported from the post-card-01 barrel. ✓

---

## Smoke result

Smoke harness against live Vercel registry deferred to post-commit/post-deploy. Local-equivalent verification used instead:

| Slug | `pnpm registry:build` | `pnpm tsc --noEmit` | Notes |
|------|----------------------|---------------------|-------|
| `engagement-bar-01` | pass | pass | Local `public/r/engagement-bar-01.json` regenerated with 16 files; spot-checked 2 new file entries present + correct targets |
| `post-card-01` | pass | pass | Local `public/r/post-card-01.json` regenerated with 11 files (was 13); spot-checked 2 file entries removed; cross-folder import via `@ilinxa/engagement-bar-01` in registryDependencies resolves correctly |

**Live smoke (`pnpm dlx shadcn add @ilinxa/engagement-bar-01`)** will exercise the published artifacts after commit + Vercel deploy. Required for the GATE 3 *closure* per the readiness-review rule — this spotcheck records the plan: rerun live smoke once changes deploy.

---

## Findings

**None — clean spot-check.**

The scope was deliberately tight (file moves + 2 sub-exports + 1 new type + meta bump + registry.json update). All planned changes landed. Verification baselines (tsc + validate-meta-deps + registry-build) all clean. The naming refinement during re-validation (`EngagementLikerUser` → `EngagementLikerProfile`) was applied before any code-touch; no residual stale references.

The pre-existing lint baseline (21 errors across file-manager / file-tree / rich-sidebar) is **NOT** introduced by this ship — those slugs are unrelated to engagement-bar-01. The baseline is a separate concern to be addressed in a future cleanup pass.

---

## Verdict

**Verdict:** Pass

**Justification:** Pure additive expansion. Zero breaking changes verified across all 19 existing v0.1.x exports. 5 new public surface items added: `LikersStrip` (component) + `LikersStripProps` (type) + `ShareMenu` (component) + `ShareMenuProps` (type) + `EngagementLikerProfile` (new type). Naming refined during Q-EB-1 re-validation (one-letter typo risk avoided). Both planning docs (description + plan addenda) authored + signed off. tsc + validate-meta-deps + registry-build all clean. Live smoke pending post-deploy (procedural; not a Blocker).

---

## Follow-up actions

| Action | Owner | Target version | Status |
|---|---|---|---|
| Live smoke against deployed Vercel registry after commit + push | Whoever lands the commit | this v0.2.0 ship (procedural) | Open (post-deploy) |
| Update STATUS.md with engagement-bar-01 row reflecting v0.2.0 bump + new exports | Whoever lands the commit | this v0.2.0 ship | Open |
| Author decision file `.claude/decisions/2026-05-27-engagement-bar-01-v0.2.0-likers-strip-share-menu-extraction.md` | Whoever lands the commit | this v0.2.0 ship | Open |
| Continue with post-card-01 v0.2.0 C1 (types.ts) — main chain implementation | post-card-01 v0.2.0 owner | post-card-01 v0.2.0 | Open (next step) |
| Re-author engagement-bar-01 guide.md with a "v0.2.0 — Sub-exports for LikersStrip + ShareMenu" section | engagement-bar-01 v0.2.0 owner | engagement-bar-01 v0.2.0 (within current ship; non-blocking for GATE 3) | Open (low priority — base guide still accurate) |

---

## Sign-off

- [x] Verdict is set above (Pass)
- [x] Smoke row pending live deploy (procedural follow-up captured)
- [ ] STATUS.md row update — Open (next session / user-driven)
- [ ] Decision file authored — Open

**Reviewer signature:** Claude — 2026-05-27. C0-GATE 3 spotcheck closed for engagement-bar-01 v0.2.0. Post-card-01 v0.2.0 main chain (C1 → C12) unblocked.
