# Spot-check — `kanban-board-01` v0.4.0

> GATE 3 readiness review for the v0.3.0 → v0.4.0 minor bump (consumer-issue-report fixes K1–K10 + R3 kanban-side).

---

## Header

| Field | Value |
|---|---|
| **Component** | `kanban-board-01` |
| **Category** | `data` |
| **Version under review** | `0.4.0` |
| **Status at review time** | `alpha` |
| **Reviewer** | Claude (AI-assisted, author) |
| **Date** | 2026-06-18 |
| **Git SHA** | on top of `d886573` |
| **Scope** | `spot-check` |
| **Trigger** | `bug-report` (public-API-touching minor bump) |
| **Verdict** | **Pass with follow-ups** |

---

## Dimensions covered

### Fixed core

| Dim | Name | Verified? | Notes |
|-----|------|-----------|-------|
| 1 | Procomp planning docs | yes | description / plan / guide all reconciled to v0.4.0 + object-shape callbacks (K1/K4); dep table + hard-mode promise corrected (K3/K5). |
| 9 | Registry distribution | yes | `registry.json` kanban base deps = 7 (dead `tooltip` removed, K3); `public/r/kanban-board-01.json` regenerated + spot-checked (no `tooltip`; `onDataChange` present). No `demo`/`usage`/`meta` shipped. |
| 10 | Meta + manifest sync | yes | `meta.ts` version `0.4.0`, `updatedAt 2026-06-18`; meta shadcn deps (7) now agree with `registry.json` (the meta-vs-registry mismatch the report missed is closed). |
| 12 | Verification | yes | `tsc --noEmit` clean; lint at repo baseline (0 new); `validate:meta-deps` 55/55 clean; `pnpm build` succeeds; `registry:build` clean. |

### Rotating dimension

- **Chosen:** Public API
- **Why:** the bump narrows two public types (`KanbanCardData.meta[].value` K2, `onColumnCreate` arg K7) and adds an optional `KanbanRenderContext.onDataChange` (R3) — API surface is the load-bearing risk.
- **Verified?** yes
- **Notes:** `value: ReactNode → string | number` and `onColumnCreate: Partial<KanbanColumn> → KanbanColumn` are type-narrowings; verified all in-repo usages (dummy-data, demo, internal handler) already conform, so no internal break. `onDataChange` is additive + optional (non-breaking); kept non-generic (`unknown`) after a generic attempt tripped built-in-renderer variance.

---

## Smoke result (post-deploy, 2026-06-18 — F-cross-11 path-b)

`shadcn add @ilinxa/kanban-board-01` from the live registry into the base-nova / Base UI consumer:
**install pass, consumer `tsc --noEmit` clean (0 kanban-board-01 errors).** The dead `tooltip` dep is
gone from the install graph. F-02 closed.

| Slug | Install | TSC | Notes |
|------|---------|-----|-------|
| `kanban-board-01` | pass | **pass (0)** | clean on first install; no tooltip pulled |

---

## Findings

#### F-01 — `maxItems` remains soft-only (documented, not enforced)

- **Severity:** 🔹 Low
- **Dimension:** Public API
- **Location:** `lib/permissions.ts` `canDrop()`
- **Observed:** Per the Hybrid scope decision, hard `maxItems` enforcement was NOT built (K5); the unfulfilled "v0.2 hard mode" promise was removed from docs instead.
- **Suggested fix:** None required — intentional. If a consumer asks, add `maxItemsMode: "soft" | "hard"` enforced in `canDrop` in a later minor.

#### F-02 — Post-deploy cross-procomp smoke ✅ DONE

- **Severity:** 🔹 Low → **Closed**
- **Dimension:** Registry distribution
- **Observed:** F-cross-11 path-b smoke run 2026-06-18 — `kanban-board-01` installs clean from the live registry with 0 consumer-tsc errors (Base UI consumer). The cross-procomp edit-flow's todo-rich-card side needed a v0.2.1 patch (see that review's F-04); kanban-board-01 itself was clean.

---

## Verdict

**Verdict:** **Pass with follow-ups**

**Justification:** All ten report findings (K1–K10) plus the bonus meta-vs-registry dep mismatch and the kanban side of R3 are resolved and verified green (tsc / lint / meta-deps / build / registry artifact). The two open follow-ups are Low and non-blocking.

---

## Follow-up actions

| Action | Linked finding | Owner | Target version | Status |
|---|---|---|---|---|
| Post-deploy cross-procomp consumer-tsc smoke (kanban × todo-rich-card) | F-02 | author | v0.4.x | Open |
| Hard `maxItems` mode if a consumer asks | F-01 | author | v0.5+ | Deferred |

---

## Sign-off

- [x] Verdict is set above
- [x] Smoke deferred (post-deploy) — rationale recorded
- [ ] Tracker row updated at `docs/reviews/sweep-tracker.md` (n/a — not a sweep finding)

**Reviewer signature:** Claude (AI-assisted) — 2026-06-18
