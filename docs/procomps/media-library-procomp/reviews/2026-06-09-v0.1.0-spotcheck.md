# media-library-01 — v0.1.0 readiness spot-check (GATE 3)

| | |
|---|---|
| **Slug** | `media-library-01` |
| **Tier** | pro-component |
| **Version** | 0.1.0 (first ship) |
| **Reviewer** | Author (self-review; permitted for pro-component v0.1.0 per readiness-review rule) |
| **Date** | 2026-06-09 |
| **Git SHA** | uncommitted (working tree) |
| **Scope** | Full first-ship review |
| **Trigger** | First ship — v0.1.0 |
| **Verdict** | ✅ **Pass with follow-ups** |

Template: 4 fixed dims + 1 rotating. Rotating dim = **Robustness** (the risk surface is the upload pipeline + dnd + lazy text-fetch + 5-way cross-procomp composition state).

---

## Dim 1 — Planning docs ✅
- Description (GATE 1) + plan (GATE 2) both signed off 2026-06-09, with a recorded re-validation pass (the F-01 `FsNode`-import trap was caught + fixed at the plan stage). Guide authored alongside the implementation.
- Compound shadcn model (§0.5 / §1.2 / §4 of the plan) is reflected in the code: `MediaLibraryRoot` headless provider + flat à-la-carte parts + `MediaLibrary01` assembly; flat exports (no namespace object). **Accurate.**
- One intentional deviation from plan, documented: file-grid **virtualization deferred** to v0.2 (the `virtualizeThreshold` prop ships reserved). See F-02.

## Dim 2 — Registry distribution ✅
- `registry.json`: base `media-library-01` (31 files) + `media-library-01-fixtures` (dummy-data) sibling. Locked convention held: every file `type: "registry:component"`, `target: "components/media-library-01/<sub-path>"`.
- **No `demo.tsx` / `usage.tsx` / `meta.ts` shipped** (artifact audit: `files: 31, has demo/usage/meta: false`).
- `registryDependencies` (11): 5 internal (`@ilinxa/file-tree`, `pdf-viewer`, `code-block`, `markdown-editor`, `video-player-01`) + 6 shadcn URLs (button, context-menu, dialog, input, progress, skeleton). All five internal deps are genuine **value** deps (F-01: an install entry is required — satisfied).
- `pnpm registry:build` regenerated `public/r/media-library-01.json` + fixtures cleanly.

## Dim 3 — Meta + manifest sync ✅
- `meta.ts`: version 0.1.0 / alpha / accurate description + 7 features + tags. `dependencies` = shadcn[6] + npm{@dnd-kit/core, date-fns, lucide-react} + internal[5].
- `validate:meta-deps`: **54 slugs, 54 clean** — declared deps match shipped imports exactly (no @dnd-kit/sortable, @tanstack/react-virtual, card, badge, scroll-area, tooltip — none imported).
- `manifest.ts` row added; docs page builds (`/components/media-library-01` in the 54-slug prerender).
- STATUS.md row + Recent activity + decision file: authored in this close.

## Dim 4 — Verification ✅ (with one pending)
- `pnpm tsc --noEmit`: **clean** (whole project).
- `pnpm lint`: **clean** for the folder (1 error + several warnings caught + fixed: synchronous-setState-in-effect → microtask-yield restructure; `react-hooks/refs` false-positive on dnd returns → destructured per the kanban-board precedent; `aria-selected`→`aria-pressed` on role=button; canonical `aspect-4/3`; removed dead eslint-disables).
- `pnpm build`: **clean** (54 slugs prerendered, RSC + React.lazy viewer boundaries hold under SSG).
- ⏳ **Consumer-tsc local-registry smoke NOT yet run** → F-01.
- ⏳ **Live browser click-through NOT yet performed** (the de-facto visual gate) → F-06.

## Dim 5 (rotating) — Robustness 🔸
- **Object-URL ownership**: `useUpload` tracks owned URLs in a ref, revokes on temp→real swap / dismiss / unmount. Demo mints object URLs per uploaded File. ✅
- **Upload race / FileList**: the browse-input snapshots `Array.from(files)` **before** resetting `value` (the v0.1.2 browse-FileList lesson applied pre-emptively). ✅
- **Lazy text fetch**: `useTextContent` aborts on node-change / unmount via `AbortController`; loading + error states surfaced; setState only after a microtask yield. ✅
- **Drop validation**: `canDropInto` rejects self / cycle / non-folder / no-op, using a `parentId` map with a cycle guard. ✅
- **Controlled/uncontrolled**: selection + currentFolder via the sealed `useControllableState` (mode-lock + dev-warn). ✅
- **Capability gating**: every mutate affordance keyed off `can.*` (handler presence) — read-only gallery verified in the demo's "Read-only" tab. ✅
- Residual robustness risks are the deferred items (virtualization, sidebar lazy-expand) + the un-run smoke — all tagged below.

---

## Findings

| # | Sev | Location | Finding | Resolution |
|---|---|---|---|---|
| **F-01** | 🔸 Med | registry / cross-procomp | Consumer-tsc local-registry smoke — ✅ **DONE 2026-06-10 (v0.1.1)**. Found + fixed 4 cross-procomp install bugs; re-smoke **0 consumer-tsc errors** in a base-nova (Base UI) consumer. See §"F-01 smoke (v0.1.1)". | Closed. |
| **F-02** | 🔹 Low | file-grid | **Virtualization deferred** — `virtualizeThreshold` ships reserved; a plain responsive grid renders all files. Very large folders (>~500) may lag. | **v0.2** — row-virtualize the grid with `@tanstack/react-virtual` + a measured column count. Owner: author. |
| **F-03** | 🔹 Low | context-menu / store | **Copy / duplicate deferred** — only cut → paste-into-folder (= move) is wired; there's no duplicate backend. | **v0.2** — add `onCopy`/duplicate handler + Copy menu item. Owner: author. |
| **F-04** | 🔹 Low | sidebar | `file-tree` composed **without `onLoadChildren`** → deep unvisited folders don't expand in the sidebar until navigated (the grid drives loading). | **v0.2** — thread a folders-only lazy loader into the composed tree. Owner: author. |
| **F-05** | 🔹 Low | file-preview | Markdown preview lazy-loads the **full `MarkdownEditor`** (CodeMirror chunk) rather than the lighter standalone `parseMarkdown` path (chosen for sanitization safety + rostered-path certainty at impl). | **v0.1.x** — evaluate slimming to `parseMarkdown` + sanitizer once the roster/sanitization is confirmed. Owner: author. |
| **F-06** | 🔹 Low | verification | **Live browser click-through** — ✅ **DONE 2026-06-10** (user-driven). Surfaced 7 real runtime issues none of tsc/lint/build caught; all fixed (see §"Browser walkthrough fixes"). | Closed. |

No 🚫 Blocker / ⚠️ High findings.

## Browser walkthrough fixes (2026-06-10) — F-06 closed

The live walkthrough (the de-facto visual gate) found + fixed 7 runtime issues, all of which passed tsc/lint/build clean — reaffirming the "visual walkthrough is the de-facto gate" lesson:

1. **SSR hydration mismatch** — `@dnd-kit` derived `aria-describedby` ("DndDescribedBy-N") from a module-level counter that incremented in different order server vs client. Fix: `useId()`-derived stable `id` on `DndContext` (`media-library-root.tsx`). *(Same class as the rich-card auto-id SSR lesson: any id minted during render must be deterministic.)*
2. **Thrown viewer could crash the page** — added a `PreviewErrorBoundary` around the dispatched viewers (`file-preview.tsx`): a viewer that throws degrades to a graceful "Couldn't load + Download" card.
3. **PDF worker fetch failure + no async error path** — added a `pdfWorkerSrc` prop (threaded root → context → details-pane / lightbox / dispatcher) and wired react-pdf's async `onError` to the fallback (a boundary can't catch the async failure). Demo points at a jsdelivr worker.
4. **Sidebar had no root** — added a synthetic "Library" root node (expanded) so root is visible + clickable.
5. **Sidebar drag did nothing** — `file-tree` (native HTML5 drag) was draggable but its `onMove` was never wired → now `onMove → ctx.moveTo` (drop a folder/file onto a folder, or onto "Library", to move).
6. **Folders shown as "(empty)" when non-empty** — `foldersOnly()` had stripped files, leaving folders with `children:[]`. Sidebar now shows the **full tree**; "(empty)" is honest. Activation guarded (folder → navigate, file → preview).
7. **Cards crushed by the preview pane** — grid used viewport breakpoints (`lg:`/`xl:`) that don't know the sidebar + details pane eat ~510px, so it over-columnized and folder names collapsed. Fix: width-aware `auto-fill minmax()` grids. Also hardened the grid drag (read dragged ids off the drag event, not lagging React state; `pointerWithin` collision; visible `cursor-grab` grips).

All re-verified clean: tsc 0 / lint 0 / meta-deps 54-54 / build / registry:build (31 files). No public-API removals; additions only (`pdfWorkerSrc`).

## F-01 smoke (v0.1.1, 2026-06-10) — passed, 4 cross-procomp install bugs fixed

Local-registry consumer smoke (serve `public/r` → base-nova/Base-UI tmp consumer → `shadcn@4.6.0 add @ilinxa/media-library-01` → consumer `tsc`). Surfaced **4 real install bugs invisible to the dev gates**, now fixed; re-smoke = **0 consumer-tsc errors**. These precise shadcn-rewriter rules are the lasting lesson:

1. **Cross-category RELATIVE imports break.** `../../../code/code-block/...` keeps the dev-only `code/` category segment (the rewriter doesn't touch relative paths) → no such dir in the consumer's flat `components/`. **Same-category** relative (`../../pdf-viewer/...`, media→media) is fine — the category is implied, not named. → cross-category must use the `@/registry/...` alias.
2. **The rewriter rewrites `import` declarations but NOT `export … from` re-exports.** My first wrapper used `export { X as default } from "@/registry/…"` → category NOT stripped (`@/components/code/code-block`). Fixed with `import { X } … ; export default X` (two statements).
3. **The rewriter rewrites static imports but NOT dynamic `import()` calls.** So `React.lazy(() => import("@/registry/components/code/…"))` keeps the category. → lazy a **local re-export wrapper** (`./lazy-code-block`) whose *static import* gets stripped; the local dynamic path is left alone. Code-split preserved.
4. **F-cross-13 — `context-menu` `variant="destructive"`** isn't on the consumer's base-nova primitive → style the destructive item via `className` instead.

Net rule (now in [`media-library-01-procomp-plan.md` §3]): same-category cross-procomp → relative; cross-category → `@/registry/...` **static `import`**; cross-category **lazy** → local re-export wrapper (`import`+`export default`) + `lazy(() => import("./wrapper"))`. v0.1.1 added 2 wrapper files (registry 31→33); all gates re-green (tsc/lint/meta-deps 54-54/build/registry:build); consumer-tsc 0.

## Verdict
**Pass with follow-ups.** F-01 closed (smoke passed). Remaining open findings are Low (F-02 grid virtualization / F-03 copy-duplicate / F-04 sidebar lazy-expand / F-05 markdown light-path), all v0.2. The component is tsc/lint/meta-deps/build/registry-build clean, browser-verified, and ships its full v0.1.0 scope (compound model, full-screenshot chrome, multi-type preview in pane + lightbox, upload, dnd-move, context menus, lazy load).
