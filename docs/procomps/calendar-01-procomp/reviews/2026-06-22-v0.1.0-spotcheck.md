# Spot-check — `calendar-01` v0.1.0

> Tier 2 readiness review (5 dimensions). First ship.

---

## Header

| Field | Value |
|---|---|
| **Component** | `calendar-01` |
| **Category** | `data` |
| **Version under review** | `0.1.0` |
| **Status at review time** | `alpha` |
| **Reviewer** | Author (self-review; permitted for pro-component v0.1.0) |
| **Date** | 2026-06-22 |
| **Git SHA** | v0.1.0 feat commit (this commit; parent `425a952`) |
| **Scope** | `spot-check` |
| **Trigger** | `first-ship` (v0.1.0) |
| **Verdict** | **Pass with follow-ups** |

---

## Dimensions covered

### Fixed core

| Dim | Name | Verified? | Notes |
|-----|------|-----------|-------|
| 1 | Procomp planning docs | yes | description (GATE 1) + plan (GATE 2) both signed off + consistency-reviewed (2 external + 5 internal findings fixed across the two docs); guide authored alongside. All three current with the shipped API. |
| 9 | Registry distribution | yes | `registry.json` base (23 files) + `calendar-01-fixtures` (dummy-data only). `pnpm registry:build` ✓ — `public/r/calendar-01.json` = 23 files, regdeps `[avatar,badge,button,calendar,popover,skeleton,toggle-group,tooltip,@ilinxa/todo-rich-card]`, deps `[date-fns,lucide-react]`; locked target convention; **no demo/usage/meta** in the base artifact. types.ts re-export band present in artifact. |
| 10 | Meta + manifest sync | yes | `meta.ts` version 0.1.0 / status alpha / category data; manifest 3-line entry added (66 build pages = 65 + calendar-01). STATUS.md row added (this session). |
| 12 | Verification | partial | tsc **0 errors**; lint **0** (calendar-01 folder; project baseline unchanged); `validate:meta-deps` **57/57 clean**; `pnpm build` **66/66 pages, no SSR errors** (the `/components/calendar-01` page statically generated). `registry:build` ✓. **Live-registry consumer smoke NOT run in-environment** → F-02 (post-deploy, 4-ship pattern). |

### Rotating dimension

- **Chosen:** Robustness
- **Why:** the load-bearing novelty is the pure date layer — `classify` (3-layer all-day/timed/milestone), `occurrences` (finite guards), `segments` (month lane layout + exclusive-midnight all-day end), `lane-pack` (overlap columns). Layout correctness lives or dies here.
- **Verified?** yes (by construction + reasoning; visual confirmation is F-01)
- **Notes:**
  - **Finite-date guards** throughout `occurrences.ts` — an unparseable `startAt`/`expireAt` flags the occurrence `invalid` and renders label-only; never a `new Date(NaN).toISOString()` throw (gantt v0.3.1 G2 lesson applied).
  - **Date-only floating-local parse** in `classify.parseDateValue` (`new Date(y, m-1, d)`, not `Date.parse`) — no negative-UTC off-by-one for all-day events.
  - **Classification ordering** correct: a date-only start with no end resolves to all-day (not milestone) because the no-end branch checks `start.dateOnly` first.
  - **All-day end exclusive** (iCal/Google) in `segments.coveredDays`; clamps `lastMs ≥ firstMs`.
  - **Lane-pack** clusters break on non-overlap; `laneCount` per cluster bounds block width; `blockOffsets` clamps top/height within the day.
  - SSR-safe `now` (seed + rAF post-mount; no setState-synchronous-in-effect — lint-clean).

---

## Smoke result

Live-registry consumer smoke not run in this environment (no browser / no deployed artifact yet). Deferred to **post-deploy** per the 4-ship pattern → **F-02**. Local artifact verification (registry:build + 23-file spot-check) stands in for the producer side.

| Slug | Install | TSC | Time (ms) | Notes |
|------|---------|-----|-----------|-------|
| `calendar-01` | deferred | deferred | — | post-deploy cross-backend (Radix + Base UI) consumer-tsc smoke — F-02 |

---

## Findings (max 5)

#### F-01 — Live visual walkthrough owed

- **Severity:** 🔸 Medium
- **Dimension:** Verification
- **Location:** `/components/calendar-01` (all views)
- **Observed:** v0.1.0 verified by tsc + lint + meta-deps + `pnpm build` (SSR/static-gen) + code-path reasoning. The in-browser walkthrough — month spanning-bar lanes + "+N more", week/day time-grid block positioning + now-line, mini-nav, native + rich tooltips, keyboard shortcuts — was **not** run (the headless environment has no X server for browser automation). This project treats the visual walkthrough as the de-facto gate.
- **Suggested fix:** Run the walkthrough on the live dev server / post-deploy; file any visual fixes as v0.1.1.

#### F-02 — Post-deploy cross-backend smoke (new primitives)

- **Severity:** 🔸 Medium
- **Dimension:** Verification / Registry distribution
- **Location:** `parts/calendar-toolbar.tsx` (toggle-group), `parts/calendar-month-view.tsx` (popover), `parts/calendar-event.tsx` + `parts/calendar-root.tsx` (tooltip), `parts/calendar-mini-nav.tsx` (calendar)
- **Observed:** calendar-01 introduces shadcn primitives gantt didn't use — `toggle-group` / `popover` / `tooltip` / `calendar`. This re-arms the F-cross-13 Radix-vs-Base-UI divergence class (cf. engagement-bar-01 Popover, news-card dropdown). Usage is defensive (no `asChild` gymnastics beyond the standard `TooltipTrigger asChild` on forwardRef'd buttons), but unproven cross-backend.
- **Suggested fix:** Post-deploy consumer-tsc smoke on both backends; patch any divergence in v0.1.1.

#### F-03 — Week/Day all-day band shows per-day chips, not spanning bars

- **Severity:** 🔹 Low
- **Dimension:** Robustness (composition integrity)
- **Location:** `parts/calendar-time-grid.tsx`
- **Observed:** the time-grid all-day band renders each multi-day event as a chip in every day it covers, rather than one column-spanning bar (the month grid already spans correctly). Functionally correct, visually less polished.
- **Suggested fix:** reuse `layoutMonthWeek` (generalized to N columns) for the week band in v0.1.x.

#### F-04 — Month overflow cap is fixed (3), not height-responsive

- **Severity:** 🔹 Low
- **Dimension:** Robustness
- **Location:** `parts/calendar-month-view.tsx` (`DEFAULT_CAP = 3`)
- **Observed:** the plan's Q4 leaned "height-responsive"; v1 ships a fixed cap of 3 (overridable via `maxEventsPerCell`) to avoid measurement flicker. Honest scope trim.
- **Suggested fix:** measured/height-responsive cap in v0.1.x.

---

## Verdict

**Verdict:** **Pass with follow-ups**

**Justification:** All four fixed dimensions hold (docs current, registry artifact correct, meta/manifest synced, the static gates green) and the rotating Robustness pass confirms the pure date layer is sound with finite + timezone guards. The two Medium follow-ups (F-01 live walkthrough, F-02 cross-backend smoke) are the established post-deploy verification steps, not blockers; F-03/F-04 are Low scope trims. None touch the public API.

---

## Follow-up actions

| Action | Linked finding | Owner | Target version | Status |
|---|---|---|---|---|
| Live in-browser walkthrough of all four views | F-01 | author | v0.1.0 (post-deploy) → fixes in v0.1.1 | Open |
| Cross-backend (Radix + Base UI) consumer-tsc smoke | F-02 | author | v0.1.1 | Open |
| Week/Day all-day band → column-spanning bars | F-03 | author | v0.1.x | Open |
| Height-responsive month overflow cap | F-04 | author | v0.1.x | Open |

---

## Sign-off

- [x] Verdict is set above
- [x] Smoke row recorded (deferred → F-02)
- [ ] Tracker row at `docs/reviews/sweep-tracker.md` (add on next sweep touch)
- [x] No Needs-revision/Block (no STATUS mirror of blockers needed)

**Reviewer signature:** Author — 2026-06-22
