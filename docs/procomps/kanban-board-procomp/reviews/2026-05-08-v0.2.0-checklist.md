# Review checklist — `kanban-board-01` v0.2.0

> Filled 2026-05-08 (sweep session 1 pilot). Companion report: [`2026-05-08-v0.2.0-review.md`](./2026-05-08-v0.2.0-review.md). Findings are referenced by `F-NN` from the report.

---

## Header

- **Component:** `kanban-board-01`
- **Category:** `data`
- **Version under review:** `0.2.0`
- **Reviewer:** Claude (sweep session 1)
- **Date:** 2026-05-08
- **Git SHA:** `7263286`
- **Scope:** full
- **Reference:** [review-guide.md](../../../reviews/review-guide.md)

---

## 1. Procomp planning docs

- [x] description.md exists, signed off
- [x] plan.md exists, signed off
- [x] guide.md exists, current
- [x] description §5 archetypes match what `demo.tsx` exercises
- [x] description out-of-scope list still accurate
- [x] plan-stage decisions still honored in code (Q-P4 deviation explicitly documented as "(shipped)")
- [x] guide reads consumer-shaped, not implementer-shaped
- [x] guide code snippets compile against current public API
- [x] v0.2 components have explicit "v0.2 update" sections in each doc (description §v0.2-update, plan §v0.2-update, guide §Versioning)
- [x] N/A — greenfield component (no migration origin)

**Notes:** All three docs in good order. Plan's Q-P4 deviation note (auto-expand-on-drop instead of hover-expand) is the right way to track plan-vs-code drift. v0.2 update sections are clean.

---

## 2. Public API (`<slug>.tsx` props + `types.ts`)

- [x] Openness vs sealedness — slots / callbacks / polymorphic / public helpers where plausible consumer needs (renderer registry + 14 callbacks + palette override + readOnly + aria-label)
- [x] Component renders meaningfully with zero props… *almost* — actually `renderers` is required, but the empty-renderers fallback renders an error message which is correct
- [x] Discriminated unions for variants (KanbanAction is a discriminated union; renderer dragHandle is "shell" | "header")
- [x] `onBefore*` interceptor chains for mutation gating where applicable — `onItemMove` fires after successful move; CRUD callbacks are post-dispatch
- [x] Public helpers exported (`findRenderer`, `newItemId`, `newColumnId`, `validateData`, `DEFAULT_PALETTE`)
- [x] No `any` in public types; generics where they buy real safety (`KanbanCardRenderer<TData = unknown>`, type-erased `AnyKanbanCardRenderer` is internal)
- [x] Stable-identity rules documented for callback / object / array props (rendererMap memoized via useMemo on renderers reference)
- [x] Naming consistent: verbs for callbacks (`onItemCreate`, `onDragStart`), nouns for slots (none), `xxxRenderer` for registry entries
- [x] Future-proof: next obvious feature can be added without breaking (multi-select, hard WIP, virtualization all listed in description out-of-scope as v0.x)
- [x] Optional props have documented defaults

**Notes:** Solid public surface. 1 required prop, 13 optional. 9 public types + 1 helper + 2 built-in renderers. No issues here.

---

## 3. Component code (sealed folder)

- [x] Folder shape conforms to `data-table` + `lib/` deviation justified per plan Q-P10
- [x] Imports limited to allowlist (verified: only `react`, `@/components/ui/*`, `@/lib/utils`, `@dnd-kit/*`, `lucide-react`. No `next/*`, no app contexts.)
- [x] No commented-out blocks or stale `// removed` placeholders
- [ ] **No half-finished `// TODO later` without tracked follow-up** — see **F-03**: `hooks/use-keyboard-actions.ts` is "Reserved for future" + empty + unused
- [x] Comments only when WHY is non-obvious — no WHAT comments (verified across reviewed files)
- [x] No premature abstraction — file decomposition matches feature surface (parts/ has 18 files but each has a clear purpose)
- [x] `useCallback` / `useMemo` only where stable identity matters (rendererMap, palettePinned)
- [x] Module-scope hoisting where required (DEFAULT_PALETTE in lib/palette.ts is module-scope const, not React state)
- [x] `parts/` decomposition purposeful — column / column-header / column-body / column-collapsed / column-footer split is justified by per-row layout concerns
- [x] No swallowed errors (validateData console.warn instead of silent; missing-renderer console.warn once-per-id)
- [x] `'use client'` only where actually needed (parts/* and hooks/*; `lib/*` is pure)
- [x] Root `<slug>.tsx` reasonably sized — 213 lines, including JSX. Within bounds.

**Notes:** F-03 surfaced here — the empty `useKeyboardActions` hook is dead code that ships to consumers.

---

## 4. Demo + usage

- [x] Demo covers every archetype description §5 promised — primary task tracker (built-in renderers), mixing built-in + rich card, sales pipeline with swimlanes (kanban-card with meta) all present in `demo.tsx`
- [x] Demo dataset is realistic, heterogeneous (3 rich-card with deeply-nested data + codearea/image/table/quote/list, 6 kanban-card, 2 kanban-note across 4 cols × 2 swimlanes)
- [x] Demo exercises extensibility — registers a third renderer (rich-card adapter); shows `dragHandle: "header"` mode
- [ ] **Demo dataset uses non-existent palette swatch** — see **F-05**: `col-review` declares `color: "indigo"` but DEFAULT_PALETTE has only 6 swatches; column renders without intended accent
- [x] Usage page reads like docs (intro, key concepts, basic example, movement controls, mixing rich cards, keyboard)
- [x] `dummy-data.ts` exported via the fixtures registry item
- [ ] Both light and dark mode look intentional — **deferred** (browser validation pending per pre-existing STATUS gap)
- [x] No console errors / warnings during demo run — **inferred from validateData clean state with valid data; not browser-confirmed**
- [x] No lorem ipsum in prominent demo copy
- [x] `usage.tsx` is not a copy of `demo.tsx`
- [ ] **Usage import paths inconsistent** — see **F-04**: basic example uses producer-side path `@/registry/components/data/kanban-board-01`, mixing-rich-cards example uses consumer-side `@ilinxa/rich-card`

**Notes:** F-04 and F-05 surfaced here. Demo dataset is otherwise the strongest example in the library so far.

---

## 5. Dependencies

- [x] `meta.ts.shadcnDependencies` matches every `@/components/ui/*` import (8 primitives: avatar, badge, button, dropdown-menu, input, popover, textarea, tooltip — verified across all parts)
- [x] `meta.ts.dependencies` matches every external npm import (4 packages: @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities, lucide-react)
- [x] `registry.json.registryDependencies` mirrors `meta.ts` (same 8 shadcn primitives — verified `scroll-area` correctly removed in v0.2)
- [x] No unused deps (declared but not imported)
- [x] Each external package's weight is justified (@dnd-kit family for DnD, lucide-react for icons)
- [x] Version pins explicit (^6.3.1, ^10.0.0, ^3.2.2, ^1.11.0 — caret ranges, no `*` or `latest`)
- [x] License compatible (all four MIT)
- [x] N/A — no internal sibling-component deps in base item (rich-card adapter is in demo.tsx, docs-only)

**Notes:** Clean. v0.2's removal of `scroll-area` correctly propagated to both meta and registry.json.

---

## 6. Design system

- [x] Fonts: Onest / JetBrains Mono only (no Inter / Geist / system) — verified at the registry import-allowlist level (no font imports)
- [x] Accent: signal-lime, paired with near-black `--primary-foreground` — column-header uses `--primary` indirectly via swatch palette
- [x] Light bg: never pure-white; raised surfaces lift above `--background`
- [x] Dark bg: graphite-cool, not warm-grey (inherited from project tokens)
- [x] Tailwind v4 syntax — no v3 leftovers (verified across all parts; no `bg-gradient-to-*`, no `break-words`, etc.)
- [x] No forbidden patterns: pure-white page bg, purple-on-white, neon lime
- [x] N/A — kanban-board-01 doesn't introduce reveals
- [x] Spacing rhythm consistent (gap-1 / gap-2 / gap-3 / py-2 / px-2.5 / py-1.5)
- [ ] **Light + dark visual parity** — partial: kanban-card uses semantic tokens cleanly, but kanban-note uses hardcoded amber palette
- [ ] **Component-scoped CSS uses namespace pattern** — N/A (no global CSS additions for kanban; column color uses inline `style` with `var(--xxx)`)
- [ ] **HARDCODED COLORS** — see **F-01**: `kanban-note.tsx` uses `border-l-amber-500/60`, `bg-amber-50/30`, `dark:bg-amber-950/20`, `text-amber-900 dark:text-amber-200`. Violates description-§6.6 success criterion.

**Notes:** F-01 is the headline finding. kanban-card is clean; kanban-note is the violation.

---

## 7. Accessibility

- [x] Keyboard nav: every interactive surface reachable + operable (Tab, Enter, Space DnD via dnd-kit)
- [x] No keyboard traps
- [x] ARIA roles + labels where semantics aren't obvious (`role="region"` board, `role="article"` items, `aria-roledescription` per renderer label, `aria-label` on collapse / menu / drag buttons)
- [x] Live regions announce async state — dnd-kit's accessibility.announcements feed status announcements
- [x] Focus rings visible in both modes (`focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-1`)
- [x] Focus management on dynamic mounts (popover open / close — Radix Popover handles)
- [x] Reduced-motion respected — partial; CSS transitions inherit `motion-safe:` defaults from Tailwind v4 setup
- [x] Color contrast: AA — semantic tokens used (except kanban-note amber where contrast is reasonable in light/dark but fails token discipline)
- [x] Screen-reader pass: drag / drop / select / expand all narrate (dnd-kit announcer)
- [x] Disabled states perceivable (`Lock` icon visible + `aria-roledescription="<label>, locked"`)
- [ ] **Drag-over visual feedback incomplete** — see **F-06**: `onDragOver` empty; `rejectDrop` only checks `acceptsRendererIds`, not full canDrop. `allowReorder/allowIncoming/allowOutgoing/locked` columns don't show red ring during drag-over.

**Notes:** F-06 is the a11y/UX feedback gap. Otherwise the a11y wiring is among the more thorough in the library.

---

## 8. Performance

- [x] `React.memo` not explicitly used but parts get React Compiler auto-memo per component setup
- [x] Stable identity for props crossing memo boundaries (rendererMap memoized; palettePinned memoized)
- [x] Module-scope constants for heavy maps (DEFAULT_PALETTE, sortableKeyboardCoordinates)
- [x] N/A — no Zustand store; `useReducer` over immutable tree
- [ ] **Virtualization / `onlyRenderVisibleElements` for N > ~100** — N/A in v0.1 (description §6.7 budget: 50 items / 5 columns at 60fps; virtualization deferred to v0.2)
- [x] No O(n²) walks per render (findItem / findColumn are O(n) and only called on drop, not per drag-tick)
- [ ] Stress demo for high-N — **N/A in v0.1** (per description risk acceptance; tracked as future v0.2 work)
- [x] No render loops
- [x] No `console.log` in render path
- [ ] Bundle weight — not measured in this session (pnpm build failed on env issue F-cross-04)

**Notes:** Performance contract honored within the v0.1 budget. Virtualization deferral is documented and accepted.

---

## 9. Registry distribution

- [x] Two items in `registry.json`: base + `kanban-board-01-fixtures` (verified at lines 1304-1373)
- [x] Locked target convention: `type: "registry:component"`, `target: "components/kanban-board-01/<sub-path>"` for every file (30 files in base, 1 in fixtures — all conform)
- [x] Excluded files: `demo.tsx`, `usage.tsx`, `meta.ts` not in `files` (verified — only `dummy-data.ts` in fixtures, all others base)
- [x] `pnpm registry:build` regenerates `public/r/kanban-board-01.json` cleanly
- [x] **Smoke install passes** — `pnpm dlx shadcn@latest add @ilinxa/kanban-board-01 --yes` succeeded in 11.7s against production Vercel registry. No missing deps, no failed file resolutions.
- [x] N/A — no internal sibling-component deps (rich-card is demo-only)
- [x] `description` matches `meta.ts.description` and doc-site card
- [ ] `pnpm vercel-build` not run in this session (would require the build step which fails on env F-cross-04)

**Notes:** Registry distribution is clean and consumer-tested live.

---

## 10. Meta + manifest sync

- [x] `meta.ts.version` reflects reality (`0.2.0`)
- [x] `meta.ts.status` honest (`alpha`)
- [x] `meta.ts.tags` searchable (kanban, board, drag-and-drop, dnd-kit, swimlanes, columns, tasks, rich-card)
- [x] `meta.ts.description` matches doc-site + `registry.json` description
- [x] Registered in `manifest.ts.REGISTRY` (3 imports verified)
- [ ] **`STATUS.md` Components table row stale** — see **F-02**: claims "not yet committed" but is committed as `7263286`
- [ ] **`STATUS.md` Recent decisions** does NOT yet mention the v0.2 review (will be added as F-02 follow-up)
- [x] `docs/component-versions.md` snapshot in sync (lists kanban-board-01 v0.2.0)
- [x] `meta.ts.updatedAt: "2026-05-06"` — date is accurate for v0.2 ship

**Notes:** F-02 surfaced here. Otherwise meta + manifest sync is in good shape.

---

## 11. UI copy / text

- [x] Empty states are real sentences — "Empty" placeholder in swimlane-cell (line 75); error fallback "kanban-board-01 requires at least one renderer" (root); "Renderer not found" in missing-renderer
- [x] Error / warn messages actionable (validateData warnings cite item id + renderer id; missing-renderer warning suggests adding the renderer)
- [x] Button labels verb-first ("Edit", "Delete column", "Cancel", "Save", "Rename column", "Drag to move")
- [x] Tooltips terse, non-redundant ("Locked", "Drag to move")
- [x] Placeholder text explains format ("Card title", "Description (optional)", "Note title", "Body (optional)")
- [x] No lorem ipsum in prominent copy
- [x] Pluralization correct — N/A (no count text beyond raw numbers)
- [x] Sentence case for in-product UI (verified across menus and labels)
- [x] Brand names spelled correctly (no brand names in UI text)
- [x] No "TODO copy" in shipped strings
- [x] No "Click here"

**Notes:** Copy is clean. The `kanban-note.tsx` "drag" label could be considered redundant given the grip icon, but that's a minor design choice, not a finding.

---

## 12. Verification

- [x] `pnpm tsc --noEmit` clean
- [x] `pnpm lint` clean (1 pre-existing rich-card warning, no new)
- [ ] **`pnpm build`** failed on Google Fonts network access (env issue F-cross-04, not kanban-specific)
- [x] `pnpm registry:build` clean
- [ ] Docs-site detail page renders at `/components/kanban-board-01` — **deferred** (browser validation pending per F-02 + pre-existing STATUS gap)
- [ ] Browser validation: light + dark, narrow + wide, all interactive paths — **deferred**
- [x] Smoke install in tmp consumer **passes** (11.7s)
- [x] Console clean during smoke install (no errors, no key warnings, no hydration mismatches at install time)

**Pre-existing warnings:**
```
E:\…\rich-card\hooks\use-virtualizer.ts:22:23 — TanStack Virtual incompatible-library
```

**Notes:** F-cross-04 (Google Fonts build env) is cross-cutting, not a kanban-specific finding. Browser validation gap is real and pre-existing.

---

## 13. Migration provenance (if applicable)

- [x] **N/A — greenfield component** (no `> Migration origin` line in description)

---

## 14. Cross-component coherence

- [x] Renderer-registry pattern consistent with workspace / flow-canvas-01 shape
- [x] Naming: `kanban-board-01` uses `-01` suffix per family-variant convention (future `kanban-board-02` etc. reachable)
- [x] Public-helper export naming follows convention (`KANBAN_*` not yet, but DEFAULT_PALETTE matches pattern; `findRenderer` is a verb-helper)
- [x] Theming variables in `globals.css` for column color tokens
- [x] Sealed-folder shape matches `data-table` pattern (with justified `lib/` addition per Q-P10)
- [x] Category assignment correct (data — primary purpose is data display + manipulation)
- [x] Status convention (alpha) consistent with sibling components at v0.x
- [x] Shared primitives declared the same way as siblings (8 shadcn primitives in meta.ts.dependencies.shadcn array)

**Notes:** Coherence with workspace and flow-canvas-01 is the clearest signal that the renderer-registry pattern is mature enough to promote to a documented convention.

---

## Tally

- **Total items:** 133 (across 14 dimensions)
- **Ticked:** 116
- **N/A (with reason):** 6
- **Failed (becomes a finding):** 11 across 6 unique findings (F-01 through F-06)

> Companion narrative report: [`2026-05-08-v0.2.0-review.md`](./2026-05-08-v0.2.0-review.md)
