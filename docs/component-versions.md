# Component Versions

Snapshot of every shipped procomponent in the registry, with version + release status. Pulled directly from each component's `src/registry/components/<category>/<slug>/meta.ts`.

> **Updated 2026-06-03** (patch layer): **media-editor-01 v0.1.0 → v0.1.1** + **story-composer-01 v0.2.0 → v0.2.1** SHIPPED + PUSHED ([decision](../.claude/decisions/2026-06-03-media-editor-01-v0.1.1-walkthrough-fixes.md), [GATE 3 v0.1.1](procomps/media-editor-01-procomp/reviews/2026-06-03-v0.1.1-spotcheck.md)). Walkthrough-fix sweep (F-06…F-13) + Instagram chrome model (single-pointer drag-to-pan, capture-only mode tabs → back-to-capture arrow, full-bleed canvas + overlaid bottom controls, container-query shutter, dialog/inline min-size clamp) + a 4-agent deep review (code/wrapper/docs/registry → fixes + full doc alignment; dead-code removal incl. `composer-shell`; over-declared `dialog` dep dropped). Commits `2030b08` + `3dd9bdd`. Gates green (tsc 0 / lint 81-22 / meta-deps 51-51 / build). Procomp count unchanged (**51**). Open follow-ups: v0.1.2 (analytics callbacks, renderPermissionDenied, keyboard focus-scope) + v0.2 (imperative capture handle, C17 labels flattening, library media source).
>
> **Updated 2026-06-02** (additive layer): **media-editor-01 v0.1.0 alpha SHIPPED + PUSHED — 51st procomp** ([decision](../.claude/decisions/2026-06-02-media-editor-01-v0.1.0-extraction-and-story-composer-01-v0.2.0.md)). Same release: **story-composer-01 v0.1.5 → v0.2.0 alpha** (245-line wrapper refactor, was 1145 lines). 32-commit chain `849c577..b74556a` across Phase A (extraction C1–C15 + R1–R4 UI backfill) / Phase B (wrapper refactor C16–C19) / Phase C (GATE 3 reviews C20–C22). **FIRST inter-procomp registry-dep install path in the library** (`@ilinxa/story-composer-01` → triggers `@ilinxa/media-editor-01` via registryDependency; konva/react-konva resolve transitively). Both GATE 3 reviews Pass-with-follow-ups (8 open / 2 closed-in-review; v0.1.1 / v0.2.1 / v0.3.0 cohorts). **Biggest executional lesson:** planning docs need to enumerate UI mount points alongside type/state surfaces — Phase A's plan optimistically assumed UI was complete at C8; C15 review caught the gap; R1–R4 backfill closed it. Total procomps: **51** (was 49 in the 2026-05-22 banner below; intervening ships — todo-tree v0.1.3 + rich-sidebar v0.2.0 + account-switcher-01 v0.1.0 + workspace v0.1.3 + engagement-bar-01 v0.3.2 + post-card-01 v0.2.1 + story-viewer-01 v0.4.4 + story-composer-01 v0.1.5 + content-card-news-01 v0.3.0 + media-editor-01 v0.1.0 — not catalog-refreshed individually but tallied here). Canonical per-component table: STATUS.md Components section. Full refresh queued; this banner is the bridge.

> Snapshot date: **2026-05-22** (most-recent update at the bottom of the Highlights below — supersedes the 2026-05-17 banner that anchored this section) (**rich-card-in-flow v0.2.0 SHIPPED — port editor (Workstream B)**: adds `PortEditorStrip` — opt-in editor for the ports carried by each rich-card canvas node or its `__rcid`-tagged subcards. Six editable fields per port (id / type / side / dir / multi / label); live-save; add-popover with `[✓in][✓out]` atomic create-flow that splits to `{base}-in` / `{base}-out` rows after save (Q3 lock); doc-port type forces `bottom` side editor-side (Q4 lock); orphan-doc-target tooltip on doc-typed ports today (Q-O2 lock — separate doc-file procomp will resolve in v0.3+). No breaking changes vs v0.1.0. New public surface: `PortEditorStrip` + `PortEditorPermissions` + `PortField` union. shadcn primitive deps grew 0→7 (popover/select/checkbox/input/tooltip/label/button). Uncontrolled-by-design (Q9 lock). 8 implementation commits + B4 (registry distribution + path-b smoke + 3 primitive-divergence fixes — see F-cross-13 below). GATE 3 spotcheck Pass with follow-ups; rotating dim Public API; 3 findings (F-01 F-cross-13 closed pre-ship; F-02 per-field ports + F-03 custom port-type registration both v0.3 candidates). See [decision file](../.claude/decisions/2026-05-17-rich-card-in-flow-v0.2.0-port-editor.md). **flow-canvas-01 v0.2.5 SHIPPED — built-in `"doc"` port type (Workstream A prerequisite)**: one-line `defaultPortTypes` add — `{ id: "doc", color: "var(--chart-3)", label: "Doc" }`. Required upstream for rcif v0.2.0's PortEditorStrip — doc-port type is the only port type with editor-side side-enforcement (auto-corrects to `bottom`). Runtime stays neutral about doc ports. Patch-bump exemption per readiness-review rule — additive, non-breaking, no public-API touch; GATE 3 skipped, v0.2.0 spotcheck verdict carries forward. See [decision file](../.claude/decisions/2026-05-17-flow-canvas-v0.2.5-doc-port-type.md). **NEW F-cross-13 (Medium) — shadcn primitive Radix → Base UI divergence**: producer ships Radix-based primitives but `shadcn@4.6.0 add` ships Base UI primitives; `TooltipProvider.delayDuration` (Radix) vs `delay` (Base UI); `Select.onValueChange` is `(v: string)` on Radix vs `(v: string | null, eventDetails)` on Base UI. Path (a) defensive callback pattern shipped per-procomp in rcif v0.2.0 B4 (parameter contravariance — widen onValueChange to `(v: string | null)` to satisfy both backends); path (b) producer-primitive refresh = standalone hygiene task. See [`docs/reviews/sweep-tracker.md`](reviews/sweep-tracker.md) F-cross-13. **Earlier today: rich-card v0.4.2 SHIPPED — empty-card add-affordances fix**: lifted `+ FIELD` / `+ BLOCK` action row out of the `hasBody`-gated body wrapper in `parts/card.tsx` so empty cards (zero fields, zero predefined) — including any freshly-added child card — can be populated through the UI. Pre-v0.4.2, `dispatchers.cardAdd` created `fields:[]/predefined:[]` cards that had only `+ CHILD` exposed (nestable, never fillable). Surfaced during a post-v0.2.4 rich-card-in-flow editor audit; user-reported "ADD BLOCK options disappear" + "root has no add options" turned out to be by-design and a misperception respectively, but the investigation surfaced THIS distinct latent bug. Patch-bump exemption — GATE 3 skipped, rich-card stays `beta`. Earlier today: **flow-canvas-01 v0.2.4 SHIPPED — suppress mid-drag onChange + microtask-time drag guard**: third and final follow-up in the v0.2.2 → v0.2.3 → v0.2.4 controlled-mode saga. After v0.2.3, xyflow's #015 "trying to drag a node that is not initialized" warning was STILL firing during drag (10–20+ per drag) — mid-drag mixed batches slipped through the position-only short-circuit, queued microtasks via v0.2.2's defer, by microtask-fire time further drag ticks had moved internal state on, and v0.2.3's structural-check correctly identified the divergence but its "fix" (wholesale replace) wiped `measured` → #015 next tick. Two complementary defenses in `hooks/use-canvas-data.ts`: (1) `onNodesChange` suppresses ALL consumer onChange during drag (not just position-only batches); (2) `fireOnChange` microtask checks `isDraggingRef` at fire time + bails early if drag in progress. `onNodeDragStop` continues flushing the single authoritative post-drag snapshot. Patch-bump exemption — GATE 3 skipped. v0.2.4 is the floor for safe controlled-mode use. Earlier: v0.2.3 round-trip echo guard via `canvasMatchesInternalState` (still load-bearing for steady-state round-trips). Earlier: v0.2.2 setState-during-render fix (`queueMicrotask` wrap of `fireOnChange`). Earlier (2026-05-16): **rich-card-in-flow v0.1.0 SHIPPED**: 43rd component, canonical implementation of flow-canvas-01@v0.2.0's popup-edit renderer convention. flow-canvas-01 v0.2.1 (`onEditRequest` API + `updateNodeData` helper). v0.2.0 Tier 1 + Tier 2 perf bundle.)
> Total: **48 components** across **8 categories**
>
> **Updated 2026-05-20** (additive to 2026-05-17 snapshot above): **todo-rich-card v0.1.0 → v0.1.1 SHIPPED** — first ship was the time-aware task card sibling to rich-card (full detail in [`decisions/2026-05-20-todo-rich-card-v0.1.0-first-ship.md`](../.claude/decisions/2026-05-20-todo-rich-card-v0.1.0-first-ship.md)); same-day F-cross-13 patch loop ([`decisions/2026-05-20-todo-rich-card-v0.1.1-f-cross-13-fix.md`](../.claude/decisions/2026-05-20-todo-rich-card-v0.1.1-f-cross-13-fix.md)) widened Select.onValueChange callbacks + dropped TooltipProvider.delayDuration; F-04 closed. **todo-tree v0.1.0 scaffolded** (C1 of C1–C11 chain per [todo-tree-procomp-plan.md §18](procomps/todo-tree-procomp/todo-tree-procomp-plan.md#18-implementation-order-commit-chain)) — single feature-complete v0.1, no v0.2/v0.3 deferrals; full type catalog locked, stub component renders; implementation lands across C2–C11. **Live rich+kanban composition demo** added to todo-rich-card detail page (docs-only).
>
> **Updated 2026-05-21 (later still)** (additive): **todo-tree v0.1.3 SHIPPED** — hotfix patch for an `@dnd-kit/core` SSR hydration mismatch surfaced in browser dev mode after v0.1.2 deployed. Each `<DndContext>` auto-allocates `DndDescribedBy-N` IDs from a module-level counter; in Next.js dev React StrictMode double-renders the client tree, so the CSR counter increments at 2× the SSR counter and every grip button's `aria-describedby` diverges (exact `-1 → +2, -2 → +4` ratio pattern). Fix is one line at [`todo-tree.tsx:110`](../src/registry/components/data/todo-tree/todo-tree.tsx) — pass `id={useId()}` to `<DndContext>` so dnd-kit's counter is bypassed and the ID is tied to React tree position (stable across SSR + CSR). Patch-bump exemption per readiness-review rule (internal-only fix, no public-API touch, GATE 3 skipped — v0.1.2 spotcheck verdict carries forward).
>
> **Updated 2026-05-21 (later)** (additive): **todo-tree v0.1.2 SHIPPED** — same-day follow-on after v0.1.1's GATE 3 review. Closes F-perm (keyboard Space/Delete now consult the declarative permissions matrix + `item.locked` + `readOnly`, fire `onPermissionDenied` on denial) and ships a UX/API batch: new `editable` prop (gates the toolbar "+ New" button + bulk Delete), new `createItem` factory (built-in fallback uses `statusOptions[0]`), new `onCreateRequest` hook + deferred-commit pending-item flow in `<TodoTreeWithEditor>` (dialog opens immediately on "+ New"; Submit commits, Cancel discards), F-grip-clip closed (grip moved from `absolute -left-4` into the row's flex flow), Dialog width 1.5× + scroll cap + `aria-describedby={undefined}` + Radix focus-blur race fix. Public-API additive — no breakage. v0.1.2 GATE 3 spotcheck file at [reviews/2026-05-21-v0.1.2-spotcheck.md](procomps/todo-tree-procomp/reviews/2026-05-21-v0.1.2-spotcheck.md).
>
> **Updated 2026-05-21** (additive): **todo-tree v0.1.0 → v0.1.1 SHIPPED** — 45th component formally closed under the GATE 3 readiness-review rule. C1–C11 of plan §18 chain complete; commit chain `aa623a0..5aba0cd` (19 commits incl. 6 audit fixups). 48 base + 1 fixtures = 49 files via registry. Same-day F-cross-13 patch loop ([`decisions/2026-05-21-todo-tree-v0.1.0-and-v0.1.1-first-ship.md`](../.claude/decisions/2026-05-21-todo-tree-v0.1.0-and-v0.1.1-first-ship.md)) replaced `=== "indeterminate"` check at `parts/todo-tree-checkbox.tsx:37` with `typeof next === "boolean" ? next : true` — third same-day F-cross-13 patch loop in a row; adds **Checkbox.onCheckedChange to the carrier list** alongside Select.onValueChange + TooltipProvider.delayDuration. F-S1 verified clean (third same-category cross-procomp ship to pass; lock now battle-tested project default). GATE 3 spotcheck **Pass with follow-ups** ([reviews/2026-05-21-v0.1.1-spotcheck.md](procomps/todo-tree-procomp/reviews/2026-05-21-v0.1.1-spotcheck.md)) — rotating dim Public API; 5 findings (F-perm Medium permissions matrix not threaded through keyboard handlers v0.1.2 candidate; F-event Low Enter handler event-union widening walkthrough; F-grip-clip Low cosmetic; F-04 High path-b smoke closed in v0.1.1; F-html5-edge Low HTML5 drops no edge-zone v0.2 candidate). Procomp guide.md authored at C11. 45 components total — todo-tree promoted from scaffold to shipped.
>
> Source of truth: `meta.ts` per component (this doc is a generated snapshot — re-run when versions change)

## Summary

| Category   | Count | Notes                                                                 |
|------------|-------|-----------------------------------------------------------------------|
| `data`     | 24    | Largest category; hosts the canonical `data-table` template + `stat-card` + `rich-card-in-flow` (2026-05-16) + `todo-rich-card` (2026-05-20) + `todo-tree` (2026-05-20 scaffold) |
| `forms`    | 8     | `json-form` added 2026-05-13 (first cross-registry `dependencies.internal` consumer; lazy-loads `@ilinxa/code-block`); `registration-form-01` added 2026-05-22 |
| `marketing`| 5     | `pricing-table-01` added 2026-05-22                                   |
| `media`    | 4     | `pdf-viewer` added 2026-05-10                                         |
| `navigation`| 4    | New category 2026-05-10 (`file-tree` + `file-manager`); `rich-sidebar` added 2026-05-22; `account-switcher-01` added 2026-05-23 |
| `layout`   | 2     |                                                                       |
| `code`     | 1     | New category 2026-05-11 (`code-block`)                                |
| `feedback` | 1     |                                                                       |
| **Total**  | **49**|                                                                       |

## Highlights

- **v0.3.0 → v0.3.2 (`engagement-bar-01`, 2026-05-28):** multi-kind reactions ship. Closes ILX-1 (HIGH) + ILX-2 (MEDIUM) from `social-moduls-python` backend team spec. **v0.3.0** (`d6e72e9`): new `kind: "reaction"` union arm + `EngagementReactionKind` interface + 3 delta variants + 3 nullable state fields + `reaction-select` reducer action + `<ReactionPicker>` + `<ReactionAction>` parts (popover assembly, 350ms long-press, tap-vs-clear matrix) + `reactionsPreview` slot + 4 new labels + `triggerReaction` + `getCurrentReaction` handle methods + Defense 1 (microtask) + Defense 2 (sync effect for controlled viewerReaction). Q-P locks: Q-P1=(b) single `kinds[]` catalog · Q-P2=(c) `clearOnTap` default true · Q-P3=(a) like + reaction coexist. Interactive demo: `InteractiveDefaultDemo` wires all 4 callbacks (LikersStrip + CommentThread01 + ShareMenu + bookmark); `InteractiveReactionsDemo` adds scrollable + lazy-loaded reactors panel. GATE 3 Pass with follow-ups; 16 re-validation findings caught + fixed pre-spotcheck. **v0.3.1** (`0c803ee`): smoke surfaced PopoverAnchor not exported by Base UI → revert to PopoverTrigger + queueMicrotask override for auto-toggle. **v0.3.2** (`6f99a88`): smoke surfaced asChild not accepted by Base UI's PopoverTrigger → drop asChild, render PopoverTrigger directly with HTML props spread. Live smoke ✅ green post-v0.3.2 (install OK + consumer-tsc 0 errors). F-cross-13 carrier list extended with 3-facet Popover sub-trap. See [decision file](../.claude/decisions/2026-05-28-engagement-bar-01-v0.3.0-reactions-multi-kind-ship.md).
- **v0.1.1 (`rich-sidebar`, 2026-05-22):** same-day hydration-error patch. User-reported `<li> cannot be a descendant of <li>` warning on the sectioned demo. Two structural bugs from C3/C11 surfaced together when the section's sidebar-collapsed branch rendered: `parts/sidebar-nav-list.tsx` wrapped every returned row in `<span key={…}>` inside `<ul role="list">` (producing `<ul><span><li>` and a nested `<span><li>` inside section bodies), and `parts/sidebar-nav-section.tsx`'s sidebar-collapsed body wrapped its items in `<div role="group">` instead of `<ul role="group">`. Fix: drop the span wrappers (place keys on the returned `<li>` directly; `<Fragment key={…}>` for the renderSection slot path); switch the collapsed-sidebar section body to `<ul role="group">`. Patch-bump exemption per the readiness-review rule. See [decision file](../.claude/decisions/2026-05-22-rich-sidebar-v0.1.1-hydration-li-nesting-fix.md).
- **v0.1.0 (`rich-sidebar`, 2026-05-22) — 48th component SHIPPED:** Full C1→C13 chain closed in a resume-after-pause session. Navigation-category app-shell sidebar with mobile-drawer mode (Sheet, CSS-gated render path — no SSR flash), 12 prefab/render-prop slots, 16 events, 22-method imperative handle, 5 prefab parts (`RichSidebarTrigger` + `NavBadge` + `NavBrand` + `NavPrimaryAction` + `NavUser`), headless `useRichSidebarState` hook, 5 active-state variants, CSS-variable theme surface, full WAI-ARIA keyboard nav (roving tabindex + Arrow/Home/End + ArrowRight-expand / ArrowLeft-collapse on section headers), opt-in skip link, permissions filtering with diff-based `onPermissionDenied`, localStorage persist. Migration origin: kasder's `SocialSidebar.tsx`. **Largest single-procomp F-cross-13 surface** in the project so far — Tooltip + Sheet + DropdownMenu all defensively pre-wired before push. Latent Icon-helper bug surfaced + fixed mid-close (`52e5f33`): lucide-react v0.475+ icons are `forwardRef` objects (`typeof === "object"` with `$$typeof + render + displayName`), the previous typeof-only check fell through to the ReactNode branch and crashed static prerender (`Objects are not valid as a React child`). meta deps: 5 shadcn primitives (tooltip + sheet + avatar + button + dropdown-menu), 1 npm peer (lucide-react), 0 internal. GATE 3 spotcheck Pass with follow-ups; 5 findings (F-01 closed; F-02 path-b smoke deferred to post-deploy per pattern; F-03 docs-only; F-04 + F-05 v0.2.0 logical-direction sweep). See [decision file](../.claude/decisions/2026-05-22-rich-sidebar-v0.1.0-first-ship.md).
- **v0.1.1 (`registration-form-01`, 2026-05-22):** submit-row layout fix. In single-step mode (and step-1 of two-step), `SubmitRow` previously rendered `<div className="flex flex-col-reverse gap-2 sm:flex-row sm:justify-between">` with an empty left-side wrapper — pushing the lone submit button to the right edge of the form and visually misaligning it against the centered `SignInLink` below. v0.1.1 collapses that case to a bare `<Button className="w-full" type="submit">` (no flex wrapper, no `justify-between`) so the primary action spans the form width, matching the email/password input rhythm and reading as the intended next step under the centered "Already have an account? Sign in" link. Step-2 of two-step is unchanged — Back + (optional) Skip on the left, submit on the right, `justify-between` preserved. No public-API change; demo-side `Section` width tightened to `max-w-md p-6` so the card hugs the form's natural width on the docs preview surface (demos are not registry-shipped). Patch-bump exemption per the readiness-review rule. See [decision file](../.claude/decisions/2026-05-22-registration-form-01-v0.1.1-submit-row-fullwidth.md).
- **v0.1.0 (`pricing-table-01`, 2026-05-22) — 47th component SHIPPED; CMS conversion-block batch fully closed:** Sibling of `registration-form-01` (shipped a few commits earlier); both halves of the batch (1 + 2 of 2) now live. Marketing-category pricing surface: 2 layout variants (`cards` / `table`), optional monthly/annual toggle (radiogroup, arrow-key navigable), highlighted-tier badge with signal-lime accent, per-feature tooltip rows, controlled-or-uncontrolled billing state (mirrors `newsletter-card-01`), ReactNode-or-CtaSpec polymorphic CTAs, 3 tones, full i18n bag, RTL-safe. ~1,500 LOC across 11 shipped files + 1 fixtures. meta deps: 2 shadcn primitives (button + tooltip), 1 npm peer (lucide-react), 0 internal. **F-cross-13 carrier `<TooltipProvider delayDuration={150}>` dropped pre-ship** (third consecutive v0.1.0 first ship to avoid the same-day patch loop). GATE 3 spotcheck Pass with follow-ups; 5 non-blocking findings (F-04 path-b smoke deferred to post-deploy per established pattern). See [decision file](../.claude/decisions/2026-05-22-pricing-table-01-v0.1.0-first-ship.md).
- **v0.1.0 (`registration-form-01`, 2026-05-22) — 46th component SHIPPED:** Sibling of in-flight `pricing-table-01` in the CMS conversion-block batch (2 of 2). Hand-rolled sign-up surface on RHF v7 + zod v4 (does NOT depend on `@ilinxa/json-form` per Q1 lock). Two flow variants (`single-step` / `two-step`) × two password strategies (`password` / `magic-link`) × two densities × optional OAuth row + declarative optional-fields bag + off-screen honeypot. Three load-bearing public-API contracts: (1) **discriminated submit payload** `{ stepCompleted: "single" | "step1" | "step2" }` forces consumers to switch on the discriminant (rejects optional-field narrowing because it silently lets naïve destructuring send `undefined` to backend APIs); (2) **status mutual-exclusion** — controlled `status` makes internal state read-only; (3) **honeypot two-key duality** — HTML `name="website"` + RHF key `_honeypot`, off-screen position (NOT `display: none` — bot-detectable), JSX attribute order matters. 11-commit chain; ~1,400 LOC across 25 shipped files; 6 fixtures × 6-tab demo. meta deps: 4 shadcn primitives, 4 npm peers, `internal: []`. GATE 3 spotcheck Pass with follow-ups; 5 non-blocking findings (path-b smoke deferred to post-deploy per established pattern). See [decision file](../.claude/decisions/2026-05-22-registration-form-01-v0.1.0-first-ship.md).
- **v0.2.5 (`json-form`, 2026-05-22):** `FieldRadioGroup` no longer flips from uncontrolled to controlled on first interaction. Before v0.2.5 the renderer passed `value={value == null ? undefined : String(value)}` to Radix `RadioGroup`, so for a radio-group field with no `defaultValue` (e.g., `kind` on the Conditional tab demo, `radioRequiredField` on any consumer schema), the initial render passed `undefined` → Radix treated the group as uncontrolled → first click made `value` defined → Radix flipped to controlled → React logged `RadioGroup is changing from uncontrolled to controlled` (radio-group.tsx:13 — the shadcn `RadioGroupPrimitive.Root` props spread). Mechanical fix mirroring `FieldSelect`'s already-correct pattern: pass `""` instead of `undefined` for the "no selection" state, keeping the group always-controlled. Patch-bump exemption per the readiness-review rule. See [decision file](../.claude/decisions/2026-05-22-json-form-v0.2.5-radio-group-controlled-fix.md).
- **v0.2.4 (`json-form`, 2026-05-21):** crash fix — the documented headless pattern (`<JsonFormProvider value={{ ...handle, rhf: form, ... }}>{children}</JsonFormProvider>`) no longer requires consumers to also wrap with RHF's `<FormProvider {...form}>`. `JsonFormProvider` now bridges the RHF context internally via `<FormProvider {...value.rhf}>`. Before v0.2.4, every `<JsonFormField>` / `<FieldWrapper>` inside a headless tree crashed with `Cannot destructure 'control' of useFormContext() as it is null` — the demo's own `HeadlessExample` (inside the Imperative tab's `<details>` block) triggered this on every json-form page render; the failure surfaced after the v0.2.0 watch-drop work made FieldWrapper's RHF dependency load-bearing on first render. No public-API change, no consumer migration. Patch-bump exemption per the readiness-review rule. See [decision file](../.claude/decisions/2026-05-21-json-form-v0.2.4-headless-formprovider-bridge.md).
- **Past beta gate:** `rich-card` only — v0.4.2, status `beta` (v0.4.2 patch 2026-05-17: empty-card add-affordances fix; lifted `+ FIELD` / `+ BLOCK` row out of the `hasBody` wrapper so freshly-added child cards can be populated through the UI; see [decision file](../.claude/decisions/2026-05-17-rich-card-v0.4.2-empty-card-add-affordances.md)).
- **v0.3.x:** `kanban-board-01` (F-cross-12 v0.2 cutover). `content-card-news-01` (v0.3.0 — backend-shaped editorial features: role-aware editor/viewer mode + 19-capability `ContentCardPermissions` matrix + moderator section + 16 mutation handlers + kebab system + paywall gate (media-only scope per Q-PG; preview substitutes for excerpt) with polymorphic CTA + sensitive content gate with compact mode for tight thumbs + 7-badge editorial stack with frozen priority AND state/curation placement split per Q-PC2 (state at overlay; curation as kicker above title) + uniform badge shape with saturation hierarchy + quoted-article compact preview via inner variant=small with all gates suppressed + light engagement counts with slot-based engagement-bar-01 composition for detail pages + 11-method imperative handle + ~50 i18n labels + 11 sub-exports; strictly additive on v0.2; GATE 3 Pass with follow-ups + 8-commit post-ship visual-review polish arc folded into ship; see [decision file](../.claude/decisions/2026-06-02-content-card-news-01-v0.3.0-backend-shaped-editorial-features.md)).
- **v0.2.x:** `flow-canvas-01` (v0.2.5 — additive built-in `"doc"` port type in `defaultPortTypes` for use by `rich-card-in-flow@v0.2.0`'s port editor; patch-bump exemption — no GATE 3; see [decision file](../.claude/decisions/2026-05-17-flow-canvas-v0.2.5-doc-port-type.md). v0.2.4 — third and final follow-up in the controlled-mode saga: suppresses ALL consumer onChange during drag (not just position-only batches) + fireOnChange microtask bails if drag started post-queue; closes the stale-snapshot race that v0.2.3's structural-check correctly identified but couldn't fix without wiping `measured`; v0.2.4 is the floor for safe controlled-mode use; see [decision file](../.claude/decisions/2026-05-17-flow-canvas-v0.2.4-stale-snapshot-fix.md). v0.2.3 — round-trip echo guard; structural-equality check in controlled-mode useEffect skips wholesale-replace of xyflow internal node refs when consumer's new data prop matches current internal state; still load-bearing for steady-state round-trips; see [decision file](../.claude/decisions/2026-05-16-flow-canvas-v0.2.3-resync-echo-guard.md). v0.2.2 — setState-during-render fix; `fireOnChange` body wrapped in `queueMicrotask` so all 13 reducer-side-effect sites in `use-canvas-data.ts` uniformly defer consumer notify to post-commit; promotes the v0.3-deferred F-V4 cleanup forward; surfaced by `rich-card-in-flow@v0.1.0`'s demo (first controlled-mode consumer); patch-bump exemption — no GATE 3; see [decision file](../.claude/decisions/2026-05-16-flow-canvas-v0.2.2-microtask-defer.md). v0.2.1 — additive `onEditRequest` API on `FlowCanvasProps` + `RenderContext` + new exported `updateNodeData` helper; popup-edit renderer convention canonical first consumer is `rich-card-in-flow@v0.1.0` (now shipped); patch-bump exemption — no GATE 3; see [decision file](../.claude/decisions/2026-05-16-flow-canvas-v0.2.1-on-edit-request.md). v0.2.0 — Tier 1 + Tier 2 perf bundle: default flip + drag-batching + narrow `portEqual` + CSS-driven selection ring + new `lib/shallow.ts` + sealed-folder `flow-canvas-01.css`; one default flip + one soft behavior change + zero breaking type changes; see [decision file](../.claude/decisions/2026-05-16-flow-canvas-v0.2.0-perf-bundle.md)), `article-body-01`, `grid-layout-news-01` + `content-card-news-01` + `project-card-01` + `story-rail-01` (F-cross-12 v0.2 cutover — positional callbacks removed; canonical names now object-shape).
- **v0.1.4 (now superseded by v0.2.0):** `flow-canvas-01` patch — custom-json renderer was missing `<PortsAt>` calls for declared ports; xyflow's edge layer failed `getEdgePosition` thousands of times per second and warning-spammed the console; ~6-line fix = 15-20× FPS improvement at the light-fixture cliff; surfaced during v0.2 perf-tier pre-work.
- **v0.2.2 (`article-body-01`) + v0.2.3 (`json-form`), 2026-05-21:** root-cause fix for the richtext controlled-mode echo loop. `ArticleBodyEditor`'s sync effect now uses **content-equality** (`serializeValueKey(controlledValue) !== lastSyncedKeyRef.current`, JSON-stringify of the Plate tree) instead of reference-equality, so RHF's per-render fresh refs no longer trigger spurious `editor.tf.setValue` calls that reset Slate's selection (the bug surfaced first as React #185, then as "type one letter, focus lost" after v0.2.2's consumer-side stabilization attempt). json-form v0.2.2's `useStableRichtextValue` band-aid REVERTED in v0.2.3 — `field-richtext.tsx` returns to its simple v0.2.1 shape since the upstream fix in article-body-01 makes consumer-layer stabilization unnecessary. Patch-bump exemption for both. See [decision file](../.claude/decisions/2026-05-21-article-body-01-v0.2.2-content-equality-echo-guard.md).
- **v0.2.2 (`json-form`, 2026-05-21):** richtext controlled-mode echo loop fix (React #185). The v0.2.0 substrate work surfaced a latent loop in `<ArticleBodyEditor>`'s reference-equality controlled-value sync effect — RHF's per-render fresh refs caused setValue → Plate.onChange → setState → setValue → ... ping-pong until React capped at 50 updates. Single-file consumer-side guard in `parts/field-richtext.tsx`: new internal `useStableRichtextValue()` hook returns a content-stable ref (JSON-hash-keyed `useMemo`) + `useCallback` for the onChange wrapper. Strictly additive — zero public-API change, zero behavior change for non-loop scenarios. The deeper fix lives in article-body-01's sync effect (content-equality instead of ref-equality); tracked for a future article-body-01 v0.3 ship. Patch-bump exemption per the readiness-review rule. See [decision file](../.claude/decisions/2026-05-21-json-form-v0.2.2-richtext-loop-fix.md).
- **v0.2.1 (`json-form`, 2026-05-21):** docs + devtools patch closing 4 of 5 v0.2.0 GATE 3 follow-ups. **F-01** `dependsOn` JSDoc rewritten — drops v0.1.7 future-framing, adds stable-identity guidance (module-scope const or `useMemo`), documents the final-renderer-identity check. **F-02** `allValues` snapshot-freshness JSDoc covers snapshot / narrow / fullbag modes. **F-06** `<JsonFormDevtools>` soft-warning fallback when no `<JsonFormProvider>` is in the tree — new internal `useJsonFormContextOptional()` accessor (NOT exported from the package barrel) returns `null` instead of throwing; the panel renders an explanatory body instead of crashing. **F-08** `prettyReplacer` returns `${value.toString()}n` for BigInt values so the panel doesn't crash on bigint form state. Patch-bump exemption — GATE 3 skipped per the readiness-review rule; v0.2.0 verdict carries forward. Open follow-ups reduced 5 → 2 (only F-03 ⚠️ High whitelist drift lint + F-07 `defineFieldRenderer` config-key narrowing indefinite-defer remain). See [decision file](../.claude/decisions/2026-05-21-json-form-v0.2.1-docs-patch.md).
- **v0.2.0 (`json-form`, 2026-05-21):** the perf cliff lifter + deep-merge `defaultValues` — behavioral counterpart to the additive v0.1.7 substrate. **A2 — Default-registry watch drop:** `parts/field-wrapper.tsx` resolves a per-field subscription mode (snapshot / narrow / fullbag). Built-in default renderers (whitelist ∩ identity check against `defaultJsonFormRegistry`) skip the FieldWrapper-level `useWatch({ control })` entirely; `dependsOn: []` opts custom renderers into snapshot; `dependsOn: ['a','b']` narrow-watches; omitted `dependsOn` keeps v0.1.x full-bag behavior. FieldWrapper renders per keystroke on a 20-field form drop **20 → 1–4**. **B1 — Deep-merge `defaultValues`:** form-level overlays walked per leaf via new `walkLeaves` + `isPlainObject` + array-path `setByPath` in `hooks/use-json-form.ts`; sibling per-field defaults at shared parent paths no longer dropped. Conditional-count dev-warn threshold bumped 50 → 200. Strictly auto-applied — zero consumer code changes. **GATE 3 full-checklist Pass with follow-ups** ([reviews/2026-05-21-v0.2.0-checklist.md](procomps/json-form-procomp/reviews/2026-05-21-v0.2.0-checklist.md)). See [decision file](../.claude/decisions/2026-05-21-json-form-v0.2.0-watch-drop-and-deep-merge.md).
- **v0.1.7 (`json-form`, 2026-05-21):** substrate-grade upgrade in three back-to-back bumps. **v0.1.5** F-S1 cross-procomp `/types` rewrite fix (companion `article-body-01@v0.2.1` additive re-export); echo-guard in `ChangeBridge` via key-sorted `stableStringify`. **v0.1.6** narrow-deps `useConditional` + `useComputed` + `AbortController` in `useAsyncOptions` + submit focus-walk + shared `lib/flatten-errors.ts` + number empty→undef + `requiredWhen` eager trigger + error-summary anchor scroll+focus + `onSubmitAttempt` + `onReady` + `translatable` typed flag. **v0.1.7** internal compile split (`compileStructural` + `injectStrings`) + `dependsOn?: ReadonlyArray<string>` typed metadata + `BUILTIN_RENDERER_TYPES_SKIPPING_ALL_VALUES` 25-type whitelist (defined-but-unused; gates v0.2.0) + `useJsonFormFieldValue<T>(name)` / `useJsonFormFieldsValue<T>(names)` narrow-deps headless hooks + `defineFieldRenderer<TValue, TConfig>` typed factory + `<JsonFormDevtools>` panel with `React.lazy()` body split + prod no-op gate. v0.1.7 is **strictly additive** — every v0.1.x consumer keeps working unchanged. **GATE 3 spotcheck v0.1.7 Pass with follow-ups** ([reviews/2026-05-21-v0.1.7-spotcheck.md](procomps/json-form-procomp/reviews/2026-05-21-v0.1.7-spotcheck.md)); F-01 path-b smoke **CLOSED** post-deploy (0 errors in json-form + article-body-01 on consumer-tsc). **v0.2.0 GATE 2 plan LOCKED** ([plan-v0.2.0](procomps/json-form-procomp/json-form-procomp-plan-v0.2.0.md)) — default-registry watch drop (perf cliff lifter) + deep-merge `defaultValues`; trigger satisfied (≥7-day OR external smoke-clean — smoke just landed clean). See [decision file](../.claude/decisions/2026-05-21-json-form-v0.1.5-through-v0.1.7-substrate-upgrade.md).
- **v0.1.3:** `pdf-viewer` (AnnotationLayer fix — wrap page list in `<Document className="contents">`); `flow-canvas-01` (paint-side perf patch — `defaultViewport` snapshot + collapsed `<CustomJsonNode>` no-`<pre>` hot path; preceded by v0.1.2 cascading-callback round); `todo-tree` (dnd-kit SSR hydration mismatch fix).
- **v0.1.3 (2026-05-25):** `workspace` — F-03 close-out micro-patch. `useResizeKeyboard` in `hooks/use-keyboard-actions.ts` is now `@internal`-tagged (the hook is sibling-imported by `workspace.tsx`; it was never re-exported from `index.ts` so this is convention-only — consumers deep-importing it should stop, its inputs are derived state). Procomp guide's "Public API surface" reference clarified: only `index.ts` re-exports are stable; anything else is internal. Migration-notes block added for v0.1.2 → v0.1.3. Spotcheck F-03 marked Closed. Strictly non-breaking; no public API change (patch-bump GATE 3 exempt per the readiness-review rule).
- **v0.1.2 (2026-05-24):** `workspace` — deep-review close-out (12 findings: dead-code cleanup, `computeLayoutBoundsForPath` relocation, `Alt+Shift+Arrow` consolidated into `useResizeKeyboard` hook, `cap=0` no-longer-forces-card-stack at desktop, `<SplitDivider>` Arrow-key resize, new `onError` callback, new `cardStackItemHeight` prop, `<DropdownMenuGroup>` semantics, per-instance `inertLogged` ref, `onLayoutChange` 60Hz storm doc warning, first-render flash doc, full v0.1.1→v0.1.2 changelog). All non-breaking. See [decision file](../.claude/decisions/2026-05-24-workspace-v0.1.2-deep-review-closeout.md).
- **v0.1.2:** `engagement-bar-01` (Phase 7 utils→lib), `media-carousel-01` (Phase 7 a11y), `progress-timeline-01` (Phase 7 status colors), `page-hero-news-01` (Phase 7 lime mandate), `video-player-01` (Pexels CDN fixture swap).
- **v0.1.1 (patch shipped):** `post-card-01`, `data-table`, `flow-canvas-01`, `entity-picker`, `markdown-editor`, `properties-form`, `workspace`, plus Phase 7 batch — `event-card-01`, `registration-card-01`, `detail-panel`, `story-viewer-01`, `grid-layout-news-01`. `stat-card` patched 2026-05-09.
- **All remaining components** sit at the initial v0.1.0 ship.
- **New 2026-05-10:** `pdf-viewer` (media), `file-tree` (new `navigation` category), `file-manager` (navigation, composes file-tree + ships shared `@ilinxa/file-clipboard` primitive).
- **New 2026-05-11:** `code-block` (new `code` category at order 9 — Shiki dual-theme + CodeMirror 6 + custom HighlightStyle; substrate for chat/markdown/rich-card/terminal surfaces).
- **New 2026-05-16:** `rich-card-in-flow` (data; canonical implementation of flow-canvas-01@v0.2.0 perf description Q33's popup-edit renderer convention; cross-registry deps on `@ilinxa/rich-card` + `@ilinxa/flow-canvas-01@^0.2.1`; F-V2 smoke harness path-b PASS after F-S1 fix-up; GATE 3 Pass with follow-ups). **Bumped 2026-05-17 to v0.2.0** — opt-in `PortEditorStrip` for per-card / per-subcard port editing (id / type / side / dir / multi / label); cross-registry dep bumped to `@ilinxa/flow-canvas-01@^0.2.5` for the built-in `"doc"` port type; new F-cross-13 (shadcn primitive Radix→Base UI divergence) surfaced + closed via defensive callback pattern in B4. See [decision file](../.claude/decisions/2026-05-17-rich-card-in-flow-v0.2.0-port-editor.md).
- `force-graph` was **removed 2026-05-08** pending recreation under a new design + plan. v0.2 source + procomp docs archived to `docs/migrations/force-graph/`. The future v3 will go through the standard procomp planning pipeline.
- `flow-canvas-01` v0.1.0 is feature-rich despite the version (39 files; renderers/portTypes/edgeTypes registries; typed-port validation; sub-object drag-extract; right-click menus; 200-node stress demo).

## data (24)

| Slug                    | Name                       | Version | Status |
|-------------------------|----------------------------|---------|--------|
| `rich-card`             | Rich Card                  | 0.4.2   | beta   |
| `article-body-01`       | Article Body 01            | 0.2.2   | alpha  |
| `kanban-board-01`       | Kanban Board 01            | 0.3.0   | alpha  |
| `engagement-bar-01`     | Engagement Bar 01          | 0.3.2   | alpha  |
| `post-card-01`          | Post Card 01               | 0.3.2   | alpha  |
| `article-meta-01`       | Article Meta 01            | 0.1.0   | alpha  |
| `comment-thread-01`     | Comment Thread 01          | 0.2.1   | alpha  |
| `content-card-news-01`  | Content Card (News 01)     | 0.3.0   | alpha  |
| `data-table`            | Data Table                 | 0.1.1   | alpha  |
| `event-card-01`         | Event Card 01              | 0.1.1   | alpha  |
| `expandable-text-01`    | Expandable Text 01         | 0.1.0   | alpha  |
| `flow-canvas-01`        | Flow Canvas                | 0.2.5   | alpha  |
| `info-list-01`          | Info List 01               | 0.1.0   | alpha  |
| `people-grid-01`        | People Grid 01             | 0.1.0   | alpha  |
| `progress-timeline-01`  | Progress Timeline 01       | 0.1.2   | alpha  |
| `project-card-01`       | Project Card 01            | 0.2.0   | alpha  |
| `registration-card-01`  | Registration Card 01       | 0.1.1   | alpha  |
| `schedule-list-01`      | Schedule List 01           | 0.1.0   | alpha  |
| `story-rail-01`         | Story Rail 01              | 0.2.1   | alpha  |
| `thumb-list-01`         | Thumb List 01              | 0.1.0   | alpha  |
| `stat-card`             | Stat Card                  | 0.1.1   | alpha  |
| `rich-card-in-flow`     | Rich Card in Flow          | 0.2.0   | alpha  |
| `todo-rich-card`        | Todo Rich Card             | 0.1.1   | alpha  |
| `todo-tree`             | Todo Tree                  | 0.1.3   | alpha  |

## forms (8)

| Slug                | Name              | Version | Status |
|---------------------|-------------------|---------|--------|
| `category-cloud-01` | Category Cloud 01 | 0.1.0   | alpha  |
| `entity-picker`     | Entity Picker     | 0.1.1   | alpha  |
| `filter-bar-01`     | Filter Bar 01     | 0.1.0   | alpha  |
| `filter-stack`      | Filter Stack      | 0.1.0   | alpha  |
| `json-form`         | Json Form         | 0.2.5   | alpha  |
| `registration-form-01` | Registration Form 01 | 0.1.1 | alpha |
| `markdown-editor`   | Markdown Editor   | 0.1.1   | alpha  |
| `properties-form`   | Properties Form   | 0.1.1   | alpha  |

## layout (2)

| Slug                  | Name                    | Version | Status |
|-----------------------|-------------------------|---------|--------|
| `grid-layout-news-01` | Grid Layout (News 01)   | 0.2.0   | alpha  |
| `workspace`           | Workspace               | 0.1.3   | alpha  |

## marketing (5)

| Slug                   | Name                | Version | Status |
|------------------------|---------------------|---------|--------|
| `author-card-01`       | Author Card 01      | 0.1.0   | alpha  |
| `newsletter-card-01`   | Newsletter Card 01  | 0.1.0   | alpha  |
| `page-hero-news-01`    | Page Hero (News 01) | 0.1.2   | alpha  |
| `share-bar-01`         | Share Bar 01        | 0.1.0   | alpha  |
| `pricing-table-01`     | Pricing Table 01    | 0.1.0   | alpha  |

## media (4)

| Slug                | Name              | Version | Status |
|---------------------|-------------------|---------|--------|
| `media-carousel-01` | Media Carousel 01 | 0.1.3   | alpha  |
| `pdf-viewer`        | PDF Viewer        | 0.1.3   | alpha  |
| `story-viewer-01`   | Story Viewer 01   | 0.3.9   | alpha  |
| `video-player-01`   | Video Player 01   | 0.1.2   | alpha  |

## navigation (4)

| Slug              | Name            | Version | Status |
|-------------------|-----------------|---------|--------|
| `file-tree`       | File Tree       | 0.1.0   | alpha  |
| `file-manager`    | File Manager    | 0.1.0   | alpha  |
| `rich-sidebar`  | Rich Sidebar  | 0.3.0   | alpha  |
| `account-switcher-01` | Account Switcher | 0.1.0 | alpha  |

## code (1)

| Slug          | Name        | Version | Status |
|---------------|-------------|---------|--------|
| `code-block`  | Code Block  | 0.1.1   | alpha  |

## feedback (1)

| Slug           | Name         | Version | Status |
|----------------|--------------|---------|--------|
| `detail-panel` | Detail Panel | 0.1.1   | alpha  |

---

## Verification method

Counts cross-checked three ways and all agreed at **49**:

1. **Filesystem:** `ls src/registry/components/<category>/` per category — 24 (data) + 8 (forms) + 5 (marketing) + 4 (media) + 4 (navigation) + 2 (layout) + 1 (code) + 1 (feedback) = 49.
2. **Manifest:** `src/registry/manifest.ts` REGISTRY array has 49 entries (each component has 3 imports — Demo / Usage / meta — plus 2 type-and-categories imports at the bottom).
3. **Per-component meta:** every directory contains a `meta.ts` with populated `slug`, `category`, `version`, and `status` fields, and each is registered in `REGISTRY` in the manifest.

The `_template/_template/` folder is excluded — it ships its own placeholder `meta.ts` for `tsc` compatibility but is never registered or installable.

Cross-component shared primitives (e.g., `@ilinxa/file-clipboard` shipped at `src/registry/components/navigation/_shared/`) are NOT counted as components — they ship as separate registry items but are not registered in the manifest's REGISTRY array.

> **Removed 2026-05-08:** `force-graph` v0.2.0 (data) — pending recreation. v0.2 source + procomp docs archived to `docs/migrations/force-graph/`.
